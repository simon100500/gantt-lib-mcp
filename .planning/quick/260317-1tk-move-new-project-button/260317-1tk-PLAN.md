---
phase: quick-260317-1tk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/web/src/App.tsx]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Button '+ Новый проект' appears on the right side of topbar"
    - "Button is positioned near login/user area (not in left section)"
    - "Button maintains existing styling and behavior"
    - "Button only shows for authenticated users (no share token)"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Topbar header layout"
      contains: "+ Новый проект button in right section"
  key_links:
    - from: "App.tsx header right section"
      to: "handleCreateProject"
      via: "Button onClick handler"
      pattern: "handleCreateProject"
---

<objective>
Move the "+ Новый проект" button from the left section (next to project name breadcrumb) to the right section of the topbar (next to login/user dropdown).

Purpose: Improve visual balance and make the primary action more prominent in the header right area, alongside authentication controls.
Output: Updated App.tsx with button repositioned to the right side of the header.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/UX-GUIDE.md

# Current button location (lines 774-783)
The button is currently in the left section, after the share button:
```tsx
{!hasShareToken && auth.isAuthenticated && (
  <Button
    variant="ghost"
    size="sm"
    onClick={handleCreateProject}
    className="h-7 shrink-0 px-2.5 text-xs text-primary hover:bg-primary/10 hover:text-primary"
  >
    + Новый проект
  </Button>
)}
```

# Target location
Move the button to the right section (lines 786-825), placing it before the login/user dropdown controls.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move "+ Новый проект" button to right side of topbar</name>
  <files>packages/web/src/App.tsx</files>
  <action>
    Remove the "+ Новый проект" button from the left section (currently at lines 774-783, inside the project name breadcrumb div).

    Add the button to the right section of the header:
    - Place it BEFORE the conditional rendering of hasShareToken/auth status (line 788)
    - Wrap it in the same condition: `{!hasShareToken && auth.isAuthenticated && (...)}`
    - Keep all existing props and styling unchanged:
      - variant="ghost"
      - size="sm"
      - onClick={handleCreateProject}
      - className="h-7 shrink-0 px-2.5 text-xs text-primary hover:bg-primary/10 hover:text-primary"
    - Ensure proper spacing with surrounding elements (use gap-2 or similar if needed)

    The button should appear before the "Только чтение" badge for shared projects, or before the "Войдите, чтобы сохранить график" + login button area, or before the user email dropdown for authenticated users.
  </action>
  <verify>
    <automated>grep -n "Новый проект" packages/web/src/App.tsx</automated>
  </verify>
  <done>
    - Button no longer appears in left section (after project breadcrumb)
    - Button appears in right section of header
    - Button maintains existing styling and behavior
    - Button only displays for authenticated users without share token
  </done>
</task>

</tasks>

<verification>
Visual verification in browser:
1. Open application and authenticate
2. Verify "+ Новый проект" button is on the right side of topbar
3. Verify button is positioned near login/user dropdown (not in left section)
4. Click button - verify it still opens create project modal
5. Check unauthenticated state - button should not appear
6. Check shared project state - button should not appear
</verification>

<success_criteria>
- Button repositioned to right side of topbar
- All existing functionality preserved
- No visual regressions or layout issues
- Conditional rendering logic intact (only for authenticated, non-shared sessions)
</success_criteria>

<output>
After completion, create `.planning/quick/260317-1tk-move-new-project-button/260317-1tk-SUMMARY.md`
</output>
