---
phase: quick
plan: 260330-m7o
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/prisma/schema.prisma
  - packages/mcp/src/types.ts
  - packages/mcp/src/services/project.service.ts
  - packages/server/src/routes/auth-routes.ts
  - packages/web/src/stores/useAuthStore.ts
  - packages/web/src/App.tsx
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
  - packages/web/src/components/layout/Toolbar.tsx
  - packages/web/src/components/GanttChart.tsx
autonomous: true
requirements:
  - QUICK-001: Хранить режим расчёта графика на уровне проекта в БД
  - QUICK-002: Возвращать режим в auth/project payload и обновлять через PATCH /api/projects/:id
  - QUICK-003: Дать пользователю переключатель в меню toolbar и прокинуть его в gantt-lib businessDays

must_haves:
  truths:
    - "Пользователь выбирает режим 'Рабочие дни' или 'Календарные дни' в меню с тремя точками над графиком."
    - "После переключения режим сохраняется у текущего проекта и не сбрасывается после перезагрузки страницы или повторного входа."
    - "GanttChart получает корректный boolean в проп businessDays: для рабочего режима true, для календарного false."
  artifacts:
    - path: "packages/mcp/prisma/schema.prisma"
      provides: "Поле проекта для постоянного хранения режима расчёта дней"
      contains: "ganttDayMode"
    - path: "packages/server/src/routes/auth-routes.ts"
      provides: "Auth/project API возвращает и обновляет режим проекта"
      contains: "ganttDayMode"
    - path: "packages/web/src/stores/useAuthStore.ts"
      provides: "AuthProject и store-синхронизация нового поля"
      contains: "ganttDayMode"
    - path: "packages/web/src/components/layout/Toolbar.tsx"
      provides: "UI-переключатель режима в ellipsis menu"
      contains: "Рабочие дни"
    - path: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      provides: "Прокидывание project.ganttDayMode в GanttChart.businessDays"
      contains: "businessDays="
  key_links:
    - from: "packages/mcp/prisma/schema.prisma"
      to: "packages/mcp/src/services/project.service.ts"
      via: "Prisma Project model -> domain Project mapping"
      pattern: "ganttDayMode"
    - from: "packages/server/src/routes/auth-routes.ts"
      to: "packages/web/src/stores/useAuthStore.ts"
      via: "project payload login/switch/list/patch"
      pattern: "project: \\{.*ganttDayMode"
    - from: "packages/web/src/components/layout/Toolbar.tsx"
      to: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      via: "callback изменения режима"
      pattern: "onGanttDayModeChange|ganttDayMode"
    - from: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      to: "packages/web/src/components/GanttChart.tsx"
      via: "prop businessDays"
      pattern: "businessDays=\\{"
---

<objective>
Добавить проектную настройку режима расчёта длительностей графика: рабочие дни или календарные дни.

Purpose: Сейчас `gantt-lib` уже поддерживает `businessDays`, но у пользователя нет постоянной настройки на уровне проекта. Нужен один сквозной контракт: БД -> API -> auth store -> toolbar menu -> `GanttChart`.
Output: Проект хранит `ganttDayMode`, frontend получает и обновляет его через существующий auth/project поток, а toolbar переключает режим и сразу влияет на `businessDays`.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/mcp/prisma/schema.prisma
@packages/mcp/src/types.ts
@packages/mcp/src/services/project.service.ts
@packages/server/src/routes/auth-routes.ts
@packages/web/src/stores/useAuthStore.ts
@packages/web/src/App.tsx
@packages/web/src/components/workspace/ProjectWorkspace.tsx
@packages/web/src/components/layout/Toolbar.tsx
@packages/web/src/components/GanttChart.tsx
@node_modules/gantt-lib/dist/index.d.ts

<interfaces>
From `packages/mcp/src/types.ts`:
```typescript
export interface Project {
  id: string;
  userId: string;
  name: string;
  createdAt: string;
}
```

From `packages/web/src/stores/useAuthStore.ts`:
```typescript
export interface AuthProject {
  id: string;
  name: string;
  taskCount?: number;
}
```

From `packages/server/src/routes/auth-routes.ts`:
```typescript
reply.send({
  accessToken,
  refreshToken,
  user: { id: user.id, email: user.email },
  project: { id: project.id, name: project.name },
});

fastify.patch<{ Params: { id: string } }>('/api/projects/:id', ... )
```

From `node_modules/gantt-lib/dist/index.d.ts`:
```typescript
interface GanttChartProps {
  businessDays?: boolean;
  customDays?: CustomDayConfig[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить project-level контракт режима дней в Prisma, domain types и project API</name>
  <files>packages/mcp/prisma/schema.prisma, packages/mcp/src/types.ts, packages/mcp/src/services/project.service.ts, packages/server/src/routes/auth-routes.ts</files>
  <action>
1. В `Project` модели добавь явное поле режима, лучше как строковый enum на уровне доменной модели (`'business' | 'calendar'`) с дефолтом для текущего поведения `business`. Не храни raw `businessDays` как имя поля в БД: на UI нужен человекочитаемый режим, а `businessDays` останется транспортным boolean для `gantt-lib`.
2. Расшири `packages/mcp/src/types.ts`:
   - добавь `type GanttDayMode = 'business' | 'calendar'`;
   - включи `ganttDayMode: GanttDayMode` в `Project`.
3. Обнови `ProjectService.projectToDomain`, `create`, `findById`, `listByUser`, `update` так, чтобы поле стабильно читалось и писалось. `update` должен принимать partial-обновление проекта, а не только имя:
   - сохранить совместимость с rename;
   - разрешить обновлять `name`, `ganttDayMode` или оба сразу;
   - по-прежнему проверять ownership перед update.
4. В `packages/server/src/routes/auth-routes.ts` включи `ganttDayMode` во все project payloads, которые используются вебом:
   - `POST /api/auth/verify-otp`
   - `POST /api/auth/switch-project`
   - `GET /api/projects`
   - `POST /api/projects`
   - `PATCH /api/projects/:id`
5. Для `PATCH /api/projects/:id` прими body вида `{ name?: string; ganttDayMode?: 'business' | 'calendar' }`. Не делай валидацию так, чтобы запрос без `name` падал: этот endpoint теперь должен уметь отдельное обновление режима. Верни `400`, только если не передано ни одного допустимого поля или `ganttDayMode` вне контракта.
  </action>
  <verify>
    <automated>cmd /c npx prisma generate --schema packages\\mcp\\prisma\\schema.prisma && npm.cmd run build -w packages\\server</automated>
  </verify>
  <done>
    - В Prisma и domain types есть поле `ganttDayMode` с дефолтом `business`
    - Все project payloads backend возвращают `ganttDayMode`
    - `PATCH /api/projects/:id` обновляет имя и режим независимо друг от друга
  </done>
</task>

<task type="auto">
  <name>Task 2: Протащить ganttDayMode через auth store и текущие frontend update flows</name>
  <files>packages/web/src/stores/useAuthStore.ts, packages/web/src/App.tsx</files>
  <action>
1. Расшири `AuthProject` полем `ganttDayMode: 'business' | 'calendar'` и обнови все места, где store:
   - читает project из localStorage;
   - сохраняет project/projects в localStorage;
   - получает project из `/api/auth/verify-otp`, `/api/auth/switch-project`, `/api/projects`, `/api/projects/:id`.
2. Не вводи отдельный UI-store для этой настройки: источник истины должен остаться в `auth.project` и в списке `projects`, потому что настройка проектная, а не локально-браузерная.
3. В `App.tsx` обнови существующие вызовы `PATCH /api/projects/:id` для переименования так, чтобы они не теряли `ganttDayMode` при повторном `auth.login(...)` или локальном обновлении project object.
4. Добавь в auth store удобный метод уровня проекта, например `updateProject(partial)` или `updateProjectDayMode(mode)`, который:
   - шлёт `PATCH /api/projects/:id`;
   - обновляет `project` и соответствующий элемент в `projects`;
   - сохраняет refresh/access token и user без сброса auth state;
   - использует уже существующий retry-through-refresh pattern, а не дублирует auth логику.
  </action>
  <verify>
    <automated>npm.cmd run build -w packages\\web</automated>
  </verify>
  <done>
    - `AuthProject` и persisted auth state содержат `ganttDayMode`
    - После refresh/login/switch-project выбранный режим не теряется
    - Для обновления режима используется один store-level метод через существующий project PATCH endpoint
  </done>
</task>

<task type="auto">
  <name>Task 3: Добавить переключатель в ellipsis toolbar и связать его с GanttChart.businessDays</name>
  <files>packages/web/src/components/workspace/ProjectWorkspace.tsx, packages/web/src/components/layout/Toolbar.tsx, packages/web/src/components/GanttChart.tsx</files>
  <action>
1. В `Toolbar.tsx` добавь в меню с тремя точками новый блок настройки режима графика. Размести его рядом с остальными опциями dropdown, без отдельной модалки:
   - label: `Рабочие дни`
   - label: `Календарные дни`
   - текущее значение должно быть визуально отмечено через checkmark или radio-like state.
2. Расшири `ToolbarProps` новыми пропами, например:
   - `ganttDayMode?: 'business' | 'calendar'`
   - `onGanttDayModeChange?: (mode: 'business' | 'calendar') => void`
   Не завязывай toolbar на auth store напрямую; управление должно идти сверху через `ProjectWorkspace`.
3. В `ProjectWorkspace.tsx` читай текущий режим из `useAuthStore((state) => state.project?.ganttDayMode)` и пробрасывай его:
   - в `Toolbar` как value/callback;
   - в `GanttChart` как `businessDays={ganttDayMode !== 'calendar'}`.
4. В `GanttChart.tsx` явно добавь `businessDays?: boolean` в локальный wrapper interface и прокинь проп в `GanttLibChart`. Не полагайся на неявную передачу неизвестных props.
5. Для shared/guest режимов ничего нового не придумывай:
   - shared workspace только отображает текущий режим проекта, если он уже есть;
   - если в текущем shell нет project payload, оставляй безопасный дефолт `business`.
  </action>
  <verify>
    <automated>npm.cmd run build -w packages\\web</automated>
  </verify>
  <done>
    - В меню toolbar есть выбор между рабочими и календарными днями
    - Смена режима вызывает project PATCH, обновляет auth store и не требует reload
    - `GanttChart` получает `businessDays={true}` для `business` и `businessDays={false}` для `calendar`
  </done>
</task>

</tasks>

<verification>
1. Авторизоваться и открыть проект с задачами, где заметна разница между календарными и рабочими днями.
2. В toolbar открыть меню `...`, переключить на `Календарные дни`.
3. Проверить DevTools Network:
   - уходит `PATCH /api/projects/:id` с `ganttDayMode: "calendar"`
   - ответ содержит обновлённый `project.ganttDayMode`
4. Перезагрузить страницу и убедиться, что режим сохранился.
5. Переключиться на другой проект и обратно: у каждого проекта должен восстановиться свой режим.
6. Убедиться, что в `GanttChart` режим реально влияет на расчёт через `businessDays`, а не только меняет UI label.
</verification>

<success_criteria>
- Проект хранит `ganttDayMode` в БД с дефолтом `business`
- Backend возвращает `ganttDayMode` во всех project payloads, которые использует web auth/project flow
- Frontend обновляет режим через существующий `PATCH /api/projects/:id` и сохраняет его в auth store/localStorage
- В меню toolbar пользователь может выбрать `Рабочие дни` или `Календарные дни`
- `packages/web` и `packages/server` собираются без ошибок после изменений
</success_criteria>

<output>
After completion, create `.planning/quick/260330-m7o-gantt-lib/260330-m7o-SUMMARY.md`
</output>
