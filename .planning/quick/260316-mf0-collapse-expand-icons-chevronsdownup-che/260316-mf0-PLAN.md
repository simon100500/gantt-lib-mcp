---
phase: quick-260316-mf0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - UI-collapse-expand-icons
  - UI-view-toggle-purple
must_haves:
  truths:
    - "Collapse/expand buttons show ChevronsDownUp/ChevronsUpDown icons with no button background or border"
    - "Collapse/expand buttons turn slate on hover (no box/outline)"
    - "Active view mode button (День/Неделя) uses a slightly purple-tinted style instead of bg-slate-900"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Updated toolbar buttons"
      contains: "ChevronsDownUp"
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "lucide-react"
      via: "import ChevronsDownUp, ChevronsUpDown"
      pattern: "ChevronsDownUp"
---

<objective>
Update two visual elements in the Gantt toolbar:
1. Collapse/expand buttons: swap ChevronUp/ChevronDown for ChevronsDownUp/ChevronsUpDown, remove the outline border box, make them ghost-style (no background) with slate text and slate hover.
2. Active view toggle buttons (День/Неделя): change active state from near-black `bg-slate-900 text-white` to a slightly purple-tinted style for visual distinction.

Purpose: Improve icon semantics (chevrons-with-arrows better communicate collapse/expand) and introduce consistent purple-tinted active state in line with the overall UI direction.
Output: Updated App.tsx with new icons and color classes.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update collapse/expand icons and view toggle active style in App.tsx</name>
  <files>packages/web/src/App.tsx</files>
  <action>
Make two targeted changes in packages/web/src/App.tsx:

**Change 1 — Import line (line 2):**
Replace `ChevronDown, ChevronUp` with `ChevronsDownUp, ChevronsUpDown` in the lucide-react import. Remove `ChevronDown` and `ChevronUp` (they are used only for collapse/expand and the project switcher dropdown — check: ChevronDown at line 813 is a separate usage for the ProjectSwitcher dropdown, keep that icon if it is still needed there, but look at the actual usages carefully).

Specifically:
- Line 813 uses `<ChevronDown className="h-3.5 w-3.5 shrink-0 text-slate-600" />` inside the ProjectSwitcher dropdown trigger — this must remain working. Check whether ChevronDown is used elsewhere. If so, keep it in the import.
- Lines 921 and 932 use `ChevronUp` and `ChevronDown` for the collapse/expand buttons — replace these with `ChevronsUpDown` and `ChevronsDownUp` respectively.

Correct icon semantics:
- "Свернуть все" (Collapse all) → `ChevronsDownUp` (arrows pointing inward/together = collapse)
- "Развернуть все" (Expand all) → `ChevronsUpDown` (arrows pointing outward = expand)

Add `ChevronsDownUp, ChevronsUpDown` to the import. Keep `ChevronDown` only if it is used outside the collapse/expand buttons.

**Change 2 — Collapse/expand buttons (lines ~914–934):**
Replace both Button components (collapse and expand) with plain `<button>` elements styled as ghost icon-buttons. Remove `variant="outline"` and the bordered look. New style:

```tsx
<button
  type="button"
  onClick={handleCollapseAll}
  title="Свернуть все родительские задачи"
  className="h-7 w-7 flex items-center justify-center rounded text-slate-500 hover:text-slate-800 hover:bg-slate-100 transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
>
  <ChevronsDownUp className="w-3.5 h-3.5" />
</button>

<button
  type="button"
  onClick={handleExpandAll}
  title="Развернуть все родительские задачи"
  className="h-7 w-7 flex items-center justify-center rounded text-slate-500 hover:text-slate-800 hover:bg-slate-100 transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
>
  <ChevronsUpDown className="w-3.5 h-3.5" />
</button>
```

No label text — icon only. The title attribute provides tooltip accessibility.

**Change 3 — View mode toggle active state (lines ~889–904):**
Replace `bg-slate-900 text-white` (used for the active День/Неделя button) with a slightly purple-tinted active style: `bg-violet-600 text-white`.

The inactive hover state `bg-transparent text-slate-600 hover:bg-slate-100` stays unchanged.

Full updated button className for active state:
```
viewMode === 'day'
  ? 'bg-violet-600 text-white'
  : 'bg-transparent text-slate-600 hover:bg-slate-100'
```

Same for the Неделя button with `viewMode === 'week'`.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - App.tsx imports ChevronsDownUp and ChevronsUpDown from lucide-react
    - Collapse/expand buttons are icon-only ghost buttons (no border, no background at rest, slate hover)
    - Active view toggle uses bg-violet-600 instead of bg-slate-900
    - TypeScript compilation reports no new errors
  </done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` in packages/web — no TypeScript errors introduced
2. Visually verify in browser: collapse/expand buttons are borderless icon-only with slate hover
3. Active view button (День or Неделя) shows violet tint, not black
</verification>

<success_criteria>
- ChevronsDownUp/ChevronsUpDown icons replace ChevronUp/ChevronDown on the toolbar collapse/expand buttons
- Buttons have no background/border at rest; slate-100 background on hover
- Active view mode button background is violet-600, not slate-900
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/quick/260316-mf0-collapse-expand-icons-chevronsdownup-che/260316-mf0-SUMMARY.md`
</output>
