---
phase: quick-260318-enter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/web/src/components/StartScreen.tsx]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Pressing Enter in textarea does NOT submit the form"
    - "Only clicking the submit button submits the form"
    - "Shift+Enter still works for new lines (default textarea behavior)"
  artifacts:
    - path: "packages/web/src/components/StartScreen.tsx"
      provides: "Form with no Enter-key submission"
      removes: "handleKeyDown function"
  key_links:
    - from: "textarea"
      to: "submit button"
      via: "form onSubmit handler only"
      pattern: "onSubmit={handleSubmit}"
---

<objective>
Remove Enter key form submission behavior from StartScreen textarea

Purpose: Prevent accidental form submissions when user presses Enter while typing. Only the submit button should trigger form submission.
Output: Textarea that allows Enter key for new lines without submitting the form
</objective>

<execution_context>
@C:/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Current Implementation

From packages/web/src/components/StartScreen.tsx:
- Lines 48-53: `handleKeyDown` function intercepts Enter key and calls `handleSubmit()`
- Line 93: `onKeyDown={handleKeyDown}` is attached to textarea
- Line 85: `<form onSubmit={handleSubmit}>` handles button clicks

# Problem

When user types in textarea and presses Enter, the form submits immediately. User wants Enter key to work normally (create new lines) and only submit via button click.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Enter key submission handler from textarea</name>
  <files>packages/web/src/components/StartScreen.tsx</files>
  <action>
    Remove the Enter key interception behavior:

    1. Delete the `handleKeyDown` function (lines 48-53)
    2. Remove `onKeyDown={handleKeyDown}` prop from textarea (line 93)

    This restores default textarea behavior where Enter creates new lines and Shift+Enter also works normally.

    Keep the form's `onSubmit={handleSubmit}` (line 85) so the submit button still works correctly.
  </action>
  <verify>
    <automated>grep -n "onKeyDown" packages/web/src/components/StartScreen.tsx returns exit code 1 (no matches)</automated>
  </verify>
  <done>
    - Enter key creates new lines in textarea (does not submit form)
    - Submit button still works and triggers form submission
    - No code references to handleKeyDown function remain
  </done>
</task>

</tasks>

<verification>
1. Type text in textarea and press Enter - should create new line, not submit
2. Click submit button with text - should submit form
3. Test that Shift+Enter works normally for new lines
</verification>

<success_criteria>
- Enter key no longer submits the form
- Submit button still triggers form submission
- Textarea behaves like standard textarea (Enter = new line)
- No TypeScript errors after changes
</success_criteria>

<output>
After completion, create `.planning/quick/260318-enter/260318-SUMMARY.md`
</output>
