# Quick Task 260322-q76: SUMMARY

## Выполнено

### 1. GanttChart.tsx
- Добавлен проп `showChart?: boolean` в `GanttChartProps`
- Прокинут в `GanttLibChart`

### 2. useUIStore.ts
- Добавлено состояние `showChart: boolean` (по умолчанию `true`)
- Добавлен action `setShowChart`

### 3. Toolbar.tsx
- Добавлен импорт иконки `Calendar`
- Получены `showChart` и `setShowChart` из store
- Реализована логика двойного toggle:
  - `handleToggleTaskList` - переключает видимость списка
  - `handleToggleChart` - переключает видимость календаря
  - Логика: при скрытии обоих автоматически переключается на другой
- Заменена кнопка "Список задач" на две кнопки: "Список" и "Календарь"

### 4. ProjectWorkspace.tsx
- Получен `showChart` из useUIStore
- Прокинут в `GanttChart`

## Результат
Теперь в тулбаре есть два переключателя: "Список" и "Календарь". При попытке скрыть оба одновременно, второй автоматически показывается.
