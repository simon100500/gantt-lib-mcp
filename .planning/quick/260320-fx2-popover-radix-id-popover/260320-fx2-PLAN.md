---
phase: quick-fx2
plan: 260320-fx2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/package.json
  - packages/web/src/components/ui/popover.tsx
  - packages/web/src/components/RelationChip.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "При клике на чип связи появляется Popover с id предшественника"
    - "Popover компонент создан по аналогии с dropdown-menu.tsx"
    - "Пакет @radix-ui/react-popover установлен"
  artifacts:
    - path: "packages/web/package.json"
      provides: "Зависимость @radix-ui/react-popover"
      contains: "@radix-ui/react-popover"
    - path: "packages/web/src/components/ui/popover.tsx"
      provides: "UI компонент Popover (Radix)"
      exports: ["Popover", "PopoverTrigger", "PopoverContent", "PopoverAnchor"]
    - path: "packages/web/src/components/RelationChip.tsx"
      provides: "Компонент чипа связи с Popover"
      exports: ["RelationChip"]
  key_links:
    - from: "RelationChip.tsx"
      to: "ui/popover.tsx"
      via: "import"
      pattern: "from.*ui/popover"
    - from: "RelationChip.tsx"
      to: "Radix UI Popover"
      via: "React component composition"
      pattern: "Popover.*PopoverTrigger.*PopoverContent"
---

<objective>
Добавить Popover от Radix UI для чипа связи между задачами, показывающий id предшественника при клике.

Purpose: Улучшить UX при работе со связями задач, позволяя быстро увидеть id связанной задачи.
Output: Установленный пакет Popover, UI компонент, компонент RelationChip с Popover
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/src/components/ui/dropdown-menu.tsx
@packages/web/src/components/layout/Toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Установить @radix-ui/react-popover и создать UI компонент</name>
  <files>packages/web/package.json, packages/web/src/components/ui/popover.tsx</files>
  <action>
1. Установить пакет:
   ```bash
   cd packages/web && npm install @radix-ui/react-popover
   ```

2. Создать файл `packages/web/src/components/ui/popover.tsx` по аналогии с `dropdown-menu.tsx`:
   - Импортировать из @radix-ui/react-popover: Root, Trigger, Content, Anchor, Close, Arrow
   - Экспортировать компоненты: Popover (Root), PopoverTrigger, PopoverContent, PopoverAnchor, PopoverClose, PopoverArrow
   - Использовать те же паттерны стилизации что в dropdown-menu.tsx (className через cn, Portal, правильные data-state атрибуты для анимации)
   - Content должен иметь: position, sideOffset, align, alignOffset props как в dropdown-menu
   - Добавить fade-in/zoom-in анимации через data-state

Следуй структуре dropdown-menu.tsx, но для Popover API.
  </action>
  <verify>
    <automated>grep -q "@radix-ui/react-popover" packages/web/package.json && test -f packages/web/src/components/ui/popover.tsx</automated>
  </verify>
  <done>Пакет установлен, файл popover.tsx создан с экспортируемыми компонентами</done>
</task>

<task type="auto">
  <name>Task 2: Создать компонент RelationChip с Popover</name>
  <files>packages/web/src/components/RelationChip.tsx</files>
  <action>
Создать файл `packages/web/src/components/RelationChip.tsx`:

```typescript
import * as React from 'react';
import * as Popover from './ui/popover';
import { cn } from '@/lib/utils';

export interface RelationChipProps {
  predecessorId: string;
  successorId: string;
  relationType?: 'finish-to-start' | 'start-to-start' | 'finish-to-finish' | 'start-to-finish';
  className?: string;
}

export function RelationChip({ predecessorId, successorId, relationType = 'finish-to-start', className }: RelationChipProps) {
  return (
    <Popover.Popover>
      <Popover.PopoverTrigger asChild>
        <button
          type="button"
          className={cn(
            "inline-flex items-center gap-1 rounded-full border border-slate-200 bg-slate-50 px-2 py-0.5 text-xs text-slate-600",
            "hover:border-slate-300 hover:bg-slate-100",
            "transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1",
            className
          )}
        >
          <span className="font-medium">ID: {predecessorId}</span>
        </button>
      </Popover.PopoverTrigger>
      <Popover.PopoverContent align="start" className="w-auto p-3">
        <div className="space-y-1.5">
          <div className="text-xs font-medium text-slate-900">Детали связи</div>
          <div className="grid grid-cols-[auto_1fr] gap-x-3 gap-y-1 text-xs">
            <span className="text-slate-500">Предшественник:</span>
            <span className="font-mono text-slate-700">{predecessorId}</span>
            <span className="text-slate-500">Преемник:</span>
            <span className="font-mono text-slate-700">{successorId}</span>
            <span className="text-slate-500">Тип связи:</span>
            <span className="text-slate-700">{relationType}</span>
          </div>
        </div>
      </Popover.PopoverContent>
    </Popover.Popover>
  );
}
```

Ключевые моменты:
- Использовать Popover компонент из ui/popover.tsx
- Trigger показывает predecessorId на чипе
- В Content Popover показываем полную информацию: predecessorId, successorId, relationType
- Стили совпадают с остальными чипами в проекте (см. ChatSidebar.tsx, StartScreen.tsx)
  </action>
  <verify>
    <automated>test -f packages/web/src/components/RelationChip.tsx && grep -q "export function RelationChip" packages/web/src/components/RelationChip.tsx</automated>
  </verify>
  <done>Компонент RelationChip создан, принимает predecessorId, показывает его в Popover при клике</done>
</task>

</tasks>

<verification>
- Пакет @radix-ui/react-popover установлен в package.json
- Файл packages/web/src/components/ui/popover.tsx существует и экспортирует Popover компоненты
- Файл packages/web/src/components/RelationChip.tsx существует
- RelationChip показывает predecessorId в Popover при клике
- npm run build --workspace=packages/web проходит без ошибок
</verification>

<success_criteria>
- Popover UI компонент создан по аналогии с dropdown-menu.tsx
- RelationChip компонент создан и использует Popover
- При клике на чип показывается Popover с id предшественника и деталями связи
- Код соответствует существующим паттернам стилизации в проекте
</success_criteria>

<output>
After completion, create `.planning/quick/260320-fx2-popover-radix-id-popover/260320-fx2-SUMMARY.md`
</output>
