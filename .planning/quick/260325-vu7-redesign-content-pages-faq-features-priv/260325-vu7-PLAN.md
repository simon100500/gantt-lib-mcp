---
phase: quick
plan: 260325-vu7
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/site/src/pages/features.astro
  - packages/site/src/pages/faq.astro
  - packages/site/src/pages/privacy.astro
  - packages/site/src/pages/terms.astro
autonomous: true
requirements: [visual-cleanup]
must_haves:
  truths:
    - "features.astro has no fake visual panels — feature bands are full-width text sections"
    - "privacy.astro has no decorative Scope card in the hero"
    - "terms.astro has no decorative legal overview stamp in the hero"
    - "faq.astro aside has aria-label and DOM order matches visual order"
    - "All pages look clean and professional without pretentious decorative elements"
    - "All page content/copy text is preserved unchanged"
  artifacts:
    - path: "packages/site/src/pages/features.astro"
      provides: "Clean features page without fake visual column"
    - path: "packages/site/src/pages/faq.astro"
      provides: "FAQ with fixed DOM order and aria"
    - path: "packages/site/src/pages/privacy.astro"
      provides: "Clean document-style privacy page"
    - path: "packages/site/src/pages/terms.astro"
      provides: "Clean terms page without stamp"
  key_links: []
---

<objective>
Redesign content pages (features, FAQ, privacy, terms) to be clean and professional. Remove all pretentious decorative elements that add no content value. Keep all existing copy text intact.

Purpose: Pages currently have fake UI panels, useless decorative cards, and DOM order issues that make them look unprofessional.
Output: Four clean, honest content pages.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/site/src/pages/features.astro
@packages/site/src/pages/faq.astro
@packages/site/src/pages/privacy.astro
@packages/site/src/pages/terms.astro
@packages/site/src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up features.astro — remove fake visual column from feature bands</name>
  <files>packages/site/src/pages/features.astro</files>
  <action>
The feature bands section (`.feature-bands`) has 4 split-band sections where each band has a `.feature-band__copy` (good content) and a `.feature-band__visual` (fake decorative panel with "Focus: {eyebrow}" text and a quote — zero content value).

Changes:
1. Remove the entire `.feature-band__visual` div from each band in the template loop. Keep only `.feature-band__copy`.
2. Remove the `featureBands[].eyebrow` quote logic (lines 179-186 with the index-based quotes) since the visual panel is gone.
3. Update `.feature-band__shell` CSS: remove the 2-column grid (`grid-template-columns` in the `@media (min-width: 900px)` block). Make bands full-width single-column layout. Keep the existing padding and spacing.
4. Remove the `.feature-band--reverse` class logic from the template (the `index % 2 === 1 && "feature-band--reverse"` in `class:list`). No longer needed without 2 columns.
5. Remove ALL CSS rules related to the visual panel: `.feature-band__visual`, `.feature-band__visual-inner`, `.feature-band__line`, `.feature-band__metric`, `.feature-band__metric span` (from the grouped selector too), `.feature-band__metric strong`, `.feature-band__quote`, and the `.feature-band--reverse` order rules.
6. Keep `.feature-band__copy` max-width at around 48rem so text doesn't stretch too wide on large screens. Add `max-width: 48rem` to `.feature-band__copy`.
7. In the hero section: change `<i style="width: XX%;">` elements inside `.feature-frame__row` to `<span style="width: XX%;">` — `<i>` is semantically wrong for progress bars.
8. Keep the hero frame (`.feature-frame`) as is — it shows a simple visual of how the product works (input -> AI outline -> timeline) and is honest content, not fake decoration.
9. Do NOT change any text content (titles, bodies, points, eyebrows in the data arrays).
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>Feature bands are single-column full-width text sections. No fake visual panels. Progress bars use span not i. All copy preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up faq.astro — fix DOM order and aria</name>
  <files>packages/site/src/pages/faq.astro</files>
  <action>
Two issues in faq.astro:

1. DOM/visual order mismatch in hero: On desktop (960px+), CSS moves `.faq-hero__aside` to `grid-column: 1` and `.faq-hero__copy` to `grid-column: 2`, but in HTML the copy comes first and aside comes second. Fix by swapping the HTML order: put `.faq-hero__aside` FIRST in the DOM, then `.faq-hero__copy` SECOND. Then update CSS: remove the `grid-column` overrides in the `@media (min-width: 960px)` for `.faq-hero__copy` and `.faq-hero__aside` since DOM order now matches visual order. Keep the grid layout (`grid-template-columns`) as is.

2. The `<aside class="faq-nav">` element has no `aria-label`. Add `aria-label="Навигация по разделам"` to the aside element.

Do NOT change any FAQ content text.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>DOM order matches visual order on desktop. Aside has aria-label. All FAQ content preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Clean up privacy.astro and terms.astro — remove decorative elements</name>
  <files>packages/site/src/pages/privacy.astro, packages/site/src/pages/terms.astro</files>
  <action>
**privacy.astro:**
1. Remove the `.privacy-hero__note` div entirely from the hero section (lines 47-50). It's a decorative "Scope" card that says "Маркетинговый сайт и приложение" and "Рабочее описание без лишних юридических украшений" — this duplicates the hero paragraph and adds no new information.
2. Remove all CSS rules for `.privacy-hero__note`: the `.privacy-hero__note` block, `.privacy-hero__note span` (remove from the grouped `.privacy-label, .privacy-hero__note span` selector — keep only `.privacy-label`), `.privacy-hero__note strong`, `.privacy-hero__note p` (already covered by grouped selector).
3. Since the hero is now single-column (just content), remove the 2-column grid from `.privacy-hero .privacy-shell` in the `@media (min-width: 920px)` block. Let it be a simple single-column layout. Keep `max-width: 48rem` on `.privacy-hero__content`.
4. Remove the decorative radial-gradient from `.privacy-hero` background. Use the same clean gradient as other pages: `linear-gradient(180deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 250, 252, 0.94) 100%)`.

**terms.astro:**
1. Remove the `.legal-hero__stamp` paragraph ("GetGantt legal overview") from the `.legal-hero__meta` div.
2. Since `.legal-hero__meta` now only has the "Terms" label, simplify it: remove the `display: flex; justify-content: space-between` since there's only one child. It can just be a simple block with `padding-bottom` and `border-bottom`.
3. Remove `.legal-hero__stamp` from the grouped CSS selector (`.legal-label, .legal-row__index, .legal-hero__stamp` — keep only `.legal-label, .legal-row__index`).

Do NOT change any privacy or terms content text.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>Privacy page has no Scope card, clean hero. Terms page has no stamp, clean meta bar. All content preserved.</done>
</task>

</tasks>

<verification>
- `npm run build` in packages/site passes without errors
- All 4 pages render correctly with preserved content
- No fake decorative panels visible on features page
- No Scope card on privacy page
- No stamp on terms page
- FAQ DOM order matches visual order
</verification>

<success_criteria>
All four content pages are clean, professional, and free of pretentious decorative elements. All original copy text is preserved intact. Pages build without errors.
</success_criteria>

<output>
After completion, update `.planning/STATE.md` quick tasks table.
</output>
