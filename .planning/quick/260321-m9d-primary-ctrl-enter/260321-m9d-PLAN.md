---
phase: quick-260321-m9d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/web/src/components/TaskSearch.tsx]
autonomous: false
requirements: []
must_haves:
  truths:
    - "Кнопка создания задачи имеет primary стиль (синий фон, белый текст)"
    - "Текст кнопки: '+ Задача' (с большой буквы)"
    - "Ctrl+Enter в поле поиска создаёт задачу"
    - "После создания курсор остаётся в поле ввода"
    - "После создания происходит прокрутка к новой задаче"
    - "Новая задача выделяется в графике"
  artifacts:
    - path: "packages/web/src/components/TaskSearch.tsx"
      provides: "Обновлённый компонент TaskSearch"
      contains: "variant=\"default\"|variant=\"primary\""
    - path: "packages/web/src/components/TaskSearch.tsx"
      provides: "Обработчик Ctrl+Enter"
      contains: "Ctrl.*Enter|ctrlKey.*Enter"
  key_links:
    - from: "TaskSearch.handleCreateTask"
      to: "onTaskNavigate"
      via: "callback prop"
      pattern: "onTaskNavigate\\(newTask\\.id\\)"
    - from: "TaskSearch.Input.onKeyDown"
      to: "handleCreateTask"
      via: "event handler"
      pattern: "ctrlKey.*metaKey.*Enter"
---

<objective>
Улучшить кнопку создания задачи в компоненте TaskSearch: изменить стиль на primary, текст на '+ Задача', добавить горячую клавишу Ctrl+Enter, сохранить фокус в поле ввода после создания, прокрутить и выделить новую задачу.

Purpose: Улучшение UX для быстрого создания задач без потери контекста работы.
Output: Обновлённый компонент TaskSearch с улучшенной кнопкой создания.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/src/components/TaskSearch.tsx
@packages/web/src/components/layout/ProjectMenu.tsx
@packages/web/src/stores/useUIStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить кнопку создания задачи</name>
  <files>packages/web/src/components/TaskSearch.tsx</files>
  <action>
    Внести следующие изменения в packages/web/src/components/TaskSearch.tsx:

    1. **Изменить стиль кнопки на primary** (строка 213-220):
       - Заменить `variant="ghost"` на `variant="default"` (или другой primary вариант в проекте)
       - Убрать кастомные цвета text-indigo-600, hover:text-indigo-700, hover:bg-indigo-50 (использовать стандартные primary стили)
       - Итоговый вид: синий фон, белый текст, стандартные hover состояния

    2. **Изменить текст кнопки** (строка 219):
       - Заменить `+ задача` на `+ Задача` (с большой буквы)

    3. **Добавить обработку Ctrl+Enter** (в onKeyDown обработчик Input, строка 139-154):
       - Добавить условие: `else if ((event.ctrlKey || event.metaKey) && event.key === 'Enter')`
       - Вызывать `handleCreateTask()`
       - НЕ вызывать `event.preventDefault()` чтобы не блокировать стандартное поведение

    4. **Обновить handleCreateTask** (строки 87-120):
       - После создания задачи и обновления стейтов:
         - Убрать вызов `clearSearch()` (строка 119) — курсор должен остаться в поле
         - Сохранить `newTask.id` в переменную
         - После успешного создания вызвать `onTaskNavigate(newTask.id)` для прокрутки
         - Добавить новую задачу в highlightedTaskIds через store (нужно будет добавить метод в useUIStore или использовать setSearchQuery для подсветки)

    5. **Добавить временное выделение новой задачи**:
       - В useUIStore добавить поле `tempHighlightedTaskId: string | null` и метод `setTempHighlightedTaskId`
       - В handleCreateTask вызвать `setTempHighlightedTaskId(newTask.id)`
       - В ProjectWorkspace использовать это поле в дополнение к highlightedSearchTaskIds
       - (Альтернатива: можно использовать setSearchQuery с id задачи чтобы временно подсветить её)

    Примечание: Если в проекте используется другой primary variant (например, "primary"), используй его вместо "default".
  </action>
  <verify>
    <automated>npm run build --workspace=packages/web</automated>
  </verify>
  <done>
    - Кнопка имеет primary стиль (синий фон, белый текст)
    - Текст кнопки: '+ Задача'
    - Ctrl+Enter создаёт задачу
    - После создания фокус остаётся в поле ввода (searchQuery не очищается)
    - После создания происходит прокрутка к новой задаче
    - Новая задача выделяется в графике
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Обновлённая кнопка создания задачи с primary стилем, текстом '+ Задача', поддержкой Ctrl+Enter, прокруткой и выделением новой задачи</what-built>
  <how-to-verify>
    1. Открой приложение в браузере
    2. Проверь визуальный стиль кнопки — должен быть синий фон, белый текст
    3. Нажми Ctrl+Enter в пустом поле — должна создаться задача "Новая задача"
    4. Введи текст "Тестовая задача" и нажми Ctrl+Enter — должна создаться задача с этим именем
    5. После каждого создания проверь:
       - Фокус остаётся в поле ввода
       - Происходит прокрутка к новой задаче
       - Новая задача выделяется в графике
    6. Проверь что обычный Enter по-прежнему работает для навигации по результатам поиска
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Проверка после завершения:
1. Билд проходит без ошибок
2. Кнопка имеет правильный primary стиль
3. Горячая клавиша Ctrl+Enter работает
4. Фокус сохраняется после создания
5. Прокрутка и выделение работают корректно
</verification>

<success_criteria>
- Кнопка создания задачи имеет primary стиль и текст '+ Задача'
- Ctrl+Enter создаёт задачу из текста в поле поиска
- После создания курсор остаётся в поле ввода
- Происходит прокрутка к созданной задаче
- Новая задача временно выделяется в графике
- Обычная навигация по результатам поиска не нарушена
</success_criteria>

<output>
After completion, create `.planning/quick/260321-m9d-primary-ctrl-enter/260321-m9d-SUMMARY.md`
</output>
