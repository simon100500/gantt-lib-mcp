---
phase: quick-260321-m63
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/TaskSearch.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Пользователь видит кнопку '+ задача' справа от поля поиска"
    - "При нажатии на кнопку создаётся новая задача в конце списка"
    - "Если поле поиска заполнено, новая задача получает это имя"
    - "Если поле поиска пустое, создаётся задача с именем 'Новая задача'"
    - "Для локального режима задача создаётся в localStorage"
    - "Для авторизованных пользователей задача сохраняется на сервере"
  artifacts:
    - path: "packages/web/src/components/TaskSearch.tsx"
      provides: "Компонент поиска с кнопкой создания задачи"
      contains: "Button with '+ задача' text"
  key_links:
    - from: "TaskSearch.tsx"
      to: "useTaskStore.setTasks or useTaskMutation.createTask"
      via: "onClick handler"
      pattern: "setTasks|createTask"
---

<objective>
Добавить кнопку "+ задача" справа от поля поиска для быстрого создания новых задач.

Purpose: Упростить создание задач — пользователь может ввести имя в поиск и нажать кнопку, вместо того чтобы заполнять форму.
Output: Обновлённый компонент TaskSearch с кнопкой создания задачи
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/src/components/TaskSearch.tsx
@packages/web/src/stores/useTaskStore.ts
@packages/web/src/stores/useAuthStore.ts
@packages/web/src/hooks/useTaskMutation.ts
@packages/web/src/types.ts

# Existing interfaces
```typescript
// From useAuthStore.ts
interface AuthState {
  isAuthenticated: boolean;
  accessToken: string | null;
}

// From useTaskStore.ts
interface TaskState {
  tasks: Task[];
  setTasks: (tasks: Task[] | ((prev: Task[]) => Task[])) => void;
}

// From useTaskMutation.ts
interface UseTaskMutationResult {
  createTask: (input: CreateTaskInput) => Promise<Task>;
}

// From types.ts
interface Task {
  id: string;
  name: string;
  startDate: string | Date;
  endDate: string | Date;
  color?: string;
  parentId?: string;
  progress?: number;
  dependencies?: TaskDependency[];
  sortOrder?: number;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add create task button to TaskSearch</name>
  <files>packages/web/src/components/TaskSearch.tsx</files>
  <action>
    1. Импортировать useAuthStore для доступа к accessToken:
       ```tsx
       import { useAuthStore } from '../stores/useAuthStore';
       import { useTaskMutation } from '../hooks/useTaskMutation';
       ```

    2. Добавить в компонент TaskSearch:
       ```tsx
       const accessToken = useAuthStore((state) => state.accessToken);
       const setTasks = useTaskStore((state) => state.setTasks);
       const tasks = useTaskStore((state) => state.tasks);
       const { createTask } = useTaskMutation(accessToken);
       ```

    3. Создать функцию handleCreateTask:
       ```tsx
       const handleCreateTask = async () => {
         const taskName = searchQuery.trim() || 'Новая задача';
         const today = new Date().toISOString().split('T')[0];

         const newTask: Task = {
           id: crypto.randomUUID(),
           name: taskName,
           startDate: today,
           endDate: today,
         };

         // Optimistic update
         setTasks(prev => [...prev, newTask]);

         // Server update for authenticated users
         if (accessToken) {
           try {
             const created = await createTask({
               name: taskName,
               startDate: today,
               endDate: today,
             });
             // Replace with server response
             setTasks(prev => prev.map(t => t.id === newTask.id ? created : t));
           } catch (error) {
             console.error('Failed to create task:', error);
             // Revert on error
             setTasks(prev => prev.filter(t => t.id !== newTask.id));
           }
         }

         // Clear search after creation
         clearSearch();
       };
       ```

    4. Добавить кнопку "+ задача" в DOM, рядом с полем поиска (после kbd):
       ```tsx
       {/* Кнопка создания задачи */}
       <Button
         variant="ghost"
         size="sm"
         onClick={handleCreateTask}
         className="h-7 px-2 text-xs font-medium text-indigo-600 hover:text-indigo-700 hover:bg-indigo-50"
       >
         + задача
       </Button>
       ```

    ВАЖНО: Не удалять существующий функционал поиска. Кнопка должна быть добавлена, а не заменять существующие элементы.
  </action>
  <verify>
    <automated>
      npm run build --workspace=packages/web
    </automated>
  </verify>
  <done>
    - Кнопка "+ задача" отображается справа от поля поиска
    - При нажатии создаётся новая задача
    - Если searchQuery заполнен, задача получает это имя
    - Если searchQuery пустой, задача называется "Новая задача"
    - Задача создаётся с сегодняшней датой (startDate = endDate = today)
    - Для авторизованных пользователей задача сохраняется на сервер
    - Для локального режима задача сохраняется в localStorage
    - После создания поле поиска очищается
  </done>
</task>

</tasks>

<verification>
1. Проверить что TypeScript компилируется без ошибок
2. Проверить что кнопка "+ задача" видна в UI
3. Проверить создание задачи с именем из поиска
4. Проверить создание задачи с именем "Новая задача" когда поиск пустой
5. Проверить что для авторизованных пользователей задача сохраняется на сервер
6. Проверить что для локального режима задача сохраняется в localStorage
</verification>

<success_criteria>
- Кнопка "+ задача" добавлена в TaskSearch компонент
- При нажатии создаётся новая задача в конце списка
- Имя задачи берётся из searchQuery или используется "Новая задача"
- Задача создаётся с сегодняшней датой
- Для авторизованных пользователей задача синхронизируется с сервером
- После создания поле поиска очищается
- Build проходит без ошибок
</success_criteria>

<output>
After completion, create `.planning/quick/260321-m63/260321-m63-SUMMARY.md`
</output>
