---
quick_id: "260417-wr7"
description: "Excel export for gantt chart with detail level selection"
mode: quick
date: 2026-04-17
---

# Quick Plan: Excel Export

## Task 1: Install xlsx + create export utility

**Files:**
- `packages/web/package.json` — add xlsx dependency
- `packages/web/src/lib/exportToExcel.ts` — NEW

**Action:**
- Install `xlsx` (SheetJS) library
- Create `exportToExcel(tasks, projectName, detailLevel)` function
- Detail levels: `brief` (#, Name, Start, End), `standard` (+Progress, Type, Parent), `full` (+Dependencies, SortOrder)
- Column headers in Russian
- Format dates as DD.MM.YYYY
- Auto-fit column widths

**Verify:** `exportToExcel` can be imported and generates valid xlsx blob

## Task 2: Create ExcelExportModal + wire into Toolbar/App

**Files:**
- `packages/web/src/components/ExcelExportModal.tsx` — NEW
- `packages/web/src/components/layout/Toolbar.tsx` — add button
- `packages/web/src/App.tsx` — wire handler + state

**Action:**
- Create modal with 3 radio options for detail level
- Add "Excel" button in Toolbar (next to PDF button, same pattern)
- Wire in App.tsx: state for modal visibility, handler calls exportToExcel
- Use same billing gate as PDF export

**Verify:** Button appears in toolbar, modal opens, selecting level generates and downloads .xlsx

## Task 3: Wire into workspaces

**Files:**
- `packages/web/src/components/workspace/ProjectWorkspace.tsx`
- `packages/web/src/components/workspace/GuestWorkspace.tsx`
- `packages/web/src/components/workspace/SharedWorkspace.tsx` (if needed)

**Action:**
- Pass `onExportExcel` through workspace props
- Follow same pattern as `onExportPdf`

**Verify:** Excel export works from all workspace contexts
