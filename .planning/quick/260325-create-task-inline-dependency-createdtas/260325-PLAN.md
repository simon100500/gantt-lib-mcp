# Quick Task 260325: Устранить лишний create_task при inline dependency из-за потери createdTaskId

**Created:** 2026-03-25
**Status:** Completed

## Task 1

- Files: `packages/mcp/agent/prompts/system.md`, `packages/server/src/agent.ts`
- Action: Уточнить prompt/runtime для последовательного создания подзадач так, чтобы dependency `taskId` брался только из реального tool result, без угадывания UUID.
- Verify: Проверить, что prompt/runtime явно требуют использовать точный `createdTaskId` из предыдущего `create_task` и запрещают speculative dependency при потере ID.
- Done: Добавлены жёсткие инструкции про reuse exact `createdTaskId`, запрет на выдуманные UUID и safe fallback при неуверенности в predecessor ID.

## Task 2

- Files: `packages/server/src/agent.test.ts`
- Action: Дополнить тесты на новое правило про exact `createdTaskId` и запрет выдумывания task IDs.
- Verify: `cmd /c npx tsc -p packages\server\tsconfig.json --noEmit`, `cmd /c npx tsc -p packages\mcp\tsconfig.json --noEmit`, `cmd /c npx tsc -p packages\mcp\agent\tsconfig.json --noEmit`, `node --test packages\server\dist\agent.test.js`.
- Done: Тест на system prompt расширен проверками для `exact createdTaskId` и запрета fabricated IDs; typecheck и compiled JS test прошли.
