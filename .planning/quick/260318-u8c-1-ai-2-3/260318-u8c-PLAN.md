---
phase: quick-260318-u8c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/components/ChatSidebar.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Высота таскбара над графиком равна высоте заголовка чата AI агент"
    - "При выборе проекта в сайдбаре - сайдбар скрывается"
    - "Сайдбар появляется/скрывается с анимацией"
  artifacts:
    - path: "packages/web/src/components/ChatSidebar.tsx"
      provides: "Заголовок чата с унифицированной высотой"
      contains: "h-12"
    - path: "packages/web/src/App.tsx"
      provides: "Обработчик переключения проекта с закрытием сайдбара"
      contains: "setProjectSidebarVisible(false)"
    - path: "packages/web/src/App.tsx"
      provides: "Анимация появления/скрытия сайдбара"
      contains: "transition-all duration-300 ease-in-out"
  key_links:
    - from: "ProjectSwitcher.onSwitch"
      to: "handleSwitchProject"
      via: "выбор проекта"
      pattern: "handleSwitchProject.*setProjectSidebarVisible\\(false\\)"
    - from: "Project sidebar aside"
      to: "CSS transition"
      via: "анимация"
      pattern: "transition-all.*duration-300"
---

<objective>
Косметические улучшения UI: унифицировать высоту панелей, добавить закрытие сайдбара при выборе проекта, добавить анимацию сайдбара.

Purpose: Улучшить визуальную согласованность и UX интерфейса
Output: Единообразный UI с плавными анимациями
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/src/App.tsx
@packages/web/src/components/ChatSidebar.tsx
@packages/web/src/components/ProjectSwitcher.tsx

**Текущее состояние:**
- Top Bar в App.tsx: h-12 (line 690)
- ChatSidebar header: h-11 (line 108 в ChatSidebar.tsx)
- Project sidebar: без анимации (line 658-684 в App.tsx)
- handleSwitchProject: не закрывает сайдбар (line 444-450 в App.tsx)

**Что нужно изменить:**
1. Унифицировать высоту: h-11 → h-12 в ChatSidebar.tsx
2. Добавить setProjectSidebarVisible(false) в handleSwitchProject
3. Добавить CSS transition для project sidebar
</context>

<tasks>

<task type="auto">
  <name>Task 1: Унифицировать высоту заголовков и добавить анимацию сайдбара</name>
  <files>packages/web/src/components/ChatSidebar.tsx, packages/web/src/App.tsx</files>
  <action>
    1. В ChatSidebar.tsx (line 108):
       - Изменить `className="flex h-11 shrink-0...` на `className="flex h-12 shrink-0...`

    2. В App.tsx (line 658):
       - Изменить `<aside className="w-60 shrink-0 border-r border-slate-200...`
       - На `<aside className="w-60 shrink-0 border-r border-slate-200 transition-all duration-300 ease-in-out...`

    Это унифицирует высоту заголовков (таскбар и чат) и добавит плавную анимацию появления/скрытия project sidebar.
  </action>
  <verify>
    <automated>grep -n "h-12" packages/web/src/components/ChatSidebar.tsx && grep -n "transition-all duration-300" packages/web/src/App.tsx | grep -i aside</automated>
  </verify>
  <done>ChatSidebar header имеет высоту h-12, project sidebar имеет CSS transition</done>
</task>

<task type="auto">
  <name>Task 2: Закрывать сайдбар при выборе проекта</name>
  <files>packages/web/src/App.tsx</files>
  <action>
    В функции handleSwitchProject (line 444-450):
    - Добавить `setProjectSidebarVisible(false);` после line 448
    - Перед `setWorkspace({ kind: 'project', projectId, chatOpen: false });`

    Это обеспечит автоматическое закрытие project sidebar при выборе проекта из списка.
  </action>
  <verify>
    <automated>grep -A 5 "handleSwitchProject = useCallback" packages/web/src/App.tsx | grep "setProjectSidebarVisible(false)"</automated>
  </verify>
  <done>При выборе проекта в ProjectSwitcher - project sidebar автоматически скрывается</done>
</task>

</tasks>

<verification>
1. Проверить визуально: высота таскбара и заголовка чата одинаковая
2. Проверить функционально: при клике на проект в сайдбаре - сайдбар плавно закрывается
3. Проверить анимацию: открытие/закрытие сайдбара происходит плавно (~300ms)
</verification>

<success_criteria>
- [ ] Высота ChatSidebar header изменена с h-11 на h-12
- [ ] Project sidebar имеет CSS transition (transition-all duration-300 ease-in-out)
- [ ] handleSwitchProject вызывает setProjectSidebarVisible(false)
- [ ] Визуально: таскбар и заголовок чата одинаковой высоты
- [ ] Функционально: выбор проекта скрывает сайдбар
- [ ] Анимация: открытие/закрытие плавное
</success_criteria>

<output>
After completion, create `.planning/quick/260318-u8c-1-ai-2-3/260318-u8c-01-SUMMARY.md`
</output>
