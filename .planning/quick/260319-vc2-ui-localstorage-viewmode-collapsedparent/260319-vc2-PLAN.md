---
phase: quick
plan: 260319-vc2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/stores/useUIStore.ts
  - packages/web/src/stores/useTaskStore.ts
  - packages/web/src/components/GanttChart.tsx
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
autonomous: true
requirements:
  - QUICK-001: Сохранить viewMode в localStorage
  - QUICK-002: Сохранить collapsedParentIds в localStorage
  - QUICK-003: Подключить collapsedParentIds к GanttChart

must_haves:
  truths:
    - "Пользователь переключает viewMode на 'неделя' → перезагружает страницу → viewMode остается 'неделя'"
    - "Пользователь сворачивает задачи → перезагружает страницу → задачи остаются свёрнутыми"
    - "В DevTools localStorage есть ключи gantt_view_mode и gantt_collapsed_parents"
  artifacts:
    - path: "packages/web/src/stores/useUIStore.ts"
      provides: "Persist для viewMode через zustand persist"
      contains: "persist({ name: 'gantt_view_mode' })"
    - path: "packages/web/src/stores/useTaskStore.ts"
      provides: "collapsedParentIds с persist"
      contains: "collapsedParentIds: Set<string>"
    - path: "packages/web/src/components/GanttChart.tsx"
      provides: "Проп collapsedParentIds для gantt-lib"
      exports: ["collapsedParentIds"]
  key_links:
    - from: "packages/web/src/stores/useUIStore.ts"
      to: "localStorage"
      via: "zustand persist middleware"
      pattern: "persist\\(\\{.*name:.*'gantt_view_mode'"
    - from: "packages/web/src/stores/useTaskStore.ts"
      to: "localStorage"
      via: "zustand persist middleware"
      pattern: "persist\\(\\{.*name:.*'gantt_collapsed_parents'"
    - from: "ProjectWorkspace.tsx"
      to: "GanttChart"
      via: "collapsedParentIds prop"
      pattern: "collapsedParentIds=\\{collapsedParentIds\\}"
---

<objective>
Сохранить состояние UI графика в localStorage (viewMode + collapsedParentIds)

Purpose: При перезагрузке страницы пользователи теряют настройки вида и сворачивания задач. Нужно сохранять эти настройки локально в браузере.
Output: viewMode и collapsedParentIds сохраняются в localStorage и восстанавливаются при загрузке
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/reference/ticklish-mapping-backus.md
@packages/web/src/stores/useUIStore.ts
@packages/web/src/stores/useTaskStore.ts
@packages/web/src/components/GanttChart.tsx
@packages/web/src/components/workspace/ProjectWorkspace.tsx

<interfaces>
<!-- Из useUIStore.ts -->
```typescript
export type ViewMode = 'day' | 'week' | 'month';

interface UIState {
  viewMode: ViewMode;
  setViewMode: (viewMode: ViewMode) => void;
  // ... другие поля
}
```

<!-- Из GanttChart.tsx -->
```typescript
export interface GanttChartProps {
  collapsedParentIds?: Set<string>;
  // ... другие пропы
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить persist для viewMode в useUIStore</name>
  <files>packages/web/src/stores/useUIStore.ts</files>
  <action>
1. Импортировать persist middleware из zustand:
   ```typescript
   import { create } from 'zustand';
   import { persist } from 'zustand/middleware';
   ```

2. Обернуть store в persist middleware для сохранения viewMode:
   ```typescript
   export const useUIStore = create<UIState>()(
     persist(
       (set) => ({
         // ... существующий код
       }),
       {
         name: 'gantt_ui_storage',
         partialize: (state) => ({
           viewMode: state.viewMode,
           showTaskList: state.showTaskList,
           autoSchedule: state.autoSchedule,
           highlightExpiredTasks: state.highlightExpiredTasks,
         }),
       }
     )
   );
   ```

Важно: использовать partialize чтобы сохранять только UI-настройки, а не всё состояние (workspace, modals и т.д. не должны персиститься).
  </action>
  <verify>
    <automated>cd packages/web && npm run build 2>&1 | grep -E "(error|Error|FAILED)" && exit 1 || echo "Build successful"</automated>
  </verify>
  <done>
    - viewMode сохраняется в localStorage ключ 'gantt_ui_storage'
    - При загрузке страницы viewMode восстанавливается из localStorage
    - workspace, modals и другие transient состояния НЕ сохраняются
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить collapsedParentIds в useTaskStore с persist</name>
  <files>packages/web/src/stores/useTaskStore.ts</files>
  <action>
1. Добавить импорт persist из zustand:
   ```typescript
   import { create } from 'zustand';
   import { persist } from 'zustand/middleware';
   ```

2. Добавить поле collapsedParentIds в интерфейс TaskState:
   ```typescript
   interface TaskState {
     // ... существующие поля
     collapsedParentIds: Set<string>;
     toggleCollapse: (parentId: string) => void;
     collapseAll: () => void;
     expandAll: () => void;
   }
   ```

3. Инициализировать collapsedParentIds и добавить методы управления:
   ```typescript
   export const useTaskStore = create<TaskState>()(
     persist(
       (set, get) => ({
         // ... существующие поля
         collapsedParentIds: new Set<string>(),
         toggleCollapse: (parentId) => {
           set((state) => {
             const newSet = new Set(state.collapsedParentIds);
             if (newSet.has(parentId)) {
               newSet.delete(parentId);
             } else {
               newSet.add(parentId);
             }
             return { collapsedParentIds: newSet };
           });
         },
         collapseAll: () => {
           const allParentIds = get().tasks
             .filter(t => t.parentId === null && get().tasks.some(c => c.parentId === t.id))
             .map(t => t.id);
           set({ collapsedParentIds: new Set(allParentIds) });
         },
         expandAll: () => set({ collapsedParentIds: new Set() }),
         // ... остальные методы
       }),
       {
         name: 'gantt_collapsed_parents',
         partialize: (state) => ({
           collapsedParentIds: Array.from(state.collapsedParentIds),
         }),
         merge: (persistedState: any, currentState) => ({
           ...currentState,
           ...persistedState,
           collapsedParentIds: new Set(persistedState.collapsedParentIds || []),
         }),
       }
     )
   );
   ```

Важно: Set не сериализуется в JSON напрямую, поэтому используем partialize для преобразования в массив, а merge для обратного преобразования в Set при загрузке.
  </action>
  <verify>
    <automated>cd packages/web && npm run build 2>&1 | grep -E "(error|Error|FAILED)" && exit 1 || echo "Build successful"</automated>
  </verify>
  <done>
    - collapsedParentIds сохраняется в localStorage как JSON-массив
    - При загрузке массив преобразуется обратно в Set
    - Методы toggleCollapse/collapseAll/expandAll работают корректно
  </done>
</task>

<task type="auto">
  <name>Task 3: Подключить collapsedParentIds к GanttChart и ProjectWorkspace</name>
  <files>packages/web/src/components/GanttChart.tsx, packages/web/src/components/workspace/ProjectWorkspace.tsx</files>
  <action>
1. В GanttChart.tsx добавьте обработчик onToggleCollapse (проп уже есть в интерфейсе, нужно реализацию):
   ```typescript
   export interface GanttChartProps {
     // ... существующие пропы
     collapsedParentIds?: Set<string>;
     onToggleCollapse?: (parentId: string) => void;
   }
   ```

   Передайте в GanttLibChart:
   ```typescript
   <GanttLibChart
     // ... существующие пропы
     collapsedParentIds={collapsedParentIds}
     onToggleCollapse={onToggleCollapse}
   />
   ```

2. В ProjectWorkspace.tsx:
   - Добавьте селекторы для collapsedParentIds и toggleCollapse:
     ```typescript
     const collapsedParentIds = useTaskStore((state) => state.collapsedParentIds);
     const toggleCollapse = useTaskStore((state) => state.toggleCollapse);
     ```

   - Передайте в GanttChart:
     ```typescript
     <GanttChart
       ref={ganttRef as Ref<GanttChartRef>}
       // ... существующие пропы
       collapsedParentIds={collapsedParentIds}
       onToggleCollapse={toggleCollapse}
     />
     ```

3. Обновите onCollapseAll/onExpandAll в Toolbar вызовы (Toolbar уже получает эти callbacks, нужно убедиться что они используют store):
   ```typescript
   const collapseAll = useTaskStore((state) => state.collapseAll);
   const expandAll = useTaskStore((state) => state.expandAll);
   ```
  </action>
  <verify>
    <automated>cd packages/web && npm run build 2>&1 | grep -E "(error|Error|FAILED)" && exit 1 || echo "Build successful"</automated>
  </verify>
  <done>
    - GanttChart получает collapsedParentIds из store
    - При клике на collapse/expand вызывается toggleCollapse
    - Кнопки "Свернуть все" / "Развернуть все" используют методы store
    - Состояние сворачивания сохраняется и восстанавливается при перезагрузке
  </done>
</task>

</tasks>

<verification>
1. Открыть DevTools → Application → Local Storage → проверить ключи:
   - `gantt_ui_storage` → содержит viewMode
   - `gantt_collapsed_parents` → содержит массив parentId

2. Тест viewMode:
   - Открыть график, переключить на "Неделя"
   - Перезагрузить страницу (F5)
   - Убедиться: viewMode остался "Неделя"

3. Тест collapsedParentIds:
   - Создать задачи с подзадачами
   - Свернуть несколько родительских задач
   - Перезагрузить страницу
   - Убедиться: задачи остаются свёрнутыми

4. Тест collapseAll/expandAll:
   - Нажать "Свернуть все"
   - Перезагрузить
   - Убедиться: все задачи свёрнуты
   - Нажать "Развернуть все"
   - Перезагрузить
   - Убедиться: все задачи развёрнуты
</verification>

<success_criteria>
- viewMode сохраняется в localStorage и восстанавливается при загрузке
- collapsedParentIds сохраняется в localStorage как JSON-массив
- При перезагрузке страницы сворачивание задач сохраняется
- Кнопки "Свернуть все" / "Развернуть все" работают
- Build проходит без ошибок: `npm run build --workspace=packages/web`
</success_criteria>

<output>
After completion, create `.planning/quick/260319-vc2-ui-localstorage-viewmode-collapsedparent/260319-vc2-SUMMARY.md`
</output>
