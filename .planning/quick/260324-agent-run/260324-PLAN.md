# Quick Task 260324: Снизить стоимость agent-run для структурных запросов на добавление нового блока с подзадачами

**Created:** 2026-03-25
**Status:** Completed

## Task 1

- Files: `packages/mcp/agent/prompts/system.md`, `packages/server/src/agent.ts`
- Action: Уточнить prompt/runtime так, чтобы при создании последовательных новых подзадач агент предпочитал передавать `dependencies` сразу в `create_task`, а для коротких запросов вида "отдельным блоком" сохранялся cheap/simple path без лишних reasoning-turn.
- Verify: Проверить, что `isSimpleMutationRequest` считает короткие structural block-запросы простыми, а prompt/runtime явно переводят `set_dependency` в fallback.
- Done: Prompt получил явные инструкции про inline dependencies и короткий reasoning path, runtime перенёс запросы "отдельным блоком" в простой mutation path и добавил ту же инструкцию в retry protocol.

## Task 2

- Files: `packages/server/src/agent.test.ts`, `packages/server/src/agent.ts`
- Action: Закрыть изменение тестами и устранить зависимость unit-тестов от Prisma/database env на import-уровне.
- Verify: `cmd /c npx tsc -p packages\server\tsconfig.json --noEmit`, `cmd /c npx tsc -p packages\mcp\tsconfig.json --noEmit`, `cmd /c npx tsc -p packages\mcp\agent\tsconfig.json --noEmit`, `node --test packages\server\dist\agent.test.js`.
- Done: Тесты расширены под новую инструкцию и эвристику, а runtime-зависимости `agent.ts` переведены на lazy import, чтобы helper-тесты не требовали `DATABASE_URL` при простом импорте модуля.
