---
phase: quick-260317-1qv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - UI-error-non-blocking
  - UI-error-compact-display
must_haves:
  truths:
    - "Frontend UI loads and displays even when task fetching fails with HTTP 500"
    - "Error message is shown in a compact, non-intrusive banner instead of full-screen overlay"
    - "Gantt chart renders with empty tasks array when API error occurs"
    - "User can interact with the UI (create new tasks, open chat, etc.) despite the error"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Non-blocking error display with graceful degradation"
      contains: "error && !loading"
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "Error display state"
      via: "Conditional error rendering"
      pattern: "error.*loading"
---

<objective>
Remove the intrusive full-screen "Не удалось загрузить задачи: Error: HTTP 500" message and replace it with a compact, non-blocking error banner. The frontend should load and display the Gantt chart UI even when the task API fails.

Purpose: Improve UX by not blocking the entire interface when the backend has issues. Users should still be able to see the UI and potentially create new tasks or use other features.
Output: Updated App.tsx with graceful error handling that doesn't block UI rendering.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/src/App.tsx
@packages/web/src/hooks/useTasks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace full-screen error with compact error banner</name>
  <files>packages/web/src/App.tsx</files>
  <action>
In packages/web/src/App.tsx, remove the full-screen error overlay (lines 642-653) and replace it with a compact error banner that appears within the layout.

Current code (lines 642-653):
```tsx
  // ── Error state ──────────────────────────────────────────────────────────
  if (error) {
    return (
      <div className="flex items-center justify-center h-screen bg-background">
        <div className="text-sm text-destructive bg-destructive/10 border border-destructive/20 rounded-lg px-4 py-3 max-w-sm text-center">
          {sharedProject.shareToken
            ? `Не удалось открыть ссылку: ${error}`
            : `Не удалось загрузить задачи: ${error}`}
        </div>
      </div>
    );
  }
```

Replace with (remove this entire early return block and instead add error banner in the layout):

Find the layout section (after line 655: `// ── Layout ───────────────────────────────────────────────────────────────`) and add an error banner right after the opening `<div className="flex h-screen bg-background overflow-hidden">`:

```tsx
  // ── Layout ───────────────────────────────────────────────────────────────
  return (
    <div className="flex h-screen bg-background overflow-hidden">
      {/* Error banner - non-blocking */}
      {error && (
        <div className="absolute top-0 left-0 right-0 z-50 flex items-center justify-center p-2">
          <div className="text-xs text-destructive bg-destructive/10 border border-destructive/20 rounded-lg px-3 py-1.5 max-w-md text-center shadow-sm">
            {sharedProject.shareToken
              ? `Не удалось открыть ссылку: ${error}`
              : `Не удалось загрузить задачи: ${error}`}
          </div>
        </div>
      )}
```

Key changes:
1. Remove the early return that blocks the entire UI
2. Add error banner as absolute positioned overlay at top of screen
3. Use `z-50` to ensure it's visible above other content
4. Use smaller text (`text-xs`) and padding for compact appearance
5. Add `shadow-sm` for subtle elevation
6. Center horizontally with `left-0 right-0 flex items-center justify-center`
7. UI continues to render below the error banner (Gantt chart shows with empty tasks)
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Full-screen error overlay removed
    - Compact error banner added at top of screen
    - UI renders normally even when error occurs
    - Error message still clearly visible but non-blocking
    - No TypeScript compilation errors
  </done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` in packages/web — no TypeScript errors
2. Simulate error scenario:
   - Stop the backend server or break the API endpoint
   - Load the frontend application
   - Verify error banner appears at top of screen
   - Verify Gantt chart UI still renders (empty state)
   - Verify user can interact with UI (menus, buttons, chat toggle)
</verification>

<success_criteria>
- No full-screen error overlay blocks the UI
- Compact error banner appears at top of screen when API fails
- Frontend loads and displays Gantt chart interface even with HTTP 500 error
- User can interact with UI elements despite the error
- Error message is still visible and informative
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/quick/260317-1qv-error-http-500/260317-1qv-SUMMARY.md`
</output>
