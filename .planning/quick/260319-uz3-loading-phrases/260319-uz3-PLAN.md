# Quick Task 260319-uz3: Loading Phrases — Plan

**Date:** 2026-03-19
**Status:** Ready for execution

## Objective

Add loading phrases from reference file to ChatSidebar and show them in random order (instead of sequential).

## Tasks

### Task 1: Merge phrases and implement random order

**Files:** `packages/web/src/components/ChatSidebar.tsx`

**Action:**
1. Add all phrases from `.planning/reference/loading-phrases.md` to `LOADING_PHRASES` array (combine with existing 6 phrases)
2. Change loading phrase logic from sequential to random:
   - Replace `(prev) => (prev + 1) % LOADING_PHRASES.length` with random selection
   - Use `Math.floor(Math.random() * LOADING_PHRASES.length)`

**Verify:**
- Phrases appear in random order (not sequential)
- All 67 phrases are in the array (6 existing + 61 from reference file)
- No duplicate phrases

**Done:**
- Loading phrases show in random order every 1.8 seconds
- Chat sidebar still works correctly

---

## Files to Read
- `.planning/reference/loading-phrases.md` — source of new phrases
- `packages/web/src/components/ChatSidebar.tsx` — target file to modify
