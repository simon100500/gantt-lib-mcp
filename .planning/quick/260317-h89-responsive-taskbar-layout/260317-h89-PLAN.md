---
phase: quick-260317-h89
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/App.tsx
autonomous: false
requirements: [RESPONSIVE-TOOLBAR]
must_haves:
  truths:
    - "Gantt toolbar does not overflow horizontally on screens 768-1280px wide"
    - "All toolbar functionality remains accessible via overflow menu on medium screens"
    - "Top bar (header) does not overflow on medium screens"
    - "On wide screens (>1280px) layout looks identical to current"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Responsive toolbar with overflow menu"
  key_links:
    - from: "overflow menu"
      to: "toolbar controls"
      via: "items hidden at md breakpoint move into DropdownMenu"
      pattern: "DropdownMenu.*MoreHorizontal|Ellipsis"
---

<objective>
Make the Gantt toolbar and top bar header responsive for medium-width screens (768-1280px). On narrower viewports, less-critical toolbar controls collapse into an overflow "three dots" menu. No mobile layout needed.

Purpose: Prevent horizontal overflow and clipped controls on medium screens.
Output: Updated App.tsx with responsive toolbar layout.
</objective>

<execution_context>
@C:/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/web/src/App.tsx
@packages/web/src/index.css
@packages/web/tailwind.config.js

Current layout analysis:
- **Top bar** (header, line ~689): `flex items-center gap-3 h-12 px-4` — single row with burger menu, logo, breadcrumb, share button, "+ New project", email dropdown. No wrapping or overflow logic.
- **Gantt toolbar** (line ~844): `flex items-center gap-1.5 h-11 px-4 flex-wrap` — contains: show/hide task list button, collapse/expand buttons, spacer, Today button, view mode split button (Day/Week/Month), two toggle switches (auto-schedule, highlight expired), AI assistant button, validation errors badge. Many items, no responsive breakpoints.
- **Status bar** (footer, line ~1027): `h-7` — compact, no issues.
- DropdownMenu from radix is already imported in App.tsx (used for account menu).
- `MoreHorizontal` or `Ellipsis` icon from lucide-react is NOT currently imported — needs adding.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive overflow menu to Gantt toolbar</name>
  <files>packages/web/src/App.tsx</files>
  <action>
Modify the Gantt toolbar section (line ~844, the `div` with `flex items-center gap-1.5 h-11 px-4`) to be responsive for medium screens.

**Strategy — use CSS `hidden`/`flex` with Tailwind breakpoints to show/hide items:**

1. Import `Ellipsis` (or `MoreHorizontal`) from `lucide-react` at the top of the file.

2. **Items that stay visible at all widths** (primary actions):
   - Show/hide task list button (PanelLeft)
   - Collapse/Expand buttons
   - Today button
   - View mode split button (Day/Week/Month)
   - AI assistant button (when visible)
   - Validation errors badge (when visible)

3. **Items that move into overflow menu below `xl` (1280px):**
   - "Закрепить связи" (auto-schedule) toggle switch
   - "Просроченные" (highlight expired) toggle switch

4. **Implementation approach:**
   - Wrap the two SwitchControl components and their surrounding ToolbarSep in a `div` with `hidden xl:flex items-center gap-2` — this hides them below 1280px.
   - Add an overflow DropdownMenu (three dots button) that is ONLY visible below 1280px: `<div className="xl:hidden">`. Inside this dropdown, render the same two toggles as DropdownMenuItem-like items with click handlers that toggle the respective state. Use a simple layout: label text + a small indicator (colored dot or checkmark) showing on/off state.
   - The overflow button: `Ellipsis` icon, styled similarly to other toolbar buttons (`h-7 px-2 rounded text-slate-500 hover:text-slate-800 hover:bg-slate-100`), with border like outline style.

5. **Top bar header adjustments** (line ~689):
   - Hide the text label "Войдите, чтобы сохранить график" on screens below `lg` (1024px): add `hidden lg:inline` to that `<span>`.
   - Hide the "+ Новый проект" button text below `lg`, show only "+" : Actually simpler — just hide below `md`: add `hidden md:inline-flex` to the Button.
   - On the share button, hide the text "Поделиться"/"Скопировано" below `lg` — keep only the icon. Wrap the text in `<span className="hidden lg:inline">`.
   - Truncate email in account dropdown more aggressively: change `max-w-[280px]` to `max-w-[180px] lg:max-w-[280px]`.

6. **Toolbar height on medium screens:**
   - Change toolbar container from `h-11` to `h-auto min-h-[2.75rem]` so if flex-wrap kicks in, it grows gracefully. Keep `flex-wrap` that is already there.

7. **Keep exact same appearance on xl+ screens** — the overflow menu is hidden, all controls are inline. This is purely additive responsive behavior.
  </action>
  <verify>
    <automated>cd packages/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TypeScript compiles without errors
    - On screens >= 1280px: layout identical to current (overflow menu hidden, all controls inline)
    - On screens 768-1279px: toggle switches hidden from toolbar, accessible via three-dots overflow menu
    - Top bar header items condense gracefully (share text hidden, email truncated, login prompt hidden on smaller)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive Gantt toolbar with overflow menu for medium screens</what-built>
  <how-to-verify>
    1. Run `cd packages/web && npm run dev`
    2. Open the app in browser with some tasks visible (so toolbar and status bar are shown)
    3. At full width (1280px+): verify everything looks exactly as before — all controls inline, no overflow menu visible
    4. Resize browser to ~1000px wide: verify the two toggle switches disappear from toolbar and a three-dots menu appears; click it to confirm toggles are accessible inside
    5. Resize to ~800px: verify top bar does not overflow — share button shows icon only, email is truncated, login text is hidden
    6. Verify all controls still function correctly through the overflow menu
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in packages/web
- Visual inspection at multiple viewport widths
</verification>

<success_criteria>
- Gantt toolbar has no horizontal overflow at 768-1280px
- Toggle switches accessible via overflow menu on medium screens
- Top bar condenses gracefully on medium screens
- Wide-screen layout unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260317-h89-responsive-taskbar-layout/260317-h89-SUMMARY.md`
</output>
