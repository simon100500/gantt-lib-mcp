---
phase: quick
plan: 260316-113
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/index.html
  - packages/web/src/index.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "All app text uses Roboto font"
    - "Logo text still uses Cascadia Mono (font-cascadia class is preserved)"
  artifacts:
    - path: "packages/web/index.html"
      provides: "Google Fonts preconnect + Roboto stylesheet link"
    - path: "packages/web/src/index.css"
      provides: "body font-family set to Roboto via @layer base"
  key_links:
    - from: "packages/web/index.html"
      to: "fonts.googleapis.com"
      via: "<link> tag loading Roboto 400/500/600"
    - from: "packages/web/src/index.css body rule"
      to: "Google Fonts Roboto"
      via: "font-family: 'Roboto', sans-serif"
---

<objective>
Add Roboto as the global UI font for the entire app, excluding the logo which already uses `.font-cascadia` (Cascadia Mono).

Purpose: Consistent, clean typography across all app text.
Output: Roboto loaded from Google Fonts, set as the base font on body. Logo unaffected.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Current font situation:
- `packages/web/src/index.css` — no font-family set on `body`, only `bg-background text-foreground` Tailwind classes
- `.font-cascadia` class uses `'Cascadia Mono', 'Consolas', monospace` — this is for the logo only
- `packages/web/index.html` — no Google Fonts link yet
- `packages/web/tailwind.config.js` — no `fontFamily` extension defined
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load Roboto from Google Fonts</name>
  <files>packages/web/index.html</files>
  <action>
    Add two `<link>` tags inside `<head>` before the closing tag, immediately after the manifest link:

    1. Preconnect to Google Fonts origins (speeds up font load):
    ```html
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    ```

    2. Load Roboto weights 400, 500, 600 (regular + medium + semi-bold, display=swap):
    ```html
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;600&display=swap" rel="stylesheet" />
    ```

    These go between the existing `<link rel="manifest">` line and `</head>`.
  </action>
  <verify>
    Open packages/web/index.html and confirm the three new link tags are present in `<head>`.
  </verify>
  <done>index.html contains preconnect links and Roboto stylesheet link</done>
</task>

<task type="auto">
  <name>Task 2: Set Roboto as global body font</name>
  <files>packages/web/src/index.css</files>
  <action>
    In the `@layer base` block, update the existing `body` rule to add `font-family`:

    ```css
    body {
      @apply bg-background text-foreground;
      font-family: 'Roboto', sans-serif;
    }
    ```

    Do NOT touch the `.font-cascadia` class — it must remain as-is for the logo.
    Do NOT add `font-family` to the `:root` block or anywhere else — body rule is sufficient.
  </action>
  <verify>
    Run `grep -n "Roboto" packages/web/src/index.css` — should return the font-family line in the body rule.
    Run `grep -n "font-cascadia" packages/web/src/index.css` — should still return the unchanged .font-cascadia class.
  </verify>
  <done>body rule includes font-family: 'Roboto', sans-serif; .font-cascadia class unchanged</done>
</task>

</tasks>

<verification>
- `grep "Roboto" packages/web/index.html` returns the googleapis link
- `grep "Roboto" packages/web/src/index.css` returns font-family in body rule
- `grep "font-cascadia" packages/web/src/index.css` still returns the Cascadia Mono declaration
- Dev server (`npm run dev` in packages/web) renders app text in Roboto, logo in monospace
</verification>

<success_criteria>
All UI text (headings, buttons, chat, labels) renders in Roboto. Logo text continues to render in Cascadia Mono.
</success_criteria>

<output>
After completion, create `.planning/quick/260316-113-roboto/260316-113-SUMMARY.md`
</output>
