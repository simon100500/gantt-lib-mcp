---
phase: 31-usage-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - packages/server/src/services/billing-service.ts
  - packages/server/src/routes/billing-routes.ts
  - packages/web/src/stores/useBillingStore.ts
autonomous: true
requirements:
  - TRK-03
must_haves:
  truths:
    - "Authenticated frontend code can request current usage through a dedicated GET /api/usage endpoint."
    - "The usage response includes plan identity plus per-limit used, remaining, and configured limit values."
    - "The dedicated usage endpoint reuses canonical server tracking semantics instead of re-deriving limits in the route."
  artifacts:
    - path: "packages/server/src/routes/billing-routes.ts"
      provides: "GET /api/usage route returning the dedicated usage payload"
    - path: "packages/server/src/services/billing-service.ts"
      provides: "Shared usage snapshot assembly for billing and dedicated usage APIs"
    - path: "packages/web/src/stores/useBillingStore.ts"
      provides: "Frontend action and types for consuming the dedicated usage payload"
  key_links:
    - from: "packages/server/src/routes/billing-routes.ts"
      to: "packages/server/src/services/billing-service.ts"
      via: "GET /api/usage delegates response assembly to a shared service method"
      pattern: "/api/usage|getUsage"
    - from: "packages/web/src/stores/useBillingStore.ts"
      to: "/api/usage"
      via: "authenticated fetchWithAuthRetry consumer for dedicated usage reads"
      pattern: "/api/usage"
---

<objective>
Expose dedicated usage access for TRK-03 so frontend consumers can fetch current usage through `GET /api/usage` without dragging Phase 32 guard enforcement into this phase.

Purpose: Add the roadmap-required route contract and a frontend/store access path while reusing the canonical server-side tracking logic built in Phase 30 and hardened in Plan 31-01.
Output: Dedicated usage API contract, shared server response assembly, and frontend store support for authenticated usage fetches.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-constraint-engine/30-03-SUMMARY.md
@.planning/phases/31-usage-tracking/31-01-PLAN.md
@packages/mcp/src/constraints/catalog.ts
@packages/server/src/services/billing-service.ts
@packages/server/src/routes/billing-routes.ts
@packages/server/src/middleware/subscription-middleware.ts
@packages/web/src/stores/useBillingStore.ts

<interfaces>
From packages/server/src/routes/billing-routes.ts:
```ts
fastify.get('/api/billing/subscription', { preHandler: [authMiddleware] }, async (req, reply) => { ... });
```

From packages/web/src/stores/useBillingStore.ts:
```ts
interface BillingActions {
  fetchSubscription(): Promise<void>;
  fetchPayments(): Promise<void>;
}
```

From packages/server/src/services/billing-service.ts:
```ts
export interface BillingSubscriptionStatus {
  plan: PlanId;
  planMeta: { id: PlanId; label: string; pricing: { monthly: number; yearly: number } };
  limits: Record<LimitKey, unknown>;
  usage: Record<LimitKey, ConstraintUsageSnapshot>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a dedicated usage snapshot contract on the server</name>
  <read_first>
    - packages/server/src/services/billing-service.ts
    - packages/server/src/routes/billing-routes.ts
    - packages/server/src/services/constraint-service.ts
    - packages/mcp/src/constraints/catalog.ts
    - .planning/phases/31-usage-tracking/31-01-PLAN.md
  </read_first>
  <files>packages/server/src/services/billing-service.ts, packages/server/src/routes/billing-routes.ts</files>
  <behavior>
    - Test 1: `GET /api/usage` is authenticated and returns the current plan identity.
    - Test 2: The payload contains canonical entries for `projects`, `ai_queries`, `archive`, `resource_pool`, and `export`.
    - Test 3: Each entry includes the current `usage` snapshot and `remaining` snapshot for that limit.
  </behavior>
  <action>
    Introduce a dedicated server-side usage response builder, preferably in `BillingService`, so both `/api/billing/subscription` and the new `/api/usage` route can reuse the same canonical assembly. The `/api/usage` payload must include `plan`, `planMeta`, `limits`, `usage`, and `remaining`, with canonical keys from `@gantt/mcp/constraints`. Keep compatibility fields like `aiUsed`, `aiLimit`, and `legacyLimits` out of this dedicated route unless they are explicitly required by a current consumer; this route should be the clean normalized contract for TRK-03.
  </action>
  <acceptance_criteria>
    - `packages/server/src/routes/billing-routes.ts` contains `fastify.get('/api/usage'`.
    - `packages/server/src/routes/billing-routes.ts` applies `{ preHandler: [authMiddleware] }` to `/api/usage`.
    - The `/api/usage` reply body includes exact keys `plan`, `planMeta`, `limits`, `usage`, and `remaining`.
    - `packages/server/src/routes/billing-routes.ts` does not add new Phase 32-style generic enforcement middleware to unrelated endpoints.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/server/tsconfig.json</automated>
  </verify>
  <done>The server exposes a dedicated authenticated usage contract that reuses canonical limit and usage assembly instead of building an ad-hoc route payload.</done>
</task>

<task type="auto">
  <name>Task 2: Wire frontend store access to the dedicated usage API</name>
  <read_first>
    - packages/web/src/stores/useBillingStore.ts
    - packages/server/src/routes/billing-routes.ts
    - packages/server/src/services/billing-service.ts
    - .planning/phases/30-constraint-engine/30-03-SUMMARY.md
  </read_first>
  <files>packages/web/src/stores/useBillingStore.ts, packages/server/src/routes/billing-routes.ts, packages/server/src/services/billing-service.ts</files>
  <action>
    Extend `useBillingStore` with explicit type support and an authenticated action for the dedicated `/api/usage` payload, reusing the existing `fetchWithAuthRetry` pattern from Phase 28. Keep the current `/api/billing/subscription` flow intact for billing-page compatibility, but make it possible for frontend code to fetch normalized usage directly through the new route. If the store keeps both subscription and usage state, ensure the normalized usage type is not shaped around `legacyLimits`.
  </action>
  <acceptance_criteria>
    - `packages/web/src/stores/useBillingStore.ts` contains the exact string `/api/usage`.
    - `packages/web/src/stores/useBillingStore.ts` exposes a dedicated usage fetch action or equivalent typed access path distinct from hard-coded reliance on `/api/billing/subscription`.
    - `packages/web/src/stores/useBillingStore.ts` declares `planMeta`, `limits`, `usage`, and `remaining` for the dedicated usage payload type.
    - No code in this task adds proactive UI disabling or new modal-trigger enforcement logic reserved for Phase 33.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/server/tsconfig.json && npx tsc -p packages/web/tsconfig.json</automated>
  </verify>
  <done>Frontend code has an authenticated, typed path to fetch dedicated usage data from `/api/usage`, while billing compatibility flows continue to work unchanged.</done>
</task>

</tasks>

<verification>
Compile server and web TypeScript after adding the dedicated route and store wiring. Confirm `/api/usage` is present, authenticated, normalized, and not overloaded with legacy compatibility or Phase 32 enforcement behavior.
</verification>

<success_criteria>
TRK-03 is satisfied when the repo contains an authenticated `GET /api/usage` route returning `plan`, `planMeta`, `limits`, `usage`, and `remaining`, and when frontend store code has a typed authenticated access path to that route.
</success_criteria>

<output>
After completion, create `.planning/phases/31-usage-tracking/31-02-SUMMARY.md`
</output>
