---
phase: 31-usage-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/services/constraint-service.ts
  - packages/server/src/services/constraint-service.test.ts
  - packages/server/src/services/billing-service.ts
  - packages/server/src/billing-service.test.ts
autonomous: true
requirements:
  - TRK-01
  - TRK-02
must_haves:
  truths:
    - "Free users read AI usage from a lifetime bucket capped at 20 total requests."
    - "Paid users read AI usage from a server-day bucket capped by the canonical plan catalog."
    - "Project usage reflects the current count of active projects per user instead of a cached counter."
    - "Legacy billing compatibility fields remain derived from canonical usage data rather than becoming a second source of truth."
  artifacts:
    - path: "packages/server/src/services/constraint-service.ts"
      provides: "Authoritative usage-read and increment semantics for projects and ai_queries"
    - path: "packages/server/src/services/constraint-service.test.ts"
      provides: "Server tests for free lifetime, paid daily, and active-project counting behavior"
    - path: "packages/server/src/services/billing-service.ts"
      provides: "Compatibility mapping from canonical usage snapshots to billing payload fields"
    - path: "packages/server/src/billing-service.test.ts"
      provides: "Tests for compatibility fields sourced from canonical usage snapshots"
  key_links:
    - from: "packages/server/src/services/billing-service.ts"
      to: "packages/server/src/services/constraint-service.ts"
      via: "getSubscriptionStatus() derives aiUsed/aiLimit and usage data from ConstraintService"
      pattern: "constraintService\\.(getUsage|getRemaining)"
    - from: "packages/server/src/services/constraint-service.ts"
      to: "packages/mcp/prisma/schema.prisma"
      via: "UsageCounter day/lifetime buckets plus prisma.project.count for active projects"
      pattern: "usageCounter|project\\.count"
---

<objective>
Make Phase 31 tracking semantics authoritative on the server for TRK-01 and TRK-02 without expanding Phase 32 enforcement scope.

Purpose: Lock AI-query and active-project tracking behavior to the canonical constraint engine, and make legacy billing fields compatibility-only instead of a second writable counter system.
Output: Constraint and billing services with explicit tracking semantics, compatibility boundaries, and automated coverage for free lifetime AI, paid daily AI, and active project counts.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-constraint-engine/30-CONTEXT.md
@.planning/phases/30-constraint-engine/30-VERIFICATION.md
@.planning/phases/30-constraint-engine/30-02-SUMMARY.md
@.planning/phases/30-constraint-engine/30-03-SUMMARY.md
@.planning/phases/28-billing/28-CONTEXT.md
@packages/mcp/src/constraints/catalog.ts
@packages/mcp/prisma/schema.prisma
@packages/server/src/services/constraint-service.ts
@packages/server/src/services/constraint-service.test.ts
@packages/server/src/services/billing-service.ts
@packages/server/src/billing-service.test.ts

<interfaces>
From packages/server/src/services/constraint-service.ts:
```ts
export interface ConstraintUsageSnapshot { ... }
export interface ConstraintRemainingSnapshot { ... }
export interface ConstraintCheckResult { ... }
export class ConstraintService {
  checkLimit(userId: string, limitKey: string): Promise<ConstraintCheckResult>;
  getUsage(userId: string, limitKey: string): Promise<ConstraintUsageSnapshot>;
  getRemaining(userId: string, limitKey: string): Promise<ConstraintRemainingSnapshot>;
  incrementUsage(userId: string, limitKey: string, amount?: number, now?: Date): Promise<ConstraintUsageSnapshot>;
}
```

From packages/server/src/services/billing-service.ts:
```ts
export interface BillingSubscriptionStatus {
  plan: PlanId;
  periodEnd: string | null;
  aiUsed: number;
  aiLimit: number;
  isActive: boolean;
  planMeta: { id: PlanId; label: string; pricing: { monthly: number; yearly: number } };
  limits: Record<LimitKey, unknown>;
  usage: Record<LimitKey, ConstraintUsageSnapshot>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tracking regression coverage around canonical AI and project usage</name>
  <read_first>
    - packages/server/src/services/constraint-service.test.ts
    - packages/server/src/services/constraint-service.ts
    - packages/server/src/billing-service.test.ts
    - packages/server/src/services/billing-service.ts
    - packages/mcp/src/constraints/catalog.ts
    - packages/mcp/prisma/schema.prisma
  </read_first>
  <files>packages/server/src/services/constraint-service.test.ts, packages/server/src/billing-service.test.ts</files>
  <behavior>
    - Test 1: free users read `ai_queries` from the `lifetime` bucket with `limit === 20`.
    - Test 2: paid users read `ai_queries` from `day:YYYY-MM-DD` buckets with the plan-specific daily limit.
    - Test 3: `projects` usage is sourced from `prisma.project.count({ where: { userId, status: 'active' } })`.
    - Test 4: billing compatibility fields `aiUsed` and `aiLimit` are derived from canonical `ai_queries` usage snapshots rather than legacy `Subscription.aiUsed`.
  </behavior>
  <action>
    Add or expand node:test coverage so Phase 31 semantics are locked before implementation changes. Cover both tracked AI bucket modes from TRK-01 and active-project counting from TRK-02. Add a billing-service regression that proves compatibility fields come from `ConstraintService` snapshots; do not add any new tests that bless writes into `Subscription.aiUsed`.
  </action>
  <acceptance_criteria>
    - `packages/server/src/services/constraint-service.test.ts` contains assertions for `periodBucket` values `lifetime` and `day:2026-04-02`.
    - `packages/server/src/services/constraint-service.test.ts` contains `project.count` with `status: 'active'`.
    - `packages/server/src/billing-service.test.ts` contains assertions for `aiUsed` and `aiLimit` derived from mocked or stubbed canonical usage.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c node --test packages/server/src/services/constraint-service.test.ts packages/server/src/billing-service.test.ts</automated>
  </verify>
  <done>Regression coverage fails if AI period-bucket semantics, active-project counting, or compatibility-field derivation regress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make billing compatibility explicitly derive from canonical tracking semantics</name>
  <read_first>
    - packages/server/src/services/billing-service.ts
    - packages/server/src/services/constraint-service.ts
    - packages/server/src/services/constraint-service.test.ts
    - packages/server/src/billing-service.test.ts
    - packages/mcp/src/constraints/catalog.ts
    - packages/mcp/prisma/schema.prisma
  </read_first>
  <files>packages/server/src/services/constraint-service.ts, packages/server/src/services/billing-service.ts, packages/server/src/services/constraint-service.test.ts, packages/server/src/billing-service.test.ts</files>
  <action>
    Implement whatever service refactor is required so the server has one authoritative path for Phase 31 tracking reads: `ConstraintService` remains the source for `projects` and `ai_queries`, and `BillingService` compatibility fields (`aiUsed`, `aiLimit`) are derived from those canonical snapshots. Be explicit in code comments or naming that `Subscription.aiUsed` stays legacy compatibility storage only for now; Phase 31 must not expand writes or reads that make it authoritative again. Keep scope limited to tracking semantics and compatibility assembly, not new middleware coverage or broader protected endpoint enforcement reserved for Phase 32.
  </action>
  <acceptance_criteria>
    - `packages/server/src/services/billing-service.ts` includes `constraintService.getUsage(userId, 'ai_queries')`.
    - `packages/server/src/services/billing-service.ts` does not read `sub.aiUsed` when computing `aiUsed` or `aiLimit`.
    - `packages/server/src/services/constraint-service.ts` still uses `prisma.project.count({ where: { userId, status: 'active' } })` for `projects`.
    - No new code path writes incremented AI usage into `Subscription.aiUsed`.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/server/tsconfig.json && node --test packages/server/src/services/constraint-service.test.ts packages/server/src/billing-service.test.ts</automated>
  </verify>
  <done>Server-side tracking semantics are authoritative, compatibility fields are derived from canonical usage, and the Phase 31 scope stays out of Phase 32 enforcement work.</done>
</task>

</tasks>

<verification>
Run the server TypeScript build and the focused server tests. Confirm the resulting code still models free AI usage as lifetime, paid AI usage as daily, active projects via current-count queries, and legacy billing fields as derived compatibility output only.
</verification>

<success_criteria>
TRK-01 and TRK-02 are satisfied when the codebase has automated proofs for lifetime-vs-daily AI usage semantics and active project counting, and the billing compatibility payload clearly derives from canonical usage snapshots rather than legacy counters.
</success_criteria>

<output>
After completion, create `.planning/phases/31-usage-tracking/31-01-SUMMARY.md`
</output>
