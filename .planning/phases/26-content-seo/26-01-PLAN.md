---
phase: 26-content-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/site/src/layouts/Layout.astro
  - packages/site/src/components/Header.astro
  - packages/site/src/components/Footer.astro
  - packages/site/src/pages/features.astro
  - packages/site/src/pages/faq.astro
autonomous: false
requirements:
  - CONTENT-01
  - CONTENT-02
  - CONTENT-07
user_setup: []

must_haves:
  truths:
    - Layout supports page-specific title, description, and Open Graph metadata instead of one hardcoded default
    - Header exposes /features and /faq in both desktop and mobile navigation
    - Footer only links to real site pages in the current roadmap scope
    - /features presents the product as AI-first, then interactive editing, dependencies, hierarchy, and read-only sharing
    - /features uses 5-7 large split sections rather than a dense feature-card grid
    - /faq contains 10 concise Russian-first answers covering product usage, onboarding, pricing status, and data/privacy topics
  artifacts:
    - path: packages/site/src/layouts/Layout.astro
      provides: Shared metadata contract for content pages
      contains: "<meta property=\"og:title\""
      exports: ["Layout"]
    - path: packages/site/src/components/Header.astro
      provides: Site navigation with discoverable content routes
      contains: "/features"
      contains: "/faq"
    - path: packages/site/src/components/Footer.astro
      provides: Footer links aligned to actual published pages
      contains: "/privacy"
      contains: "/terms"
    - path: packages/site/src/pages/features.astro
      provides: Product feature overview page
      contains: "ai.getgantt.ru"
    - path: packages/site/src/pages/faq.astro
      provides: FAQ page with concise answers
      contains: "Privacy Policy"
  key_links:
    - from: packages/site/src/pages/features.astro
      to: packages/site/src/layouts/Layout.astro
      via: "Page-level metadata props"
      pattern: "<Layout.*title=.*description="
    - from: packages/site/src/pages/faq.astro
      to: packages/site/src/layouts/Layout.astro
      via: "Page-level metadata props"
      pattern: "<Layout.*title=.*description="
    - from: packages/site/src/components/Header.astro
      to: packages/site/src/pages/features.astro
      via: "Primary site navigation"
      pattern: "href=\"/features\""
    - from: packages/site/src/components/Header.astro
      to: packages/site/src/pages/faq.astro
      via: "Primary site navigation"
      pattern: "href=\"/faq\""
---

<objective>
Create the user-facing content layer for Phase 26: shared metadata plumbing, real navigation to content pages, a dedicated /features page, and a concise /faq page.

*USE /web-design-guidlines SKILL*

Purpose: establish the product story and search/snippet metadata foundation before legal pages and crawler assets are added in the next wave.

Output: content routes users can navigate to, with Russian-first copy, distinct metadata, and no dead links in shared chrome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-content-seo/26-CONTEXT.md

@packages/site/src/layouts/Layout.astro
@packages/site/src/components/Header.astro
@packages/site/src/components/Footer.astro
@packages/site/src/pages/index.astro
@packages/site/src/styles/global.css

<content_rules>
- Russian-first copy and metadata
- Homepage remains demo-led; deep explanation belongs on /features
- /features should use large Google-style split sections, not a small-card grid
- Lead with AI chat and plan creation from text, then interactive editing
- Keep FAQ answers short and practical, usually 2-4 sentences
- Pricing is not defined yet; answer honestly without inventing a pricing page
- Data/privacy FAQ answers should summarize and route to /privacy for detail
</content_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Layout metadata contract for reusable SEO props</name>
  <files>packages/site/src/layouts/Layout.astro</files>
  <action>
    Replace the current title-only contract with a reusable metadata interface that content pages can pass explicitly.

    Requirements:
    1. Add props for `title`, `description`, and optional Open Graph overrides (`ogTitle`, `ogDescription`).
    2. Keep safe defaults for the homepage, but stop hardcoding one description for every route.
    3. Render at minimum:
       - `<title>`
       - `<meta name="description">`
       - `<meta property="og:title">`
       - `<meta property="og:description">`
       - `<meta property="og:type" content="website">`
       - `<meta property="og:locale" content="ru_RU">`
    4. Preserve the existing font and favicon setup.

    Do not add future-scope SEO work such as JSON-LD or alternate language tags in this phase.
  </action>
  <verify>
    <automated>rg -n "og:title|og:description|name=\\\"description\\\"" packages/site/src/layouts/Layout.astro</automated>
  </verify>
  <done>Layout accepts page metadata props and renders shared SEO tags for all content pages</done>
</task>

<task type="auto">
  <name>Task 2: Align header and footer navigation with Phase 26 routes</name>
  <files>packages/site/src/components/Header.astro, packages/site/src/components/Footer.astro</files>
  <action>
    Update shared navigation so users and crawlers can discover the new content pages naturally.

    Header:
    1. Add `/features` and `/faq` links to the desktop nav.
    2. Add the same links to the mobile drawer nav.
    3. Keep the current CTA to `https://ai.getgantt.ru/?auth=otp`.
    4. Highlight the active route using `currentPage`.

    Footer:
    1. Remove dead roadmap-external links such as `/about` and `/contact`.
    2. Keep or add links for `/features`, `/faq`, `/privacy`, and `/terms`.
    3. Keep copy tone and visual system consistent with the existing site.
  </action>
  <verify>
    <automated>rg -n 'href="/(features|faq|privacy|terms)"' packages/site/src/components/Header.astro packages/site/src/components/Footer.astro</automated>
  </verify>
  <done>Shared chrome exposes only real Phase 26 content routes</done>
</task>

<task type="auto">
  <name>Task 3: Build /features as the primary product story page</name>
  <files>packages/site/src/pages/features.astro</files>
  <action>
    Create `packages/site/src/pages/features.astro` using the shared Layout and existing design tokens.

    Content requirements:
    1. Include a strong page hero that frames the product around turning text into a working gantt plan.
    2. Build 5-7 large sections with one primary concept each. The first section must emphasize AI chat plus generating a plan from natural language.
    3. Cover these capabilities clearly:
       - AI assistant chat
       - plan creation from text
       - interactive gantt editing
       - dependencies and automatic rescheduling
       - task hierarchy and stages
       - read-only share links
    4. Keep the page product-led and persuasive, but avoid promising collaboration or pricing that do not exist yet.
    5. Add a clear CTA back to `ai.getgantt.ru`.

    Implementation guidance:
    - Prefer arrays + mapping for section data if it keeps copy maintainable.
    - Reuse existing utility classes and section spacing from the current site instead of creating a separate design language.
    - Set distinct title/description props on Layout for /features.
  </action>
  <verify>
    <automated>rg -n "ai.getgantt.ru|dependencies|read-only|иерарх|AI|чат" packages/site/src/pages/features.astro</automated>
  </verify>
  <done>/features explains the product with 6-ish substantial sections and distinct page metadata</done>
</task>

<task type="auto">
  <name>Task 4: Build /faq with 10 concise product-first questions</name>
  <files>packages/site/src/pages/faq.astro</files>
  <action>
    Create `packages/site/src/pages/faq.astro` using a maintainable question-and-answer data structure.

    Content requirements:
    1. Provide exactly 10 FAQ entries.
    2. Keep answers short and direct, usually 2-4 sentences.
    3. Cover these areas:
       - what the product does
       - how AI input works
       - whether manual editing is available
       - dependencies / rescheduling
       - onboarding speed
       - pricing status
       - what happens to data
       - privacy/legal routing
       - share links / read-only access
       - whether the site itself is the editor or a gateway to `ai.getgantt.ru`
    4. When data/privacy comes up, summarize briefly and point to `/privacy`.
    5. Set distinct title/description props on Layout for /faq.

    Presentation can be a simple stacked list or accordion, but avoid overengineering the interaction.
  </action>
  <verify>
    <automated>rg -c "<(section|details|article|li)\\b" packages/site/src/pages/faq.astro</automated>
  </verify>
  <done>/faq exists with 10 concise answers and page-specific metadata</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify discoverable content pages and metadata</name>
  <what-built>Shared navigation plus /features and /faq content routes</what-built>
  <how-to-verify>
    1. Start the site: `npm run dev -w packages/site`
    2. Open `http://localhost:4321/features`
    3. Confirm the page has:
       - a strong hero
       - 5-7 large content sections
       - AI-first positioning before interactive editing
       - CTA pointing to `ai.getgantt.ru`
    4. Open `http://localhost:4321/faq`
    5. Confirm the page contains 10 concise answers and no invented pricing promises
    6. Use the header and mobile menu to navigate between `/`, `/features`, and `/faq`
    7. Open page source for `/features` and `/faq` and confirm each page has distinct `<title>`, description, and OG tags
  </how-to-verify>
  <resume-signal>Type "approved" if the content pages and metadata look correct, or describe the gaps</resume-signal>
</task>

</tasks>

<verification>
- /features and /faq are reachable from shared navigation
- Shared layout renders distinct metadata per page
- /features reflects the locked product story from 26-CONTEXT.md
- /faq contains 10 concise answers without drifting into future-scope claims
- Footer links no longer point to missing routes
</verification>

<success_criteria>
- User can open /features and understand 6-8 core capabilities
- User can open /faq and find 10 practical answers
- Search/social metadata is distinct for the two pages
- Header and footer make the new content routes discoverable
</success_criteria>

<output>
After completion, create `.planning/phases/26-content-seo/26-01-SUMMARY.md`
</output>
