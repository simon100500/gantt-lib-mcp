---
phase: 26-content-seo
plan: 02
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - packages/site/src/pages/privacy.astro
  - packages/site/src/pages/terms.astro
  - packages/site/src/pages/sitemap.xml.ts
  - packages/site/public/robots.txt
autonomous: false
requirements:
  - CONTENT-03
  - CONTENT-04
  - CONTENT-05
  - CONTENT-06
  - CONTENT-07
user_setup: []

must_haves:
  truths:
    - /privacy exists and explains data/privacy handling in clear Russian-first language
    - /terms exists and explains service usage rules, limitations, and contact/legal framing without pretending to be a lawyer-only placeholder
    - sitemap.xml lists all shipped marketing pages in this milestone scope
    - robots.txt allows indexing of the marketing site and points crawlers to sitemap.xml
    - Privacy and Terms pages also use the shared metadata contract from Wave 1
  artifacts:
    - path: packages/site/src/pages/privacy.astro
      provides: Privacy Policy page
      contains: "/privacy"
    - path: packages/site/src/pages/terms.astro
      provides: Terms of Service page
      contains: "/terms"
    - path: packages/site/src/pages/sitemap.xml.ts
      provides: Generated sitemap endpoint for static output
      exports: ["GET"]
    - path: packages/site/public/robots.txt
      provides: Crawl rules for search engines
      contains: "Sitemap: https://getgantt.ru/sitemap.xml"
  key_links:
    - from: packages/site/public/robots.txt
      to: packages/site/src/pages/sitemap.xml.ts
      via: "Sitemap declaration"
      pattern: "Sitemap: https://getgantt.ru/sitemap.xml"
    - from: packages/site/src/pages/sitemap.xml.ts
      to: packages/site/src/pages/privacy.astro
      via: "Static route list entry"
      pattern: "/privacy"
    - from: packages/site/src/pages/sitemap.xml.ts
      to: packages/site/src/pages/terms.astro
      via: "Static route list entry"
      pattern: "/terms"
---

<objective>
Complete Phase 26 by publishing legal pages and the crawl/index artifacts search engines need: sitemap.xml and robots.txt.

Purpose: finish the content surface so the site is both user-readable and crawler-discoverable, building on the shared metadata/navigation foundation from 26-01.

Output: legal routes plus static SEO assets generated by the Astro site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-content-seo/26-CONTEXT.md
@.planning/phases/26-content-seo/26-01-SUMMARY.md

@packages/site/astro.config.js
@packages/site/src/layouts/Layout.astro
@packages/site/src/components/Footer.astro
@packages/site/src/pages/features.astro
@packages/site/src/pages/faq.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a clear /privacy page</name>
  <files>packages/site/src/pages/privacy.astro</files>
  <action>
    Create `packages/site/src/pages/privacy.astro` with Russian-first legal/product copy that matches the site voice.

    Content requirements:
    1. Explain what information the marketing site and app may process at a practical level.
    2. Cover at minimum:
       - contact/project text users submit to the product
       - account/auth basics if applicable
       - analytics/logging only if actually used or planned in current codebase wording
       - how users can contact the team about privacy questions
    3. Keep claims honest. Do not promise compliance processes or certifications that are not documented in the repo.
    4. Structure the content into readable sections with headings, not one long wall of text.
    5. Use distinct page metadata via Layout.
  </action>
  <verify>
    <automated>rg -n "данн|privacy|конфиденц|contact|email" packages/site/src/pages/privacy.astro</automated>
  </verify>
  <done>/privacy exists with structured, honest privacy copy and page-specific metadata</done>
</task>

<task type="auto">
  <name>Task 2: Create a clear /terms page</name>
  <files>packages/site/src/pages/terms.astro</files>
  <action>
    Create `packages/site/src/pages/terms.astro` with readable service terms aligned to the current product scope.

    Content requirements:
    1. Cover acceptable use, user responsibility for uploaded/generated plans, service availability caveats, and limitation of guarantees.
    2. Keep the language plain and direct instead of overly formal boilerplate.
    3. Do not invent billing/subscription clauses that do not exist yet.
    4. Include a contact path for legal or support questions.
    5. Use distinct page metadata via Layout.
  </action>
  <verify>
    <automated>rg -n "услов|responsib|доступ|гарант|contact|email" packages/site/src/pages/terms.astro</automated>
  </verify>
  <done>/terms exists with structured, current-scope terms and page-specific metadata</done>
</task>

<task type="auto">
  <name>Task 3: Generate sitemap.xml from the actual marketing routes</name>
  <files>packages/site/src/pages/sitemap.xml.ts</files>
  <action>
    Create an Astro endpoint at `packages/site/src/pages/sitemap.xml.ts` that emits XML for the static marketing pages shipped in v4.0.

    Requirements:
    1. Include exactly these routes unless implementation reveals an additional shipped route in packages/site:
       - /
       - /features
       - /faq
       - /privacy
       - /terms
    2. Use `https://getgantt.ru` as the base URL, consistent with `astro.config.js`.
    3. Return valid XML with `Content-Type: application/xml`.
    4. Keep the implementation simple and explicit; a static array is acceptable for this phase.

    Do not add future routes like `/templates` or app-domain URLs.
  </action>
  <verify>
    <automated>rg -n "/features|/faq|/privacy|/terms|application/xml" packages/site/src/pages/sitemap.xml.ts</automated>
  </verify>
  <done>sitemap.xml endpoint enumerates the published marketing pages only</done>
</task>

<task type="auto">
  <name>Task 4: Add robots.txt for indexable marketing pages</name>
  <files>packages/site/public/robots.txt</files>
  <action>
    Create `packages/site/public/robots.txt` with simple, explicit crawl rules for the marketing site.

    Requirements:
    1. Allow indexing of the marketing pages.
    2. Include `Sitemap: https://getgantt.ru/sitemap.xml`.
    3. Keep the file minimal and readable, for example:
       - `User-agent: *`
       - `Allow: /`
       - `Sitemap: https://getgantt.ru/sitemap.xml`

    Do not add app-domain disallow rules here; that belongs to the future app-domain SEO work, not this site package.
  </action>
  <verify>
    <automated>rg -n "User-agent: \\*|Allow: /|Sitemap: https://getgantt.ru/sitemap.xml" packages/site/public/robots.txt</automated>
  </verify>
  <done>robots.txt allows indexing and points crawlers to sitemap.xml</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify legal pages and crawl assets in a built site</name>
  <what-built>/privacy, /terms, sitemap.xml, and robots.txt</what-built>
  <how-to-verify>
    1. Run `npm run build -w packages/site`
    2. Inspect the build output or preview server for:
       - `/privacy`
       - `/terms`
       - `/sitemap.xml`
       - `/robots.txt`
    3. Open `/privacy` and `/terms` and confirm the copy is readable, structured, and free of invented pricing/billing clauses
    4. Open `/sitemap.xml` and confirm it lists `/`, `/features`, `/faq`, `/privacy`, and `/terms`
    5. Open `/robots.txt` and confirm it includes `Allow: /` plus the sitemap URL
    6. View source on `/privacy` and `/terms` to confirm each page has distinct title, description, and OG tags
  </how-to-verify>
  <resume-signal>Type "approved" if the legal pages and crawl assets look correct, or describe the gaps</resume-signal>
</task>

</tasks>

<verification>
- /privacy and /terms are reachable and readable
- sitemap.xml contains only shipped marketing routes
- robots.txt points to sitemap.xml and allows indexing
- Legal pages inherit distinct metadata from the shared Layout contract
</verification>

<success_criteria>
- User can read Privacy Policy on /privacy
- User can read Terms of Service on /terms
- Search engines can discover all marketing pages through sitemap.xml
- Search engines can read crawl rules through robots.txt
- All new pages have distinct title/description/OG metadata
</success_criteria>

<output>
After completion, create `.planning/phases/26-content-seo/26-02-SUMMARY.md`
</output>
