---
phase: 44-undo-redo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/types.ts
  - packages/mcp/prisma/schema.prisma
  - packages/mcp/prisma/migrations/20260417190000_add_mutation_groups_and_event_history/migration.sql
  - packages/mcp/src/services/command.service.ts
  - packages/mcp/src/services/command-history.test.ts
autonomous: true
requirements:
  - HIS-01

must_haves:
  truths:
    - "Every authoritative command commit can belong to one user-visible MutationGroup with stable ordering and undo metadata"
    - "Accepted history-aware commands persist enough inverse information to undo safely without reading patches as the rollback source"
    - "Group version boundaries stay aligned with the existing optimistic concurrency and Project.version contract"
  artifacts:
    - path: "packages/mcp/prisma/schema.prisma"
      provides: "MutationGroup model and ProjectEvent history fields"
      contains: "model MutationGroup"
    - path: "packages/mcp/src/types.ts"
      provides: "Shared history request, response, and record contracts"
      exports: ["MutationGroupRecord", "HistoryGroupContext", "ProjectEventInverseCommand", "CommitProjectCommandRequest"]
    - path: "packages/mcp/src/services/command.service.ts"
      provides: "History-aware authoritative commit path with inverse-command persistence"
      exports: ["CommandService"]
  key_links:
    - from: "packages/mcp/src/services/command.service.ts"
      to: "packages/mcp/prisma/schema.prisma"
      via: "MutationGroup creation/finalization and ProjectEvent persistence"
      pattern: "mutationGroup|projectEvent"
    - from: "packages/mcp/src/services/command.service.ts"
      to: "packages/mcp/src/types.ts"
      via: "Commit request history metadata and inverse-command typing"
      pattern: "history"
---

<objective>
Lay the history data model under the existing authoritative command pipeline.

Purpose: undo/redo is unsafe until grouped operations, event ordering, and inverse-command persistence are first-class parts of `CommandService.commitCommand`.
Output: shared history contracts, Prisma schema/migration for grouped history, and a command commit path that persists mutation-group and inverse-command metadata.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-undo-redo/44-CONTEXT.md
@.planning/reference/history-undo-redo-prd.md
@packages/mcp/prisma/schema.prisma
@packages/mcp/src/types.ts
@packages/mcp/src/services/command.service.ts
@.planning/phases/36-unified-scheduling-core/36-04-SUMMARY.md

<interfaces>
From packages/mcp/src/types.ts:
```typescript
export type CommitProjectCommandRequest = {
  projectId: string;
  clientRequestId: string;
  baseVersion: number;
  command: ProjectCommand;
};

export type ProjectEventRecord = {
  id: string;
  projectId: string;
  baseVersion: number;
  version: number;
  command: ProjectCommand;
  result: {
    changedTaskIds: string[];
    changedDependencyIds: string[];
    conflicts: Conflict[];
  };
  patches: Patch[];
};
```

From packages/mcp/src/services/command.service.ts:
```typescript
async commitCommand(
  request: CommitProjectCommandRequest,
  actorType: ActorType,
  actorId?: string,
): Promise<CommitProjectCommandResponse>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grouped-history schema and shared contracts</name>
  <files>packages/mcp/src/types.ts, packages/mcp/prisma/schema.prisma, packages/mcp/prisma/migrations/20260417190000_add_mutation_groups_and_event_history/migration.sql</files>
  <read_first>
    packages/mcp/src/types.ts
    packages/mcp/prisma/schema.prisma
    .planning/phases/44-undo-redo/44-CONTEXT.md
    .planning/reference/history-undo-redo-prd.md
    .planning/phases/36-unified-scheduling-core/36-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "model MutationGroup" packages/mcp/prisma/schema.prisma` finds the new Prisma model
    - `rg -n "groupId|ordinal|inverseCommand|metadata|requestContextId" packages/mcp/prisma/schema.prisma` finds all required `ProjectEvent` fields
    - `rg -n "export type MutationGroupOrigin = 'user_ui' \\| 'agent_run' \\| 'system' \\| 'undo' \\| 'redo'" packages/mcp/src/types.ts` finds the locked origin enum
    - `rg -n "export type HistoryGroupContext" packages/mcp/src/types.ts` finds the commit-path history metadata contract
    - `rg -n "CREATE TABLE \"mutation_groups\"|ALTER TABLE \"project_events\"" packages/mcp/prisma/migrations/20260417190000_add_mutation_groups_and_event_history/migration.sql` finds the migration statements
  </acceptance_criteria>
  <action>
    Implement HIS-01 contract-first.

    In `packages/mcp/src/types.ts`, add these exact shared history contracts:
    - `MutationGroupOrigin = 'user_ui' | 'agent_run' | 'system' | 'undo' | 'redo'`
    - `MutationGroupStatus = 'applied' | 'undone'`
    - `HistoryGroupContext = { groupId: string; origin: MutationGroupOrigin; title: string; requestContextId?: string; finalizeGroup: boolean; redoOfGroupId?: string | null; targetGroupId?: string | null; }`
    - `ProjectEventInverseCommand = ProjectCommand | null`
    - `MutationGroupRecord = { id: string; projectId: string; baseVersion: number; newVersion: number | null; actorType: ActorType; actorId?: string; origin: MutationGroupOrigin; title: string; status: MutationGroupStatus; undoable: boolean; undoneByGroupId?: string | null; redoOfGroupId?: string | null; createdAt: string; }`
    - extend `CommitProjectCommandRequest` with `history?: HistoryGroupContext`
    - extend `ProjectEventRecord` with `groupId?: string`, `ordinal?: number`, `inverseCommand?: ProjectEventInverseCommand`, `metadata?: JsonValue`, `requestContextId?: string`

    In `packages/mcp/prisma/schema.prisma`, add:
    - enum `MutationGroupOrigin` with `user_ui`, `agent_run`, `system`, `undo`, `redo`
    - enum `MutationGroupStatus` with `applied`, `undone`
    - model `MutationGroup` with fields `id`, `projectId`, `baseVersion`, `newVersion`, `actorType`, `actorId`, `origin`, `title`, `status`, `undoable`, `undoneByGroupId`, `redoOfGroupId`, `createdAt`
    - relation from `Project` to `MutationGroup`
    - relation from `ProjectEvent.groupId` to `MutationGroup.id`
    - `ProjectEvent` fields `groupId`, `ordinal`, `inverseCommand`, `metadata`, `requestContextId`

    In `packages/mcp/prisma/migrations/20260417190000_add_mutation_groups_and_event_history/migration.sql`, create the `mutation_groups` table, backfill-free nullable columns on `project_events`, foreign keys, and indexes on:
    - `mutation_groups(project_id, created_at desc)`
    - `mutation_groups(project_id, new_version desc)`
    - `project_events(group_id, ordinal)`
    - `project_events(request_context_id)`

    Keep `Project.version`, `ProjectEvent`, and the current command pipeline in place. Do not introduce snapshots-by-version, branching history, or checkpoint tables.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/mcp</automated>
  </verify>
  <done>
    Shared history contracts exist, Prisma can represent grouped event history, and the schema exposes exactly the fields required by the Phase 44 PRD.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist mutation-group ordering and inverse commands in `CommandService.commitCommand`</name>
  <files>packages/mcp/src/services/command.service.ts, packages/mcp/src/services/command-history.test.ts</files>
  <read_first>
    packages/mcp/src/services/command.service.ts
    packages/mcp/src/types.ts
    packages/mcp/prisma/schema.prisma
    .planning/phases/44-undo-redo/44-CONTEXT.md
    .planning/reference/history-undo-redo-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "request\\.history|history\\?:" packages/mcp/src/services/command.service.ts` finds the new history-aware request handling
    - `rg -n "inverseCommand" packages/mcp/src/services/command.service.ts` finds persisted inverse-command generation
    - `rg -n "ordinal" packages/mcp/src/services/command.service.ts` finds per-group ordering
    - `rg -n "mutationGroup\\.create|mutationGroup\\.update|projectEvent\\.create" packages/mcp/src/services/command.service.ts` finds group create/finalize persistence
    - `rg -n "delete_task|delete_tasks|reparent_task|reorder_tasks|create_dependency|remove_dependency" packages/mcp/src/services/command-history.test.ts` finds inverse coverage for destructive and structural commands
  </acceptance_criteria>
  <action>
    Extend `packages/mcp/src/services/command.service.ts` so `commitCommand(...)` remains the only authoritative mutation boundary but becomes history-aware.

    Add these exact behaviors:
    1. If `request.history` is absent, synthesize a single-command group with:
       - `origin: 'system'`
       - `title: command.type`
       - `finalizeGroup: true`
       - generated `groupId`
    2. If `request.history` is present:
       - create the `MutationGroup` row on first event with `baseVersion = request.baseVersion`
       - assign `ordinal = previous max ordinal + 1` within the same `groupId`
       - keep `MutationGroup.newVersion = null` until the event with `finalizeGroup: true`
       - finalize the group by setting `newVersion = newVersion`, `status = 'applied'`, and `undoable = true | false` depending on whether every accepted event in that group has a non-null `inverseCommand`
    3. Persist `requestContextId` from `request.history.requestContextId` onto every `ProjectEvent`
    4. Compute and persist typed `inverseCommand` for these command types:
       - `move_task`, `resize_task`, `set_task_start`, `set_task_end`, `change_duration`
       - `update_task_fields`, `update_tasks_fields_batch`
       - `create_task`, `create_tasks_batch`
       - `delete_task`, `delete_tasks`
       - `create_dependency`, `remove_dependency`, `change_dependency_lag`
       - `reparent_task`, `reorder_tasks`
    5. For `delete_task` and `delete_tasks`, capture enough before-context to recreate:
       - deleted task rows
       - `parentId`
       - `sortOrder`
       - dependency rows involving the deleted tasks
    6. Store this delete before-context in `ProjectEvent.metadata` and make `inverseCommand` a concrete recreate/update command sequence encoded as a typed batch-friendly `ProjectCommand`, not a patch diff.

    Add `packages/mcp/src/services/command-history.test.ts` that proves:
    - a finalized group sets `baseVersion` from the first event and `newVersion` from the last accepted event
    - two commands in one group get ordinals `1` then `2`
    - `delete_task` and `delete_tasks` persist non-null `inverseCommand` plus delete metadata
    - a group with any null inverse command finalizes as `undoable = false`

    Do not create a parallel executor. Do not rewrite old events during undo preparation. Do not use `patches` as the rollback source.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/mcp/src/services/command-history.test.ts && npm run build -w packages/mcp</automated>
  </verify>
  <done>
    `CommandService.commitCommand` persists grouped history metadata and typed inverse commands while preserving the existing version-conflict and atomic commit semantics.
  </done>
</task>

</tasks>

<verification>
Confirm that command commits still go only through `CommandService.commitCommand`, but now every accepted event can be grouped, ordered, and marked undoable from persisted inverse-command data.
</verification>

<success_criteria>
Phase 44 has the minimum safe backend substrate: grouped operations, event order, inverse-command persistence, and version-aligned finalization all exist on the current command pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/44-undo-redo/44-01-SUMMARY.md`
</output>
