---
phase: 44-undo-redo
plan: 03
type: execute
wave: 2
depends_on:
  - "44-01"
files_modified:
  - packages/server/src/mutation/types.ts
  - packages/server/src/mutation/orchestrator.ts
  - packages/server/src/agent.ts
  - packages/server/src/agent.test.ts
  - packages/web/src/types.ts
  - packages/web/src/hooks/useCommandCommit.ts
  - packages/web/src/hooks/useProjectCommands.ts
autonomous: true
requirements:
  - HIS-04

must_haves:
  truths:
    - "One agent-visible turn is recorded as one MutationGroup with one shared requestContextId"
    - "Manual web mutations carry stable group metadata through the same authoritative commit route"
    - "History titles and undoability state are explicit instead of inferred from raw event rows"
  artifacts:
    - path: "packages/server/src/agent.ts"
      provides: "Agent run-scoped history group propagation and title generation"
      exports: ["runAgentWithHistory"]
    - path: "packages/server/src/mutation/orchestrator.ts"
      provides: "Staged mutation execution that preserves one groupId/requestContextId across a run"
      exports: ["runStagedMutation"]
    - path: "packages/web/src/hooks/useCommandCommit.ts"
      provides: "Web commit metadata propagation for user-visible mutation groups"
      exports: ["useCommandCommit"]
  key_links:
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/mutation/orchestrator.ts"
      via: "shared groupId/requestContextId on staged mutation input"
      pattern: "groupId|requestContextId"
    - from: "packages/web/src/hooks/useCommandCommit.ts"
      to: "packages/mcp/src/types.ts"
      via: "history metadata on CommitProjectCommandRequest"
      pattern: "history"
---

<objective>
Propagate grouped history metadata through agent and web mutation entrypoints.

Purpose: undoing "the last AI action" or "my last drag/edit" only works if both channels describe one user-visible operation consistently when they call the authoritative commit path.
Output: agent-run grouping, stable titles/requestContextIds, and web commit hooks that send MutationGroup metadata on manual mutations.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-undo-redo/44-CONTEXT.md
@.planning/reference/history-undo-redo-prd.md
@packages/server/src/agent.ts
@packages/server/src/mutation/types.ts
@packages/server/src/mutation/orchestrator.ts
@packages/web/src/hooks/useCommandCommit.ts
@packages/web/src/hooks/useProjectCommands.ts
@packages/web/src/types.ts
@.planning/phases/42-mcp-mutation-refactor/42-03-SUMMARY.md

<interfaces>
From packages/server/src/mutation/types.ts:
```typescript
export type MutationOrchestrationResult = {
  handled: boolean;
  status: MutationExecutionStatus;
  intent: MutationIntent;
  executionMode: MutationExecutionMode;
  result: MutationExecutionResult;
};
```

From packages/web/src/types.ts:
```typescript
export type FrontendProjectCommand =
  | { type: 'move_task'; taskId: string; startDate: string; }
  | { type: 'update_task_fields'; taskId: string; fields: { name?: string; color?: string | null; progress?: number; parentId?: string | null; dependencies?: TaskDependency[]; }; }
  | { type: 'create_task'; task: Omit<Task, 'id'> & { id?: string }; }
  | { type: 'delete_task'; taskId: string; };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make one agent turn map to one mutation group with stable titles</name>
  <files>packages/server/src/mutation/types.ts, packages/server/src/mutation/orchestrator.ts, packages/server/src/agent.ts, packages/server/src/agent.test.ts</files>
  <read_first>
    packages/server/src/agent.ts
    packages/server/src/agent.test.ts
    packages/server/src/mutation/types.ts
    packages/server/src/mutation/orchestrator.ts
    .planning/phases/44-undo-redo/44-CONTEXT.md
    .planning/reference/history-undo-redo-prd.md
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "groupId|requestContextId|historyTitle" packages/server/src/mutation/types.ts packages/server/src/mutation/orchestrator.ts packages/server/src/agent.ts` finds shared grouping fields
    - `rg -n "origin: 'agent_run'" packages/server/src/agent.ts packages/server/src/mutation/orchestrator.ts` finds the agent history origin
    - `rg -n "AI — " packages/server/src/agent.ts` finds the human-readable title prefix
    - `rg -n "one agent turn|groupId|requestContextId|undoable" packages/server/src/agent.test.ts` finds the grouping coverage
  </acceptance_criteria>
  <action>
    Implement HIS-04 for the agent channel.

    In `packages/server/src/mutation/types.ts`, extend the staged mutation input/result contracts with:
    - `groupId: string`
    - `requestContextId: string`
    - `historyTitle: string`
    - `historyUndoable: boolean`

    In `packages/server/src/agent.ts`:
    - generate one `requestContextId = runId`
    - generate one `groupId = crypto.randomUUID()` for the whole run before staged mutation execution
    - generate `historyTitle` with these exact prefixes:
      - successful AI mutation: `AI — `
      - controlled non-undoable agent mutation: `AI — Неотменяемое действие`
    - pass `groupId`, `requestContextId`, and `historyTitle` into `runStagedMutation(...)`
    - when a staged agent run is not safely undoable, set `historyUndoable = false` instead of silently presenting it as normal undoable history

    In `packages/server/src/mutation/orchestrator.ts`, ensure every deterministic/hybrid command commit for one run reuses the same `groupId`, `requestContextId`, and `origin: 'agent_run'`, and only the last committed command sets `finalizeGroup: true`.

    Add `packages/server/src/agent.test.ts` coverage proving:
    - one multi-command staged mutation run reuses one `groupId`
    - the last command in the run finalizes the group
    - the request context equals the run-scoped context
    - the stored title starts with `AI — `
    - non-undoable staged failures are marked explicitly rather than silently falling back to undoable history
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/mutation/orchestrator.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Agent runs now produce one user-visible history group with stable identity, stable request context, and explicit undoability semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Propagate manual web mutation groups through the command commit hooks</name>
  <files>packages/web/src/types.ts, packages/web/src/hooks/useCommandCommit.ts, packages/web/src/hooks/useProjectCommands.ts</files>
  <read_first>
    packages/web/src/types.ts
    packages/web/src/hooks/useCommandCommit.ts
    packages/web/src/hooks/useProjectCommands.ts
    packages/mcp/src/types.ts
    .planning/phases/44-undo-redo/44-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "history\\?: \\{|groupId|requestContextId|finalizeGroup" packages/web/src/types.ts packages/web/src/hooks/useCommandCommit.ts` finds the web history metadata contract
    - `rg -n "origin: 'user_ui'" packages/web/src/hooks/useCommandCommit.ts packages/web/src/hooks/useProjectCommands.ts` finds the manual-edit origin
    - `rg -n "historyTitle|title:" packages/web/src/hooks/useProjectCommands.ts` finds stable user-visible titles
    - `rg -n "commitCommand\\(command, history" packages/web/src/hooks/useProjectCommands.ts packages/web/src/hooks/useCommandCommit.ts` finds grouped commit propagation
  </acceptance_criteria>
  <action>
    Extend the web commit hooks so manual edits participate in grouped history through the same command endpoint.

    In `packages/web/src/types.ts`, add:
    - `FrontendHistoryGroupContext = { groupId: string; origin: 'user_ui'; title: string; requestContextId: string; finalizeGroup: boolean; }`

    In `packages/web/src/hooks/useCommandCommit.ts`:
    - change `commitCommand(command)` to `commitCommand(command, history?)`
    - include `history` in the POST body to `/api/commands/commit`
    - keep the existing queueing and version-conflict retry behavior unchanged

    In `packages/web/src/hooks/useProjectCommands.ts`, create concrete titles for manual operations:
    - create task: `Пользователь — Создал задачу`
    - delete task: `Пользователь — Удалил задачу`
    - rename/update metadata: `Пользователь — Изменил задачу`
    - move/resize/reparent/reorder/dependency change: `Пользователь — Изменил график`

    Use these exact grouping rules:
    - one single-command manual action gets one generated `groupId`, one generated `requestContextId`, and `finalizeGroup: true`
    - multi-command `applyTaskChanges(...)` reuses one generated `groupId` and `requestContextId` across the whole command loop and marks only the last command `finalizeGroup: true`

    Do not create a separate manual history API. Do not bypass the existing `/api/commands/commit` route.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    Manual web edits now carry grouped history metadata through the existing authoritative commit hook, making them undoable as one user-visible operation.
  </done>
</task>

</tasks>

<verification>
Confirm that agent and web mutations both send stable group metadata through the same commit path, and that group finalization happens only once per user-visible operation.
</verification>

<success_criteria>
The backend no longer infers user-visible actions from raw events alone: both AI turns and manual web edits explicitly create grouped history units suitable for undo.
</success_criteria>

<output>
After completion, create `.planning/phases/44-undo-redo/44-03-SUMMARY.md`
</output>
