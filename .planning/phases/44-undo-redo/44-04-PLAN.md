---
phase: 44-undo-redo
plan: 04
type: execute
wave: 3
depends_on:
  - "44-02"
  - "44-03"
files_modified:
  - packages/web/src/lib/apiTypes.ts
  - packages/web/src/stores/useUIStore.ts
  - packages/web/src/hooks/useProjectHistory.ts
  - packages/web/src/components/HistoryPanel.tsx
  - packages/web/src/components/layout/Toolbar.tsx
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
autonomous: true
requirements:
  - HIS-05

must_haves:
  truths:
    - "The user can open a history panel that shows grouped actions with actor, title, status, and undo/redo availability"
    - "Ctrl+Z undoes the latest undoable group and Ctrl+Shift+Z redoes the latest redoable group"
    - "After undo or redo, the web app refreshes from the authoritative server snapshot instead of trusting optimistic local state"
  artifacts:
    - path: "packages/web/src/hooks/useProjectHistory.ts"
      provides: "History API client and authoritative refresh flow for undo/redo"
      exports: ["useProjectHistory"]
    - path: "packages/web/src/components/HistoryPanel.tsx"
      provides: "Visible grouped history list with undo and redo controls"
      exports: ["HistoryPanel"]
  key_links:
    - from: "packages/web/src/hooks/useProjectHistory.ts"
      to: "/api/history"
      via: "list and replay HTTP calls"
      pattern: "/api/history"
    - from: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      to: "packages/web/src/hooks/useProjectHistory.ts"
      via: "history panel state and keyboard shortcut actions"
      pattern: "useProjectHistory"
---

<objective>
Ship the web history panel and hotkeys on top of the authoritative history API.

Purpose: the backend history model is not useful until the user can see grouped actions, trigger undo/redo from the UI, and reconcile the chart from the server snapshot after replay.
Output: history API client hook, panel UI, toolbar entrypoint, keyboard shortcuts, and authoritative refresh wiring.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-undo-redo/44-CONTEXT.md
@.planning/reference/history-undo-redo-prd.md
@packages/web/src/lib/apiTypes.ts
@packages/web/src/stores/useUIStore.ts
@packages/web/src/components/layout/Toolbar.tsx
@packages/web/src/components/workspace/ProjectWorkspace.tsx
@packages/web/src/hooks/useCommandCommit.ts

<interfaces>
From packages/web/src/lib/apiTypes.ts:
```typescript
export interface ProjectLoadResponse {
  version: number;
  project: AuthProject;
  snapshot: {
    tasks: Task[];
    dependencies: ProjectDependency[];
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the web history client hook and panel state</name>
  <files>packages/web/src/lib/apiTypes.ts, packages/web/src/stores/useUIStore.ts, packages/web/src/hooks/useProjectHistory.ts</files>
  <read_first>
    packages/web/src/lib/apiTypes.ts
    packages/web/src/stores/useUIStore.ts
    packages/web/src/hooks/useCommandCommit.ts
    packages/server/src/routes/history-routes.ts
    .planning/phases/44-undo-redo/44-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "HistoryListResponse|HistoryItem|HistoryMutationResponse" packages/web/src/lib/apiTypes.ts` finds the frontend API contracts
    - `rg -n "showHistoryPanel|setShowHistoryPanel" packages/web/src/stores/useUIStore.ts` finds panel state
    - `rg -n "export function useProjectHistory" packages/web/src/hooks/useProjectHistory.ts` finds the history hook
    - `rg -n "/api/history|/api/history/undo|/api/history/.*/redo" packages/web/src/hooks/useProjectHistory.ts` finds all HTTP calls
    - `rg -n "setConfirmed\\(|clearTransientState\\(" packages/web/src/hooks/useProjectHistory.ts` finds authoritative refresh handling
  </acceptance_criteria>
  <action>
    Implement the frontend data layer for HIS-05.

    In `packages/web/src/lib/apiTypes.ts`, add exact interfaces:
    - `HistoryItem = { id: string; actorType: 'user' | 'agent' | 'system'; title: string; status: 'applied' | 'undone'; baseVersion: number; newVersion: number | null; commandCount: number; createdAt: string; undoable: boolean; redoable: boolean; }`
    - `HistoryListResponse = { items: HistoryItem[]; nextCursor?: string }`
    - `HistoryMutationResponse = { groupId: string; version: number; snapshot: ProjectLoadResponse['snapshot']; historyTitle: string; status: 'applied' | 'undone'; }`

    In `packages/web/src/stores/useUIStore.ts`, add:
    - `showHistoryPanel: boolean`
    - `setShowHistoryPanel(visible: boolean): void`
    - default `false`

    Create `packages/web/src/hooks/useProjectHistory.ts` that:
    - fetches `GET /api/history`
    - posts `POST /api/history/undo` for latest undo
    - posts `POST /api/history/:groupId/undo` and `POST /api/history/:groupId/redo`
    - after every successful undo/redo, calls `useProjectStore.getState().setConfirmed(version, snapshot)` and `clearTransientState()` so persisted history replaces any optimistic pending state
    - returns `{ items, loading, error, refreshHistory, undoLatest, undoGroup, redoGroup }`

    Keep pagination cursor-based. Do not implement client-side diffing or local synthetic redo stacks.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    The web client can read grouped history from the API and perform authoritative undo/redo refreshes without relying on optimistic local replay.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the history panel, toolbar toggle, and fixed undo/redo hotkeys</name>
  <files>packages/web/src/components/HistoryPanel.tsx, packages/web/src/components/layout/Toolbar.tsx, packages/web/src/components/workspace/ProjectWorkspace.tsx</files>
  <read_first>
    packages/web/src/components/layout/Toolbar.tsx
    packages/web/src/components/workspace/ProjectWorkspace.tsx
    packages/web/src/hooks/useProjectHistory.ts
    packages/web/src/stores/useUIStore.ts
    .planning/phases/44-undo-redo/44-CONTEXT.md
    .planning/reference/history-undo-redo-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "HistoryPanel" packages/web/src/components/HistoryPanel.tsx packages/web/src/components/workspace/ProjectWorkspace.tsx` finds the panel component and workspace integration
    - `rg -n "Ctrl\\+Z|Ctrl\\+Shift\\+Z|keydown" packages/web/src/components/workspace/ProjectWorkspace.tsx` finds the fixed hotkey wiring
    - `rg -n "showHistoryPanel|История" packages/web/src/components/layout/Toolbar.tsx` finds the toolbar toggle
    - `rg -n "actorType|title|status|undoable|redoable" packages/web/src/components/HistoryPanel.tsx` finds the required visible fields
    - `rg -n "setConfirmed\\(|undoLatest\\(|redoGroup\\(" packages/web/src/components/workspace/ProjectWorkspace.tsx` finds authoritative replay actions in use
  </acceptance_criteria>
  <action>
    Implement the visible history UX required by HIS-05.

    Create `packages/web/src/components/HistoryPanel.tsx` that renders:
    - timestamp
    - actor label (`Пользователь`, `AI`, `Система`)
    - `title`
    - `status`
    - Undo button when `undoable === true`
    - Redo button when `redoable === true`

    Update `packages/web/src/components/layout/Toolbar.tsx`:
    - add a visible button labeled `История`
    - wire it to `showHistoryPanel` / `setShowHistoryPanel`
    - keep the existing toolbar visual language; do not add modal navigation or separate page routing

    Update `packages/web/src/components/workspace/ProjectWorkspace.tsx`:
    - mount `HistoryPanel` alongside the existing chart/chat layout as a right-side panel on desktop and stacked panel on mobile
    - use `useProjectHistory(...)` to fetch items and execute undo/redo actions
    - add a `keydown` effect with fixed shortcuts:
      - `Ctrl+Z` -> `undoLatest()`
      - `Ctrl+Shift+Z` -> redo the latest `redoable` item
    - ignore the shortcuts when the active target is `input`, `textarea`, or a contenteditable element
    - after a successful replay, rely on the hook's authoritative `setConfirmed(...)` path instead of trying to patch local task arrays manually

    Do not add branching UI, diff UI, restore-to-version controls, or checkpoint controls.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    The app shows grouped history, supports fixed undo/redo hotkeys, and refreshes the chart from authoritative server state after replay actions.
  </done>
</task>

</tasks>

<verification>
Confirm that the user can open history, see grouped records with actor/title/status, and trigger undo/redo through both the panel and the fixed keyboard shortcuts while the chart refreshes from the server snapshot.
</verification>

<success_criteria>
Phase 44 is user-visible: grouped history is understandable, undo and redo are accessible from the main workspace, and replay actions stay aligned with authoritative server state.
</success_criteria>

<output>
After completion, create `.planning/phases/44-undo-redo/44-04-SUMMARY.md`
</output>
