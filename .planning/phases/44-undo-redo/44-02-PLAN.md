---
phase: 44-undo-redo
plan: 02
type: execute
wave: 2
depends_on:
  - "44-01"
files_modified:
  - packages/mcp/src/services/history.service.ts
  - packages/mcp/src/services/index.ts
  - packages/mcp/src/services/history.service.test.ts
  - packages/server/src/routes/history-routes.ts
  - packages/server/src/routes/history-routes.test.ts
  - packages/server/src/index.ts
autonomous: true
requirements:
  - HIS-02
  - HIS-03

must_haves:
  truths:
    - "A user can undo the latest or a chosen mutation group without rewriting existing history rows"
    - "Redo only works for an undone group whose post-undo history has not diverged"
    - "Grouped history is available through a paginated API with undoable and redoable flags"
  artifacts:
    - path: "packages/mcp/src/services/history.service.ts"
      provides: "Undo, redo, divergence checks, and paginated group-history reads"
      exports: ["HistoryService", "historyService"]
    - path: "packages/server/src/routes/history-routes.ts"
      provides: "HTTP history list, undo, and redo endpoints"
      exports: ["registerHistoryRoutes"]
  key_links:
    - from: "packages/mcp/src/services/history.service.ts"
      to: "packages/mcp/src/services/command.service.ts"
      via: "append-only undo/redo replay through authoritative commit path"
      pattern: "commitCommand"
    - from: "packages/server/src/routes/history-routes.ts"
      to: "packages/mcp/src/services/history.service.ts"
      via: "history list and undo/redo route handlers"
      pattern: "historyService"
---

<objective>
Add append-only undo/redo orchestration and the grouped history API on top of the Phase 44 history substrate.

Purpose: once groups and inverse commands exist, the product still needs safe replay semantics, divergence protection, and an HTTP contract for the web client.
Output: a history service for grouped undo/redo plus authenticated history endpoints that return authoritative snapshot/version payloads.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-undo-redo/44-CONTEXT.md
@.planning/reference/history-undo-redo-prd.md
@packages/mcp/src/types.ts
@packages/mcp/src/services/command.service.ts
@packages/server/src/routes/command-routes.ts
@packages/server/src/index.ts

<interfaces>
From packages/mcp/src/types.ts:
```typescript
export type MutationGroupRecord = {
  id: string;
  projectId: string;
  baseVersion: number;
  newVersion: number | null;
  actorType: ActorType;
  origin: MutationGroupOrigin;
  title: string;
  status: MutationGroupStatus;
  undoable: boolean;
};

export type HistoryGroupContext = {
  groupId: string;
  origin: MutationGroupOrigin;
  title: string;
  requestContextId?: string;
  finalizeGroup: boolean;
  redoOfGroupId?: string | null;
  targetGroupId?: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `HistoryService` with append-only undo, redo, and divergence checks</name>
  <files>packages/mcp/src/services/history.service.ts, packages/mcp/src/services/index.ts, packages/mcp/src/services/history.service.test.ts</files>
  <read_first>
    packages/mcp/src/services/command.service.ts
    packages/mcp/src/services/index.ts
    packages/mcp/src/types.ts
    packages/mcp/prisma/schema.prisma
    .planning/phases/44-undo-redo/44-CONTEXT.md
    .planning/reference/history-undo-redo-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export class HistoryService|export const historyService" packages/mcp/src/services/history.service.ts` finds the service export
    - `rg -n "undoLatestGroup|undoGroup|redoGroup|listHistoryGroups" packages/mcp/src/services/history.service.ts` finds the required methods
    - `rg -n "redo_not_available|history_diverged|target_not_undone" packages/mcp/src/services/history.service.ts` finds the typed redo failures
    - `rg -n "origin: 'undo'|origin: 'redo'" packages/mcp/src/services/history.service.ts` finds append-only replay groups
    - `rg -n "history_diverged|undo latest|redo specific|reverse order|forward order" packages/mcp/src/services/history.service.test.ts` finds the replay and divergence coverage
  </acceptance_criteria>
  <action>
    Implement HIS-02 and the backend half of HIS-03 in `packages/mcp/src/services/history.service.ts`.

    Export these exact methods:
    - `listHistoryGroups({ projectId, cursor, limit }): Promise<{ items: HistoryGroupListItem[]; nextCursor?: string }>`
    - `undoLatestGroup({ projectId, actorType, actorId, requestContextId }): Promise<HistoryMutationResponse>`
    - `undoGroup({ projectId, groupId, actorType, actorId, requestContextId }): Promise<HistoryMutationResponse>`
    - `redoGroup({ projectId, groupId, actorType, actorId, requestContextId }): Promise<HistoryMutationResponse>`

    Use these exact semantics:
    1. Undo selects only groups with `status = 'applied'` and `undoable = true`.
    2. Undo replays the target group's `ProjectEvent.inverseCommand` values in descending `ordinal`.
    3. Undo writes a new group with:
       - `origin: 'undo'`
       - `title: 'Undo — ' + targetGroup.title`
       - `targetGroupId = targetGroup.id`
       - `finalizeGroup: true` on the last replayed command only
    4. After successful undo, mark the original target group `status = 'undone'` and `undoneByGroupId = undoGroup.id`.
    5. Redo replays the original target group's `command` values in ascending `ordinal`.
    6. Redo is allowed only if:
       - target group `status = 'undone'`
       - target group has `undoneByGroupId`
       - there is no later applied non-redo group after the undo group's `newVersion`
    7. If redo is blocked, return typed failures exactly named:
       - `redo_not_available`
       - `history_diverged`
       - `target_not_undone`
    8. Every undo/redo replay must call `commandService.commitCommand(...)` for each event and must append history instead of mutating or deleting old `ProjectEvent` rows.

    Add `packages/mcp/src/services/history.service.test.ts` that proves:
    - undo of a two-event group replays ordinals `2` then `1`
    - redo of the same group replays ordinals `1` then `2`
    - a post-undo normal edit triggers `history_diverged`
    - an already-applied target triggers `target_not_undone` on redo
    - list pagination returns grouped rows with `commandCount`, `undoable`, `redoable`, `status`, `createdAt`
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/mcp/src/services/history.service.test.ts && npm run build -w packages/mcp</automated>
  </verify>
  <done>
    Undo and redo are append-only server operations with reverse/forward replay order, divergence protection, and grouped history listing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose paginated history, undo, and redo over authenticated HTTP routes</name>
  <files>packages/server/src/routes/history-routes.ts, packages/server/src/routes/history-routes.test.ts, packages/server/src/index.ts</files>
  <read_first>
    packages/server/src/index.ts
    packages/server/src/routes/command-routes.ts
    packages/server/src/routes/command-routes.test.ts
    packages/mcp/src/services/history.service.ts
    packages/mcp/src/types.ts
    .planning/phases/44-undo-redo/44-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "registerHistoryRoutes" packages/server/src/routes/history-routes.ts packages/server/src/index.ts` finds route registration
    - `rg -n "GET '/api/history'|POST '/api/history/undo'|POST '/api/history/:groupId/undo'|POST '/api/history/:groupId/redo'" packages/server/src/routes/history-routes.ts` finds all required endpoints
    - `rg -n "version_conflict|redo_not_available|history_diverged|target_not_undone" packages/server/src/routes/history-routes.ts` finds typed response handling
    - `rg -n "authMiddleware" packages/server/src/routes/history-routes.ts` finds route auth
    - `rg -n "/api/history|/api/history/:groupId/redo|commandCount|redoable" packages/server/src/routes/history-routes.test.ts` finds route-contract coverage
  </acceptance_criteria>
  <action>
    Implement the HTTP layer for HIS-03.

    Create `packages/server/src/routes/history-routes.ts` with these exact endpoints:
    - `GET /api/history`
      - query params: `cursor?: string`, `limit?: number`
      - response shape: `{ items: Array<{ id, actorType, title, status, baseVersion, newVersion, commandCount, createdAt, undoable, redoable }>; nextCursor?: string }`
    - `POST /api/history/undo`
      - undoes the latest undoable group for `req.user.projectId`
    - `POST /api/history/:groupId/undo`
      - undoes the specific group
    - `POST /api/history/:groupId/redo`
      - redoes the specific undone group when safe

    For every undo/redo route:
    - require `authMiddleware`
    - derive `projectId`, `actorType = 'user'`, and `actorId = req.user.userId` from the session
    - return the authoritative updated `snapshot` and `version`
    - preserve current `version_conflict` behavior by returning the fresh snapshot when the service surfaces it
    - map typed failures to HTTP 409 for `version_conflict` and 400 for `redo_not_available`, `history_diverged`, `target_not_undone`, or validation errors

    Register the new routes in `packages/server/src/index.ts` next to the other route registrations.

    Add `packages/server/src/routes/history-routes.test.ts` as route-contract tests that grep-lock:
    - registration in `index.ts`
    - auth preHandlers
    - endpoint paths
    - presence of `snapshot`, `version`, `commandCount`, `undoable`, and `redoable` in the response contract
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/routes/history-routes.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The server exposes grouped history and undo/redo through authenticated routes that preserve the authoritative snapshot/version recovery contract.
  </done>
</task>

</tasks>

<verification>
Confirm that undo and redo create new groups instead of rewriting rows, grouped history is paginated over HTTP, and redo refusals surface typed reasons instead of unsafe replays.
</verification>

<success_criteria>
Users and the web client can list grouped history, undo safely, redo safely when linear history permits it, and always receive the authoritative snapshot/version after a replay attempt.
</success_criteria>

<output>
After completion, create `.planning/phases/44-undo-redo/44-02-SUMMARY.md`
</output>
