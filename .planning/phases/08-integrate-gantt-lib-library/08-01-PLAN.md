---
phase: 08-integrate-gantt-lib-library
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/package.json
  - packages/web/src/main.tsx
  - packages/web/src/components/GanttChart.tsx
autonomous: true
requirements:
  - WEB-GANTT-01
  - WEB-GANTT-02
must_haves:
  truths:
    - "gantt-lib package is installed in packages/web"
    - "gantt-lib CSS is imported in main.tsx (CRITICAL - without this, nothing renders)"
    - "GanttChart component uses gantt-lib's GanttChart instead of placeholder"
    - "Component renders without TypeScript errors"
    - "Tasks passed to GanttChart are displayed as task bars"
  artifacts:
    - path: "packages/web/package.json"
      provides: "gantt-lib dependency declared"
      contains: '"gantt-lib": "^0.1.1"'
    - path: "packages/web/src/main.tsx"
      provides: "CSS import for gantt-lib styles"
      contains: "import 'gantt-lib/styles.css'"
    - path: "packages/web/src/components/GanttChart.tsx"
      provides: "GanttChart component integrating gantt-lib"
      exports: ["GanttChart"]
      imports: ["from 'gantt-lib'"]
  key_links:
    - from: "packages/web/src/main.tsx"
      to: "gantt-lib/styles.css"
      via: "import statement loads all required styles"
      pattern: "import 'gantt-lib/styles.css'"
    - from: "packages/web/src/components/GanttChart.tsx"
      to: "gantt-lib GanttChart component"
      via: "import { GanttChart } from 'gantt-lib'"
      pattern: "from 'gantt-lib'"
    - from: "packages/web/src/components/GanttChart.tsx"
      to: "packages/web/src/types.ts"
      via: "Task type import for type safety"
      pattern: "import.*Task.*from.*types"
---

<objective>
Install gantt-lib package and integrate it into the web package, replacing the placeholder GanttChart component with the actual gantt-lib React component.

Purpose: Replace the placeholder "Gantt chart coming soon" message with a working interactive Gantt chart that displays tasks, enables drag-to-edit, and syncs with WebSocket updates.

Output:
- packages/web/package.json — adds gantt-lib@0.1.1 dependency
- packages/web/src/main.tsx — adds critical CSS import
- packages/web/src/components/GanttChart.tsx — integrates gantt-lib's GanttChart component
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-integrate-gantt-lib-library/08-RESEARCH.md
@.planning/phases/07-web-ui-with-real-time-gantt-editing-via-ai-dialogue/07-05-PLAN.md

<!-- Existing Task interface (already compatible with gantt-lib) -->

From packages/web/src/types.ts:
```typescript
export interface TaskDependency {
  taskId: string;
  type: 'FS' | 'SS' | 'FF' | 'SF';
  lag?: number;
}

export interface Task {
  id: string;
  name: string;
  startDate: string;   // YYYY-MM-DD
  endDate: string;     // YYYY-MM-DD
  color?: string;
  progress?: number;
  dependencies?: TaskDependency[];
}
```

<!-- useTasks hook provides setTasks for onChange callbacks -->

From packages/web/src/hooks/useTasks.ts:
```typescript
export interface UseTasksResult {
  tasks: Task[];
  setTasks: React.Dispatch<React.SetStateAction<Task[]>>;
  loading: boolean;
  error: string | null;
}
```

<!-- App.tsx already has WebSocket integration for real-time updates -->
<!-- App.tsx line 67: <GanttChart tasks={tasks} /> -->
<!-- We need to add: onChange={setTasks} to enable drag-to-edit persistence -->
</context>

<interfaces>
<!-- gantt-lib GanttChart component props (from RESEARCH.md) -->

```typescript
interface GanttChartProps {
  tasks: Task[];
  dayWidth?: number;              // default: 40
  rowHeight?: number;             // default: 40
  headerHeight?: number;          // default: 40
  containerHeight?: number | string; // default: undefined (auto)
  onChange?: (tasks: Task[] | ((currentTasks: Task[]) => Task[])) => void;
  onValidateDependencies?: (result: ValidationResult) => void;
  enableAutoSchedule?: boolean;   // default: false
  showTaskList?: boolean;         // default: false
  taskListWidth?: number;         // default: 520
}
```

<!-- IMPORTANT: CSS import is REQUIRED -->
<!-- Without: import 'gantt-lib/styles.css' in main.tsx, the chart will NOT render correctly -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install gantt-lib dependency</name>
  <files>packages/web/package.json</files>
  <action>
    Add gantt-lib@0.1.1 to packages/web/package.json dependencies:

    Run: npm install gantt-lib@0.1.1 -w packages/web

    This will add:
    "gantt-lib": "^0.1.1"

    to the dependencies section in packages/web/package.json

    Peer dependencies (react, react-dom) are already satisfied per package.json.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && grep -q '"gantt-lib"' packages/web/package.json && echo "gantt-lib found in package.json"</automated>
  </verify>
  <done>gantt-lib@0.1.1 is listed in packages/web/package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS import to main.tsx (CRITICAL)</name>
  <files>packages/web/src/main.tsx</files>
  <action>
    Add the CSS import to packages/web/src/main.tsx:

    Current file content:
    ```tsx
    import { StrictMode } from 'react';
    import { createRoot } from 'react-dom/client';
    import App from './App.tsx';

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>
    );
    ```

    Change to:
    ```tsx
    import { StrictMode } from 'react';
    import { createRoot } from 'react-dom/client';
    import App from './App.tsx';
    import 'gantt-lib/styles.css'; // CRITICAL: Must import for Gantt chart rendering

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>
    );
    ```

    **CRITICAL:** The CSS import MUST be a separate import line. Without it:
    - Task bars will not render
    - Grid lines will not appear
    - Layout will be broken
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && grep -q "import 'gantt-lib/styles.css'" packages/web/src/main.tsx && echo "CSS import found"</automated>
  </verify>
  <done>packages/web/src/main.tsx imports 'gantt-lib/styles.css'</done>
</task>

<task type="auto">
  <name>Task 3: Replace GanttChart.tsx with gantt-lib integration</name>
  <files>packages/web/src/components/GanttChart.tsx</files>
  <action>
    Replace the placeholder GanttChart component with gantt-lib integration:

    Current file content:
    ```tsx
    import type { Task } from '../types.ts';

    interface GanttChartProps {
      tasks: Task[];
    }

    export function GanttChart({ tasks }: GanttChartProps) {
      if (tasks.length === 0) {
        return (
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%', color: '#888' }}>
            <p>No tasks yet. Start a conversation to create your Gantt chart.</p>
          </div>
        );
      }

      return (
        <div style={{ padding: 20 }}>
          <p style={{ color: '#888' }}>{tasks.length} task(s) loaded. Gantt chart coming soon.</p>
        </div>
      );
    }
    ```

    Change to:
    ```tsx
    import { GanttChart } from 'gantt-lib';
    import type { Task } from '../types.ts';

    interface GanttChartProps {
      tasks: Task[];
      onChange?: (tasks: Task[] | ((prev: Task[]) => Task[])) => void;
    }

    export function GanttChart({ tasks, onChange }: GanttChartProps) {
      // Preserve empty state for better UX
      if (tasks.length === 0) {
        return (
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%', color: '#888' }}>
            <p>No tasks yet. Start a conversation to create your Gantt chart.</p>
          </div>
        );
      }

      return (
        <GanttChart
          tasks={tasks}
          onChange={onChange}
          dayWidth={40}
          rowHeight={40}
          containerHeight="100%"
        />
      );
    }
    ```

    Key changes:
    1. Import GanttChart from 'gantt-lib'
    2. Add onChange prop to interface (for drag-to-edit persistence)
    3. Keep empty state handling for better UX
    4. Pass tasks, onChange, dayWidth, rowHeight, containerHeight to gantt-lib's GanttChart
    5. Use "100%" for containerHeight to fill available space

    Note: We're NOT enabling showTaskList (default false) to keep layout simple with just the chart.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web 2>&1 | tail -5</automated>
  </verify>
  <done>
    - packages/web builds without TypeScript errors
    - GanttChart.tsx imports from 'gantt-lib'
    - GanttChart component accepts onChange prop
    - Empty state is preserved for 0 tasks
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run build -w packages/web` — must exit 0 with no TypeScript errors
2. Verify packages/web/package.json contains "gantt-lib": "^0.1.1"
3. Verify packages/web/src/main.tsx contains `import 'gantt-lib/styles.css'`
4. Verify packages/web/src/components/GanttChart.tsx imports `{ GanttChart } from 'gantt-lib'`
</verification>

<success_criteria>
- gantt-lib package installed in packages/web
- CSS import added to main.tsx (prevents rendering issues)
- GanttChart.tsx uses gantt-lib's GanttChart component
- TypeScript compilation succeeds
- Component accepts onChange prop for drag-to-edit (will be wired in App.tsx in next plan)
- Empty state preserved for better UX when no tasks exist
</success_criteria>

<output>
After completion, create `.planning/phases/08-integrate-gantt-lib-library/08-01-SUMMARY.md` using the standard summary template.
</output>
