---
phase: 34-feature-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/middleware/constraint-middleware.ts
  - packages/server/src/middleware/constraint-middleware.test.ts
  - packages/server/src/routes/auth-routes.ts
  - packages/server/src/routes/auth-routes.test.ts
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - "Free users receive a structured 403 denial instead of archiving a project."
    - "Paid users keep the existing archive route behavior without a frontend-only loophole."
    - "Archive denials use the same payload vocabulary as prior constraint routes."
  artifacts:
    - path: "packages/server/src/middleware/constraint-middleware.ts"
      provides: "Reusable feature-gate Fastify guard for boolean/non-tracked limits"
    - path: "packages/server/src/routes/auth-routes.ts"
      provides: "Archive route preHandler wired to archive tariff enforcement"
    - path: "packages/server/src/routes/auth-routes.test.ts"
      provides: "Route contract coverage for archive enforcement ordering"
  key_links:
    - from: "packages/server/src/routes/auth-routes.ts"
      to: "packages/server/src/middleware/constraint-middleware.ts"
      via: "requireArchiveAccess preHandler"
      pattern: "requireFeatureGate\\('archive'"
    - from: "packages/server/src/middleware/constraint-middleware.ts"
      to: "packages/server/src/services/constraint-service.ts"
      via: "constraintService.checkLimit"
      pattern: "checkLimit\\(userId, limitKey\\)"
---

<objective>
Add server-side archive tariff enforcement so the archive route itself denies free-plan users with a structured feature-gate payload.

Purpose: Close the backend gap for GATE-01 before shell UX intercepts are added.
Output: Reusable feature-gate middleware plus archive route contract tests.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-backend-enforcement/32-01-SUMMARY.md
@packages/mcp/src/constraints/catalog.ts
@packages/server/src/services/constraint-service.ts
@packages/server/src/middleware/constraint-middleware.ts
@packages/server/src/routes/auth-routes.ts

<interfaces>
From packages/server/src/middleware/constraint-middleware.ts:
```ts
type DenialPayload = {
  code: string;
  limitKey: LimitKey | null;
  reasonCode: ConstraintCheckResult['reasonCode'] | 'subscription_expired';
  remaining: number | 'unlimited' | null;
  plan: PlanId;
  planLabel: string;
  upgradeHint: string;
  used?: number;
  limit?: number | 'unlimited';
};
```

From packages/server/src/services/constraint-service.ts:
```ts
export type ConstraintReasonCode = 'allowed' | 'limit_reached' | 'feature_disabled';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable server feature-gate middleware for boolean limits</name>
  <files>packages/server/src/middleware/constraint-middleware.ts, packages/server/src/middleware/constraint-middleware.test.ts</files>
  <read_first>
packages/server/src/middleware/constraint-middleware.ts
packages/server/src/middleware/constraint-middleware.test.ts
packages/server/src/services/constraint-service.ts
packages/mcp/src/constraints/catalog.ts
  </read_first>
  <behavior>
    - Test 1: A denied `archive` check returns HTTP 403 payload with `code: 'ARCHIVE_FEATURE_LOCKED'`, `limitKey: 'archive'`, `reasonCode: 'feature_disabled'`, and `remaining: null`.
    - Test 2: Feature-gate denials for non-tracked limits do not populate tracked `used` or numeric `limit` fields.
  </behavior>
  <action>Extend `packages/server/src/middleware/constraint-middleware.ts` with a dedicated non-tracked feature helper named `requireFeatureGate(limitKey, options)` that calls `constraintService.checkLimit(userId, limitKey)` and denies only when `allowed === false`. Use it for boolean/access-level limits, not the tracked-counter helper from Phase 32. Keep the payload shape aligned with the existing `DenialPayload`, but for `archive` send `code: 'ARCHIVE_FEATURE_LOCKED'`, `limitKey: 'archive'`, `reasonCode: 'feature_disabled'`, `remaining: null`, and `upgradeHint: 'Архив проектов доступен на тарифе Старт и выше.'`. Update `constraint-middleware.test.ts` with an explicit archive denial case that asserts `feature_disabled` semantics and omits tracked usage fields.</action>
  <acceptance_criteria>
    - `rg -n "requireFeatureGate\\(" packages/server/src/middleware/constraint-middleware.ts` returns a match.
    - `rg -n "ARCHIVE_FEATURE_LOCKED|feature_disabled" packages/server/src/middleware/constraint-middleware.test.ts` returns matches.
    - `rg -n "remaining: null" packages/server/src/middleware/constraint-middleware.test.ts` returns a match for the archive denial assertion.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/server/src/middleware/constraint-middleware.test.ts</automated>
  </verify>
  <done>`constraint-middleware.ts` exports a reusable feature-gate guard and the server test suite proves the archive denial payload uses `feature_disabled` semantics for GATE-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the archive route with the new archive feature gate</name>
  <files>packages/server/src/routes/auth-routes.ts, packages/server/src/routes/auth-routes.test.ts</files>
  <read_first>
packages/server/src/routes/auth-routes.ts
packages/server/src/routes/auth-routes.test.ts
packages/server/src/middleware/constraint-middleware.ts
.planning/phases/32-backend-enforcement/32-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `POST /api/projects/:id/archive` composes `authMiddleware` then `requireArchiveAccess` before `authService.archiveProject(...)`.
    - Test 2: `DELETE /api/projects/:id` remains unguarded by archive/project limit middleware.
  </behavior>
  <action>In `packages/server/src/routes/auth-routes.ts`, define `const requireArchiveAccess = requireFeatureGate('archive', { code: 'ARCHIVE_FEATURE_LOCKED', upgradeHint: 'Архив проектов доступен на тарифе Старт и выше.' })` and wire it into the archive route preHandler array as `[authMiddleware, requireArchiveAccess]`. Keep restore on `requireProjectLimit` and delete unguarded. Update `auth-routes.test.ts` so the archive regex now positively asserts the new guard and delete still negatively asserts no accidental tariff guard. This task implements GATE-01 on the authoritative HTTP route, not only in frontend UX.</action>
  <acceptance_criteria>
    - `rg -n "const requireArchiveAccess = requireFeatureGate\\('archive'" packages/server/src/routes/auth-routes.ts` returns one match.
    - `rg -n "/api/projects/:id/archive.*requireArchiveAccess" packages/server/src/routes/auth-routes.ts` returns the archive route wiring.
    - `rg -n "requireArchiveAccess|archive route" packages/server/src/routes/auth-routes.test.ts` returns updated route-contract coverage.
  </acceptance_criteria>
  <verify>
    <automated>node --test packages/server/src/routes/auth-routes.test.ts packages/server/src/middleware/constraint-middleware.test.ts &amp;&amp; npm run build -w packages/server</automated>
  </verify>
  <done>The archive route is server-enforced with a structured `ARCHIVE_FEATURE_LOCKED` denial, and source-level contract tests prove the guard runs before `authService.archiveProject(...)`.</done>
</task>

</tasks>

<verification>
Run the server route and middleware tests together, then build `@gantt/server` so the new feature-gate helper compiles through the existing Fastify route graph.
</verification>

<success_criteria>
Free-plan archive attempts are denied by the server with a structured archive payload, paid plans still reach the archive service, and the route contract is locked by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/34-feature-gates/34-feature-gates-01-SUMMARY.md`
</output>
