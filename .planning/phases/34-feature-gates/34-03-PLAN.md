---
phase: 34-feature-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 34-01
  - 34-02
files_modified:
  - packages/web/src/stores/useAuthStore.ts
  - packages/web/src/App.tsx
  - packages/web/src/components/ProjectSwitcher.tsx
autonomous: true
requirements:
  - GATE-01
  - GATE-02
must_haves:
  truths:
    - "Archive actions open the upgrade modal for free users before or after backend denial instead of silently failing."
    - "Archive still works for paid tiers through the existing project switcher surface."
    - "The project shell exposes a resource-pool upsell entrypoint for free users using the same modal contract."
  artifacts:
    - path: "packages/web/src/stores/useAuthStore.ts"
      provides: "Generic shell denial bridge for archive/backend feature-gate responses"
    - path: "packages/web/src/App.tsx"
      provides: "Archive/resource-pool proactive modal wiring"
    - path: "packages/web/src/components/ProjectSwitcher.tsx"
      provides: "Archive interception surface plus resource-pool entrypoint"
  key_links:
    - from: "packages/web/src/components/ProjectSwitcher.tsx"
      to: "packages/web/src/App.tsx"
      via: "archive and resource-pool callbacks"
      pattern: "onArchive|onOpenResourcePool"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/stores/useAuthStore.ts"
      via: "shared pending denial state"
      pattern: "constraintDenial"
---

<objective>
Wire archive and resource-pool gates into the project shell using the shared modal contract from Phase 33 plus the new Phase 34 helpers.

Purpose: Deliver the user-visible gating for GATE-01 and GATE-02 on the surfaces users already use today.
Output: Archive interception in the switcher, shared denial propagation in auth state, and a resource-pool upsell entrypoint.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-frontend-constraints-ux/33-VERIFICATION.md
@packages/web/src/App.tsx
@packages/web/src/stores/useAuthStore.ts
@packages/web/src/components/ProjectSwitcher.tsx
@packages/web/src/lib/constraintUi.ts
@packages/web/src/stores/useBillingStore.ts

<interfaces>
From packages/web/src/App.tsx:
```ts
const openLimitModal = useCallback(async (denial: Partial<ConstraintDenialPayload> | null | undefined) => { ... });
const handleArchiveProject = useCallback(async (projectId: string) => {
  await auth.archiveProject(projectId);
}, [auth]);
```

From packages/web/src/stores/useAuthStore.ts:
```ts
projectLimitDenial: Partial<ConstraintDenialPayload> | null;
archiveProject(projectId: string): Promise<AuthProject>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize shell denial state and archive modal interception</name>
  <files>packages/web/src/stores/useAuthStore.ts, packages/web/src/App.tsx</files>
  <read_first>
packages/web/src/stores/useAuthStore.ts
packages/web/src/App.tsx
packages/web/src/lib/constraintUi.ts
packages/server/src/routes/auth-routes.ts
  </read_first>
  <behavior>
    - Test 1: If billing state says `archive` is unavailable, `handleArchiveProject()` opens the modal with `ARCHIVE_FEATURE_LOCKED` and does not call `auth.archiveProject`.
    - Test 2: If the archive HTTP route still returns 403, the parsed structured denial reaches App through shared auth-store state and opens the same modal.
  </behavior>
  <action>Replace the narrow `projectLimitDenial` bridge in `packages/web/src/stores/useAuthStore.ts` with a generic `constraintDenial` field so project-create and archive 403 payloads share one state path. Update both `createProject()` and `archiveProject()` to parse structured 403 bodies as `Partial<ConstraintDenialPayload>` and write them into `constraintDenial`. Then update `packages/web/src/App.tsx` to read `constraintDenial`, extend `isConstraintCode(...)` with `ARCHIVE_FEATURE_LOCKED`, and derive a proactive archive denial from billing state using `limitKey: 'archive'`, `reasonCode: 'feature_disabled'`, and `upgradeHint: 'Архив проектов доступен на тарифе Старт и выше.'`. In `handleArchiveProject(projectId)`, short-circuit to `openLimitModal(...)` when archive access is false; otherwise keep the paid-tier archive flow unchanged.</action>
  <acceptance_criteria>
    - `rg -n "constraintDenial" packages/web/src/stores/useAuthStore.ts packages/web/src/App.tsx` returns matches in both files.
    - `rg -n "ARCHIVE_FEATURE_LOCKED" packages/web/src/App.tsx packages/web/src/stores/useAuthStore.ts` returns matches.
    - `rg -n "handleArchiveProject" packages/web/src/App.tsx` shows the proactive archive modal path.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json &amp;&amp; rg -n "constraintDenial|ARCHIVE_FEATURE_LOCKED|handleArchiveProject" packages/web/src/App.tsx packages/web/src/stores/useAuthStore.ts</automated>
  </verify>
  <done>Archive gating uses one shared denial bridge for proactive and backend-driven denials, and free users no longer attempt the archive mutation path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add project-switcher resource-pool upsell entrypoint and preserve archive UX</name>
  <files>packages/web/src/components/ProjectSwitcher.tsx, packages/web/src/App.tsx</files>
  <read_first>
packages/web/src/components/ProjectSwitcher.tsx
packages/web/src/App.tsx
packages/web/src/lib/constraintUi.ts
packages/web/src/stores/useBillingStore.ts
  </read_first>
  <behavior>
    - Test 1: `ProjectSwitcher` exposes a dedicated resource-pool row or section that can call an App callback without inventing local modal state.
    - Test 2: The archive row/menu remains available for paid tiers and still uses the existing archive/restore actions.
  </behavior>
  <action>Extend `ProjectSwitcher.tsx` with a new prop `onOpenResourcePool` and render a dedicated shell surface for `Пул ресурсов` below the archive section. For free plans, clicking the surface should call App so it opens `RESOURCE_POOL_FEATURE_LOCKED` via `openLimitModal(...)`; for paid tiers, render the surface as visibly available without opening the upsell modal. Keep archive/restore menu items intact and do not regress project switching. Wire the new callback from `App.tsx`, using the shared helper from `constraintUi.ts` rather than inline copy. This task implements GATE-02 on an existing project-shell surface even though the full resource-pool feature is not built yet.</action>
  <acceptance_criteria>
    - `rg -n "onOpenResourcePool" packages/web/src/components/ProjectSwitcher.tsx packages/web/src/App.tsx` returns matches.
    - `rg -n "Пул ресурсов|RESOURCE_POOL_FEATURE_LOCKED" packages/web/src/components/ProjectSwitcher.tsx packages/web/src/App.tsx` returns matches.
    - `rg -n "onArchive|onRestore" packages/web/src/components/ProjectSwitcher.tsx` still returns the existing archive/restore wiring.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json &amp;&amp; rg -n "onOpenResourcePool|RESOURCE_POOL_FEATURE_LOCKED|Пул ресурсов" packages/web/src/components/ProjectSwitcher.tsx packages/web/src/App.tsx</automated>
  </verify>
  <done>The project switcher exposes a resource-pool tariff surface and archive actions still work through the existing UI for eligible tiers, satisfying GATE-01 and GATE-02 at the shell layer.</done>
</task>

</tasks>

<verification>
Compile the web package and grep for the shared denial state, archive gate code, and resource-pool entrypoint so the executor proves the shell is wired to the common modal path.
</verification>

<success_criteria>
Free users hit upsell flows for archive and resource pool from the project shell, while paid users retain archive functionality and see resource-pool availability without a second modal system.
</success_criteria>

<output>
After completion, create `.planning/phases/34-feature-gates/34-feature-gates-03-SUMMARY.md`
</output>
