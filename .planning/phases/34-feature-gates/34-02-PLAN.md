---
phase: 34-feature-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/stores/useBillingStore.ts
  - packages/web/src/lib/constraintUi.ts
  - packages/web/src/components/LimitReachedModal.tsx
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
must_haves:
  truths:
    - "Frontend helpers can describe archive, resource-pool, and export denials without ad-hoc copy in components."
    - "The modal can explain both boolean feature locks and export tier locks with plan-aware upgrade guidance."
    - "Billing selectors expose archive/resource_pool/export access in grep-obvious helpers for downstream shell wiring."
  artifacts:
    - path: "packages/web/src/stores/useBillingStore.ts"
      provides: "Typed selectors for archive/resource_pool/export access"
    - path: "packages/web/src/lib/constraintUi.ts"
      provides: "Expanded gate normalization for boolean and access-level limits"
    - path: "packages/web/src/components/LimitReachedModal.tsx"
      provides: "Modal copy for archive/resource_pool/export gates"
  key_links:
    - from: "packages/web/src/lib/constraintUi.ts"
      to: "packages/web/src/stores/useBillingStore.ts"
      via: "archive/resource_pool/export selectors"
      pattern: "getArchiveAccess|getResourcePoolAccess|getExportAccessLevel"
    - from: "packages/web/src/components/LimitReachedModal.tsx"
      to: "packages/web/src/lib/constraintUi.ts"
      via: "buildConstraintModalContent"
      pattern: "buildConstraintModalContent"
---

<objective>
Expand the shared frontend constraint contract from tracked counters only to feature gates and export access levels.

Purpose: Give later shell plans one canonical source of truth for archive/resource-pool/export copy, upgrade targets, and allowed-level semantics.
Output: Billing selectors, normalized denial helpers, and modal rendering for all Phase 34 limit keys.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-paywall-enhance/29-02-SUMMARY.md
@.planning/phases/33-frontend-constraints-ux/33-02-SUMMARY.md
@packages/mcp/src/constraints/catalog.ts
@packages/web/src/stores/useBillingStore.ts
@packages/web/src/lib/constraintUi.ts
@packages/web/src/components/LimitReachedModal.tsx

<interfaces>
From packages/mcp/src/constraints/catalog.ts:
```ts
export type ExportAccessLevel = 'none' | 'pdf' | 'pdf_excel' | 'pdf_excel_api';
export type LimitKey = 'projects' | 'ai_queries' | 'archive' | 'resource_pool' | 'export';
```

From packages/web/src/lib/constraintUi.ts:
```ts
export type ConstraintLimitKey = 'projects' | 'ai_queries';
export interface ConstraintDenialPayload {
  code: string;
  limitKey: ConstraintLimitKey | null;
  reasonCode: ConstraintReasonCode | string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed billing selectors for archive, resource pool, and export access</name>
  <files>packages/web/src/stores/useBillingStore.ts</files>
  <read_first>
packages/web/src/stores/useBillingStore.ts
packages/mcp/src/constraints/catalog.ts
packages/server/src/services/billing-service.ts
  </read_first>
  <behavior>
    - Test 1: Frontend code can call `getArchiveAccess(status)` and `getResourcePoolAccess(status)` without parsing `limits` manually.
    - Test 2: Frontend code can call `getExportAccessLevel(status)` and receive one of `none`, `pdf`, `pdf_excel`, or `pdf_excel_api`.
  </behavior>
  <action>Add explicit exported helpers in `packages/web/src/stores/useBillingStore.ts` for non-tracked gates: `getArchiveAccess(status)`, `getResourcePoolAccess(status)`, and `getExportAccessLevel(status)`. These should read from the normalized `limits` payload already provided by `BillingService.getUsageStatus()` and preserve the existing tracked usage helpers unchanged. Keep return types concrete: boolean for archive/resource pool and `'none' | 'pdf' | 'pdf_excel' | 'pdf_excel_api'` for export. Do not force downstream components to inspect raw `Record<string, unknown>` fields.</action>
  <acceptance_criteria>
    - `rg -n "export function getArchiveAccess|export function getResourcePoolAccess|export function getExportAccessLevel" packages/web/src/stores/useBillingStore.ts` returns three matches.
    - `rg -n "pdf_excel_api" packages/web/src/stores/useBillingStore.ts` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json</automated>
  </verify>
  <done>`useBillingStore.ts` exports concrete access helpers for `archive`, `resource_pool`, and `export`, so later plans can gate shell actions without raw object casting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend constraint UI normalization and modal copy for feature-gate denials</name>
  <files>packages/web/src/lib/constraintUi.ts, packages/web/src/components/LimitReachedModal.tsx</files>
  <read_first>
packages/web/src/lib/constraintUi.ts
packages/web/src/components/LimitReachedModal.tsx
packages/web/src/stores/useBillingStore.ts
packages/web/src/lib/billing.ts
.planning/phases/33-frontend-constraints-ux/33-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `ConstraintLimitKey` accepts `archive`, `resource_pool`, and `export`, and denial normalization keeps `feature_disabled` payloads intact.
    - Test 2: `buildConstraintModalContent()` can render export access copy that differentiates `pdf`, `pdf_excel`, and `pdf_excel_api`.
    - Test 3: `LimitReachedModal` continues to support the legacy scenario shim while using the expanded structured content for new gates.
  </behavior>
  <action>Update `packages/web/src/lib/constraintUi.ts` so `ConstraintLimitKey` includes `archive`, `resource_pool`, and `export`. Add denial-code support for `ARCHIVE_FEATURE_LOCKED`, `RESOURCE_POOL_FEATURE_LOCKED`, and `EXPORT_FEATURE_LOCKED`. Use the new billing-store selectors to produce modal content with concrete labels: `archive -> Архив проектов`, `resource_pool -> Пул ресурсов`, `export -> Экспорт`; and concrete export-level descriptions: `pdf`, `pdf_excel`, `pdf_excel_api`. For export, compute the next upgrade target from the current plan so free upgrades to `start` for PDF, `start` upgrades to `team` for Excel, and `team` upgrades to `enterprise` for API export. Then adjust `LimitReachedModal.tsx` so it renders that structured content without reintroducing scenario-specific branching.</action>
  <acceptance_criteria>
    - `rg -n "archive' \\| 'resource_pool' \\| 'export" packages/web/src/lib/constraintUi.ts` returns the expanded `ConstraintLimitKey`.
    - `rg -n "ARCHIVE_FEATURE_LOCKED|RESOURCE_POOL_FEATURE_LOCKED|EXPORT_FEATURE_LOCKED" packages/web/src/lib/constraintUi.ts packages/web/src/components/LimitReachedModal.tsx` returns matches.
    - `rg -n "pdf_excel|pdf_excel_api|Архив проектов|Пул ресурсов" packages/web/src/lib/constraintUi.ts` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json &amp;&amp; rg -n "ARCHIVE_FEATURE_LOCKED|RESOURCE_POOL_FEATURE_LOCKED|EXPORT_FEATURE_LOCKED|pdf_excel_api" packages/web/src/lib/constraintUi.ts packages/web/src/components/LimitReachedModal.tsx</automated>
  </verify>
  <done>The shared frontend contract can normalize archive/resource-pool/export denials and the modal renders plan-aware upgrade copy for all three Phase 34 gates.</done>
</task>

</tasks>

<verification>
Compile the web package after the helper expansion and grep for the new denial codes plus export tiers to confirm the contract is explicit and reusable.
</verification>

<success_criteria>
Frontend helper code no longer treats feature gates as ad-hoc cases: archive/resource_pool/export all have typed selectors, modal content, and upgrade semantics in one place.
</success_criteria>

<output>
After completion, create `.planning/phases/34-feature-gates/34-feature-gates-02-SUMMARY.md`
</output>
