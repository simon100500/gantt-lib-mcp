---
phase: 34-feature-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 34-02
  - 34-03
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/components/layout/ProjectMenu.tsx
autonomous: true
requirements:
  - GATE-03
must_haves:
  truths:
    - "The project shell explicitly shows which export formats are available on the current tariff."
    - "Locked export levels route through the same upsell modal instead of dead buttons or hidden copy."
    - "Start, Team, and Enterprise tiers are differentiated as PDF, PDF+Excel, and PDF+Excel+API in one visible surface."
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Export gate modal requests derived from billing export access level"
    - path: "packages/web/src/components/layout/ProjectMenu.tsx"
      provides: "Plan-aware export access card in the shell billing area"
  key_links:
    - from: "packages/web/src/components/layout/ProjectMenu.tsx"
      to: "packages/web/src/App.tsx"
      via: "onRequestExportLevel callback"
      pattern: "onRequestExportLevel"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/lib/constraintUi.ts"
      via: "EXPORT_FEATURE_LOCKED modal payload"
      pattern: "EXPORT_FEATURE_LOCKED"
---

<objective>
Add a concrete export-tier surface to the project shell that differentiates PDF, Excel, and API access by plan and routes locked levels through the shared modal.

Purpose: Complete GATE-03 with a visible, plan-aware export access contract even before the actual exporter feature lands.
Output: Export access card in the shell plus modal requests for locked levels.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/web/src/App.tsx
@packages/web/src/components/layout/ProjectMenu.tsx
@packages/web/src/lib/constraintUi.ts
@packages/web/src/stores/useBillingStore.ts
@packages/web/src/lib/billing.ts

<interfaces>
From packages/web/src/stores/useBillingStore.ts:
```ts
getExportAccessLevel(status): 'none' | 'pdf' | 'pdf_excel' | 'pdf_excel_api'
```

From packages/web/src/lib/billing.ts:
```ts
export const PLAN_FEATURES: Record<'start' | 'team' | 'enterprise', string[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export gate requests in App with requested level semantics</name>
  <files>packages/web/src/App.tsx</files>
  <read_first>
packages/web/src/App.tsx
packages/web/src/lib/constraintUi.ts
packages/web/src/stores/useBillingStore.ts
packages/mcp/src/constraints/catalog.ts
  </read_first>
  <behavior>
    - Test 1: App can request an export upsell for `pdf`, `pdf_excel`, or `pdf_excel_api` without introducing separate modal state.
    - Test 2: Locked export levels use `code: 'EXPORT_FEATURE_LOCKED'`, `limitKey: 'export'`, and `reasonCode: 'feature_disabled'`.
  </behavior>
  <action>Add an App-level callback such as `handleRequestExportLevel(requestedLevel)` that reads the current `getExportAccessLevel(billingStatus)` result and opens `openLimitModal(...)` only when the requested level exceeds the current plan. Use a structured payload with `code: 'EXPORT_FEATURE_LOCKED'`, `limitKey: 'export'`, `reasonCode: 'feature_disabled'`, and an upgrade hint that names the requested access (`PDF`, `Excel`, or `API`). Keep allowed levels non-blocking so the shell can display them as available without a modal. Pass this callback into `ProjectMenu` under an explicit prop name like `onRequestExportLevel`.</action>
  <acceptance_criteria>
    - `rg -n "handleRequestExportLevel|onRequestExportLevel" packages/web/src/App.tsx` returns matches.
    - `rg -n "EXPORT_FEATURE_LOCKED|limitKey: 'export'" packages/web/src/App.tsx` returns matches.
    - `rg -n "pdf_excel|pdf_excel_api" packages/web/src/App.tsx` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json &amp;&amp; rg -n "handleRequestExportLevel|EXPORT_FEATURE_LOCKED|pdf_excel_api" packages/web/src/App.tsx</automated>
  </verify>
  <done>`App.tsx` owns one explicit export-gate callback that can distinguish PDF, Excel, and API tier locks without inventing a second modal system.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render plan-aware export access tiers in the shell billing card</name>
  <files>packages/web/src/components/layout/ProjectMenu.tsx</files>
  <read_first>
packages/web/src/components/layout/ProjectMenu.tsx
packages/web/src/lib/billing.ts
packages/web/src/stores/useBillingStore.ts
packages/web/src/App.tsx
  </read_first>
  <behavior>
    - Test 1: The shell billing area visibly distinguishes `PDF`, `Excel`, and `API` export tiers by current plan.
    - Test 2: Locked levels call `onRequestExportLevel(...)`; unlocked levels render as available, non-upsell badges.
  </behavior>
  <action>Extend `ProjectMenu.tsx` with a compact export access card inside the existing billing footer. It should render three labeled levels (`PDF`, `Excel`, `API`) and style them by availability using the current `subscription` plan/export limit from the billing store. Add a new prop `onRequestExportLevel(level)` from App; call it only for locked levels. Concrete tier rules: free locks all three, start marks `PDF` available and `Excel`/`API` locked, team marks `PDF` and `Excel` available and `API` locked, enterprise marks all three available. Keep the existing `Расширить` button intact.</action>
  <acceptance_criteria>
    - `rg -n "onRequestExportLevel" packages/web/src/components/layout/ProjectMenu.tsx` returns a match.
    - `rg -n "PDF|Excel|API" packages/web/src/components/layout/ProjectMenu.tsx` returns three matches in the export card.
    - `rg -n "pdf_excel|pdf_excel_api|Расширить" packages/web/src/components/layout/ProjectMenu.tsx` returns matches proving tier logic and existing CTA coexist.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json &amp;&amp; rg -n "onRequestExportLevel|PDF|Excel|API|pdf_excel_api" packages/web/src/components/layout/ProjectMenu.tsx packages/web/src/App.tsx</automated>
  </verify>
  <done>The project shell shows plan-aware export availability and routes locked tiers through the shared modal, completing GATE-03.</done>
</task>

</tasks>

<verification>
Compile the web package and grep the shell/export files so the executor proves the export surface differentiates PDF, Excel, and API access by plan.
</verification>

<success_criteria>
Users can see exactly which export tiers their current plan grants, and locked tiers open the same structured upsell modal used by other tariff gates.
</success_criteria>

<output>
After completion, create `.planning/phases/34-feature-gates/34-feature-gates-04-SUMMARY.md`
</output>
