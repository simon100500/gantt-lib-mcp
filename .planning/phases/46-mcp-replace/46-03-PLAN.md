---
phase: 46-mcp-replace
plan: 03
type: execute
wave: 3
depends_on:
  - "46-01"
  - "46-02"
files_modified:
  - packages/server/package.json
  - packages/server/src/agent.ts
  - packages/server/src/agent.direct-tools.test.ts
  - packages/server/src/mutation/orchestrator.ts
  - packages/server/src/mutation/execution-routing.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Ordinary server-side agent execution uses an in-process tool surface instead of spawning `packages/mcp/dist/index.js`"
    - "The direct path preserves accepted/rejected tool accounting, authoritative verification, and compatibility fallback behavior"
    - "The server package owns its Qwen SDK integration explicitly rather than relying on MCP as the runtime boundary"
  artifacts:
    - path: "packages/server/src/agent.ts"
      provides: "Direct in-process tool execution for the ordinary app path"
      exports: ["runAgentWithHistory", "assessMutationOutcome", "buildHistoryContext"]
    - path: "packages/server/src/agent.direct-tools.test.ts"
      provides: "Regression coverage for the new in-process execution path"
      exports: []
    - path: "packages/server/package.json"
      provides: "Explicit direct-path runtime dependencies for the server package"
      exports: []
  key_links:
    - from: "packages/server/src/agent.ts"
      to: "packages/runtime-core/src/tool-core/index.ts"
      via: "SDK-embedded tool surface for ordinary agent execution"
      pattern: "createSdkMcpServer|NORMALIZED_TOOL_CATALOG"
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/mutation/orchestrator.ts"
      via: "staged mutation retained only as compatibility fallback"
      pattern: "runStagedMutation"
---

<objective>
Switch the ordinary server agent path to SDK-embedded in-process tool execution backed by the shared tool core.

Purpose: the main app path must stop paying for the external MCP stdio subprocess on ordinary requests.
Output: updated `packages/server/src/agent.ts`, explicit server dependencies, and regression coverage for the direct path plus fallback behavior.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-replace/46-CONTEXT.md
@.planning/reference/46-DIRECT-TOOLS-REPLAN.md
@.planning/reference/sdk-typescript.md
@packages/server/src/agent.ts
@packages/server/src/mutation/orchestrator.ts
@packages/server/src/mutation/execution-routing.ts
@packages/runtime-core/src/tool-core/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce the SDK-embedded direct tool path in the server runtime</name>
  <files>packages/server/package.json, packages/server/src/agent.ts</files>
  <read_first>
    packages/server/package.json
    packages/server/src/agent.ts
    .planning/reference/sdk-typescript.md
    packages/runtime-core/src/tool-core/index.ts
    packages/runtime-core/src/services/index.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "createSdkMcpServer|tool\\(" packages/server/src/agent.ts` finds the SDK-embedded tool setup
    - `rg -n "type: 'sdk'|mcpServers" packages/server/src/agent.ts` finds an in-process SDK server configuration for the ordinary path
    - `rg -n "packages/mcp/dist/index.js" packages/server/src/agent.ts` finds no unconditional ordinary-path startup of the external subprocess
    - `rg -n "@gantt/runtime-core|@qwen-code/sdk" packages/server/package.json` finds explicit direct-path dependencies
  </acceptance_criteria>
  <action>
    Update `packages/server/src/agent.ts` so ordinary agent execution uses an SDK-embedded in-process tool surface instead of launching the external MCP stdio server.

    Implement the direct path with the Qwen SDK primitives documented in `.planning/reference/sdk-typescript.md`:
    - build an SDK-embedded MCP server or equivalent in-process tool host from the shared runtime-core tool catalog
    - pass that embedded server through the ordinary `query(...)` flow for the main app path

    Preserve existing user-facing semantics:
    - targeted read before mutation
    - accepted/rejected tool-call tracking
    - authoritative changed-set verification
    - Russian-language response behavior

    Keep a compatibility-only legacy path behind an explicit feature flag or non-default branch if you still need the old subprocess during migration, but the default ordinary path must not spawn `packages/mcp/dist/index.js`.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/server</automated>
  </verify>
  <done>
    The main server agent path can expose the normalized tool surface in-process without the external MCP subprocess.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep staged mutation as compatibility fallback and lock direct-path behavior with tests</name>
  <files>packages/server/src/agent.direct-tools.test.ts, packages/server/src/agent.ts, packages/server/src/mutation/orchestrator.ts, packages/server/src/mutation/execution-routing.ts</files>
  <read_first>
    packages/server/src/agent.ts
    packages/server/src/mutation/orchestrator.ts
    packages/server/src/mutation/execution-routing.ts
    packages/server/src/mutation/orchestrator.test.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "legacyFallbackAllowed|compatibility|direct_path|embedded" packages/server/src/agent.ts packages/server/src/mutation/execution-routing.ts` finds explicit direct-path versus compatibility fallback semantics
    - `rg -n "createSdkMcpServer|direct tool path|legacy fallback|acceptedMutationCalls|changedTaskIds" packages/server/src/agent.direct-tools.test.ts` finds regression coverage for the new ordinary path
    - `rg -n "packages/mcp/dist/index.js" packages/server/src/agent.direct-tools.test.ts` returns no matches for the default execution assertions
  </acceptance_criteria>
  <action>
    Retune the server mutation orchestration so staged mutation and any legacy fallback remain safety mechanisms instead of the default path.

    In `packages/server/src/mutation/execution-routing.ts` and related orchestration code:
    - make the direct in-process tool loop the preferred ordinary path
    - retain staged mutation only where ambiguity, safety, or explicit compatibility constraints require it

    Add a focused regression suite in `packages/server/src/agent.direct-tools.test.ts` that proves:
    - ordinary mutation requests use the embedded direct path by default
    - accepted and rejected tool-call accounting still works
    - authoritative verification still gates success
    - compatibility fallback is triggered only when direct-path execution remains insufficient
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.direct-tools.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The direct path is the server default, while staged mutation remains available only as an explicit fallback mechanism.
  </done>
</task>

</tasks>

<verification>
Confirm that the ordinary server agent loop now uses SDK-embedded in-process tools by default and preserves authoritative verification semantics.
</verification>

<success_criteria>
The main app path no longer depends on the external MCP subprocess for ordinary requests, and fallback behavior remains explicit and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/46-mcp-replace/46-03-SUMMARY.md`
</output>
