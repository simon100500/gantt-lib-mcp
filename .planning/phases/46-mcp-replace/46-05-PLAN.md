---
phase: 46-mcp-replace
plan: 05
type: execute
wave: 5
depends_on:
  - "46-01"
  - "46-02"
  - "46-03"
  - "46-04"
files_modified:
  - packages/server/src/agent.ts
  - packages/server/src/agent.direct-tools.test.ts
  - .planning/phases/46-mcp-replace/46-HUMAN-UAT.md
  - .planning/phases/46-mcp-replace/46-VERIFICATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The phase ends with explicit evidence that the default app path is direct in-process, while compatibility fallback remains visible and bounded"
    - "Telemetry and regression coverage document direct-path success, fallback, and unnecessary-tool-call behavior"
    - "Human verification artifacts trace the replan acceptance criteria to concrete commands and user flows"
  artifacts:
    - path: "packages/server/src/agent.ts"
      provides: "Direct-path telemetry and cutover evidence in the main agent runtime"
      exports: ["runAgentWithHistory"]
    - path: ".planning/phases/46-mcp-replace/46-VERIFICATION.md"
      provides: "Phase verification report against the direct-tooling replan"
      exports: []
    - path: ".planning/phases/46-mcp-replace/46-HUMAN-UAT.md"
      provides: "Manual validation script for direct-path behavior and retained MCP adapter role"
      exports: []
  key_links:
    - from: ".planning/phases/46-mcp-replace/46-VERIFICATION.md"
      to: ".planning/reference/46-DIRECT-TOOLS-REPLAN.md"
      via: "success-criteria traceability for the replan"
      pattern: "Acceptance Criteria"
    - from: ".planning/phases/46-mcp-replace/46-HUMAN-UAT.md"
      to: "packages/server/src/agent.ts"
      via: "manual checks that the ordinary path uses direct in-process tooling"
      pattern: "direct path|fallback"
---

<objective>
Finish the phase with telemetry, regression evidence, and verification artifacts proving the direct in-process cutover.

Purpose: the replan requires not just refactoring but proof that the primary app path changed and that MCP remains only an adapter.
Output: bounded telemetry in the agent runtime, final regression assertions, and human verification docs.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-replace/46-CONTEXT.md
@.planning/reference/46-DIRECT-TOOLS-REPLAN.md
@packages/server/src/agent.ts
@packages/server/src/agent.direct-tools.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct-path telemetry and final regression assertions</name>
  <files>packages/server/src/agent.ts, packages/server/src/agent.direct-tools.test.ts</files>
  <read_first>
    packages/server/src/agent.ts
    packages/server/src/agent.direct-tools.test.ts
    .planning/reference/46-DIRECT-TOOLS-REPLAN.md
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "direct_tool_path|legacy_subprocess_fallback|embedded_tool_call|fallback_rate" packages/server/src/agent.ts` finds explicit direct-path telemetry fields or events
    - `rg -n "no external MCP subprocess|direct path by default|fallback only after verification|acceptedMutationCalls" packages/server/src/agent.direct-tools.test.ts` finds final regression assertions
    - `rg -n "packages/mcp/dist/index.js" packages/server/src/agent.direct-tools.test.ts` returns no matches in the default-path assertions
  </acceptance_criteria>
  <action>
    Add final cutover telemetry to `packages/server/src/agent.ts` so the direct path is observable in logs and regressions.

    Log explicit events or fields for:
    - direct in-process tool path selected
    - compatibility fallback selected
    - tool-call count for the run
    - whether authoritative verification accepted the first direct pass

    Extend `packages/server/src/agent.direct-tools.test.ts` so it asserts:
    - the ordinary path uses the direct in-process tool loop by default
    - compatibility fallback occurs only after direct-path insufficiency
    - accepted mutation calls and changed-task verification remain synchronized
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.direct-tools.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The direct-path cutover is visible in logs and locked by final regression tests.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Produce final human verification artifacts for the direct-tooling architecture</name>
  <files>.planning/phases/46-mcp-replace/46-HUMAN-UAT.md, .planning/phases/46-mcp-replace/46-VERIFICATION.md</files>
  <read_first>
    .planning/reference/46-DIRECT-TOOLS-REPLAN.md
    .planning/reference/mcp-mutation-refactor-prd.md
    .planning/phases/46-mcp-replace/46-CONTEXT.md
    .planning/phases/45-history-refactor/45-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "ordinary conversational mutation requests|direct in-process tool path|MCP adapter still works" .planning/phases/46-mcp-replace/46-HUMAN-UAT.md` finds the core manual validation flows
    - `rg -n "packages/mcp/dist/index.js|@gantt/runtime-core|createSdkMcpServer|find_tasks" .planning/phases/46-mcp-replace/46-VERIFICATION.md` finds architectural traceability and evidence targets
    - `rg -n "fallback rate|tool calls per request|verification failure rate" .planning/phases/46-mcp-replace/46-VERIFICATION.md` finds the required telemetry review points
  </acceptance_criteria>
  <action>
    Create the final verification artifacts for Phase 46.

    In `.planning/phases/46-mcp-replace/46-HUMAN-UAT.md`, document manual checks for:
    - an ordinary conversational edit completing on the direct in-process path
    - a guarded compatibility-fallback case
    - MCP adapter usage remaining functional for the same normalized surface

    In `.planning/phases/46-mcp-replace/46-VERIFICATION.md`, map the replan acceptance criteria one by one:
    1. ordinary requests no longer require `packages/mcp/dist/index.js`
    2. the primary app path uses shared in-process handlers
    3. Prisma-backed authoritative services are no longer MCP-owned
    4. MCP still works as an adapter over the same normalized contracts
    5. staged mutation is fallback-only
    6. tests and live checks prove direct-path behavior, parity, and telemetry quality

    Include concrete file references, commands, and test evidence placeholders so execution can close the phase with explicit proof instead of narrative only.
  </action>
  <verify>
    <manual>Follow `.planning/phases/46-mcp-replace/46-HUMAN-UAT.md` against a project where one ordinary mutation succeeds directly and one controlled fallback case can be observed.</manual>
  </verify>
  <done>
    The phase ends with explicit verification artifacts showing the direct-path cutover and the retained MCP adapter role.
  </done>
</task>

</tasks>

<verification>
Confirm that the phase has concrete evidence for the direct-path cutover, bounded fallback behavior, and continued MCP adapter support.
</verification>

<success_criteria>
Phase 46 is ready to execute with explicit telemetry, regression evidence, and human verification steps tied to the replan.
</success_criteria>

<output>
After completion, create `.planning/phases/46-mcp-replace/46-05-SUMMARY.md`
</output>
