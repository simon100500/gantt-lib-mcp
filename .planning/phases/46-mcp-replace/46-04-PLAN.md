---
phase: 46-mcp-replace
plan: 04
type: execute
wave: 4
depends_on:
  - "46-02"
  - "46-03"
files_modified:
  - packages/mcp/src/index.ts
  - packages/mcp/src/public-tools.ts
  - packages/mcp/src/index.test.ts
  - packages/runtime-core/src/tool-core/adapter-parity.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The MCP package becomes a transport adapter over the shared tool core rather than an owner of unique business logic"
    - "The public MCP tool list is derived from the shared catalog instead of being maintained manually in parallel"
    - "Adapter parity is locked with automated tests against the shared direct handler behavior"
  artifacts:
    - path: "packages/mcp/src/index.ts"
      provides: "Thin MCP transport adapter over shared tool-core handlers"
      exports: ["handleListToolsRequest", "handleCallToolRequest"]
    - path: "packages/mcp/src/public-tools.ts"
      provides: "Adapter-facing tool-list export sourced from the shared catalog"
      exports: ["PUBLIC_MCP_TOOLS"]
    - path: "packages/mcp/src/index.test.ts"
      provides: "Adapter-level regression coverage for MCP request/response shaping"
      exports: []
  key_links:
    - from: "packages/mcp/src/index.ts"
      to: "packages/runtime-core/src/tool-core/handlers.ts"
      via: "transport-only delegation to shared tool execution"
      pattern: "executeToolCall"
    - from: "packages/mcp/src/public-tools.ts"
      to: "packages/runtime-core/src/tool-core/catalog.ts"
      via: "shared catalog to MCP list transformation"
      pattern: "NORMALIZED_TOOL_CATALOG"
---

<objective>
Thin the MCP package into an adapter over the shared tool core and prove parity between the adapter and the direct path.

Purpose: MCP remains supported, but only as a transport shell over shared contracts and handlers.
Output: a thin `packages/mcp/src/index.ts`, a derived public-tools list, and parity tests.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-replace/46-CONTEXT.md
@packages/mcp/src/index.ts
@packages/mcp/src/public-tools.ts
@packages/runtime-core/src/tool-core/catalog.ts
@packages/runtime-core/src/tool-core/handlers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor MCP list/call handling into transport-only delegation</name>
  <files>packages/mcp/src/index.ts, packages/mcp/src/public-tools.ts</files>
  <read_first>
    packages/mcp/src/index.ts
    packages/mcp/src/public-tools.ts
    packages/runtime-core/src/tool-core/catalog.ts
    packages/runtime-core/src/tool-core/handlers.ts
    packages/runtime-core/src/tool-core/types.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "NORMALIZED_TOOL_CATALOG|executeToolCall" packages/mcp/src/index.ts packages/mcp/src/public-tools.ts` finds direct delegation to the shared tool core
    - `rg -n "if \\(name === 'get_project_summary'\\)|if \\(name === 'create_tasks'\\)|if \\(name === 'shift_tasks'\\)" packages/mcp/src/index.ts` returns no matches
    - `rg -n "PUBLIC_MCP_TOOLS = NORMALIZED_TOOL_CATALOG|map\\(" packages/mcp/src/public-tools.ts` finds derived tool-list construction instead of manual duplication
    - `rg -n "content: \\[" packages/runtime-core/src/tool-core/handlers.ts` returns no matches, keeping MCP wrapping adapter-only
  </acceptance_criteria>
  <action>
    Replace the MCP package's hand-written tool branching with shared tool-core delegation.

    In `packages/mcp/src/public-tools.ts`:
    - derive `PUBLIC_MCP_TOOLS` from the shared runtime-core catalog instead of maintaining a second manually written list

    In `packages/mcp/src/index.ts`:
    - keep only transport-level concerns such as MCP request parsing, MCP `content` envelope shaping, and tool-call logging
    - delegate actual tool semantics to the shared tool-core execution layer
    - remove the giant tool-by-tool business-logic branch chain

    Preserve the public MCP contract shape while making the implementation adapter-only.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/mcp</automated>
  </verify>
  <done>
    MCP becomes a thin transport shell over the shared tool core instead of an independent implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parity coverage between direct shared handlers and the MCP adapter</name>
  <files>packages/mcp/src/index.test.ts, packages/runtime-core/src/tool-core/adapter-parity.test.ts</files>
  <read_first>
    packages/mcp/src/index.ts
    packages/runtime-core/src/tool-core/handlers.ts
    packages/runtime-core/src/tool-core/catalog.ts
    packages/runtime-core/src/tool-core/handlers.test.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "handleCallToolRequest|handleListToolsRequest|get_project_summary|find_tasks|create_tasks|validate_schedule" packages/mcp/src/index.test.ts` finds adapter-level coverage
    - `rg -n "adapter parity|same normalized result|same tool catalog" packages/runtime-core/src/tool-core/adapter-parity.test.ts` finds direct-vs-adapter parity assertions
    - `rg -n "packages/mcp/dist/index.js" packages/mcp/src/index.test.ts packages/runtime-core/src/tool-core/adapter-parity.test.ts` returns no matches
  </acceptance_criteria>
  <action>
    Add automated parity coverage that proves the MCP adapter and the direct shared handlers expose the same normalized tool behavior.

    Add:
    - `packages/mcp/src/index.test.ts` for MCP request/list/call shaping and adapter delegation
    - `packages/runtime-core/src/tool-core/adapter-parity.test.ts` for shared-result parity assertions

    Cover at least:
    - tool list parity from the shared catalog
    - one read tool (`get_project_summary` or `find_tasks`)
    - one mutation tool (`create_tasks` or `shift_tasks`)
    - one validation tool (`validate_schedule`)

    The adapter may wrap results in MCP `content`, but the underlying semantic payload must match the direct handler output.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/mcp/src/index.test.ts && npx tsx --test packages/runtime-core/src/tool-core/adapter-parity.test.ts && npm run build -w packages/mcp && npm run build -w packages/runtime-core</automated>
  </verify>
  <done>
    The MCP adapter is proven to be a thin shell over the same shared contracts and handlers as the direct path.
  </done>
</task>

</tasks>

<verification>
Confirm that MCP list/call behavior is now adapter-only and that direct-path/tool-core semantics match the adapter path.
</verification>

<success_criteria>
MCP remains supported without owning unique tool logic, and parity between adapter and direct execution is locked by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/46-mcp-replace/46-04-SUMMARY.md`
</output>
