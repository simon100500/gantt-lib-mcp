---
phase: 46-mcp-replace
plan: 02
type: execute
wave: 2
depends_on:
  - "46-01"
files_modified:
  - packages/runtime-core/src/tool-core/catalog.ts
  - packages/runtime-core/src/tool-core/types.ts
  - packages/runtime-core/src/tool-core/context.ts
  - packages/runtime-core/src/tool-core/handlers.ts
  - packages/runtime-core/src/tool-core/index.ts
  - packages/runtime-core/src/tool-core/handlers.test.ts
  - packages/runtime-core/package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "One shared tool-core module owns normalized tool definitions, typed contracts, and transport-neutral handler execution"
    - "The tool catalog covers both the existing normalized mutation surface and the resolution-friendly additions required by the replan"
    - "The shared handler layer can be consumed by both direct in-process execution and the later MCP adapter"
  artifacts:
    - path: "packages/runtime-core/src/tool-core/catalog.ts"
      provides: "Single source of truth for normalized tool metadata"
      exports: ["NORMALIZED_TOOL_CATALOG", "NORMALIZED_TOOL_NAMES"]
    - path: "packages/runtime-core/src/tool-core/handlers.ts"
      provides: "Transport-neutral tool handler execution over runtime-core services"
      exports: ["createToolHandlers", "executeToolCall"]
    - path: "packages/runtime-core/src/tool-core/types.ts"
      provides: "Typed input/output contracts for shared tool execution"
      exports: ["ToolCallContext", "ToolCallResult"]
  key_links:
    - from: "packages/runtime-core/src/tool-core/handlers.ts"
      to: "packages/runtime-core/src/services/index.ts"
      via: "authoritative service execution without MCP transport coupling"
      pattern: "commandService|taskService|historyService"
    - from: "packages/runtime-core/src/tool-core/catalog.ts"
      to: "packages/runtime-core/src/tool-core/handlers.ts"
      via: "catalog entries resolve to shared handler implementations"
      pattern: "handler"
---

<objective>
Create the shared tool core that defines the normalized tool surface and executes handlers in-process without MCP transport dependencies.

Purpose: both the server direct path and the MCP adapter need the same catalog, contracts, and execution semantics.
Output: `packages/runtime-core/src/tool-core/*` plus automated coverage for handler parity and normalized results.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-replace/46-CONTEXT.md
@.planning/reference/46-DIRECT-TOOLS-REPLAN.md
@.planning/reference/mcp-mutation-refactor-prd.md
@packages/mcp/src/public-tools.ts
@packages/mcp/src/index.ts
@packages/runtime-core/src/services/index.ts
@packages/runtime-core/src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the canonical shared tool catalog and typed execution context</name>
  <files>packages/runtime-core/src/tool-core/catalog.ts, packages/runtime-core/src/tool-core/types.ts, packages/runtime-core/src/tool-core/context.ts, packages/runtime-core/src/tool-core/index.ts, packages/runtime-core/package.json</files>
  <read_first>
    packages/mcp/src/public-tools.ts
    packages/mcp/src/types.ts
    packages/runtime-core/src/types.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
    .planning/reference/46-DIRECT-TOOLS-REPLAN.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "get_project_summary|get_schedule_slice|find_tasks|get_task_context|create_tasks|update_tasks|move_tasks|shift_tasks|delete_tasks|link_tasks|unlink_tasks|recalculate_project|validate_schedule" packages/runtime-core/src/tool-core/catalog.ts` finds the canonical tool set
    - `rg -n "export type ToolCallContext|export type ToolCallResult" packages/runtime-core/src/tool-core/types.ts` finds the typed shared handler contracts
    - `rg -n "NORMALIZED_TOOL_CATALOG|NORMALIZED_TOOL_NAMES" packages/runtime-core/src/tool-core/index.ts` finds the package entrypoint exports
    - `rg -n "PUBLIC_MCP_TOOLS" packages/runtime-core/src/tool-core/catalog.ts` returns no matches
  </acceptance_criteria>
  <action>
    Build a new `tool-core` module inside `packages/runtime-core/src/` as the only source of truth for normalized tool definitions.

    Create:
    - `catalog.ts` for the shared tool metadata
    - `types.ts` for typed input/output and execution-context contracts
    - `context.ts` for constructing the handler dependency bundle from runtime-core services
    - `index.ts` for package-level exports

    The catalog must explicitly include the current normalized read/mutation surface plus the resolution-friendly addition `find_tasks`.

    Keep the tool metadata transport-neutral:
    - names
    - descriptions
    - JSON-schema input contracts
    - handler keys or bindings

    Do not hardcode MCP transport response envelopes or stdio behavior into the catalog.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/runtime-core</automated>
  </verify>
  <done>
    The repository has one canonical tool catalog and one typed handler contract shared across direct and MCP execution modes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transport-neutral handlers and lock normalized result behavior with tests</name>
  <files>packages/runtime-core/src/tool-core/handlers.ts, packages/runtime-core/src/tool-core/handlers.test.ts, packages/runtime-core/src/tool-core/context.ts</files>
  <read_first>
    packages/mcp/src/index.ts
    packages/runtime-core/src/services/command.service.ts
    packages/runtime-core/src/services/task.service.ts
    packages/runtime-core/src/services/history.service.ts
    packages/runtime-core/src/services/projectScheduleOptions.ts
    packages/runtime-core/src/tool-core/catalog.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export function createToolHandlers|export async function executeToolCall" packages/runtime-core/src/tool-core/handlers.ts` finds the shared handler API
    - `rg -n "buildMutationResult|buildRejectedResult|executeNormalizedMutationBatch|jsonResult" packages/runtime-core/src/tool-core/handlers.ts` finds no matches for MCP-specific response helpers copied from the adapter
    - `rg -n "find_tasks|get_project_summary|create_tasks|shift_tasks|validate_schedule" packages/runtime-core/src/tool-core/handlers.test.ts` finds coverage over read and mutation handlers
    - `rg -n "status: 'accepted'|status: 'rejected'|changedTaskIds|reason" packages/runtime-core/src/tool-core/handlers.test.ts` finds normalized mutation-result assertions
  </acceptance_criteria>
  <action>
    Implement transport-neutral handler execution in `packages/runtime-core/src/tool-core/handlers.ts`.

    The shared handler layer must:
    - resolve a tool name against the canonical catalog
    - execute the matching read or mutation logic using runtime-core services
    - return typed plain objects, not MCP `content` envelopes
    - preserve normalized mutation-result semantics (`status`, `reason`, `changedTaskIds`, `changedTasks`, `snapshot` when requested)

    Add `find_tasks` as a real shared capability that searches project tasks by normalized text and returns compact matches with score, parent path, and dates.

    Add automated tests in `packages/runtime-core/src/tool-core/handlers.test.ts` that prove:
    - read handlers return plain typed data
    - mutation handlers preserve accepted/rejected normalized result semantics
    - `find_tasks` returns deterministic compact matches
    - the shared handler output can be consumed without MCP-specific wrapping
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/runtime-core/src/tool-core/handlers.test.ts && npm run build -w packages/runtime-core</automated>
  </verify>
  <done>
    The shared tool core can execute normalized tool calls directly in-process and return transport-neutral results.
  </done>
</task>

</tasks>

<verification>
Confirm that one shared tool core defines the normalized tool surface and can execute handlers without MCP transport wrappers.
</verification>

<success_criteria>
The direct path and the MCP adapter can both consume one shared catalog and one shared handler layer rather than duplicating tool logic.
</success_criteria>

<output>
After completion, create `.planning/phases/46-mcp-replace/46-02-SUMMARY.md`
</output>
