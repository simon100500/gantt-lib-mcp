---
phase: 46-mcp-replace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime-core/package.json
  - packages/runtime-core/tsconfig.json
  - packages/runtime-core/src/index.ts
  - packages/runtime-core/src/types.ts
  - packages/runtime-core/src/prisma.ts
  - packages/runtime-core/src/services/index.ts
  - packages/runtime-core/src/services/command.service.ts
  - packages/runtime-core/src/services/history.service.ts
  - packages/runtime-core/src/services/task.service.ts
  - packages/runtime-core/src/services/project.service.ts
  - packages/runtime-core/src/services/message.service.ts
  - packages/runtime-core/src/services/projectScheduleOptions.ts
  - packages/runtime-core/src/services/enforcement.service.ts
  - packages/mcp/package.json
  - packages/mcp/src/services/index.ts
  - packages/mcp/src/types.ts
  - packages/mcp/src/prisma.ts
  - packages/server/package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Transport-neutral Prisma access, normalized types, and authoritative services live under `@gantt/runtime-core` instead of MCP-owned runtime paths"
    - "`@gantt/mcp` can consume the shared runtime core without remaining the owner of those modules"
    - "`packages/server` has a direct dependency path to the shared runtime core needed for the later in-process cutover"
  artifacts:
    - path: "packages/runtime-core/src/index.ts"
      provides: "Workspace entrypoint for shared runtime exports"
      exports: ["./types", "./prisma", "./services"]
    - path: "packages/runtime-core/src/services/index.ts"
      provides: "Authoritative Prisma-backed services under transport-neutral ownership"
      exports: ["commandService", "historyService", "taskService", "projectService", "messageService"]
    - path: "packages/runtime-core/src/types.ts"
      provides: "Normalized tool contracts and shared runtime types under transport-neutral ownership"
      exports: ["NormalizedMutationResult", "ProjectCommand", "CommitProjectCommandResponse"]
  key_links:
    - from: "packages/mcp/src/services/index.ts"
      to: "packages/runtime-core/src/services/index.ts"
      via: "compatibility re-export during migration"
      pattern: "@gantt/runtime-core/services"
    - from: "packages/server/package.json"
      to: "packages/runtime-core/package.json"
      via: "direct runtime-core dependency for the server package"
      pattern: "@gantt/runtime-core"
---

<objective>
Create the transport-neutral runtime package that owns shared Prisma-backed services, normalized types, and Prisma bootstrap.

Purpose: the direct in-process app path cannot keep depending on MCP-owned runtime modules.
Output: a new `@gantt/runtime-core` workspace package plus compatibility re-exports so later plans can switch consumers incrementally.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-replace/46-CONTEXT.md
@.planning/reference/46-DIRECT-TOOLS-REPLAN.md
@packages/mcp/src/services/index.ts
@packages/mcp/src/types.ts
@packages/mcp/src/prisma.ts
@packages/mcp/package.json
@packages/server/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `@gantt/runtime-core` and move transport-neutral runtime primitives into it</name>
  <files>packages/runtime-core/package.json, packages/runtime-core/tsconfig.json, packages/runtime-core/src/index.ts, packages/runtime-core/src/types.ts, packages/runtime-core/src/prisma.ts, packages/runtime-core/src/services/projectScheduleOptions.ts</files>
  <read_first>
    package.json
    packages/mcp/package.json
    packages/server/package.json
    packages/mcp/src/types.ts
    packages/mcp/src/prisma.ts
    packages/mcp/src/services/projectScheduleOptions.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "\"name\": \"@gantt/runtime-core\"" packages/runtime-core/package.json` finds the new workspace package
    - `rg -n "\"./types\"|\"./prisma\"|\"./services\"" packages/runtime-core/package.json` finds explicit runtime-core exports
    - `rg -n "export type ProjectCommand|export interface NormalizedMutationResult" packages/runtime-core/src/types.ts` finds migrated normalized runtime contracts
    - `rg -n "export function getPrisma|export const getPrisma" packages/runtime-core/src/prisma.ts` finds the shared Prisma bootstrap
    - `rg -n "@gantt/runtime-core" packages/server/package.json packages/mcp/package.json` finds direct workspace dependencies
  </acceptance_criteria>
  <action>
    Create a new workspace package at `packages/runtime-core` with package name `@gantt/runtime-core`.

    Move the transport-neutral runtime primitives out of MCP ownership and into this package:
    - normalized shared types from `packages/mcp/src/types.ts`
    - Prisma bootstrap from `packages/mcp/src/prisma.ts`
    - shared calendar/schedule-options helpers from `packages/mcp/src/services/projectScheduleOptions.ts`

    Keep the new package concrete and directly consumable:
    - add `package.json` with exports for `.`, `./types`, `./prisma`, and `./services`
    - add a package-level `tsconfig.json`
    - add `src/index.ts` that re-exports `types`, `prisma`, and `services`

    Update `packages/server/package.json` and `packages/mcp/package.json` to depend on `@gantt/runtime-core` directly instead of relying on transitive access.

    Do not introduce MCP transport code, stdio concerns, or public MCP server registration into this package.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/runtime-core</automated>
  </verify>
  <done>
    The repository has a transport-neutral runtime-core package that can own shared services and types independently of MCP.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move authoritative services into runtime-core and leave MCP compatibility re-exports</name>
  <files>packages/runtime-core/src/services/index.ts, packages/runtime-core/src/services/command.service.ts, packages/runtime-core/src/services/history.service.ts, packages/runtime-core/src/services/task.service.ts, packages/runtime-core/src/services/project.service.ts, packages/runtime-core/src/services/message.service.ts, packages/runtime-core/src/services/enforcement.service.ts, packages/mcp/src/services/index.ts, packages/mcp/src/types.ts, packages/mcp/src/prisma.ts</files>
  <read_first>
    packages/mcp/src/services/index.ts
    packages/mcp/src/services/command.service.ts
    packages/mcp/src/services/history.service.ts
    packages/mcp/src/services/task.service.ts
    packages/mcp/src/services/message.service.ts
    packages/mcp/src/services/enforcement.service.ts
    packages/runtime-core/src/index.ts
    .planning/phases/46-mcp-replace/46-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export \\{ commandService|export \\{ historyService|export \\{ taskService" packages/runtime-core/src/services/index.ts` finds the migrated service barrel
    - `rg -n "@gantt/runtime-core/services|@gantt/runtime-core/types|@gantt/runtime-core/prisma" packages/mcp/src/services/index.ts packages/mcp/src/types.ts packages/mcp/src/prisma.ts` finds MCP compatibility re-exports
    - `rg -n "@gantt/runtime-core" packages/runtime-core/src/services/*.ts` finds service-local imports resolved through the new package boundary
    - `rg -n "from './services/" packages/mcp/src/index.ts` still compiles only through MCP adapter concerns and not through service ownership assumptions
  </acceptance_criteria>
  <action>
    Move the authoritative Prisma-backed services that the app path depends on into `packages/runtime-core/src/services/`.

    Migrate at least these concrete modules:
    - `command.service.ts`
    - `history.service.ts`
    - `task.service.ts`
    - `project.service.ts`
    - `message.service.ts`
    - `enforcement.service.ts`

    Then make the old MCP entrypoints compatibility shims only:
    - `packages/mcp/src/services/index.ts` should re-export from `@gantt/runtime-core/services`
    - `packages/mcp/src/types.ts` should re-export from `@gantt/runtime-core/types`
    - `packages/mcp/src/prisma.ts` should re-export from `@gantt/runtime-core/prisma`

    Preserve existing runtime behavior and import signatures where possible so later plans can switch server and MCP wiring without a second ownership move.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/runtime-core && npm run build -w packages/mcp && npm run build -w packages/server</automated>
  </verify>
  <done>
    The shared runtime is owned by `@gantt/runtime-core`, while MCP remains compatible through re-export-only shims.
  </done>
</task>

</tasks>

<verification>
Confirm that the transport-neutral package owns the shared runtime and that MCP can compile as a consumer rather than as the owner.
</verification>

<success_criteria>
The repository has one shared runtime package for Prisma, normalized types, and authoritative services, and later plans can build on it without another ownership refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/46-mcp-replace/46-01-SUMMARY.md`
</output>
