---
phase: 22-zustand-frontend-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/stores/useAuthStore.ts
  - packages/web/src/hooks/useAuth.ts
autonomous: true
requirements:
  - WEB-ZUSTAND-03

must_haves:
  truths:
    - "useAuthStore fully owns auth, session, and project state"
    - "Local storage sync and visibility refresh behavior live in the auth store"
    - "Project switching, project creation, logout, and token refresh come from the store"
    - "useAuth.ts remains a compatibility wrapper during migration"
    - "Auth consumers can be moved off the old hook without changing behavior"
  artifacts:
    - path: "packages/web/src/stores/useAuthStore.ts"
      provides: "Auth store with session, project, and token actions"
      contains: "refreshAccessToken"
    - path: "packages/web/src/hooks/useAuth.ts"
      provides: "Compatibility hook delegating to the auth store"
      contains: "useAuthStore"
  key_links:
    - from: "packages/web/src/hooks/useAuth.ts"
      to: "packages/web/src/stores/useAuthStore.ts"
      via: "hook delegation"
      pattern: "useAuthStore"
    - from: "packages/web/src/stores/useAuthStore.ts"
      to: "window.localStorage"
      via: "session persistence"
      pattern: "localStorage|storage|visibilitychange"
---

<objective>
Move auth/session/project ownership into Zustand so the web app stops treating auth as a separate mutable subsystem. The store should own token refresh, project switching, project creation, and persistence side effects while the old hook remains available as a thin wrapper for the rest of the migration.
</objective>

<context>
@packages/web/src/hooks/useAuth.ts
@packages/web/src/App.tsx
@packages/web/src/components/LoginButton.tsx
@packages/web/src/components/ProjectSwitcher.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement the auth store</name>
  <files>packages/web/src/stores/useAuthStore.ts</files>
  <action>
  Create `useAuthStore.ts` as the source of truth for auth/session/project state.

  The store must own:
  - `user`, `project`, `projects`, `accessToken`, and `isAuthenticated`
  - login/logout flow
  - project switching
  - token refresh
  - project creation
  - task-count synchronization for the active project

  Move localStorage sync, token refresh wiring, and visibility listener behavior into the store module or adjacent store helpers. Keep the implementation explicit and local to the auth domain.
  </action>
  <verify>
    <automated>rg -n "refreshAccessToken|createProject|syncProjectTaskCount|logout|switchProject" packages/web/src/stores/useAuthStore.ts</automated>
    <automated>rg -n "localStorage|visibilitychange|storage" packages/web/src/stores/useAuthStore.ts</automated>
  </verify>
  <done>
    Auth state and auth side effects are centralized in the Zustand store.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repoint the legacy auth hook to the store</name>
  <files>packages/web/src/hooks/useAuth.ts</files>
  <action>
  Keep `useAuth.ts` available during the migration, but make it a thin wrapper over `useAuthStore`.

  The wrapper should preserve the existing call sites while ensuring the underlying state now comes from the store. Do not duplicate auth state or side effects in the hook.
  </action>
  <verify>
    <automated>rg -n "useAuthStore" packages/web/src/hooks/useAuth.ts</automated>
    <automated>rg -n "useState|useEffect" packages/web/src/hooks/useAuth.ts</automated>
  </verify>
  <done>
    The old hook remains stable, but the underlying source of truth is the auth store.
  </done>
</task>
</tasks>

<verification>
1. `npm run build --workspace=packages/web`
2. `rg -n "useAuthStore" packages/web/src/hooks/useAuth.ts packages/web/src/stores/useAuthStore.ts`
3. `rg -n "localStorage|visibilitychange|storage" packages/web/src/stores/useAuthStore.ts`
</verification>

<success_criteria>
- `useAuthStore` owns auth/session/project state and side effects.
- `useAuth.ts` delegates to the store instead of maintaining its own state model.
- Project switching, refresh, and persistence all run through the store.
- `packages/web` still compiles after the migration step.
</success_criteria>
