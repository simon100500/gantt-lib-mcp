---
phase: 22-zustand-frontend-refactor
plan: 04
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
  - 22-03
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/components/workspace/SharedWorkspace.tsx
  - packages/web/src/components/workspace/GuestWorkspace.tsx
  - packages/web/src/components/workspace/DraftWorkspace.tsx
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
  - packages/web/src/components/layout/Toolbar.tsx
  - packages/web/src/components/layout/ProjectMenu.tsx
autonomous: true
requirements:
  - WEB-ZUSTAND-06
  - WEB-ZUSTAND-07

must_haves:
  truths:
    - "App.tsx becomes a thin workspace router"
    - "Workspace rendering is split into explicit workspace components"
    - "Toolbar reads only UI store state and actions"
    - "ProjectWorkspace mostly composes existing building blocks and store selectors"
    - "The refactor removes the monolithic orchestration logic from App.tsx"
  artifacts:
    - path: "packages/web/src/App.tsx"
      provides: "Workspace router that selects the correct workspace shell"
      contains: "SharedWorkspace|GuestWorkspace|DraftWorkspace|ProjectWorkspace"
    - path: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      provides: "Main workspace shell composed from existing UI pieces"
      contains: "GanttChart|ChatSidebar|useTaskStore|useChatStore"
    - path: "packages/web/src/components/layout/Toolbar.tsx"
      provides: "Toolbar driven only by UI-store selectors and actions"
      contains: "useUIStore"
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/workspace/*"
      via: "workspace router"
      pattern: "workspace\\.kind"
    - from: "packages/web/src/components/layout/Toolbar.tsx"
      to: "packages/web/src/stores/useUIStore.ts"
      via: "UI-only controls"
      pattern: "useUIStore"
    - from: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      to: "packages/web/src/stores/useTaskStore.ts"
      via: "task rendering source"
      pattern: "useTaskStore"
---

<objective>
Finish the frontend refactor by turning `App.tsx` into a narrow workspace router and moving workspace-specific rendering into dedicated workspace and layout components. This is the point where the app stops behaving like a monolith and starts reading from the stores created in the earlier plans.
</objective>

<context>
@packages/web/src/App.tsx
@packages/web/src/components/ChatSidebar.tsx
@packages/web/src/components/GanttChart.tsx
@packages/web/src/components/StartScreen.tsx
@packages/web/src/components/LoginButton.tsx
@packages/web/src/components/ProjectSwitcher.tsx
@packages/web/src/components/OtpModal.tsx
@packages/web/src/components/CreateProjectModal.tsx
@packages/web/src/components/EditProjectModal.tsx
@packages/web/src/stores/useUIStore.ts
@packages/web/src/stores/useTaskStore.ts
@packages/web/src/stores/useChatStore.ts
@packages/web/src/stores/useAuthStore.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract workspace and layout components</name>
  <files>packages/web/src/App.tsx, packages/web/src/components/workspace/SharedWorkspace.tsx, packages/web/src/components/workspace/GuestWorkspace.tsx, packages/web/src/components/workspace/DraftWorkspace.tsx, packages/web/src/components/workspace/ProjectWorkspace.tsx, packages/web/src/components/layout/Toolbar.tsx, packages/web/src/components/layout/ProjectMenu.tsx</files>
  <action>
  Split the current workspace-specific rendering into explicit components under `src/components/workspace/` and `src/components/layout/`.

  `App.tsx` should only decide which workspace shell to render:
  - shared
  - guest
  - draft
  - project

  `ProjectWorkspace` should mostly compose existing building blocks and read data from the stores instead of re-owning orchestration logic.
  </action>
  <verify>
    <automated>rg -n "SharedWorkspace|GuestWorkspace|DraftWorkspace|ProjectWorkspace" packages/web/src/App.tsx</automated>
    <automated>rg -n "workspace\\.kind" packages/web/src/App.tsx</automated>
    <automated>rg -n "GanttChart|ChatSidebar|useTaskStore|useChatStore" packages/web/src/components/workspace/ProjectWorkspace.tsx</automated>
  </verify>
  <done>
    The app shell is decomposed into explicit workspace and layout modules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move UI-driven controls onto the UI store</name>
  <files>packages/web/src/components/layout/Toolbar.tsx, packages/web/src/components/layout/ProjectMenu.tsx, packages/web/src/App.tsx</files>
  <action>
  Route toolbar and project-menu interactions through `useUIStore` and any store actions they need, not through duplicated local state in `App.tsx`.

  `Toolbar` should stay intentionally narrow:
  - UI-only selectors and actions
  - no task loading
  - no auth/session orchestration

  Keep the remaining UI components connected through store selectors so the final shell is thin and predictable.
  </action>
  <verify>
    <automated>rg -n "useUIStore" packages/web/src/components/layout/Toolbar.tsx packages/web/src/components/layout/ProjectMenu.tsx</automated>
    <automated>rg -n "useAuth\\(|useTasks\\(|useLocalTasks\\(|useSharedProject\\(" packages/web/src/components/layout/Toolbar.tsx packages/web/src/components/layout/ProjectMenu.tsx</automated>
  </verify>
  <done>
    Toolbar and project menu logic is UI-store driven rather than App-local orchestration.
  </done>
</task>
</tasks>

<verification>
1. `npm run build --workspace=packages/web`
2. `rg -n "workspace\\.kind|SharedWorkspace|GuestWorkspace|DraftWorkspace|ProjectWorkspace" packages/web/src/App.tsx`
3. `rg -n "useUIStore|useTaskStore|useChatStore|useAuthStore" packages/web/src/components/workspace packages/web/src/components/layout`
4. Open the web app and confirm the four workspace shells still render correctly on desktop and mobile widths.
</verification>

<success_criteria>
- `App.tsx` is a thin workspace router.
- Workspace-specific rendering lives under `components/workspace/`.
- `Toolbar` and `ProjectMenu` are driven by store state instead of local monolith state.
- The frontend builds successfully after the decomposition.
</success_criteria>
