---
phase: 22-zustand-frontend-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
  - 22-02
files_modified:
  - packages/web/src/stores/useTaskStore.ts
  - packages/web/src/hooks/useTasks.ts
  - packages/web/src/hooks/useLocalTasks.ts
  - packages/web/src/hooks/useSharedProject.ts
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - WEB-ZUSTAND-04
  - WEB-ZUSTAND-05

must_haves:
  truths:
    - "useTaskStore is the single source of truth for task collections and load state"
    - "The store absorbs authenticated, local, and shared-project task selection logic"
    - "WebSocket task replacement writes directly to the task store via getState()"
    - "WebSocket chat side effects write directly to the chat store via getState()"
    - "Legacy task-loading hooks become thin adapters over the store"
  artifacts:
    - path: "packages/web/src/stores/useTaskStore.ts"
      provides: "Task store with source selection, loading, and replacement actions"
      contains: "setTasks|replaceFromSystem|fetchTasks|loadLocal|loadShared"
    - path: "packages/web/src/App.tsx"
      provides: "Direct WebSocket message routing into Zustand stores"
      contains: "useTaskStore.getState()"
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/stores/useTaskStore.ts"
      via: "task replacement path"
      pattern: "replaceFromSystem|getState\\(\\)"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/stores/useChatStore.ts"
      via: "chat streaming path"
      pattern: "appendToken|finishStreaming|setError"
    - from: "packages/web/src/hooks/useTasks.ts"
      to: "packages/web/src/stores/useTaskStore.ts"
      via: "auth-driven loading"
      pattern: "useTaskStore"
---

<objective>
Move task collection ownership into Zustand and remove stale callback routing from the WebSocket path. This plan absorbs the current three-source task loading logic into the task store, keeps the legacy hooks as thin adapters for the migration window, and rewires WebSocket side effects so they write directly into the stores.
</objective>

<context>
@packages/web/src/App.tsx
@packages/web/src/hooks/useTasks.ts
@packages/web/src/hooks/useLocalTasks.ts
@packages/web/src/hooks/useSharedProject.ts
@packages/web/src/hooks/useWebSocket.ts
@packages/web/src/stores/useTaskStore.ts
@packages/web/src/stores/useChatStore.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the task store and absorb task-source selection</name>
  <files>packages/web/src/stores/useTaskStore.ts, packages/web/src/hooks/useTasks.ts, packages/web/src/hooks/useLocalTasks.ts, packages/web/src/hooks/useSharedProject.ts</files>
  <action>
  Create `useTaskStore.ts` as the single source of truth for task data.

  The store must own:
  - task collections
  - loading state
  - error state
  - active source tracking
  - `setTasks`
  - `replaceFromSystem`
  - `fetchTasks`
  - `loadLocal`
  - `loadShared`

  Absorb the current authenticated, local, and shared-project selection logic into the store. Keep the old task-loading hooks available as thin adapters that forward to the store so the migration can stay incremental.
  </action>
  <verify>
    <automated>rg -n "setTasks|replaceFromSystem|fetchTasks|loadLocal|loadShared" packages/web/src/stores/useTaskStore.ts</automated>
    <automated>rg -n "useTaskStore" packages/web/src/hooks/useTasks.ts packages/web/src/hooks/useLocalTasks.ts packages/web/src/hooks/useSharedProject.ts</automated>
  </verify>
  <done>
    Task ownership and source selection live in Zustand, and the legacy loading hooks are reduced to adapters.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite WebSocket side effects to write directly into stores</name>
  <files>packages/web/src/App.tsx</files>
  <action>
  Update the WebSocket message path so the callback writes directly into Zustand stores instead of relaying through stale component state.

  Handle the key message types with direct store access:
  - `tasks` -> `useTaskStore.getState().replaceFromSystem(...)`
  - `token` -> `useChatStore.getState().appendToken(...)`
  - `done` -> `useChatStore.getState().finishStreaming()`
  - `error` -> `useChatStore.getState().setError(...)`

  Keep `useWebSocket` as a hook. Only the message routing changes.
  </action>
  <verify>
    <automated>rg -n "useTaskStore\\.getState\\(|useChatStore\\.getState\\(" packages/web/src/App.tsx</automated>
    <automated>rg -n "replaceFromSystem|appendToken|finishStreaming|setError" packages/web/src/App.tsx</automated>
  </verify>
  <done>
    WebSocket events update Zustand stores directly and no longer depend on callback-local state for task/chat updates.
  </done>
</task>
</tasks>

<verification>
1. `npm run build --workspace=packages/web`
2. `rg -n "useTaskStore|getState\\(\\)\\.replaceFromSystem|appendToken|finishStreaming|setError" packages/web/src/App.tsx`
3. `rg -n "useTaskStore" packages/web/src/hooks/useTasks.ts packages/web/src/hooks/useLocalTasks.ts packages/web/src/hooks/useSharedProject.ts`
</verification>

<success_criteria>
- `useTaskStore` owns task loading and task-source selection.
- The WebSocket message path writes directly into task and chat stores.
- `useTasks`, `useLocalTasks`, and `useSharedProject` remain usable while the migration finishes.
- `packages/web` still compiles after the store migration.
</success_criteria>
