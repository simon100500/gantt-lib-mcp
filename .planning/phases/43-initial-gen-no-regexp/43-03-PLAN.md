---
phase: 43-initial-gen-no-regexp
plan: 03
type: execute
wave: 3
depends_on: ["43-02"]
files_modified:
  - packages/server/src/agent.ts
  - packages/server/src/agent.test.ts
  - packages/server/src/initial-generation/orchestrator.ts
  - packages/server/src/initial-generation/orchestrator.test.ts
  - packages/server/src/initial-generation/classification.test.ts
  - packages/server/src/initial-generation/clarification-gate.test.ts
  - packages/server/src/initial-generation/domain/assembly.test.ts
  - packages/server/src/initial-generation/interpreter.test.ts
autonomous: true
requirements: [IGNR-04]

must_haves:
  truths:
    - "Logs reconstruct the structured interpretation payload, validation verdict, fallback reason, and normalized downstream decisions for one initial-generation run"
    - "Regression tests prove Russian and English paraphrases, ambiguity, explicit worklists, targeted-edit cases, and model-failure fallback work without runtime semantic keyword logic"
    - "Guard tests fail if forbidden semantic helpers or marker lists reappear in the initial-generation interpretation path"
  artifacts:
    - path: "packages/server/src/initial-generation/orchestrator.ts"
      provides: "Lifecycle logging for interpretation, validation, fallback, and normalized intake decisions"
      exports: ["runInitialGeneration"]
    - path: "packages/server/src/agent.test.ts"
      provides: "Top-level routing and fallback regressions across Russian and English prompts"
      contains: "Build a starter schedule"
    - path: "packages/server/src/initial-generation/interpreter.test.ts"
      provides: "Interpreter and guard coverage against semantic runtime heuristics"
      contains: "looksLikeTargetedEdit"
  key_links:
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/initial-generation/orchestrator.ts"
      via: "Initial-generation lifecycle logs visible from the production entrypoint"
      pattern: "initial_generation_interpretation"
    - from: "packages/server/src/initial-generation/orchestrator.test.ts"
      to: "packages/server/src/initial-generation/orchestrator.ts"
      via: "Structured log payload assertions"
      pattern: "initial_generation_interpretation_fallback"
---

<objective>
Harden observability and regression coverage for the new no-regexp intake path.

Purpose: the phase is only shippable when logs make the interpretation path reconstructable and the tests prevent semantic regex/includes from creeping back into route/classification/clarification/brief/domain behavior.
Output: structured interpretation lifecycle logs, top-level routing coverage, fallback and paraphrase regressions, and guard tests for forbidden semantic helpers.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
@INITIAL-GENERATION-NO-HARDCODE-PRD.md
@packages/server/src/agent.ts
@packages/server/src/agent.test.ts
@packages/server/src/initial-generation/orchestrator.ts
@packages/server/src/initial-generation/orchestrator.test.ts
@packages/server/src/initial-generation/interpreter.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured interpretation lifecycle logging and conservative-fallback telemetry</name>
  <files>packages/server/src/agent.ts, packages/server/src/initial-generation/orchestrator.ts, packages/server/src/initial-generation/orchestrator.test.ts</files>
  <read_first>
    packages/server/src/agent.ts
    packages/server/src/initial-generation/orchestrator.ts
    packages/server/src/initial-generation/orchestrator.test.ts
    packages/server/src/initial-generation/interpreter.ts
    packages/server/src/initial-generation/types.ts
    .planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
    INITIAL-GENERATION-NO-HARDCODE-PRD.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "initial_generation_interpretation|initial_generation_interpretation_validation|initial_generation_interpretation_fallback|initial_generation_normalized_decisions" packages/server/src/initial-generation/orchestrator.ts packages/server/src/agent.ts` finds all required log events
    - `rg -n "fallbackReason|repairAttempted|usedModelDecision|clarification" packages/server/src/initial-generation/orchestrator.ts` finds the telemetry fields carried into logs
    - `rg -n "initial_generation_interpretation_fallback|schema_invalid|model_unavailable" packages/server/src/initial-generation/orchestrator.test.ts` finds fallback log assertions
  </acceptance_criteria>
  <action>
    Add the locked Phase 43 observability surface:

    1. In `packages/server/src/initial-generation/orchestrator.ts`, log four new structured events around the shared interpreter call:
       - `initial_generation_interpretation`
       - `initial_generation_interpretation_validation`
       - `initial_generation_interpretation_fallback`
       - `initial_generation_normalized_decisions`
    2. Each payload must include concrete fields:
       - `route`, `requestKind`, `planningMode`, `scopeMode`, `objectProfile`, `projectArchetype`, `worklistPolicy`
       - `locationScope`, `confidence`, `signals`
       - `usedModelDecision`, `repairAttempted`, `fallbackReason`
       - normalized downstream decisions actually consumed by classification, clarification, brief, and domain assembly
    3. In `packages/server/src/agent.ts`, ensure the top-level run logs or forwards the interpretation-derived route evidence before the orchestration branch proceeds, so a single run trace shows route decision plus normalized interpretation.
    4. Extend `packages/server/src/initial-generation/orchestrator.test.ts` to assert the happy path, repaired-output path, and fallback path each emit the new event set with the concrete fields above.

    Do not add keyword snippets to logs as executable semantics. Log the model-produced signals and validated enums instead.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/orchestrator.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    One run now exposes the interpretation payload, validation/fallback provenance, and normalized downstream decisions in structured logs without reintroducing lexical inference logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the regression and guard surface against semantic runtime heuristics</name>
  <files>packages/server/src/agent.test.ts, packages/server/src/initial-generation/interpreter.test.ts, packages/server/src/initial-generation/classification.test.ts, packages/server/src/initial-generation/clarification-gate.test.ts, packages/server/src/initial-generation/domain/assembly.test.ts</files>
  <read_first>
    packages/server/src/agent.test.ts
    packages/server/src/initial-generation/interpreter.test.ts
    packages/server/src/initial-generation/classification.test.ts
    packages/server/src/initial-generation/clarification-gate.test.ts
    packages/server/src/initial-generation/domain/assembly.test.ts
    packages/server/src/initial-generation/route-selection.ts
    packages/server/src/initial-generation/classification.ts
    packages/server/src/initial-generation/clarification-gate.ts
    packages/server/src/initial-generation/brief.ts
    packages/server/src/initial-generation/domain/assembly.ts
    .planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
    INITIAL-GENERATION-NO-HARDCODE-PRD.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "Build a starter schedule|Построй график|ambiguous|explicit worklist|targeted edit|model failure" packages/server/src/agent.test.ts packages/server/src/initial-generation/interpreter.test.ts packages/server/src/initial-generation/classification.test.ts packages/server/src/initial-generation/clarification-gate.test.ts packages/server/src/initial-generation/domain/assembly.test.ts` finds the scenario matrix
    - `rg -n "looksLikeTargetedEdit|detectScopeSignals|hasAmbiguousListLanguage|hasExplicitFragmentTarget|inferWorklistPolicy" packages/server/src/agent.test.ts packages/server/src/initial-generation/interpreter.test.ts` finds guard assertions for forbidden helpers
    - `rg -n "readFileSync" packages/server/src/agent.test.ts packages/server/src/initial-generation/interpreter.test.ts` finds source-guard tests that inspect the implementation files directly
  </acceptance_criteria>
  <action>
    Expand the regression suite so Phase 43 stays closed against semantic keyword logic:

    1. In `packages/server/src/agent.test.ts`, add top-level route regressions for:
       - Russian `Построй график`
       - English `Build a starter schedule for a kindergarten`
       - targeted-edit phrasing that routes to `mutation` only when the interpreter payload says so
       - model failure fallback on empty and non-empty project state
    2. In `packages/server/src/initial-generation/interpreter.test.ts`, add source-guard tests using `readFileSync(...)` that fail if these helpers or marker lists reappear in the interpretation path:
       - `looksLikeTargetedEdit`
       - `detectScopeSignals`
       - `hasAmbiguousListLanguage`
       - `hasExplicitFragmentTarget`
       - `inferWorklistPolicy`
    3. In `classification.test.ts`, `clarification-gate.test.ts`, and `domain/assembly.test.ts`, add paired Russian/English paraphrase cases plus ambiguity, explicit worklist, partial-scope, and fallback-driven flows to prove no code changes are needed for alternate phrasings.

    Keep the guard tests scoped to the initial-generation interpretation path named in the PRD. Do not attempt repository-wide regex removal in this plan.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/initial-generation/interpreter.test.ts packages/server/src/initial-generation/classification.test.ts packages/server/src/initial-generation/clarification-gate.test.ts packages/server/src/initial-generation/domain/assembly.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Regression coverage now proves paraphrases, ambiguity, targeted-edit routing, and fallback behavior without semantic runtime helpers, and guard tests fail if the banned lexical functions come back.
  </done>
</task>

</tasks>

<verification>
Confirm the logs reconstruct interpretation and fallback decisions end to end, and the tests directly guard against reintroducing banned semantic helper functions or marker arrays.
</verification>

<success_criteria>
Phase 43 is hardened: observability exposes the structured interpretation lifecycle, and the regression suite enforces the PRD rule that runtime semantic regex/includes do not return to the initial-generation intake path.
</success_criteria>

<output>
After completion, create `.planning/phases/43-initial-gen-no-regexp/43-03-SUMMARY.md`
</output>
