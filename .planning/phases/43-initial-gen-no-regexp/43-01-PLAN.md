---
phase: 43-initial-gen-no-regexp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/initial-generation/types.ts
  - packages/server/src/initial-generation/interpreter.ts
  - packages/server/src/initial-generation/interpreter.test.ts
  - packages/server/src/initial-generation/route-selection.ts
  - packages/server/src/agent.test.ts
autonomous: true
requirements: [IGNR-01]

must_haves:
  truths:
    - "Initial-generation semantics come from one strict JSON interpretation result instead of multiple runtime keyword heuristics"
    - "Route selection can choose `initial_generation` or `mutation` from the shared interpretation contract without calling lexical fallback helpers"
    - "When the model output is unavailable or schema-invalid, fallback stays conservative and non-semantic-by-keyword"
  artifacts:
    - path: "packages/server/src/initial-generation/types.ts"
      provides: "Shared interpretation enums and typed contract consumed by the intake pipeline"
      exports: ["InitialRequestInterpretation", "InitialRequestInterpretationRoute", "InitialRequestClarificationReason"]
    - path: "packages/server/src/initial-generation/interpreter.ts"
      provides: "Strict-JSON interpretation query, validation, single repair pass, and conservative fallback"
      exports: ["interpretInitialRequest"]
    - path: "packages/server/src/initial-generation/route-selection.ts"
      provides: "Route selection derived from structured interpretation instead of lexical helpers"
      exports: ["selectAgentRoute"]
  key_links:
    - from: "packages/server/src/initial-generation/route-selection.ts"
      to: "packages/server/src/initial-generation/interpreter.ts"
      via: "Shared interpretation call before route choice is returned"
      pattern: "interpretInitialRequest"
    - from: "packages/server/src/initial-generation/interpreter.ts"
      to: "packages/server/src/initial-generation/types.ts"
      via: "Schema validation and normalized contract construction"
      pattern: "InitialRequestInterpretation"
---

<objective>
Introduce the shared structured interpretation boundary for Phase 43.

Purpose: downstream modules cannot stop using semantic regex/includes until one canonical interpretation contract exists and route selection already depends on it.
Output: a typed `InitialRequestInterpretation` contract, a dedicated interpreter module with strict JSON validation and conservative fallback, and route-selection/tests migrated to the new boundary.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
@INITIAL-GENERATION-NO-HARDCODE-PRD.md
@packages/server/src/initial-generation/types.ts
@packages/server/src/initial-generation/route-selection.ts
@packages/server/src/agent.test.ts

<interfaces>
From packages/server/src/initial-generation/types.ts:
```typescript
export type GenerationMode = 'initial_generation' | 'mutation';
export type ScopeMode = 'full_project' | 'partial_scope' | 'explicit_worklist';
export type PlanningMode = 'whole_project_bootstrap' | 'partial_scope_bootstrap' | 'worklist_bootstrap';
export type WorklistPolicy = 'strict_worklist' | 'worklist_plus_inferred_supporting_tasks';
```

From packages/server/src/initial-generation/route-selection.ts:
```typescript
export type AgentRouteSelection = {
  route: GenerationMode;
  confidence: number;
  reason: RouteDecisionReason;
  signals: string[];
  usedModelDecision: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the unified initial-request interpretation contract and interpreter module</name>
  <files>packages/server/src/initial-generation/types.ts, packages/server/src/initial-generation/interpreter.ts, packages/server/src/initial-generation/interpreter.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/types.ts
    packages/server/src/initial-generation/route-selection.ts
    packages/server/src/initial-generation/intake-normalization.ts
    .planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
    INITIAL-GENERATION-NO-HARDCODE-PRD.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export type InitialRequestInterpretationRoute = 'initial_generation' \\| 'mutation'" packages/server/src/initial-generation/types.ts` finds the route enum
    - `rg -n "requestKind: 'whole_project'\\|'partial_scope'\\|'explicit_worklist'\\|'targeted_edit'\\|'ambiguous'" packages/server/src/initial-generation/types.ts` finds the request-kind contract
    - `rg -n "export async function interpretInitialRequest|export function interpretInitialRequest" packages/server/src/initial-generation/interpreter.ts` finds the shared interpreter entrypoint
    - `rg -n "strict JSON only|Allowed enum values|targeted_edit|explicit_worklist" packages/server/src/initial-generation/interpreter.ts` finds the prompt contract
    - `rg -n "fallbackReason|schema_invalid|model_unavailable|repairAttempted" packages/server/src/initial-generation/interpreter.ts` finds conservative fallback and single-repair bookkeeping
    - `rg -n "English paraphrase|Russian paraphrase|invalid JSON|fallback" packages/server/src/initial-generation/interpreter.test.ts` finds the required interpreter coverage
  </acceptance_criteria>
  <action>
    Implement IGNR-01 by adding one canonical contract and interpreter module:

    1. Extend `packages/server/src/initial-generation/types.ts` with a strict interpretation surface named `InitialRequestInterpretation` and explicit enums for:
       - `route`: `initial_generation | mutation`
       - `requestKind`: `whole_project | partial_scope | explicit_worklist | targeted_edit | ambiguous`
       - `planningMode`: `whole_project_bootstrap | partial_scope_bootstrap | worklist_bootstrap`
       - `scopeMode`: `full_project | partial_scope | explicit_worklist`
       - `objectProfile`: `unknown | office_fitout | kindergarten | residential_multi_section`
       - `projectArchetype`: `unknown | new_building | renovation`
       - `worklistPolicy`: `strict_worklist | worklist_plus_inferred_supporting_tasks`
       - `clarification.reason`: `none | ambiguous_list | missing_scope | scope_boundary_ambiguity | fragment_target_ambiguity | worklist_completeness_ambiguity`
       - `locationScope` with `sections`, `floors`, `zones`
    2. Create `packages/server/src/initial-generation/interpreter.ts` exporting `interpretInitialRequest({ userMessage, normalizedRequest, projectState, model, interpretationQuery })`.
       - Build one strict-JSON prompt with fixed enum values, negative instruction that runtime keyword logic is forbidden, and examples for Russian and English broad bootstrap, partial scope, explicit worklist, targeted edit, and ambiguous phrasing.
       - Parse exactly one JSON object from the model output, validate all enums, allow one repair query when parsing or validation fails, and return a normalized `InitialRequestInterpretation`.
       - Implement conservative fallback for `model_unavailable`, `schema_invalid`, and `empty_response` using only technical facts: `taskCount`, `hasHierarchy`, extracted worklist count, extracted location scope presence, and whether the project is empty. Do not inspect semantic user words in fallback.
       - Include `confidence`, `signals`, `usedModelDecision`, `repairAttempted`, and `fallbackReason` in the returned payload or a paired result object so downstream logs can preserve provenance.
    3. Add `packages/server/src/initial-generation/interpreter.test.ts` covering:
       - Russian and English paraphrases that normalize to the same structured route/scope result
       - explicit worklist interpretation
       - targeted-edit interpretation
       - invalid JSON -> one repair attempt
       - model failure -> conservative fallback that does not infer targeted edit from lexical markers

    Do not move downstream classification or brief assembly into this plan yet. This plan creates the contract and the interpreter boundary only.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/interpreter.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    One shared interpretation module exists, validates strict JSON, supports one repair pass, and exposes typed semantics without any runtime semantic helper lists in the interpreter implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace route-selection lexical fallback with the shared interpreter output</name>
  <files>packages/server/src/initial-generation/route-selection.ts, packages/server/src/agent.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/route-selection.ts
    packages/server/src/agent.test.ts
    packages/server/src/initial-generation/interpreter.ts
    packages/server/src/initial-generation/types.ts
    .planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
    INITIAL-GENERATION-NO-HARDCODE-PRD.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "looksLikeTargetedEdit|targeted_edit_markers|fallback_mutation_bias" packages/server/src/initial-generation/route-selection.ts` returns no matches
    - `rg -n "interpretInitialRequest" packages/server/src/initial-generation/route-selection.ts` finds the interpreter dependency
    - `rg -n "interpretation" packages/server/src/initial-generation/route-selection.ts` finds the structured payload threaded into the route result
    - `rg -n "Построй график|Build a starter schedule|targeted edit|model_unavailable" packages/server/src/agent.test.ts` finds route-selection regressions for paraphrase and fallback coverage
  </acceptance_criteria>
  <action>
    Refactor `packages/server/src/initial-generation/route-selection.ts` to consume the new shared interpreter and stop performing semantic fallback itself:

    1. Delete `looksLikeTargetedEdit()` and any marker arrays or quote-detection shortcuts.
    2. Change `selectAgentRoute()` so it:
       - calls `interpretInitialRequest(...)` before returning
       - derives `route`, `confidence`, `signals`, and a new `interpretation` field from the validated interpreter output
       - maps fallback outcomes to explicit non-semantic reasons such as `fallback_non_empty_project_defaults_to_mutation` and `fallback_empty_project_defaults_to_initial_generation`
       - preserves `taskCount`, `hasHierarchy`, `projectStateSummary`, and `usedModelDecision`
    3. Update `packages/server/src/agent.test.ts` route-selection coverage to assert:
       - Russian `Построй график` and English `Build a starter schedule for a kindergarten` both route through `initial_generation`
       - targeted-edit payloads route to `mutation` because the interpreter returns `route: "mutation"`, not because code finds words
       - fallback on missing model query uses empty-project state rather than lexical markers

    Keep the public `selectAgentRoute()` export stable for callers. Do not migrate classification, clarification, brief, or domain assembly in this plan.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/initial-generation/interpreter.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Route selection is now interpreter-driven, carries the shared interpretation payload forward, and contains no semantic word-list fallback helpers.
  </done>
</task>

</tasks>

<verification>
Confirm the initial-generation intake now has exactly one interpretation boundary, route selection imports that boundary directly, and the old lexical route fallback helpers are absent.
</verification>

<success_criteria>
Phase 43 has a real interface-first foundation: one typed interpretation result exists, route-selection consumes it, and the code no longer picks `mutation` vs `initial_generation` by runtime semantic marker matching.
</success_criteria>

<output>
After completion, create `.planning/phases/43-initial-gen-no-regexp/43-01-SUMMARY.md`
</output>
