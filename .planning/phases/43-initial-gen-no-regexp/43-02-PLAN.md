---
phase: 43-initial-gen-no-regexp
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - packages/server/src/initial-generation/types.ts
  - packages/server/src/initial-generation/intake-normalization.ts
  - packages/server/src/initial-generation/classification.ts
  - packages/server/src/initial-generation/clarification-gate.ts
  - packages/server/src/initial-generation/brief.ts
  - packages/server/src/initial-generation/domain/assembly.ts
  - packages/server/src/initial-generation/orchestrator.ts
  - packages/server/src/initial-generation/classification.test.ts
  - packages/server/src/initial-generation/clarification-gate.test.ts
  - packages/server/src/initial-generation/domain/assembly.test.ts
  - packages/server/src/initial-generation/planner.test.ts
autonomous: true
requirements: [IGNR-02, IGNR-03]

must_haves:
  truths:
    - "Classification, clarification, brief assembly, and domain assembly all consume the same structured interpretation output"
    - "Technical parsing such as ranges, floors, numbering, whitespace cleanup, and JSON extraction remains, but semantic scope/profile intent is no longer derived from raw-text regex or includes"
    - "Deterministic orchestration still runs after interpretation, using validated enums and normalized technical structures only"
  artifacts:
    - path: "packages/server/src/initial-generation/intake-normalization.ts"
      provides: "Technical-only normalization helpers with no semantic scope intent inference"
      exports: ["normalizeInitialRequest"]
    - path: "packages/server/src/initial-generation/classification.ts"
      provides: "Projection from shared interpretation to downstream classification contract"
      exports: ["classifyInitialRequest"]
    - path: "packages/server/src/initial-generation/orchestrator.ts"
      provides: "Single interpretation call whose output is reused by all downstream intake modules"
      exports: ["runInitialGeneration"]
  key_links:
    - from: "packages/server/src/initial-generation/orchestrator.ts"
      to: "packages/server/src/initial-generation/interpreter.ts"
      via: "One shared interpretation call for the full intake path"
      pattern: "interpretInitialRequest"
    - from: "packages/server/src/initial-generation/classification.ts"
      to: "packages/server/src/initial-generation/types.ts"
      via: "Mapping from validated interpretation enums to classification output"
      pattern: "InitialRequestInterpretation"
    - from: "packages/server/src/initial-generation/domain/assembly.ts"
      to: "packages/server/src/initial-generation/brief.ts"
      via: "Common interpretation-driven object/scope inputs"
      pattern: "interpretation"
---

<objective>
Migrate the initial-generation intake consumers off runtime semantic heuristics and onto the shared interpretation contract.

Purpose: Phase 43 only succeeds if every semantic intake decision after route selection reads the same structured result and the remaining normalization layer is strictly technical.
Output: technical-only normalization, interpretation-driven classification/clarification/brief/domain assembly, and orchestrator wiring that computes interpretation once and reuses it downstream.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
@INITIAL-GENERATION-NO-HARDCODE-PRD.md
@packages/server/src/initial-generation/intake-normalization.ts
@packages/server/src/initial-generation/classification.ts
@packages/server/src/initial-generation/clarification-gate.ts
@packages/server/src/initial-generation/brief.ts
@packages/server/src/initial-generation/domain/assembly.ts
@packages/server/src/initial-generation/orchestrator.ts

<interfaces>
From packages/server/src/initial-generation/types.ts:
```typescript
export type NormalizedInitialRequest = {
  rawRequest: string;
  normalizedRequest: string;
  explicitWorkItems: string[];
  locationScope?: LocationScope;
  sourceConfidence: 'low' | 'medium' | 'high';
};
```

```typescript
export type InitialRequestInterpretation = {
  route: 'initial_generation' | 'mutation';
  requestKind: 'whole_project' | 'partial_scope' | 'explicit_worklist' | 'targeted_edit' | 'ambiguous';
  planningMode: 'whole_project_bootstrap' | 'partial_scope_bootstrap' | 'worklist_bootstrap';
  scopeMode: 'full_project' | 'partial_scope' | 'explicit_worklist';
  objectProfile: 'unknown' | 'office_fitout' | 'kindergarten' | 'residential_multi_section';
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip semantic inference out of normalization, classification, and clarification</name>
  <files>packages/server/src/initial-generation/types.ts, packages/server/src/initial-generation/intake-normalization.ts, packages/server/src/initial-generation/classification.ts, packages/server/src/initial-generation/clarification-gate.ts, packages/server/src/initial-generation/classification.test.ts, packages/server/src/initial-generation/clarification-gate.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/types.ts
    packages/server/src/initial-generation/intake-normalization.ts
    packages/server/src/initial-generation/classification.ts
    packages/server/src/initial-generation/clarification-gate.ts
    packages/server/src/initial-generation/classification.test.ts
    packages/server/src/initial-generation/clarification-gate.test.ts
    packages/server/src/initial-generation/interpreter.ts
    .planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
    INITIAL-GENERATION-NO-HARDCODE-PRD.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "function detectScopeSignals|fragment\\s*=|wholeProject\\s*=|handoverIntent" packages/server/src/initial-generation/intake-normalization.ts` returns no matches
    - `rg -n "inferScopeMode|inferPlanningMode|inferProjectArchetype|inferObjectProfile|inferWorklistPolicy" packages/server/src/initial-generation/classification.ts` returns no matches
    - `rg -n "hasAmbiguousListLanguage|hasExplicitFragmentTarget" packages/server/src/initial-generation/clarification-gate.ts` returns no matches
    - `rg -n "interpretation" packages/server/src/initial-generation/classification.ts packages/server/src/initial-generation/clarification-gate.ts` finds interpretation-driven inputs
    - `rg -n "English paraphrase|Russian paraphrase|ambiguous|explicit worklist" packages/server/src/initial-generation/classification.test.ts packages/server/src/initial-generation/clarification-gate.test.ts` finds updated regression coverage
  </acceptance_criteria>
  <action>
    Remove semantic raw-text inference from the first half of the intake path:

    1. In `packages/server/src/initial-generation/intake-normalization.ts` keep only technical parsing:
       - whitespace cleanup
       - bullet/numbered-list extraction
       - section range expansion like `5.1-5.4`
       - floor extraction from `этаж 2` / `floor 2`
       - zone extraction for literal structural locations such as `подвал`, `кровля`, `фасад`, `паркинг`
       - source-confidence derived from structural evidence like work-item count and location presence
       Remove semantic booleans such as `fragment`, `wholeProject`, and `handoverIntent` from `NormalizedInitialRequest`.
    2. Refactor `packages/server/src/initial-generation/classification.ts` so `classifyInitialRequest({ normalizedRequest, interpretation })` becomes a deterministic projection of validated interpretation fields:
       - `scopeMode`, `planningMode`, `projectArchetype`, `objectProfile`, `worklistPolicy`, `locationScope`, `confidence`
       - no regex over `normalizedRequest.normalizedRequest`
       - no semantic defaulting from raw text
    3. Refactor `packages/server/src/initial-generation/clarification-gate.ts` so `decideInitialClarification({ normalizedRequest, interpretation, classification })` reads only structured fields:
       - `interpretation.clarification.needed`
       - `interpretation.clarification.reason`
       - `interpretation.scopeMode`
       - `interpretation.requestKind`
       Build the ask/proceed result from those enums and technical location data only.
    4. Rewrite `classification.test.ts` and `clarification-gate.test.ts` so cases are driven by injected interpretation payloads, including Russian/English paraphrase equivalence, ambiguous-list requests, partial-scope fragment requests, and explicit worklists.

    Do not reintroduce helper wrappers that scan the message for synonyms. If a semantic distinction is needed, it must already exist on `interpretation`.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/classification.test.ts packages/server/src/initial-generation/clarification-gate.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Normalization is technical-only, and both classification and clarification are pure consumers of the shared interpretation contract rather than raw-text semantic analyzers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate brief, domain assembly, and orchestrator to reuse one shared interpretation result</name>
  <files>packages/server/src/initial-generation/brief.ts, packages/server/src/initial-generation/domain/assembly.ts, packages/server/src/initial-generation/orchestrator.ts, packages/server/src/initial-generation/domain/assembly.test.ts, packages/server/src/initial-generation/planner.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/brief.ts
    packages/server/src/initial-generation/domain/assembly.ts
    packages/server/src/initial-generation/orchestrator.ts
    packages/server/src/initial-generation/domain/assembly.test.ts
    packages/server/src/initial-generation/planner.test.ts
    packages/server/src/initial-generation/classification.ts
    packages/server/src/initial-generation/clarification-gate.ts
    packages/server/src/initial-generation/interpreter.ts
    .planning/phases/43-initial-gen-no-regexp/43-CONTEXT.md
    INITIAL-GENERATION-NO-HARDCODE-PRD.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "detectScopeSignals|new_build|renovation|material_mentioned|starter_generation_request" packages/server/src/initial-generation/brief.ts` returns no matches
    - `rg -n "/подвал/i|handoverIntent|fallbackAssumption" packages/server/src/initial-generation/domain/assembly.ts` returns no matches
    - `rg -n "interpretInitialRequest" packages/server/src/initial-generation/orchestrator.ts` finds exactly one shared interpretation call in the intake sequence
    - `rg -n "interpretation" packages/server/src/initial-generation/brief.ts packages/server/src/initial-generation/domain/assembly.ts packages/server/src/initial-generation/orchestrator.ts` finds shared-contract plumbing
    - `rg -n "partial scope|explicit worklist|Russian|English" packages/server/src/initial-generation/domain/assembly.test.ts packages/server/src/initial-generation/planner.test.ts` finds interpretation-driven downstream tests
  </acceptance_criteria>
  <action>
    Complete the consumer migration for IGNR-02 and IGNR-03:

    1. Refactor `packages/server/src/initial-generation/brief.ts` so `buildGenerationBrief(...)` derives:
       - `objectType` from `interpretation.objectProfile` then `interpretation.projectArchetype`
       - `scopeSignals` from interpretation enums like `full_project`, `partial_scope`, `explicit_worklist`, `targeted_edit`, `ambiguous`
       - `domainContextSummary` from interpretation enums and technical location scope
       Remove `detectScopeSignals()` and all raw-text semantic regexes.
    2. Refactor `packages/server/src/initial-generation/domain/assembly.ts` so fragment and profile decisions come from structured fields only:
       - use `interpretation.scopeMode`, `interpretation.clarification.reason`, `interpretation.locationScope`, `classification.objectProfile`, and `classification.projectArchetype`
       - keep technical use of `locationScope.sections/floors/zones`
       - do not inspect `normalizedRequest.normalizedRequest` for `подвал`, `handover`, or similar semantic cues
    3. Update `packages/server/src/initial-generation/orchestrator.ts` to compute:
       - `normalizedRequest = normalizeInitialRequest(userMessage)`
       - `interpretation = interpretInitialRequest(...)`
       - `classification = classifyInitialRequest({ normalizedRequest, interpretation })`
       - `clarificationDecision = decideInitialClarification({ normalizedRequest, interpretation, classification })`
       - `brief` and `domainSkeleton` from the same `interpretation`
       Ensure there is one interpretation call per run, reused by all downstream modules.
    4. Update `domain/assembly.test.ts` and `planner.test.ts` to cover:
       - partial-scope basement-like location decisions coming from interpretation plus technical location scope
       - explicit worklist policy flow
       - broad whole-project and office-fitout cases using the same downstream interfaces

    Preserve deterministic planner/compiler/orchestrator behavior from Phase 41. This plan changes semantic input sourcing, not the compile pipeline.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/domain/assembly.test.ts packages/server/src/initial-generation/planner.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    All downstream intake consumers read the same interpretation object, and the remaining parsing/orchestration code is deterministic and technical rather than lexical-semantic.
  </done>
</task>

</tasks>

<verification>
Confirm the initial-generation intake path now computes one interpretation object and all semantic decisions after that point are projections of its enums, not raw-text regex/includes.
</verification>

<success_criteria>
Phase 43 now has a single semantic source of truth: classification, clarification, brief assembly, and domain assembly no longer infer intent from user words in code, while technical parsing and deterministic orchestration remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/43-initial-gen-no-regexp/43-02-SUMMARY.md`
</output>
