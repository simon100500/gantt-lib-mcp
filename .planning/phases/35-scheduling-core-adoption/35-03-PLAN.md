---
phase: 35-scheduling-core-adoption
plan: 03
type: execute
wave: 2
depends_on:
  - 35-01
  - 35-02
files_modified:
  - packages/mcp/agent/prompts/system.md
  - packages/server/src/agent.ts
  - packages/web/src/hooks/useBatchTaskUpdate.ts
  - packages/web/src/hooks/useAutoSave.ts
  - packages/web/src/components/GanttChart.tsx
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Agent guidance prefers scheduling intents over arbitrary direct date mutation"
    - "Mutation verification compares the full changed-set returned by the server, not just the edited task"
    - "Web save flow treats server schedule result as authoritative for persisted changes"
    - "UI/backend stop fighting each other on linked-task edits because client applies the returned server state"
  artifacts:
    - path: "packages/mcp/agent/prompts/system.md"
      provides: "agent rules for schedule-aware planning mutations"
      contains: "move_task"
    - path: "packages/server/src/agent.ts"
      provides: "changed-set aware mutation verification"
      contains: "changedTasks"
    - path: "packages/web/src/hooks/useBatchTaskUpdate.ts"
      provides: "server-authoritative merge/apply path for gantt edits"
      contains: "affected"
  key_links:
    - from: "packages/mcp/agent/prompts/system.md"
      to: "packages/server/src/agent.ts"
      via: "agent mutation protocol and verification expectations"
      pattern: "move_task|resize_task|changedTasks"
    - from: "packages/web/src/hooks/useBatchTaskUpdate.ts"
      to: "packages/mcp/src/index.ts"
      via: "client applies final affected-task state returned by server"
      pattern: "changedIds|changedTasks"
---

<objective>
Сделать server result authoritative для agent и web flows, чтобы persisted schedule всегда отражал серверный cascade, а не локальные догадки клиента.

Purpose: Даже правильный scheduling engine не устранит drift, если agent и web продолжают мыслить через raw date rewrites и частичные локальные commits.
Output: Updated prompts, verification, and client save/reconcile flow that apply the server-returned final state.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-scheduling-core-adoption/35-CONTEXT.md
@.planning/phases/35-scheduling-core-adoption/35-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agent mutation contract and server verification</name>
  <files>
    packages/mcp/agent/prompts/system.md
    packages/server/src/agent.ts
  </files>
  <read_first>
    packages/mcp/agent/prompts/system.md
    packages/server/src/agent.ts
    packages/mcp/src/index.ts
  </read_first>
  <action>
Revise the agent prompt and verification layer so that:
- agent prefers `move_task` / `resize_task` over arbitrary date rewrites when dependencies are relevant
- tool descriptions explain that linked tasks cascade server-side
- verification compares all changed tasks from the scheduling result, not only the directly edited task or a blind task-list diff
- stale or partial mutation results are surfaced explicitly
  </action>
  <verify>
    <automated>npm run build -w packages/server</automated>
  </verify>
  <acceptance_criteria>
    - prompt text clearly nudges toward schedule-intent mutations
    - server verification logic can reason about multi-task changed sets
    - mutation success is no longer inferred from a single edited task
  </acceptance_criteria>
  <done>
    Agent-side scheduling mutations now align with the server’s command model and verification can see the real cascade footprint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make web persistence apply authoritative server scheduling results</name>
  <files>
    packages/web/src/hooks/useBatchTaskUpdate.ts
    packages/web/src/hooks/useAutoSave.ts
    packages/web/src/components/GanttChart.tsx
    packages/web/src/components/workspace/ProjectWorkspace.tsx
  </files>
  <read_first>
    packages/web/src/hooks/useBatchTaskUpdate.ts
    packages/web/src/hooks/useAutoSave.ts
    packages/web/src/components/GanttChart.tsx
    packages/web/src/components/workspace/ProjectWorkspace.tsx
  </read_first>
  <action>
Ensure the web editor save flow:
- maps local gantt edits to scheduling intent or schedule-aware update calls
- receives final changed tasks from the server
- applies returned authoritative state instead of preserving stale local-only cascade results
- keeps UI state consistent after linked-task edits and parent summary recomputes

Prefer surgical reconciliation of changed tasks rather than reloading the entire project when a patch result is available.
  </action>
  <verify>
    <automated>npm run build --workspace @gantt/web</automated>
  </verify>
  <acceptance_criteria>
    - client merge path is based on server-returned changed tasks
    - linked edits stop leaving local/server drift after save
    - web build passes with the new schedule-aware response contract
  </acceptance_criteria>
  <done>
    Web persistence now treats the server as the source of truth for scheduling outcomes.
  </done>
</task>

</tasks>

<verification>
1. Server build passes with updated agent verification logic
2. Web build passes with the new response/reconcile flow
3. Linked edit flow applies server-returned changed tasks without losing hierarchy or dependency updates
</verification>

<success_criteria>
- Agent, MCP, and web all speak the same scheduling intent model
- Persisted schedule is authoritative on the server
- UI/backend drift is reduced to explicit reconciliation instead of implicit mismatch
</success_criteria>

<output>
After completion, create `.planning/phases/35-scheduling-core-adoption/35-03-SUMMARY.md`
</output>
