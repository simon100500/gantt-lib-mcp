---
phase: 35-scheduling-core-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/scheduler.ts
  - packages/mcp/src/types.ts
  - packages/mcp/src/scheduler.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "MCP scheduling core mirrors current gantt-lib scheduling semantics for FS, SS, FF, SF, positive/negative lag, parent recompute, and business/calendar day modes"
    - "Phase 35 adoption guardrail is enforced: server does NOT preserve the three documented upstream defects as command truth"
    - "Recalculate project-wide schedule handles multiple roots / multiple predecessors using a deterministic continuously updated working snapshot"
    - "Scheduling result contract distinguishes true changed tasks from full normalized snapshot"
    - "Regression tests cover all PRD-required dependency and changed-set cases"
  artifacts:
    - path: "packages/mcp/src/scheduler.ts"
      provides: "Headless scheduling engine or compatibility facade over extracted pure scheduling functions"
      contains: "moveTask"
    - path: "packages/mcp/src/types.ts"
      provides: "Scheduling command/result types compatible with MCP domain DTOs"
      contains: "ScheduleCommandResult"
    - path: "packages/mcp/src/scheduler.test.ts"
      provides: "Regression coverage for scheduling semantics and upstream-gap guardrails"
      contains: "multiple predecessors"
  key_links:
    - from: "D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/*"
      to: "packages/mcp/src/scheduler.ts"
      via: "ported or adapted headless scheduling semantics"
      pattern: "FS|SS|FF|SF"
    - from: "packages/mcp/src/types.ts"
      to: "packages/mcp/src/scheduler.ts"
      via: "schedule command/result contracts"
      pattern: "ScheduleCommandResult"
---

<objective>
Заменить текущий упрощённый `TaskScheduler` на headless scheduling core, который повторяет актуальный `gantt-lib` behavior и одновременно фиксирует три upstream gaps, явно перечисленные в PRD.

Purpose: Без корректного domain-level scheduling ядра любые дальнейшие MCP/server/web интеграции будут строиться на неверной semantics.
Output: Новый scheduling core в `packages/mcp/src/` и regression tests, доказывающие parity + guardrails.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-scheduling-core-adoption/35-CONTEXT.md
@.planning/reference/scheduling-core-adoption-prd.md

<interfaces>
Current MCP entrypoint:
```ts
class TaskScheduler {
  constructor(private taskMap: Map<string, Task>) {}
  validateDependencies(task: Task): void;
  detectCycle(taskId: string, ...): boolean;
  recalculateDates(startTaskId: string, skipStartTask?: boolean): Map<string, Task>;
}
```

Required new contract direction:
```ts
type ScheduleCommandResult = {
  changedTasks: Task[];
  changedIds: string[];
  snapshot?: Task[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port or reconstruct headless scheduling primitives from gantt-lib</name>
  <files>
    packages/mcp/src/scheduler.ts
    packages/mcp/src/types.ts
  </files>
  <read_first>
    packages/mcp/src/scheduler.ts
    packages/mcp/src/types.ts
    D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/execute.ts
    D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/cascade.ts
    D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/commands.ts
    D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/dependencies.ts
    D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/hierarchy.ts
    D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/validation.ts
  </read_first>
  <action>
Create a pure MCP scheduling module that supports:
- dependency types FS, SS, FF, SF
- positive and negative lag
- business and calendar day modes
- range building from start+duration and end+duration
- move, resize, and project-wide recalculate commands
- parent/child movement and parent summary recomputation
- cycle detection and missing dependency validation

Preserve current `gantt-lib` semantics where intentional, but apply PRD guardrails:
- `resize_task(anchor='end')` = fixed start, new end
- `resize_task(anchor='start')` = fixed end, new start
- project recalc uses deterministic working snapshot / strongest constraint across predecessors
- changedIds is a real diff only

If backward compatibility with existing `TaskScheduler` callers is needed, expose a thin facade on top of the new command-level core instead of keeping the old implementation as the source of truth.
  </action>
  <verify>
    <automated>node --test src/scheduler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp/src/scheduler.ts` no longer relies on the old recursive string-date cascade as the authoritative implementation
    - command-level scheduling supports move, resize, and project recalc
    - upstream-gap guardrails are encoded in code comments/tests, not only in the PRD
  </acceptance_criteria>
  <done>
    MCP gained a real headless scheduling core aligned to current gantt-lib semantics and protected against the three documented upstream defects.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build exhaustive regression tests for scheduling parity and guardrails</name>
  <files>
    packages/mcp/src/scheduler.test.ts
  </files>
  <read_first>
    packages/mcp/src/scheduler.test.ts
    packages/mcp/src/scheduler.ts
    D:/Проекты/gantt-lib/packages/gantt-lib/src/core/scheduling/__tests__/execute.test.ts
  </read_first>
  <action>
Expand MCP scheduling tests to cover:
- FS, SS, FF, SF with zero lag
- FS, SS, FF, SF with positive lag
- negative lag overlap behavior
- chain cascade of at least 4 tasks
- multiple predecessors on one successor
- business-day and calendar-day variants
- parent move with children
- parent summary recomputation after child/successor changes
- cycle detection
- missing dependency reference
- `resize_task(anchor='end')` fixed-start regression
- `recalculate_schedule()` multiple roots / multiple predecessors regression
- changedIds true diff regression
  </action>
  <verify>
    <automated>node --test src/scheduler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - regression exists for every explicit PRD test requirement
    - at least one test proves strongest constraint wins for a successor with two predecessors
    - at least one test proves changedIds excludes unchanged tasks
  </acceptance_criteria>
  <done>
    Scheduling semantics are now locked by regression tests rather than tribal knowledge.
  </done>
</task>

</tasks>

<verification>
1. `packages/mcp/src/scheduler.test.ts` covers PRD matrix for dependencies, lag, hierarchy, and changed-set behavior
2. `node --test src/scheduler.test.ts` passes
3. Code comments or docstrings identify intentional divergence from old upstream defects
</verification>

<success_criteria>
- MCP scheduling core is parity-oriented and headless
- Upstream bug compatibility is no longer accidental
- Regression suite makes future sync with gantt-lib auditable
</success_criteria>

<output>
After completion, create `.planning/phases/35-scheduling-core-adoption/35-01-SUMMARY.md`
</output>
