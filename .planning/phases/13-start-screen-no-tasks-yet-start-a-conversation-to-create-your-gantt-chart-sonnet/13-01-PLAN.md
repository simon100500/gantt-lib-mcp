---
phase: 13-start-screen-no-tasks-yet-start-a-conversation-to-create-your-gantt-chart-sonnet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/StartScreen.tsx
  - packages/web/src/App.tsx
autonomous: false
requirements:
  - START-01
  - START-02
  - START-03
  - START-04
  - START-05

must_haves:
  truths:
    - "When tasks.length === 0 and not loading, only the start screen is rendered — no Gantt chart, no toolbar, no chat sidebar"
    - "User can type a prompt in the textarea and press Enter or the send button — the chat sidebar opens and the first user message appears"
    - "Clicking a chip pre-fills the textarea with the corresponding Russian prompt text (not submitted automatically)"
    - "Clicking 'Пустой график' adds one placeholder task, opens the chat sidebar, and the start screen disappears (tasks.length > 0)"
    - "If tasks return to 0 (e.g. project switch), the start screen reappears and chat sidebar is hidden"
  artifacts:
    - path: "packages/web/src/components/StartScreen.tsx"
      provides: "Centered start screen component with headline, textarea, chips, and Пустой график button"
      exports: ["StartScreen", "StartScreenProps"]
    - path: "packages/web/src/App.tsx"
      provides: "Conditional rendering: tasks.length === 0 && !loading -> StartScreen; otherwise full layout"
      contains: "tasks.length === 0"
  key_links:
    - from: "packages/web/src/components/StartScreen.tsx"
      to: "packages/web/src/App.tsx"
      via: "onSend prop (calls App's handleStartScreenSend which sets chatSidebarVisible(true) then calls handleSend)"
      pattern: "onSend"
    - from: "packages/web/src/components/StartScreen.tsx"
      to: "packages/web/src/App.tsx"
      via: "onEmptyChart prop (calls handleAddTask + setChatSidebarVisible(true))"
      pattern: "onEmptyChart"
---

<objective>
Replace the "No tasks yet." empty state with a Sonnet-style centered start screen. When `tasks.length === 0` and not loading, App renders `StartScreen` full-area instead of the Gantt chart + toolbar. Chat sidebar is hidden. The start screen contains a headline, a wide auto-growing textarea, 4 example chips, and a "Пустой график" button.

Purpose: Give users a clear entry point when starting a new project — they either describe their project in natural language or create a blank chart in one click.
Output: `StartScreen.tsx` component + `App.tsx` wired to show it conditionally.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/web/src/App.tsx
@packages/web/src/components/ChatSidebar.tsx
@packages/web/src/components/ui/button.tsx
</context>

<interfaces>
<!-- Key contracts from the existing codebase. Executor uses these directly. -->

From packages/web/src/App.tsx (integration points):
```typescript
// Line 83 — controls chat sidebar visibility
const [chatSidebarVisible, setChatSidebarVisible] = useState(true);

// Line 124 — sends first user message + sets aiThinking = true
const handleSend = useCallback((text: string) => {
  setMessages(ms => [...ms, { id: String(++msgCounter), role: 'user', content: text }]);
  setAiThinking(true);
  send({ type: 'chat', message: text });
}, [send]);

// Line 150 — adds a task to state
const handleAddTask = useCallback((newTask: Task) => {
  setTasks(prev => [...prev, newTask]);
}, [setTasks]);

// Line 74 — loading state
const { tasks, setTasks, loading, error } = auth.isAuthenticated ? authenticatedTasks : localTasks;
```

From packages/web/src/components/ChatSidebar.tsx (patterns to reuse):
```typescript
// Auto-grow textarea pattern (lines 54-61):
function handleTextareaInput(e: React.FormEvent<HTMLTextAreaElement>) {
  const el = e.currentTarget;
  el.style.height = 'auto';
  const newHeight = el.scrollHeight;
  el.style.height = newHeight + 'px';
  el.style.overflowY = newHeight > 120 ? 'auto' : 'hidden';
}

// Chip pill style (lines 162-169):
className={cn(
  'text-[11px] px-2.5 py-1 rounded-full border border-slate-200 text-slate-500',
  'transition-colors hover:border-primary hover:text-primary',
  'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1',
)}

// Submit on Enter (not Shift+Enter) — lines 63-68
```

From packages/web/src/App.tsx (Task type usage):
```typescript
import type { Task } from './types.ts';
// Task has: id, name, startDate, endDate (string | Date), color?, progress?, dependencies?
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create StartScreen component</name>
  <files>packages/web/src/components/StartScreen.tsx</files>
  <action>
Create `packages/web/src/components/StartScreen.tsx` as a new file. The component renders a full-area centered layout (flex, items-center, justify-center, h-full, bg-background) with max-width ~640px container centered horizontally.

Props interface:
```typescript
interface StartScreenProps {
  onSend: (text: string) => void;           // submit prompt → open chat with first message
  onEmptyChart: () => void;                 // create placeholder task + open chat
  isAuthenticated?: boolean;                // passed through for future use (default true)
  onLoginRequired?: () => void;             // trigger OTP modal for guests
}
```

Layout (top to bottom inside the centered container):
1. Headline: `<h1>` with text «С чего начнём?» — Tailwind: `text-2xl font-semibold text-slate-900 mb-6 text-center`
2. Textarea form block (full container width):
   - `<form>` with flex column gap
   - `<div>` wrapper with relative positioning for textarea + send button overlay
   - `<textarea>` — rows=3, auto-grow same as ChatSidebar (handleTextareaInput), placeholder «Опишите ваш проект или выберите пример ниже», maxHeight 12rem (~5 rows), Enter submits (Shift+Enter = newline), wide: w-full, styled with border border-slate-200 rounded-xl px-4 py-3 pr-12 text-sm bg-white resize-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:border-transparent focus-visible:outline-none
   - Send button (`<button type="submit">`) — absolute bottom-2.5 right-2.5, h-8 w-8, rounded-lg, bg-primary text-primary-foreground, disabled when inputValue.trim() is empty; contains `<ArrowUp className="w-4 h-4" />`
3. Chips row (below textarea): flex flex-wrap gap-2 mt-3

   4 chips — pill outline style copied from QUICK_CHIPS in ChatSidebar:
   - «Загородный дом» → pre-fills: «Создай график строительства загородного дома: фундамент, стены, кровля, отделка, ландшафт»
   - «Ремонт офиса» → pre-fills: «Создай график ремонта офиса: демонтаж, электрика, отделка стен, пол, мебель»
   - «ИТ-проект» → pre-fills: «Создай график разработки ИТ-проекта: аналитика, дизайн, разработка, тестирование, релиз»
   - «Мероприятие» → pre-fills: «Создай график подготовки мероприятия: площадка, кейтеринг, программа, продвижение, проведение»

   Chip click: set textarea value to the template text, focus textarea. Do NOT submit.

4. "Пустой график" button — below chips, mt-4 centered:
   Use shadcn `<Button variant="default" size="sm">Пустой график</Button>` (blue primary). On click: call `props.onEmptyChart()`.

Submit handler logic:
- If inputValue.trim() is empty: do nothing
- Call `props.onSend(inputValue.trim())`
- Reset inputValue to ''
- Reset textarea height to auto

Imports needed: `useState`, `useRef` from react; `ArrowUp` from lucide-react; `cn` from `@/lib/utils`; `Button` from `./ui/button`.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsc --noEmit -p packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>StartScreen.tsx exists, exports StartScreen component, TypeScript compiles without errors on this file, all 4 chips and the Пустой график button are present in the JSX</done>
</task>

<task type="auto">
  <name>Task 2: Wire StartScreen into App.tsx</name>
  <files>packages/web/src/App.tsx</files>
  <action>
Modify `packages/web/src/App.tsx` to conditionally render the start screen when `tasks.length === 0 && !loading`.

**Step 1 — Import StartScreen:**
Add import at top of file:
```typescript
import { StartScreen } from './components/StartScreen.tsx';
```

**Step 2 — Change chatSidebarVisible initial state:**
Change line 83 from `useState(true)` to `useState(false)`. The chat sidebar starts hidden; it opens only after the user interacts.

**Step 3 — Add useEffect to reset to start screen when tasks become empty:**
After the existing useEffect for project change (line 234), add:
```typescript
// Reset to start screen state when all tasks are removed
useEffect(() => {
  if (tasks.length === 0 && !loading) {
    setChatSidebarVisible(false);
  }
}, [tasks.length, loading]);
```
This ensures if tasks are cleared (project switch, delete all), the start screen reappears.

**Step 4 — Create handleStartScreenSend callback:**
Add after `handleSend` definition:
```typescript
const handleStartScreenSend = useCallback((text: string) => {
  setChatSidebarVisible(true);
  handleSend(text);
}, [handleSend]);
```

**Step 5 — Create handleEmptyChart callback:**
Add after `handleStartScreenSend`:
```typescript
const handleEmptyChart = useCallback(() => {
  const today = new Date().toISOString().split('T')[0];
  const placeholderTask: Task = {
    id: `task-${Date.now()}`,
    name: 'Новая задача',
    startDate: today,
    endDate: today,
  };
  handleAddTask(placeholderTask);
  setChatSidebarVisible(true);
}, [handleAddTask]);
```

**Step 6 — Replace the main content area with conditional rendering:**

Find the `<div className="flex flex-1 overflow-hidden">` block (the "Main" section, line ~327). Inside it, the current content is:
1. Gantt panel wrapper div (contains toolbar + GanttChart + footer)
2. Chat sidebar aside

Replace the content inside that wrapper div with a conditional — `tasks.length === 0 && !loading` renders `<StartScreen>`, otherwise renders the existing Gantt panel + chat sidebar:

```tsx
{/* ── Main ─────────────────────────────────────────────────────────── */}
<div className="flex flex-1 overflow-hidden">
  {tasks.length === 0 && !loading ? (
    /* ── Start Screen ────────────────────────────────────────────── */
    <StartScreen
      onSend={handleStartScreenSend}
      onEmptyChart={handleEmptyChart}
      isAuthenticated={auth.isAuthenticated}
      onLoginRequired={() => setShowOtpModal(true)}
    />
  ) : (
    <>
      {/* Gantt panel wrapper - includes chart and footer */}
      <div className="flex flex-col flex-1 overflow-hidden min-w-0">
        {/* ... existing toolbar ... */}
        {/* ... existing GanttChart + loading ... */}
        {/* ... existing footer ... */}
      </div>

      {/* Chat sidebar */}
      {chatSidebarVisible && (
        <aside className="w-80 shrink-0 border-l border-slate-200 flex flex-col">
          <ChatSidebar ... />
        </aside>
      )}
    </>
  )}
</div>
```

Keep all existing toolbar, GanttChart, footer, and ChatSidebar JSX unchanged — just wrap them in the `tasks.length > 0 || loading` branch. The loading spinner (`<div>Загрузка…</div>`) inside the Gantt section is already handled by the existing conditional inside that block.

Note: When `tasks.length === 0 && !loading`, the Gantt toolbar div is also hidden (it's inside the else branch), satisfying the toolbar-visibility requirement from CONTEXT.md.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsc --noEmit -p packages/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles clean. With tasks.length === 0: only StartScreen rendered (no toolbar, no Gantt, no chat sidebar). With tasks.length > 0: full layout shown (toolbar + Gantt + chat). chatSidebarVisible initial state is false.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of start screen</name>
  <action>Verify the complete start screen feature works correctly in the browser end-to-end.</action>
  <verify>Human visual and functional verification per how-to-verify steps below.</verify>
  <done>All verification steps pass: start screen visible with no tasks, chips pre-fill textarea without submitting, prompt submit opens chat with first message, Пустой график creates task and opens chat, returning to 0 tasks shows start screen again.</done>
  <what-built>
    StartScreen component wired into App.tsx. Visible when tasks.length === 0. Contains headline «С чего начнём?», auto-growing textarea, 4 example chips, and blue «Пустой график» button. Chat sidebar hidden on start screen, opens after user interaction.
  </what-built>
  <how-to-verify>
    1. Run dev server: `cd D:/Projects/gantt-lib-mcp && npm run dev` (starts web on http://localhost:5173)
    2. Open http://localhost:5173 in browser
    3. Use guest mode with no tasks — verify start screen appears: headline «С чего начнём?», textarea, 4 chips, blue button
    4. Click chip «ИТ-проект» — verify textarea fills with the IT project prompt text, NOT submitted automatically
    5. Edit the text and press Enter — verify chat sidebar appears on the right, user message visible, AI thinking indicator shows
    6. Refresh the page with no tasks — verify start screen reappears (no Gantt chart visible), chat sidebar is hidden
    7. Click «Пустой график» — verify one task appears in the Gantt chart, chat sidebar opens on the right, start screen gone
    8. Delete that task via Gantt UI — verify return to start screen, chat hidden
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/web/tsconfig.json` passes with no errors
- StartScreen.tsx exists at packages/web/src/components/StartScreen.tsx
- App.tsx imports StartScreen and renders it conditionally on tasks.length === 0 and !loading
- chatSidebarVisible initial state is false in App.tsx
- useEffect resets chatSidebarVisible to false when tasks return to 0
</verification>

<success_criteria>
1. tasks.length === 0: StartScreen renders full-area, no Gantt toolbar, no Gantt chart, no chat sidebar visible
2. Chip click: textarea pre-fills with descriptive Russian prompt text, no auto-submit
3. Textarea submit (Enter): handleStartScreenSend called → chat sidebar visible, first user message in chat, AI thinking
4. «Пустой график» click: one Task with id/name/startDate/endDate added to state, chat sidebar opened
5. tasks.length returns to 0: start screen reappears, chat sidebar hidden
6. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-start-screen-no-tasks-yet-start-a-conversation-to-create-your-gantt-chart-sonnet/13-01-SUMMARY.md` with what was built, decisions made, and any deviations from this plan.
</output>
