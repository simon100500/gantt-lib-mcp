---
phase: 30-constraint-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - packages/mcp/prisma/schema.prisma
  - packages/server/src/services/constraint-service.ts
  - packages/server/src/services/constraint-service.test.ts
autonomous: true
requirements:
  - ENG-02
  - ENG-03
  - ENG-04
must_haves:
  truths:
    - "ConstraintService.checkLimit() returns a structured allow/deny result for any canonical limit key."
    - "ConstraintService.getUsage() and getRemaining() return normalized objects for count, daily, lifetime, boolean, and unlimited cases."
    - "Daily and lifetime counters persist in PostgreSQL and increment atomically."
    - "Unknown limit keys fail as configuration errors instead of silently allowing access."
  artifacts:
    - path: "packages/mcp/prisma/schema.prisma"
      provides: "Persistent usage counter model"
    - path: "packages/server/src/services/constraint-service.ts"
      provides: "ConstraintService with checkLimit/getUsage/getRemaining/incrementUsage"
    - path: "packages/server/src/services/constraint-service.test.ts"
      provides: "Automated coverage for normalized limit semantics"
  key_links:
    - from: "packages/server/src/services/constraint-service.ts"
      to: "packages/mcp/prisma/schema.prisma"
      via: "Prisma usageCounter model"
      pattern: "usageCounter"
    - from: "packages/server/src/services/constraint-service.ts"
      to: "packages/mcp/src/constraints/catalog.ts"
      via: "shared plan and key metadata"
      pattern: "@gantt/mcp/constraints"
---

<objective>
Add the Prisma persistence model and the server-side ConstraintService that reads the shared plan catalog and exposes normalized limit checks.

Purpose: make tariff constraints executable instead of declarative.
Output: Prisma-backed ConstraintService with tests covering count, daily, lifetime, boolean, unlimited, and config-error paths.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-constraint-engine/30-CONTEXT.md
@.planning/phases/28-billing/28-02-SUMMARY.md
@packages/mcp/prisma/schema.prisma
@packages/server/src/services/billing-service.ts
@packages/server/src/billing-service.test.ts
@packages/mcp/src/constraints/catalog.ts

<interfaces>
From `packages/server/src/services/billing-service.ts`:

```ts
export class BillingService {
  async getOrCreateSubscription(userId: string)
  async getSubscriptionStatus(userId: string): Promise<{ plan: PlanKey; periodEnd: string | null; aiUsed: number; aiLimit: number; isActive: boolean; }>
}
```

From Phase 30 context:

```text
checkLimit() -> allow/deny + limit metadata + current usage + machine-readable reason code
unknown limitKey -> configuration error
boolean gates -> normalized readers return not_applicable
daily limits reset by server calendar day
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistence schema and failing tests for normalized constraint semantics</name>
  <read_first>
    - packages/mcp/prisma/schema.prisma
    - packages/server/src/billing-service.test.ts
    - packages/mcp/src/constraints/catalog.ts
  </read_first>
  <files>packages/mcp/prisma/schema.prisma, packages/server/src/services/constraint-service.test.ts</files>
  <action>Create a new Prisma model for persistent counters in `packages/mcp/prisma/schema.prisma` using a stable period bucket string, not a nullable date. Use fields `id`, `userId`, `limitKey`, `periodBucket`, `usage`, `createdAt`, `updatedAt` and a unique constraint on `[userId, limitKey, periodBucket]`; use bucket values exactly `lifetime` for lifetime counters and `day:YYYY-MM-DD` for daily counters computed from the server calendar date per D-08. Then add `packages/server/src/services/constraint-service.test.ts` with node:test coverage that fails before implementation. Cover these behaviors explicitly: `projects` count uses active projects from Prisma rather than counters; `ai_queries` on `free` reads `lifetime` with limit 20; `ai_queries` on paid plans reads the correct `day:YYYY-MM-DD` bucket with 25/50/100 limits; `archive` and `resource_pool` return `usageState: 'not_applicable'`; `enterprise` projects return `remainingState: 'unlimited'`; `export` returns access-level metadata and `not_applicable` usage; unknown `limitKey` throws an error with code `UNKNOWN_LIMIT_KEY`.</action>
  <behavior>
    - Test 1: `getUsage('free-user', 'ai_queries')` returns a normalized object with `period: 'lifetime'`, `limit: 20`, and `usageState: 'tracked'`.
    - Test 2: `getUsage('team-user', 'ai_queries')` reads bucket `day:2026-04-02` when the injected clock is on 2026-04-02.
    - Test 3: `getRemaining(userId, 'archive')` returns `remainingState: 'not_applicable'` and does not create a counter row.
    - Test 4: `checkLimit(userId, 'projects')` denies with `reasonCode: 'limit_reached'` when active project count equals the plan limit.
    - Test 5: `checkLimit(userId, 'missing_key')` throws `UNKNOWN_LIMIT_KEY`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "periodBucket|@@unique\\(\\[userId, limitKey, periodBucket\\]\\)" packages/mcp/prisma/schema.prisma` shows the persistence contract.
    - `rg -n "UNKNOWN_LIMIT_KEY|usageState: 'not_applicable'|remainingState: 'unlimited'" packages/server/src/services/constraint-service.test.ts` finds the required expectations.
    - After `cmd /c npx tsc -p packages\\server\\tsconfig.json`, `cmd /c node --test packages\\server\\dist\\services\\constraint-service.test.js` exits non-zero before implementation because `ConstraintService` is not implemented yet.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages\server\tsconfig.json && node --test packages\server\dist\services\constraint-service.test.js</automated>
  </verify>
  <done>The schema contract and failing tests define the exact semantics for normalized readers, server-day buckets, and config errors before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ConstraintService with atomic Prisma increments</name>
  <read_first>
    - packages/server/src/services/constraint-service.test.ts
    - packages/server/src/services/billing-service.ts
    - packages/mcp/src/constraints/catalog.ts
    - packages/mcp/prisma/schema.prisma
  </read_first>
  <files>packages/server/src/services/constraint-service.ts</files>
  <action>Implement `packages/server/src/services/constraint-service.ts` to consume the shared plan catalog and Prisma. Expose public methods `checkLimit(userId, limitKey)`, `getUsage(userId, limitKey)`, `getRemaining(userId, limitKey)`, and `incrementUsage(userId, limitKey, amount = 1, now = new Date())`. `checkLimit()` must return a structured object containing at least `allowed`, `reasonCode`, `planId`, `limitKey`, `limit`, `usage`, and `remaining`; use exact reason codes `allowed`, `limit_reached`, and `feature_disabled`. `getUsage()` and `getRemaining()` must return one normalized shape for every limit type, with `usageState` or `remainingState` set to `tracked`, `unlimited`, or `not_applicable` instead of returning `null`. Implement `projects` by counting active projects from the `projects` table (`status = active`), implement `ai_queries` with Prisma `upsert` on the usage-counter model using `update: { usage: { increment: amount } }`, implement boolean and export readers as non-tracked metadata reads, and throw an application error with code `UNKNOWN_LIMIT_KEY` if the key is absent from the catalog. Accept an injectable clock/helper so tests can verify the exact `day:YYYY-MM-DD` period bucket.</action>
  <acceptance_criteria>
    - `rg -n "checkLimit\\(|getUsage\\(|getRemaining\\(|incrementUsage\\(" packages/server/src/services/constraint-service.ts` finds all four public methods.
    - `rg -n "reasonCode: 'allowed'|reasonCode: 'limit_reached'|reasonCode: 'feature_disabled'" packages/server/src/services/constraint-service.ts` confirms the machine-readable result contract.
    - `rg -n "usage: \\{ increment: amount \\}" packages/server/src/services/constraint-service.ts` confirms atomic increment logic.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages\server\tsconfig.json && node --test packages\server\dist\services\constraint-service.test.js packages\server\dist\billing-service.test.js</automated>
  </verify>
  <done>ConstraintService passes the new test suite, uses Prisma persistence for tracked counters, and returns normalized objects for all required limit shapes.</done>
</task>

</tasks>

<verification>
Run `cmd /c npx tsc -p packages\server\tsconfig.json && node --test packages\server\dist\services\constraint-service.test.js packages\server\dist\billing-service.test.js`. Confirm the service uses server-day buckets for paid AI usage and never uses `-1` or silent fallback behavior.
</verification>

<success_criteria>
The server can answer limit questions from one canonical catalog and persistent usage storage, with deterministic responses for tracked, unlimited, and not-applicable cases.
</success_criteria>

<output>
After completion, create `.planning/phases/30-constraint-engine/30-02-SUMMARY.md`
</output>
