---
phase: 30-constraint-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
files_modified:
  - packages/server/src/services/plan-config.ts
  - packages/server/src/services/billing-service.ts
  - packages/server/src/routes/billing-routes.ts
  - packages/server/src/middleware/subscription-middleware.ts
  - packages/web/src/stores/useBillingStore.ts
  - packages/web/src/components/layout/ProjectMenu.tsx
autonomous: true
requirements:
  - ENG-01
  - ENG-02
must_haves:
  truths:
    - "Billing and enforcement surfaces read plan metadata from the shared catalog rather than legacy local tables."
    - "Current server endpoints expose normalized limit information without `-1` sentinels."
    - "Current web billing state and project menu can render unlimited and non-tracked values without assuming numeric `-1`."
  artifacts:
    - path: "packages/server/src/services/plan-config.ts"
      provides: "Compatibility adapter over the shared catalog"
    - path: "packages/server/src/routes/billing-routes.ts"
      provides: "Subscription payload backed by shared catalog and ConstraintService"
    - path: "packages/web/src/stores/useBillingStore.ts"
      provides: "Frontend types for normalized limit payloads"
  key_links:
    - from: "packages/server/src/routes/billing-routes.ts"
      to: "packages/server/src/services/constraint-service.ts"
      via: "subscription endpoint and enforcement reads"
      pattern: "ConstraintService"
    - from: "packages/web/src/components/layout/ProjectMenu.tsx"
      to: "packages/web/src/stores/useBillingStore.ts"
      via: "normalized limit rendering"
      pattern: "projects"
---

<objective>
Replace the legacy billing surface with adapters over the shared catalog and ConstraintService, while keeping the current app working.

Purpose: finish Phase 30 by making existing billing routes, middleware, and current web consumers speak the new normalized constraint vocabulary.
Output: compatibility layer for existing billing APIs plus frontend handling for explicit unlimited/non-tracked values.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-constraint-engine/30-CONTEXT.md
@.planning/phases/28-billing/28-01-SUMMARY.md
@.planning/phases/28-billing/28-02-SUMMARY.md
@.planning/phases/29-paywall-enhance/29-02-SUMMARY.md
@packages/server/src/services/plan-config.ts
@packages/server/src/services/billing-service.ts
@packages/server/src/routes/billing-routes.ts
@packages/server/src/middleware/subscription-middleware.ts
@packages/web/src/stores/useBillingStore.ts
@packages/web/src/components/layout/ProjectMenu.tsx

<interfaces>
From `packages/web/src/stores/useBillingStore.ts`:

```ts
export interface SubscriptionStatus {
  plan: string;
  periodEnd: string | null;
  aiUsed: number;
  aiLimit: number;
  isActive: boolean;
  limits: PlanLimits;
}
```

From `packages/server/src/middleware/subscription-middleware.ts`:

```ts
reply.status(403).send({
  error: '...',
  code: 'AI_LIMIT_REACHED',
  aiUsed: status.aiUsed,
  aiLimit: status.aiLimit,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint billing and middleware to the shared catalog plus ConstraintService</name>
  <read_first>
    - packages/server/src/services/plan-config.ts
    - packages/server/src/services/billing-service.ts
    - packages/server/src/routes/billing-routes.ts
    - packages/server/src/middleware/subscription-middleware.ts
    - packages/server/src/services/constraint-service.ts
    - packages/mcp/src/constraints/catalog.ts
  </read_first>
  <files>packages/server/src/services/plan-config.ts, packages/server/src/services/billing-service.ts, packages/server/src/routes/billing-routes.ts, packages/server/src/middleware/subscription-middleware.ts</files>
  <action>Turn `packages/server/src/services/plan-config.ts` into a compatibility adapter over the shared `@gantt/mcp/constraints` catalog instead of a second source of truth. Keep exported helpers needed by existing imports, but map unlimited values to the literal `unlimited` and canonical key names (`ai_queries`, `resource_pool`, `export`) internally per D-05 and D-11. Update `BillingService.getSubscriptionStatus()` to include the active plan id, plan metadata from the shared catalog, and normalized AI usage from `ConstraintService.getUsage(userId, 'ai_queries')`. Update `packages/server/src/routes/billing-routes.ts` so payment creation uses shared pricing/description and `GET /api/billing/subscription` returns a normalized payload containing `plan`, `periodEnd`, `isActive`, `limits`, and a `usage` object keyed by canonical limit keys for at least `projects`, `ai_queries`, `archive`, `resource_pool`, and `export`. Update `subscription-middleware.ts` to call `ConstraintService.checkLimit(userId, 'ai_queries')`; when denied, keep HTTP 403 and code `AI_LIMIT_REACHED`, but source `aiUsed`, `aiLimit`, and reason data from the structured check result instead of local numeric comparisons.</action>
  <acceptance_criteria>
    - `rg -n "@gantt/mcp/constraints|ConstraintService" packages/server/src/services/plan-config.ts packages/server/src/services/billing-service.ts packages/server/src/routes/billing-routes.ts packages/server/src/middleware/subscription-middleware.ts` shows the new shared dependencies.
    - `rg -n "usage: \\{|projects:|ai_queries:|archive:|resource_pool:|export:" packages/server/src/routes/billing-routes.ts` shows canonical usage payload keys in the subscription response.
    - `rg -n "AI_LIMIT_REACHED" packages/server/src/middleware/subscription-middleware.ts` still finds the existing external error code.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages\server\tsconfig.json</automated>
  </verify>
  <done>The server billing surface uses the shared plan catalog and ConstraintService while preserving current external route contracts and 403 AI limit behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update current web billing consumers for explicit unlimited and normalized limits</name>
  <read_first>
    - packages/web/src/stores/useBillingStore.ts
    - packages/web/src/components/layout/ProjectMenu.tsx
    - packages/server/src/routes/billing-routes.ts
  </read_first>
  <files>packages/web/src/stores/useBillingStore.ts, packages/web/src/components/layout/ProjectMenu.tsx</files>
  <action>Update `packages/web/src/stores/useBillingStore.ts` so `SubscriptionStatus` matches the new billing subscription payload: keep `plan`, `periodEnd`, and `isActive`, but replace numeric-only limit assumptions with normalized limit entries that can represent `tracked`, `unlimited`, and `not_applicable`. Then update `packages/web/src/components/layout/ProjectMenu.tsx` so the projects badge renders `∞` when the projects limit is explicit `unlimited`, renders `{used}/{limit}` only for tracked numeric counts, and does not rely on `-1`. Preserve the existing UI copy and billing CTA behavior; this task is only about adapting current rendering to the new payload shape.</action>
  <acceptance_criteria>
    - `rg -n "unlimited|not_applicable|tracked" packages/web/src/stores/useBillingStore.ts` finds the normalized state vocabulary.
    - `rg -n "∞|unlimited" packages/web/src/components/layout/ProjectMenu.tsx` confirms the explicit unlimited handling.
    - `rg -n "=== -1" packages/web/src/components/layout/ProjectMenu.tsx packages/web/src/stores/useBillingStore.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npm run build -w packages/web</automated>
  </verify>
  <done>The current web billing consumers render the new normalized limit payload correctly and no longer depend on legacy `-1` conventions.</done>
</task>

</tasks>

<verification>
Build the server and web packages. Confirm the billing subscription route exposes canonical keys and the project menu no longer checks `=== -1`.
</verification>

<success_criteria>
Phase 30 finishes with legacy billing/UI surfaces reading the new constraint engine vocabulary rather than maintaining a parallel quota model.
</success_criteria>

<output>
After completion, create `.planning/phases/30-constraint-engine/30-03-SUMMARY.md`
</output>
