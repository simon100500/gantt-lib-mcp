---
phase: 30-constraint-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/constraints/catalog.ts
  - packages/mcp/src/constraints/index.ts
  - packages/mcp/package.json
  - packages/web/package.json
  - packages/web/src/lib/billing.ts
autonomous: true
requirements:
  - ENG-01
  - ENG-03
must_haves:
  truths:
    - "All four tariffs expose one canonical plan catalog with explicit entries for projects, ai_queries, archive, resource_pool, and export."
    - "Unlimited values are represented as `unlimited`, not `-1`."
    - "The same pricing and plan metadata can be imported from one shared module by more than one package."
  artifacts:
    - path: "packages/mcp/src/constraints/catalog.ts"
      provides: "Canonical limit-key metadata and four-plan catalog"
    - path: "packages/mcp/src/constraints/index.ts"
      provides: "Shared constraint exports for downstream packages"
    - path: "packages/web/src/lib/billing.ts"
      provides: "Web billing adapter reading labels and prices from the shared catalog"
  key_links:
    - from: "packages/web/src/lib/billing.ts"
      to: "packages/mcp/src/constraints/catalog.ts"
      via: "@gantt/mcp export"
      pattern: "@gantt/mcp/"
    - from: "packages/mcp/package.json"
      to: "packages/mcp/src/constraints/index.ts"
      via: "package exports"
      pattern: "\"./constraints\""
---

<objective>
Create the shared plan catalog and limit-key catalog that become the only source of truth for tariff pricing and constraint definitions.

Purpose: eliminate server/web drift before service logic and persistence are added.
Output: exported shared constraint module plus a web billing adapter that reads plan labels and prices from it.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-constraint-engine/30-CONTEXT.md
@packages/server/src/services/plan-config.ts
@packages/web/src/lib/billing.ts
@packages/mcp/package.json
@packages/web/package.json

<interfaces>
From `packages/server/src/services/plan-config.ts`:

```ts
export type PlanKey = 'free' | 'start' | 'team' | 'enterprise';
export function getPlanPricing(plan: PlanKey, period: 'monthly' | 'yearly'): number;
```

From `packages/web/src/lib/billing.ts`:

```ts
export type BillingPeriod = 'monthly' | 'yearly';
export type PlanId = 'free' | 'start' | 'team' | 'enterprise';
export const PLAN_LABELS: Record<PlanId, string>;
export const PLAN_PRICES: Record<'start' | 'team' | 'enterprise', Record<BillingPeriod, number>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical shared constraint catalog</name>
  <read_first>
    - packages/server/src/services/plan-config.ts
    - packages/mcp/package.json
  </read_first>
  <files>packages/mcp/src/constraints/catalog.ts, packages/mcp/src/constraints/index.ts, packages/mcp/package.json</files>
  <action>Create `packages/mcp/src/constraints/catalog.ts` and export it through `packages/mcp/src/constraints/index.ts` plus a new `./constraints` entry in `packages/mcp/package.json`. The module must define the canonical plan ids `free | start | team | enterprise`, the canonical limit keys `projects | ai_queries | archive | resource_pool | export`, and the export access levels `none | pdf | pdf_excel | pdf_excel_api` per D-01. Define limit-key metadata exactly once with display labels and value kinds so that `projects` is count-based, `ai_queries` is usage-based, `archive` and `resource_pool` are boolean gates per D-02, and `export` is access-level based per D-01. Define one explicit plan catalog object with no inheritance per D-12; each tariff must enumerate every limit: `free` => `projects: 1`, `ai_queries: { period: 'lifetime', value: 20 }`, `archive: false`, `resource_pool: false`, `export: 'none'`; `start` => `projects: 3`, `ai_queries: { period: 'daily', value: 25 }`, `archive: true`, `resource_pool: true`, `export: 'pdf'`; `team` => `projects: 7`, `ai_queries: { period: 'daily', value: 50 }`, `archive: true`, `resource_pool: true`, `export: 'pdf_excel'`; `enterprise` => `projects: 'unlimited'`, `ai_queries: { period: 'daily', value: 100 }`, `archive: true`, `resource_pool: true`, `export: 'pdf_excel_api'`. Put pricing in the same catalog with monthly/yearly values `free 0/0`, `start 1490/11900`, `team 4990/47900`, `enterprise 12900/129000`. Export helper getters that return `unlimited` literally, never `-1`, and keep `team_members` out of the catalog per deferred scope.</action>
  <acceptance_criteria>
    - `rg -n "\"projects\"|\"ai_queries\"|\"archive\"|\"resource_pool\"|\"export\"" packages/mcp/src/constraints/catalog.ts` finds all five canonical keys.
    - `rg -n "enterprise.*projects: 'unlimited'" packages/mcp/src/constraints/catalog.ts` matches the explicit unlimited representation.
    - `rg -n "\"./constraints\"" packages/mcp/package.json` shows the new export entry.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages\mcp\tsconfig.json</automated>
  </verify>
  <done>The shared module builds, exports the canonical plan catalog, and every tariff explicitly enumerates all five limit keys without `-1` sentinels.</done>
</task>

<task type="auto">
  <name>Task 2: Move the web billing adapter onto the shared catalog</name>
  <read_first>
    - packages/web/package.json
    - packages/web/src/lib/billing.ts
    - packages/mcp/src/constraints/catalog.ts
  </read_first>
  <files>packages/web/package.json, packages/web/src/lib/billing.ts</files>
  <action>Add `@gantt/mcp` as a workspace dependency in `packages/web/package.json` if it is not already present. Rewrite `packages/web/src/lib/billing.ts` so `PlanId`, `BillingPeriod`, `PLAN_LABELS`, and `PLAN_PRICES` are derived from the shared `@gantt/mcp/constraints` catalog instead of local duplicated literals. Keep web-only helpers `formatPrice`, `formatDate`, `formatAmount`, and `loadWidgetScript`, and keep `PLAN_FEATURES` local marketing copy if needed, but remove duplicated hardcoded label/price tables so shared pricing and names now come only from the plan catalog.</action>
  <acceptance_criteria>
    - `rg -n "\"@gantt/mcp\"" packages/web/package.json` shows the workspace dependency.
    - `rg -n "from '@gantt/mcp/constraints'" packages/web/src/lib/billing.ts` confirms the shared import.
    - `rg -n "PLAN_LABELS|PLAN_PRICES" packages/web/src/lib/billing.ts` shows exported adapters still exist for current UI consumers.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npm run build -w packages/web</automated>
  </verify>
  <done>The web package reads plan labels and prices from the shared catalog while preserving the existing billing helper surface for current React components.</done>
</task>

</tasks>

<verification>
Build the shared package and the web package. Confirm there is one exported shared catalog and no duplicated hardcoded price/label tables remain in `packages/web/src/lib/billing.ts`.
</verification>

<success_criteria>
Phase 30 has a shared source of truth for tariff pricing and constraint metadata that can be imported by server and web code without copying literals.
</success_criteria>

<output>
After completion, create `.planning/phases/30-constraint-engine/30-01-SUMMARY.md`
</output>
