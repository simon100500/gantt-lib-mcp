---
phase: 45-history-refactor
plan: 05
type: execute
wave: 5
depends_on:
  - "45-01"
  - "45-02"
  - "45-03"
  - "45-04"
files_modified:
  - packages/mcp/src/services/history.service.test.ts
  - packages/server/src/routes/history-routes.test.ts
  - packages/web/src/lib/apiTypes.ts
  - packages/web/src/hooks/useProjectHistory.ts
  - .planning/phases/45-history-refactor/45-HUMAN-UAT.md
  - .planning/phases/45-history-refactor/45-VERIFICATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The final history path has synchronized package contracts and no public undo/redo leakage"
    - "Preview and restore semantics are covered by automated and manual verification"
    - "The history refactor ships without `as any` or hidden type workarounds on the version-viewing path"
  artifacts:
    - path: "packages/mcp/src/services/history.service.test.ts"
      provides: "Service coverage for preview/restore semantics"
      exports: []
    - path: "packages/server/src/routes/history-routes.test.ts"
      provides: "HTTP contract coverage for version list/snapshot/restore"
      exports: []
    - path: ".planning/phases/45-history-refactor/45-VERIFICATION.md"
      provides: "Phase verification report against PRD success criteria"
      exports: []
  key_links:
    - from: "packages/web/src/hooks/useProjectHistory.ts"
      to: "packages/web/src/lib/apiTypes.ts"
      via: "typed history client contract without workarounds"
      pattern: "HistorySnapshotResponse|HistoryRestoreResponse"
    - from: ".planning/phases/45-history-refactor/45-VERIFICATION.md"
      to: ".planning/reference/history-versioning-clean-architecture-prd.md"
      via: "phase success criteria traceability"
      pattern: "Success criteria"
---

<objective>
Close the phase with contract cleanup and verification so the version-oriented history architecture is safe to execute and verify.

Purpose: the PRD explicitly requires synchronized package contracts and forbids `as any` / hidden type workarounds on the history path.
Output: synchronized contracts, automated coverage for the new semantics, and human verification artifacts for preview/restore behavior.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-history-refactor/45-CONTEXT.md
@.planning/reference/history-versioning-clean-architecture-prd.md
@packages/mcp/src/services/history.service.test.ts
@packages/server/src/routes/history-routes.test.ts
@packages/web/src/lib/apiTypes.ts
@packages/web/src/hooks/useProjectHistory.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish type/package cleanup and lock the new history contracts with tests</name>
  <files>packages/mcp/src/services/history.service.test.ts, packages/server/src/routes/history-routes.test.ts, packages/web/src/lib/apiTypes.ts, packages/web/src/hooks/useProjectHistory.ts</files>
  <read_first>
    packages/mcp/src/services/history.service.test.ts
    packages/server/src/routes/history-routes.test.ts
    packages/web/src/lib/apiTypes.ts
    packages/web/src/hooks/useProjectHistory.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "snapshot|restore|isCurrent|canRestore" packages/mcp/src/services/history.service.test.ts packages/server/src/routes/history-routes.test.ts` finds the new contract assertions
    - `rg -n "as any" packages/web/src/lib/apiTypes.ts packages/web/src/hooks/useProjectHistory.ts packages/server/src/routes/history-routes.ts packages/mcp/src/services/history.service.ts` returns no matches
    - `rg -n "undoGroup|redoGroup|undoLatest|redoable" packages/web/src/hooks/useProjectHistory.ts packages/web/src/lib/apiTypes.ts` returns no matches
  </acceptance_criteria>
  <action>
    Do a final contract pass across the history path.

    Update tests and client code so they lock the new version-oriented semantics:
    - service tests assert preview and restore behavior, not public redo semantics
    - route tests assert the list/snapshot/restore endpoints and fields
    - web client code consumes only the new typed contracts

    Remove any remaining history-path workarounds such as:
    - `as any`
    - legacy undo/redo hook names
    - public `redoable` field usage

    Keep internal append-only mechanics private to the server implementation.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/mcp/src/services/history.service.test.ts && npx tsx --test packages/server/src/routes/history-routes.test.ts && npm run build -w packages/mcp && npm run build -w packages/server && npm run build -w packages/web</automated>
  </verify>
  <done>
    The codebase exposes one clean version-oriented history contract without leaked undo/redo API semantics or type shortcuts.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Produce human verification artifacts for version preview and restore</name>
  <files>.planning/phases/45-history-refactor/45-HUMAN-UAT.md, .planning/phases/45-history-refactor/45-VERIFICATION.md</files>
  <read_first>
    .planning/reference/history-versioning-clean-architecture-prd.md
    .planning/phases/44-undo-redo/44-VERIFICATION.md
    .planning/phases/45-history-refactor/45-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "Показать эту версию|Вернуться к текущей версии|Восстановить эту версию" .planning/phases/45-history-refactor/45-HUMAN-UAT.md` finds the manual user flows
    - `rg -n "GET /api/history|GET /api/history/:groupId/snapshot|POST /api/history/:groupId/restore" .planning/phases/45-history-refactor/45-VERIFICATION.md` finds traceability to the new API surface
    - `rg -n "as any|type workaround" .planning/phases/45-history-refactor/45-VERIFICATION.md` documents the no-workaround requirement
  </acceptance_criteria>
  <action>
    Create final verification artifacts for the phase.

    In `.planning/phases/45-history-refactor/45-HUMAN-UAT.md`, document manual flows for:
    - opening the version list
    - previewing an older version
    - returning to the current version
    - restoring an older version
    - confirming that editing is blocked while previewing

    In `.planning/phases/45-history-refactor/45-VERIFICATION.md`, verify the PRD success criteria one by one:
    1. `GET /api/history` returns a user-visible version timeline
    2. clicking a history row previews the selected version
    3. preview snapshot comes from the server
    4. frontend does not replay inverse commands for preview
    5. restore remains distinct from preview
    6. preview mode is isolated from editing state
    7. restore rolls back only the active tail
    8. restore uses the authoritative command path
    9. no `as any` or hidden type workarounds remain on the history path

    Include concrete file references and command/test evidence in the verification report.
  </action>
  <verify>
    <manual>Follow `.planning/phases/45-history-refactor/45-HUMAN-UAT.md` against a project with at least three history versions, including one restored older version.</manual>
  </verify>
  <done>
    The phase ends with explicit human validation steps and a verification report mapped to the PRD success criteria.
  </done>
</task>

</tasks>

<verification>
Confirm that the final implementation exposes a clean version-oriented history product and that both automated and manual verification cover preview/restore semantics.
</verification>

<success_criteria>
Phase 45 is ready to execute with synchronized contracts, explicit verification coverage, and no type shortcuts on the history path.
</success_criteria>

<output>
After completion, create `.planning/phases/45-history-refactor/45-05-SUMMARY.md`
</output>
