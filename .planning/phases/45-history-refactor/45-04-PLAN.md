---
phase: 45-history-refactor
plan: 04
type: execute
wave: 4
depends_on:
  - "45-03"
files_modified:
  - packages/web/src/components/HistoryPanel.tsx
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
  - packages/web/src/components/layout/Toolbar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The history UI behaves like a version browser, not like an undo/redo control surface"
    - "Preview mode is visibly read-only and provides a clear exit back to the current version"
    - "Edit actions and history hotkeys are blocked while preview mode is active"
  artifacts:
    - path: "packages/web/src/components/HistoryPanel.tsx"
      provides: "Version-oriented history list with row preview and restore action menu"
      exports: ["HistoryPanel"]
    - path: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      provides: "Preview banner, return-to-current action, and read-only gating while viewing history"
      exports: ["ProjectWorkspace"]
  key_links:
    - from: "packages/web/src/components/HistoryPanel.tsx"
      to: "packages/web/src/hooks/useProjectHistory.ts"
      via: "row click preview and restore action callbacks"
      pattern: "preview"
    - from: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      to: "packages/web/src/stores/useHistoryViewerStore.ts"
      via: "preview banner and editing lockout"
      pattern: "mode"
---

<objective>
Refactor the workspace UI so history feels like version viewing and version restore, not public undo/redo.

Purpose: the PRD changes the user's mental model, so the panel interactions and workspace state cues must change with it.
Output: clickable version rows, explicit restore actions, a visible preview banner, and enforced read-only behavior during preview.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-history-refactor/45-CONTEXT.md
@packages/web/src/components/HistoryPanel.tsx
@packages/web/src/components/workspace/ProjectWorkspace.tsx
@packages/web/src/components/layout/Toolbar.tsx
@packages/web/src/hooks/useProjectHistory.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert the history panel into a version list with preview and restore actions</name>
  <files>packages/web/src/components/HistoryPanel.tsx</files>
  <read_first>
    packages/web/src/components/HistoryPanel.tsx
    packages/web/src/lib/apiTypes.ts
    packages/web/src/hooks/useProjectHistory.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "Показать эту версию|Восстановить эту версию|Текущая версия" packages/web/src/components/HistoryPanel.tsx` finds the new product actions/labels
    - `rg -n "onPreviewVersion|onRestoreVersion" packages/web/src/components/HistoryPanel.tsx` finds the new callbacks
    - `rg -n "Отменить|Повторить" packages/web/src/components/HistoryPanel.tsx` returns no matches
    - `rg -n "item\\.isCurrent|item\\.canRestore" packages/web/src/components/HistoryPanel.tsx` finds row behavior keyed to the new product contract
  </acceptance_criteria>
  <action>
    Refactor `packages/web/src/components/HistoryPanel.tsx` into a version timeline panel.

    Make each row support these exact behaviors:
    - clicking the row previews that version
    - the row shows when it is the current version
    - the row action menu contains:
      - `Показать эту версию`
      - `Восстановить эту версию`

    Replace the old per-row buttons:
    - remove `Отменить`
    - remove `Повторить`

    Use the new row contract fields:
    - `isCurrent`
    - `canRestore`

    Keep actor, title, timestamp, and version range visible, but make the row language describe versions rather than undo/redo status.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    The history panel now behaves like a version browser with explicit preview and restore actions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add preview-mode workspace UX and hard read-only gating</name>
  <files>packages/web/src/components/workspace/ProjectWorkspace.tsx, packages/web/src/components/layout/Toolbar.tsx</files>
  <read_first>
    packages/web/src/components/workspace/ProjectWorkspace.tsx
    packages/web/src/components/layout/Toolbar.tsx
    packages/web/src/hooks/useProjectHistory.ts
    packages/web/src/stores/useHistoryViewerStore.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "Просмотр версии|Вернуться к текущей версии" packages/web/src/components/workspace/ProjectWorkspace.tsx packages/web/src/components/layout/Toolbar.tsx` finds the preview banner and exit action
    - `rg -n "historyViewer\\.mode === 'preview'" packages/web/src/components/workspace/ProjectWorkspace.tsx` finds preview-mode gating
    - `rg -n "Ctrl\\+Z|Ctrl\\+Shift\\+Z|event\\.key\\.toLowerCase\\(\\) !== 'z'" packages/web/src/components/workspace/ProjectWorkspace.tsx` finds hotkey logic that can be preview-gated
    - `rg -n "effectiveReadOnly|disabled=.*preview" packages/web/src/components/workspace/ProjectWorkspace.tsx` finds edit lockout while previewing
  </acceptance_criteria>
  <action>
    Update `packages/web/src/components/workspace/ProjectWorkspace.tsx` and `packages/web/src/components/layout/Toolbar.tsx` so preview mode is explicit and strictly read-only.

    Add these exact behaviors:
    - show a clear preview indicator labeled `Просмотр версии`
    - provide an explicit action `Вернуться к текущей версии`
    - when the selected row is already current, exit preview mode instead of staying in a pseudo-preview state
    - disable task mutations while previewing
    - disable AI/chat mutations while previewing
    - disable command hotkeys while previewing
    - do not accumulate optimistic state while previewing

    Preserve existing read-only guards for share mode and preview-rendering failures, but compose preview-mode read-only on top of them.

    Keep the workspace shell consistent with the existing application; this is a behavioral refactor, not a full layout redesign.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    The workspace clearly communicates historical preview mode and blocks editing until the user returns to the current version or restores one.
  </done>
</task>

</tasks>

<verification>
Confirm that clicking a history row enters a visible read-only preview state, that the user can return to the live current version, and that edit actions are blocked throughout preview mode.
</verification>

<success_criteria>
The visible product experience is a version browser with explicit restore, not a public undo/redo control panel.
</success_criteria>

<output>
After completion, create `.planning/phases/45-history-refactor/45-04-SUMMARY.md`
</output>
