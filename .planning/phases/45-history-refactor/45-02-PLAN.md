---
phase: 45-history-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "45-01"
files_modified:
  - packages/server/src/routes/history-routes.ts
  - packages/server/src/routes/history-routes.test.ts
  - packages/server/src/index.ts
  - packages/web/src/lib/apiTypes.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The public history HTTP surface describes versions, snapshots, and restore actions rather than undo/redo mechanics"
    - "The web package consumes typed history API contracts that match the new product model"
    - "Internal append-only mechanics stay hidden behind product-oriented responses"
  artifacts:
    - path: "packages/server/src/routes/history-routes.ts"
      provides: "Product-oriented history list, snapshot, and restore endpoints"
      exports: ["registerHistoryRoutes"]
    - path: "packages/web/src/lib/apiTypes.ts"
      provides: "Web contracts for history timeline rows, snapshot preview, and restore responses"
      exports: ["HistoryItem", "HistoryListResponse", "HistorySnapshotResponse", "HistoryRestoreResponse"]
  key_links:
    - from: "packages/server/src/routes/history-routes.ts"
      to: "packages/mcp/src/services/history.service.ts"
      via: "history list, snapshot preview, and restore route handlers"
      pattern: "historyService"
    - from: "packages/web/src/lib/apiTypes.ts"
      to: "packages/server/src/routes/history-routes.ts"
      via: "typed response contract alignment"
      pattern: "/api/history"
---

<objective>
Replace the public undo/redo HTTP contract with the version-oriented product contract from the PRD.

Purpose: the frontend should ask for version rows, version snapshots, and version restores, not for rollback mechanics.
Output: new authenticated history endpoints plus synchronized web API types.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-history-refactor/45-CONTEXT.md
@.planning/reference/history-versioning-clean-architecture-prd.md
@packages/mcp/src/services/history.service.ts
@packages/server/src/routes/history-routes.ts
@packages/server/src/index.ts
@packages/web/src/lib/apiTypes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace public history routes with list, snapshot, and restore endpoints</name>
  <files>packages/server/src/routes/history-routes.ts, packages/server/src/routes/history-routes.test.ts, packages/server/src/index.ts</files>
  <read_first>
    packages/server/src/routes/history-routes.ts
    packages/server/src/index.ts
    packages/mcp/src/services/history.service.ts
    packages/server/src/routes/command-routes.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
    .planning/reference/history-versioning-clean-architecture-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "GET '/api/history'|fastify\\.get\\('/api/history'" packages/server/src/routes/history-routes.ts` finds the list route
    - `rg -n "/api/history/:groupId/snapshot" packages/server/src/routes/history-routes.ts` finds the snapshot route
    - `rg -n "/api/history/:groupId/restore" packages/server/src/routes/history-routes.ts` finds the restore route
    - `rg -n "/api/history/undo|/api/history/:groupId/undo|/api/history/:groupId/redo" packages/server/src/routes/history-routes.ts` returns no matches
    - `rg -n "isCurrent|canRestore|currentVersion|targetGroupId" packages/server/src/routes/history-routes.ts` finds the new response fields
    - `rg -n "/api/history/:groupId/snapshot|/api/history/:groupId/restore|isCurrent|canRestore" packages/server/src/routes/history-routes.test.ts` finds route-contract coverage
  </acceptance_criteria>
  <action>
    Replace the current public history surface in `packages/server/src/routes/history-routes.ts` with the PRD contract.

    Implement these exact endpoints:
    - `GET /api/history`
      - query params: `cursor?: string`, `limit?: number`
      - response: `{ items: Array<{ id, actorType, title, createdAt, baseVersion, newVersion, commandCount, isCurrent, canRestore }>; nextCursor?: string }`
    - `GET /api/history/:groupId/snapshot`
      - response: `{ groupId, isCurrent, currentVersion, snapshot }`
    - `POST /api/history/:groupId/restore`
      - response: `{ groupId, targetGroupId, version, snapshot }`

    For every route:
    - require `authMiddleware`
    - derive `projectId` from `req.user.projectId`
    - keep `version_conflict` mapped to HTTP 409
    - keep validation failures mapped to HTTP 400

    Remove the public route handlers for:
    - `POST /api/history/undo`
    - `POST /api/history/:groupId/undo`
    - `POST /api/history/:groupId/redo`

    Keep route registration in `packages/server/src/index.ts` unchanged except for consuming the new route surface.

    Update or rewrite `packages/server/src/routes/history-routes.test.ts` so it grep-locks:
    - registration in `index.ts`
    - the new endpoint paths
    - `authMiddleware`
    - response contract fields `isCurrent`, `canRestore`, `currentVersion`, and `targetGroupId`
    - absence of public undo/redo endpoints
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/routes/history-routes.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The server now exposes a product-oriented history API instead of public rollback mechanics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize web API contracts with the new version-oriented history surface</name>
  <files>packages/web/src/lib/apiTypes.ts</files>
  <read_first>
    packages/web/src/lib/apiTypes.ts
    packages/server/src/routes/history-routes.ts
    packages/mcp/src/types.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "isCurrent: boolean|canRestore: boolean" packages/web/src/lib/apiTypes.ts` finds the new history row fields
    - `rg -n "export interface HistorySnapshotResponse" packages/web/src/lib/apiTypes.ts` finds the snapshot response type
    - `rg -n "export interface HistoryRestoreResponse" packages/web/src/lib/apiTypes.ts` finds the restore response type
    - `rg -n "redoable|historyTitle|status: 'applied' \\| 'undone'" packages/web/src/lib/apiTypes.ts` returns no matches in the public history API types
  </acceptance_criteria>
  <action>
    Update `packages/web/src/lib/apiTypes.ts` so the web layer matches the new server contract exactly.

    Replace the public history types with:
    - `HistoryItem` containing `id`, `actorType`, `title`, `createdAt`, `baseVersion`, `newVersion`, `commandCount`, `isCurrent`, `canRestore`
    - `HistoryListResponse` containing `items` and optional `nextCursor`
    - `HistorySnapshotResponse` containing `groupId`, `isCurrent`, `currentVersion`, `snapshot`
    - `HistoryRestoreResponse` containing `groupId`, `targetGroupId`, `version`, `snapshot`

    Remove the old public undo/redo-oriented fields from the web API contract:
    - `status`
    - `undoable`
    - `redoable`
    - `historyTitle`

    Keep the payload shapes aligned with `packages/server/src/routes/history-routes.ts` and `packages/mcp/src/types.ts`.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    The web package consumes the product contract directly and no longer encodes public undo/redo semantics in its API types.
  </done>
</task>

</tasks>

<verification>
Confirm that a web client can list visible versions, fetch a selected version snapshot, and restore a selected version without any public undo/redo endpoint.
</verification>

<success_criteria>
The public server and web contracts describe versions, snapshots, and restore operations only.
</success_criteria>

<output>
After completion, create `.planning/phases/45-history-refactor/45-02-SUMMARY.md`
</output>
