---
phase: 45-history-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - "45-01"
  - "45-02"
files_modified:
  - packages/web/src/stores/useHistoryViewerStore.ts
  - packages/web/src/stores/useProjectStore.ts
  - packages/web/src/hooks/useProjectHistory.ts
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Historical preview state is isolated from current editing state instead of being merged into `confirmed`, `pending`, or `dragPreview`"
    - "The history hook fetches authoritative preview snapshots and restore responses from the backend"
    - "Visible snapshot selection gives priority to preview mode without corrupting the normal editing state machine"
  artifacts:
    - path: "packages/web/src/stores/useHistoryViewerStore.ts"
      provides: "Dedicated history viewer state machine"
      exports: ["useHistoryViewerStore"]
    - path: "packages/web/src/hooks/useProjectHistory.ts"
      provides: "Version list, snapshot preview, and restore orchestration"
      exports: ["useProjectHistory"]
    - path: "packages/web/src/stores/useProjectStore.ts"
      provides: "Current editing state without embedded historical preview"
      exports: ["deriveVisibleSnapshot", "useProjectStore"]
  key_links:
    - from: "packages/web/src/hooks/useProjectHistory.ts"
      to: "packages/web/src/stores/useHistoryViewerStore.ts"
      via: "preview enter/exit orchestration"
      pattern: "preview"
    - from: "packages/web/src/components/workspace/ProjectWorkspace.tsx"
      to: "packages/web/src/stores/useProjectStore.ts"
      via: "visible snapshot derivation that preserves current editing state"
      pattern: "deriveVisibleSnapshot"
---

<objective>
Introduce an isolated frontend history viewer mode and wire it to the new server-side version preview/restore API.

Purpose: the PRD explicitly forbids mixing historical preview into the main confirmed/pending/drag model.
Output: a dedicated viewer store, a history hook that talks to the new endpoints, and a workspace-visible snapshot selection path that respects preview mode.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-history-refactor/45-CONTEXT.md
@packages/web/src/lib/apiTypes.ts
@packages/web/src/hooks/useProjectHistory.ts
@packages/web/src/stores/useProjectStore.ts
@packages/web/src/components/workspace/ProjectWorkspace.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a dedicated `HistoryViewerState` store and keep project editing state clean</name>
  <files>packages/web/src/stores/useHistoryViewerStore.ts, packages/web/src/stores/useProjectStore.ts</files>
  <read_first>
    packages/web/src/stores/useProjectStore.ts
    packages/web/src/types.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
    .planning/reference/history-versioning-clean-architecture-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "mode: 'inactive'|mode: 'preview'" packages/web/src/stores/useHistoryViewerStore.ts` finds the state machine variants
    - `rg -n "groupId: string;|snapshot: ProjectSnapshot;|isCurrent: boolean;" packages/web/src/stores/useHistoryViewerStore.ts` finds the preview payload fields
    - `rg -n "historyViewer|preview" packages/web/src/stores/useProjectStore.ts` returns no matches
    - `rg -n "dragPreview" packages/web/src/stores/useProjectStore.ts` still finds current editing preview support
  </acceptance_criteria>
  <action>
    Create `packages/web/src/stores/useHistoryViewerStore.ts` as a separate Zustand store for historical preview only.

    Implement this exact state model:
    - `{ mode: 'inactive' }`
    - `{ mode: 'preview'; groupId: string; snapshot: ProjectSnapshot; isCurrent: boolean }`

    Add store actions that:
    - enter preview for a selected group snapshot
    - exit preview back to inactive mode
    - clear preview after a successful restore

    Keep `packages/web/src/stores/useProjectStore.ts` limited to current editing state:
    - `confirmed`
    - `pending`
    - `dragPreview`
    - schedule options

    Do not add historical preview fields to `useProjectStore`.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    Historical preview has its own state machine and is no longer conflated with project editing state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire `useProjectHistory` and workspace visible-snapshot selection for preview and restore</name>
  <files>packages/web/src/hooks/useProjectHistory.ts, packages/web/src/components/workspace/ProjectWorkspace.tsx, packages/web/src/stores/useProjectStore.ts</files>
  <read_first>
    packages/web/src/hooks/useProjectHistory.ts
    packages/web/src/components/workspace/ProjectWorkspace.tsx
    packages/web/src/stores/useProjectStore.ts
    packages/web/src/stores/useHistoryViewerStore.ts
    packages/web/src/lib/apiTypes.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "/api/history/.*/snapshot" packages/web/src/hooks/useProjectHistory.ts` finds server-side snapshot preview fetches
    - `rg -n "/api/history/.*/restore" packages/web/src/hooks/useProjectHistory.ts` finds restore calls
    - `rg -n "previewVersion|showVersion|restoreVersion|returnToCurrentVersion" packages/web/src/hooks/useProjectHistory.ts` finds product-oriented hook actions
    - `rg -n "historyViewer\\.mode === 'preview'" packages/web/src/components/workspace/ProjectWorkspace.tsx` finds preview-first visible snapshot selection
    - `rg -n "undoLatest|redoGroup|undoGroup" packages/web/src/hooks/useProjectHistory.ts packages/web/src/components/workspace/ProjectWorkspace.tsx` returns no matches
  </acceptance_criteria>
  <action>
    Refactor `packages/web/src/hooks/useProjectHistory.ts` so it becomes a version viewer/restore hook instead of an undo/redo hook.

    Implement these exact behaviors:
    - fetch timeline rows from `GET /api/history`
    - fetch selected version snapshots from `GET /api/history/:groupId/snapshot`
    - restore selected versions with `POST /api/history/:groupId/restore`
    - enter `HistoryViewerState.mode = 'preview'` when a non-current version snapshot is loaded
    - exit preview when the current version is selected or when the user explicitly returns to current
    - after restore, call `setConfirmed(version, snapshot)`, clear transient state, and clear preview mode

    In `packages/web/src/components/workspace/ProjectWorkspace.tsx`, make visible snapshot selection follow this exact order:
    1. if `historyViewer.mode === 'preview'`, render `historyViewer.snapshot`
    2. else if `dragPreview` exists, render drag preview
    3. else render the current editing snapshot derived from `confirmed` and `pending`

    Remove the old hook actions and workspace usage that were built around:
    - `undoLatest`
    - `undoGroup`
    - `redoGroup`

    Preserve current authoritative reconciliation through `setConfirmed(...)` and `clearTransientState()`.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/web</automated>
  </verify>
  <done>
    The frontend consumes server-driven version preview/restore while keeping historical preview outside the editing store.
  </done>
</task>

</tasks>

<verification>
Confirm that selecting a historical version shows a server-provided preview snapshot without mutating current editing state, and restoring a version reconciles from the authoritative server response.
</verification>

<success_criteria>
The web state model has a separate history viewer mode and no longer treats history preview as another flavor of optimistic project state.
</success_criteria>

<output>
After completion, create `.planning/phases/45-history-refactor/45-03-SUMMARY.md`
</output>
