---
phase: 45-history-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/services/project-command-apply.ts
  - packages/mcp/src/services/history.service.ts
  - packages/mcp/src/services/history.service.test.ts
  - packages/mcp/src/services/index.ts
  - packages/mcp/src/types.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Preview and restore use one shared rollback-tail resolution model instead of separate semantics"
    - "The backend can apply a typed `ProjectCommand` to a `ProjectSnapshot` without persistence or version bumps"
    - "History preview remains side-effect-free while restore continues to use the authoritative commit pipeline"
  artifacts:
    - path: "packages/mcp/src/services/project-command-apply.ts"
      provides: "Pure server-side typed command application for snapshot replay"
      exports: ["applyProjectCommandToSnapshot"]
    - path: "packages/mcp/src/services/history.service.ts"
      provides: "Shared history-tail resolution plus product-oriented snapshot and restore operations"
      exports: ["HistoryService", "historyService"]
    - path: "packages/mcp/src/types.ts"
      provides: "Shared history snapshot and restore contracts"
      exports: ["HistoryGroupSnapshotResponse", "RestoreHistoryGroupResponse"]
  key_links:
    - from: "packages/mcp/src/services/history.service.ts"
      to: "packages/mcp/src/services/project-command-apply.ts"
      via: "memory-only inverse-command application for historical preview"
      pattern: "applyProjectCommandToSnapshot"
    - from: "packages/mcp/src/services/history.service.ts"
      to: "packages/mcp/src/services/command.service.ts"
      via: "persisted restore through authoritative command commits"
      pattern: "commitCommand"
---

<objective>
Refactor the history domain so version preview and version restore share one authoritative backend model.

Purpose: the PRD rejects split-brain semantics where restore is server-side but preview is a separate client replay path.
Output: a shared rollback-tail model, a pure command-application helper, and `HistoryService` methods for version snapshot preview and version restore.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-history-refactor/45-CONTEXT.md
@.planning/reference/history-versioning-clean-architecture-prd.md
@.planning/phases/44-undo-redo/44-CONTEXT.md
@packages/mcp/src/services/history.service.ts
@packages/mcp/src/services/command.service.ts
@packages/mcp/src/types.ts
@packages/mcp/prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure server-side command application for snapshot replay</name>
  <files>packages/mcp/src/services/project-command-apply.ts, packages/mcp/src/services/index.ts, packages/mcp/src/types.ts</files>
  <read_first>
    packages/mcp/src/services/command.service.ts
    packages/mcp/src/types.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
    .planning/reference/history-versioning-clean-architecture-prd.md
    .planning/phases/36-unified-scheduling-core/36-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export function applyProjectCommandToSnapshot|export const applyProjectCommandToSnapshot" packages/mcp/src/services/project-command-apply.ts` finds the shared pure helper
    - `rg -n "HistoryGroupSnapshotResponse|RestoreHistoryGroupResponse" packages/mcp/src/types.ts` finds the new shared response contracts
    - `rg -n "project-command-apply" packages/mcp/src/services/index.ts` finds the service export
    - `rg -n "commitCommand" packages/mcp/src/services/project-command-apply.ts` returns no matches
  </acceptance_criteria>
  <action>
    Create `packages/mcp/src/services/project-command-apply.ts` and make it the shared non-persist execution layer for history replay.

    Implement one exported helper with this exact intent:
    - `applyProjectCommandToSnapshot(snapshot, command, options): ScheduleExecutionResult`

    The helper must:
    - accept a `ProjectSnapshot`
    - accept one typed `ProjectCommand`
    - return the same shape the command pipeline uses for execution results: `snapshot`, `changedTaskIds`, `changedDependencyIds`, `conflicts`, `patches`
    - not read or write Prisma
    - not call `commitCommand`
    - not bump `Project.version`

    In `packages/mcp/src/types.ts`, add shared response contracts for the new history product surface:
    - `HistoryGroupSnapshotResponse = { groupId: string; isCurrent: boolean; currentVersion: number; snapshot: ProjectSnapshot; }`
    - `RestoreHistoryGroupResponse = { groupId: string; targetGroupId: string; version: number; snapshot: ProjectSnapshot; }`

    Export the new helper from `packages/mcp/src/services/index.ts`.

    Reuse the same scheduling/command semantics already trusted by the authoritative path. Do not add any frontend-only replay logic or a second divergent implementation.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm run build -w packages/mcp</automated>
  </verify>
  <done>
    The backend has one reusable pure command-application path that can power history preview without persistence.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor `HistoryService` into version-list, snapshot-preview, and restore operations</name>
  <files>packages/mcp/src/services/history.service.ts, packages/mcp/src/services/history.service.test.ts, packages/mcp/src/types.ts</files>
  <read_first>
    packages/mcp/src/services/history.service.ts
    packages/mcp/src/services/project-command-apply.ts
    packages/mcp/src/services/command.service.ts
    packages/mcp/src/types.ts
    .planning/phases/45-history-refactor/45-CONTEXT.md
    .planning/reference/history-versioning-clean-architecture-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "listHistoryGroups|getHistorySnapshot|restoreToGroup" packages/mcp/src/services/history.service.ts` finds the three product-oriented operations
    - `rg -n "applyProjectCommandToSnapshot" packages/mcp/src/services/history.service.ts` finds preview replay using the shared pure helper
    - `rg -n "origin: 'undo'|title: 'Undo — '|title: 'Redo — '" packages/mcp/src/services/history.service.ts` returns no matches in the public preview/restore path
    - `rg -n "isCurrent|canRestore" packages/mcp/src/services/history.service.ts` finds the visible history row semantics
    - `rg -n "getHistorySnapshot|restoreToGroup|memory-only|current version|active tail" packages/mcp/src/services/history.service.test.ts` finds coverage for preview/restore parity and side-effect-free preview
  </acceptance_criteria>
  <action>
    Refactor `packages/mcp/src/services/history.service.ts` so the public history domain is version-oriented instead of undo/redo-oriented.

    Implement these exact exported methods:
    - `listHistoryGroups({ projectId, cursor, limit })`
    - `getHistorySnapshot({ projectId, groupId })`
    - `restoreToGroup({ projectId, groupId, actorType, actorId, requestContextId })`

    Implement one shared internal path used by both `getHistorySnapshot` and `restoreToGroup`:
    1. Load the visible linear history for the project.
    2. Resolve the selected visible target group.
    3. Identify the active tail after that target.
    4. Validate that every tail event has a replayable `inverseCommand`.
    5. Build the inverse-command sequence in descending `ordinal`.

    Apply that shared tail in two modes:
    - `getHistorySnapshot`:
      - load the current authoritative snapshot
      - apply the inverse-command sequence in memory only through `applyProjectCommandToSnapshot`
      - return `{ groupId, isCurrent, currentVersion, snapshot }`
      - never create `MutationGroup`
      - never create `ProjectEvent`
      - never write to Prisma
    - `restoreToGroup`:
      - replay the same inverse-command sequence through `commandService.commitCommand(...)`
      - create the technical append-only rollback group needed by the current foundation
      - return `{ groupId, targetGroupId, version, snapshot }`

    Update `listHistoryGroups` to return user-visible version rows with:
    - `id`
    - `actorType`
    - `title`
    - `createdAt`
    - `baseVersion`
    - `newVersion`
    - `commandCount`
    - `isCurrent`
    - `canRestore`

    Keep internal append-only rollback mechanics if they are still needed, but do not make undo/redo status the public product contract of these methods.

    Add or rewrite `packages/mcp/src/services/history.service.test.ts` so it proves:
    - previewing the current row returns `isCurrent: true` and the current snapshot unchanged
    - previewing an older row applies the active tail in reverse ordinal order without Prisma writes
    - restoring the same older row uses `commandService.commitCommand(...)` and returns the authoritative new snapshot/version
    - preview and restore resolve the same tail and therefore land on the same target snapshot
    - a missing `inverseCommand` in the active tail fails with a typed validation error instead of partial preview or partial restore
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/mcp/src/services/history.service.test.ts && npm run build -w packages/mcp</automated>
  </verify>
  <done>
    The backend exposes one version-oriented history domain where preview is side-effect-free and restore is authoritative.
  </done>
</task>

</tasks>

<verification>
Confirm that history preview and restore share the same rollback-tail semantics, but only restore mutates persistent history.
</verification>

<success_criteria>
The server can compute an authoritative historical snapshot without side effects, and can restore that same version through the existing append-only command pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/45-history-refactor/45-01-SUMMARY.md`
</output>
