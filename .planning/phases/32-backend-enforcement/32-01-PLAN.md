---
phase: 32-backend-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/middleware/subscription-middleware.ts
  - packages/server/src/middleware/constraint-middleware.ts
  - packages/server/src/routes/auth-routes.ts
  - packages/server/src/routes/command-routes.ts
  - packages/server/src/index.ts
autonomous: true
requirements:
  - ENF-01
  - ENF-03
must_haves:
  truths:
    - "Protected HTTP mutation endpoints deny over-limit or expired-plan requests before any project or command mutation runs."
    - "The denial payload for HTTP enforcement includes structured limit information: `remaining`, `plan`, `planLabel`, and `upgradeHint`."
    - "Project-count enforcement blocks only actions that increase active project usage, while archive/delete/read flows stay available."
  artifacts:
    - path: "packages/server/src/middleware/constraint-middleware.ts"
      provides: "Reusable HTTP guard helpers and structured denial payload builder for plan-limit enforcement"
    - path: "packages/server/src/routes/auth-routes.ts"
      provides: "Project create and restore routes protected by `projects` limit enforcement"
    - path: "packages/server/src/routes/command-routes.ts"
      provides: "Authoritative command commit route protected by read-only enforcement for expired subscriptions"
    - path: "packages/server/src/index.ts"
      provides: "Chat route wired through explicit AI query enforcement and post-check usage increment"
  key_links:
    - from: "packages/server/src/routes/auth-routes.ts"
      to: "packages/server/src/middleware/constraint-middleware.ts"
      via: "preHandler chain on `/api/projects` and `/api/projects/:id/restore`"
      pattern: "/api/projects|/api/projects/:id/restore|preHandler"
    - from: "packages/server/src/routes/command-routes.ts"
      to: "packages/server/src/middleware/constraint-middleware.ts"
      via: "preHandler chain on `/api/commands/commit` before `commandService.commitCommand()`"
      pattern: "/api/commands/commit|commitCommand"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/middleware/subscription-middleware.ts"
      via: "`/api/chat` preHandler and `incrementAiUsage()` call order"
      pattern: "/api/chat|incrementAiUsage"
---

<objective>
Close the server-side enforcement gap left after Phase 31 by protecting every currently exposed HTTP mutation surface that can exceed a tariff rule or bypass read-only subscription semantics.

Purpose: Phase 31 made usage authoritative; this plan applies that authority to the API surfaces that still accept writes with only `authMiddleware`.
Output: Reusable HTTP guard helpers, guarded project mutation routes, guarded command commit route, and structured denial payloads with normalized limit metadata.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-billing/28-CONTEXT.md
@.planning/phases/30-constraint-engine/30-CONTEXT.md
@.planning/phases/30-constraint-engine/30-03-SUMMARY.md
@.planning/phases/31-usage-tracking/31-01-SUMMARY.md
@.planning/phases/31-usage-tracking/31-02-SUMMARY.md
@packages/server/src/index.ts
@packages/server/src/middleware/subscription-middleware.ts
@packages/server/src/routes/auth-routes.ts
@packages/server/src/routes/command-routes.ts
@packages/server/src/routes/billing-routes.ts
@packages/server/src/services/billing-service.ts
@packages/server/src/services/constraint-service.ts

<interfaces>
From packages/server/src/index.ts:
```ts
fastify.post('/api/chat', { preHandler: [authMiddleware, subscriptionMiddleware] }, async (req, reply) => {
  await incrementAiUsage(req.user!.userId);
  runAgentWithHistory(message, req.user!.projectId, req.user!.sessionId)
});
```

From packages/server/src/routes/auth-routes.ts:
```ts
fastify.post('/api/projects', { preHandler: [authMiddleware] }, async (req, reply) => { ... });
fastify.post<{ Params: { id: string } }>('/api/projects/:id/restore', { preHandler: [authMiddleware] }, async (req, reply) => { ... });
fastify.post<{ Params: { id: string } }>('/api/projects/:id/archive', { preHandler: [authMiddleware] }, async (req, reply) => { ... });
```

From packages/server/src/routes/command-routes.ts:
```ts
fastify.post('/api/commands/commit', { preHandler: [authMiddleware] }, async (req, reply) => {
  const response = await commandService.commitCommand(request, actorType, actorId);
});
```

From packages/server/src/services/constraint-service.ts:
```ts
export interface ConstraintCheckResult {
  allowed: boolean;
  reasonCode: ConstraintReasonCode;
  planId: PlanId;
  limitKey: LimitKey;
  usage: ConstraintUsageSnapshot;
  remaining: ConstraintRemainingSnapshot;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable HTTP enforcement helpers and a structured denial payload</name>
  <read_first>
    - packages/server/src/middleware/subscription-middleware.ts
    - packages/server/src/services/constraint-service.ts
    - packages/server/src/services/billing-service.ts
    - packages/server/src/routes/billing-routes.ts
    - .planning/phases/28-billing/28-CONTEXT.md
    - .planning/phases/30-constraint-engine/30-CONTEXT.md
  </read_first>
  <files>packages/server/src/middleware/constraint-middleware.ts, packages/server/src/middleware/subscription-middleware.ts, packages/server/src/services/billing-service.ts</files>
  <behavior>
    - Test 1: An exhausted tracked limit returns a `403` payload with exact keys `code`, `limitKey`, `reasonCode`, `remaining`, `plan`, `planLabel`, and `upgradeHint`.
    - Test 2: An expired paid subscription returns a `403` payload with exact `code: 'SUBSCRIPTION_EXPIRED'` and does not require a fake tracked `remaining` counter.
    - Test 3: `/api/chat` continues to increment AI usage only after the AI limit guard passes.
  </behavior>
  <action>
    Create `packages/server/src/middleware/constraint-middleware.ts` and move the ad-hoc response logic out of `subscription-middleware.ts` into named helpers that can be reused by `/api/chat`, `/api/projects`, `/api/projects/:id/restore`, and `/api/commands/commit`. Add concrete helpers equivalent to `requireActiveSubscriptionForMutation(request, reply)` and `requireTrackedLimit(limitKey, { code, upgradeHint })`. The denial payload must expose exact keys `code`, `limitKey`, `reasonCode`, `remaining`, `plan`, `planLabel`, and `upgradeHint`; when usage is tracked, also include `used` and `limit`. Use `ConstraintService.checkLimit(userId, 'ai_queries' | 'projects')` and `BillingService.getSubscriptionStatus(userId)` as the only read sources. Refactor `subscription-middleware.ts` so it becomes a thin chat-specific composition over the new helpers rather than its own bespoke payload builder.
  </action>
  <acceptance_criteria>
    - `packages/server/src/middleware/constraint-middleware.ts` exists.
    - `packages/server/src/middleware/constraint-middleware.ts` contains the exact strings `upgradeHint`, `planLabel`, and `remaining`.
    - `packages/server/src/middleware/subscription-middleware.ts` contains `ai_queries` and no longer hard-codes the exact strings `AI_LIMIT_REACHED` and `SUBSCRIPTION_EXPIRED` inline in separate reply bodies.
    - `packages/server/src/middleware/constraint-middleware.ts` uses `ConstraintService.checkLimit(` and `BillingService.getSubscriptionStatus(`.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/server/tsconfig.json</automated>
  </verify>
  <done>Server enforcement logic is centralized in reusable middleware helpers, and every denied HTTP mutation can return the same structured limit payload instead of route-specific ad-hoc JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard project creation and restore without blocking archive or delete flows</name>
  <read_first>
    - packages/server/src/routes/auth-routes.ts
    - packages/server/src/middleware/constraint-middleware.ts
    - packages/server/src/services/constraint-service.ts
    - packages/mcp/src/services/auth.service.ts
    - packages/mcp/src/services/project.service.ts
    - .planning/phases/31-usage-tracking/31-VERIFICATION.md
  </read_first>
  <files>packages/server/src/routes/auth-routes.ts, packages/server/src/middleware/constraint-middleware.ts</files>
  <behavior>
    - Test 1: `POST /api/projects` rejects before `authService.createProject()` when `projects` remaining is `0`.
    - Test 2: `POST /api/projects/:id/restore` rejects before `authService.restoreProject()` when restoring would exceed the active-project limit.
    - Test 3: `POST /api/projects/:id/archive` and `DELETE /api/projects/:id` remain available because they reduce or preserve active-project usage rather than increase it.
  </behavior>
  <action>
    Update `packages/server/src/routes/auth-routes.ts` so `POST /api/projects` and `POST /api/projects/:id/restore` use the new `projects` guard in their `preHandler` chain, ahead of the handler body that calls `authService.createProject()` or `authService.restoreProject()`. Keep `GET /api/projects`, `PATCH /api/projects/:id`, `POST /api/projects/:id/archive`, and `DELETE /api/projects/:id` off the `projects` guard in this phase; archive/delete are explicitly not project-limit denials, and archive feature gating belongs to Phase 34. Do not touch the OTP bootstrap path that creates the user's first default project after `verify-otp`; this plan is a delta on public authenticated routes, not a redesign of sign-up bootstrap.
  </action>
  <acceptance_criteria>
    - `packages/server/src/routes/auth-routes.ts` contains `/api/projects', { preHandler: [authMiddleware` and a `projects` guard in the same route definition.
    - `packages/server/src/routes/auth-routes.ts` contains `/api/projects/:id/restore` and a `projects` guard in the same route definition.
    - `packages/server/src/routes/auth-routes.ts` does not add a `projects` guard to `/api/projects/:id/archive`.
    - `packages/server/src/routes/auth-routes.ts` still calls `authService.createProject(` and `authService.restoreProject(` only inside handlers, not inside guard code.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/server/tsconfig.json</automated>
  </verify>
  <done>Project create and restore endpoints can no longer increase active project usage beyond the tariff limit, while archive/delete/read routes remain intentionally accessible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce read-only and AI-query limits on chat and command commit routes</name>
  <read_first>
    - packages/server/src/index.ts
    - packages/server/src/routes/command-routes.ts
    - packages/server/src/middleware/subscription-middleware.ts
    - packages/server/src/middleware/constraint-middleware.ts
    - .planning/phases/28-billing/28-CONTEXT.md
  </read_first>
  <files>packages/server/src/index.ts, packages/server/src/routes/command-routes.ts, packages/server/src/middleware/subscription-middleware.ts, packages/server/src/middleware/constraint-middleware.ts</files>
  <behavior>
    - Test 1: `POST /api/chat` still denies exhausted `ai_queries` before `incrementAiUsage()` runs.
    - Test 2: `POST /api/commands/commit` returns the structured `SUBSCRIPTION_EXPIRED` payload for expired non-free plans before `commandService.commitCommand()` runs.
    - Test 3: The command route keeps its existing `400/409/500` mapping for command-service responses after the new enforcement preHandler passes.
  </behavior>
  <action>
    Keep `/api/chat` as the only HTTP route that consumes `ai_queries`, but refactor its preHandler chain so the chat path clearly composes `authMiddleware`, `requireActiveSubscriptionForMutation`, and `requireTrackedLimit('ai_queries', { code: 'AI_LIMIT_REACHED', upgradeHint: '...' })`, with `incrementAiUsage(req.user!.userId)` still executed only after those checks pass. In `packages/server/src/routes/command-routes.ts`, add the read-only subscription guard to `POST /api/commands/commit` so expired paid plans cannot mutate tasks through the authoritative command path. Do not add project-count checks to `/api/commands/commit`; Phase 32 only needs read-only mutation blocking there, because command commits mutate the current project rather than creating a new active project.
  </action>
  <acceptance_criteria>
    - `packages/server/src/index.ts` still contains `incrementAiUsage(req.user!.userId)` after the `/api/chat` preHandler declaration.
    - `packages/server/src/index.ts` contains `/api/chat` and references both the active-subscription guard and the `ai_queries` guard path.
    - `packages/server/src/routes/command-routes.ts` contains `/api/commands/commit` and a new enforcement preHandler in addition to `authMiddleware`.
    - `packages/server/src/routes/command-routes.ts` still contains `commandService.commitCommand(request, actorType, actorId)`.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/server/tsconfig.json</automated>
  </verify>
  <done>Chat and authoritative command mutations both enforce tariff rules before executing business logic, and denials use the same structured metadata contract as project-create denials.</done>
</task>

</tasks>

<verification>
Compile the server package and inspect the guarded route declarations. Confirm that `/api/chat`, `/api/projects`, `/api/projects/:id/restore`, and `/api/commands/commit` deny before mutation execution and that every denial payload includes `remaining`, `plan`, `planLabel`, and `upgradeHint`.
</verification>

<success_criteria>
ENF-01 and the HTTP half of ENF-03 are complete when the repo contains reusable guard helpers, when the protected mutation routes are guarded before handler execution, and when denied requests emit a structured payload with exact limit metadata rather than route-specific strings.
</success_criteria>

<output>
After completion, create `.planning/phases/32-backend-enforcement/32-01-SUMMARY.md`
</output>
