---
phase: 32-backend-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - packages/mcp/src/types.ts
  - packages/mcp/src/index.ts
  - packages/mcp/src/index.test.ts
  - packages/mcp/src/services/enforcement.service.ts
  - packages/mcp/src/services/index.ts
autonomous: true
requirements:
  - ENF-02
  - ENF-03
must_haves:
  truths:
    - "Mutating public MCP tools reject expired-plan write attempts before any command commit or task mutation runs."
    - "Read-only MCP tools remain available even when mutation tools are denied."
    - "Rejected MCP mutation payloads include structured limit metadata matching the Phase 32 server denial vocabulary."
  artifacts:
    - path: "packages/mcp/src/services/enforcement.service.ts"
      provides: "MCP-side mutation guard that resolves project ownership and subscription status before tool execution"
    - path: "packages/mcp/src/types.ts"
      provides: "Normalized MCP rejection contract extended for tariff-limit denials"
    - path: "packages/mcp/src/index.ts"
      provides: "Tool dispatch path that blocks mutating tools before `commitNormalizedCommand()` or task-service writes"
    - path: "packages/mcp/src/index.test.ts"
      provides: "Regression coverage for denied mutation tools and still-allowed read tools"
  key_links:
    - from: "packages/mcp/src/index.ts"
      to: "packages/mcp/src/services/enforcement.service.ts"
      via: "mutation-tool guard before normalized command dispatch"
      pattern: "handleCallToolRequest|enforcement"
    - from: "packages/mcp/src/services/enforcement.service.ts"
      to: "packages/mcp/src/types.ts"
      via: "typed denial payload returned inside normalized mutation results"
      pattern: "limit_reached|upgradeHint|remaining"
    - from: "packages/mcp/src/index.test.ts"
      to: "packages/mcp/src/index.ts"
      via: "rejection and pass-through coverage for tool dispatch"
      pattern: "create_tasks|get_project_summary|limit_reached"
---

<objective>
Apply tariff enforcement to the MCP mutation surface so agent-driven task changes cannot bypass Phase 32 rules after the HTTP API closes its own gaps.

Purpose: Server chat enforcement is insufficient if the MCP runtime still accepts unrestricted mutation tools once an agent is running; this plan closes that bypass.
Output: MCP enforcement service, normalized limit-reached rejection shape, guarded mutation-tool dispatch, and regression tests proving read/write separation.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-constraint-engine/30-03-SUMMARY.md
@.planning/phases/31-usage-tracking/31-02-SUMMARY.md
@.planning/phases/32-backend-enforcement/32-01-PLAN.md
@packages/mcp/src/index.ts
@packages/mcp/src/index.test.ts
@packages/mcp/src/public-tools.ts
@packages/mcp/src/services/project.service.ts
@packages/mcp/src/services/auth.service.ts
@packages/server/src/services/billing-service.ts
@packages/server/src/services/constraint-service.ts

<interfaces>
From packages/mcp/src/public-tools.ts:
```ts
export const PUBLIC_MCP_TOOLS = [ { name: 'create_tasks' }, { name: 'update_tasks' }, { name: 'move_tasks' }, { name: 'delete_tasks' }, { name: 'link_tasks' }, { name: 'unlink_tasks' }, { name: 'shift_tasks' }, { name: 'recalculate_project' } ];
```

From packages/mcp/src/types.ts:
```ts
export type NormalizedMutationReason =
  | 'version_conflict'
  | 'validation_error'
  | 'conflict'
  | 'not_found'
  | 'invalid_request'
  | 'unsupported_operation';
```

From packages/mcp/src/index.ts:
```ts
export async function handleCallToolRequest(
  request: { params: { name: string; arguments?: unknown } },
  deps: MpcHandlerDeps = defaultMcpHandlerDeps,
) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the normalized MCP rejection contract for tariff denials</name>
  <read_first>
    - packages/mcp/src/types.ts
    - packages/mcp/src/index.ts
    - packages/server/src/middleware/constraint-middleware.ts
    - packages/server/src/services/constraint-service.ts
    - packages/mcp/src/public-tools.ts
  </read_first>
  <files>packages/mcp/src/types.ts, packages/mcp/src/services/enforcement.service.ts, packages/mcp/src/services/index.ts</files>
  <behavior>
    - Test 1: A denied MCP mutation can return `reason: 'limit_reached'` without overloading existing `validation_error` or `unsupported_operation`.
    - Test 2: The typed denial payload includes exact keys `code`, `limitKey`, `remaining`, `plan`, `planLabel`, and `upgradeHint`.
    - Test 3: The enforcement service resolves `projectId` to the owning `userId` before evaluating subscription status for a mutating tool.
  </behavior>
  <action>
    Extend `packages/mcp/src/types.ts` so `NormalizedMutationReason` includes the exact literal `limit_reached`, and add a typed denial metadata field such as `enforcement` or `constraint` on `NormalizedMutationResult` with exact keys `code`, `limitKey`, `remaining`, `plan`, `planLabel`, and `upgradeHint`. Create `packages/mcp/src/services/enforcement.service.ts` that accepts `{ toolName, projectId }`, loads the owning project via Prisma, resolves `userId`, reads the subscription row, and returns an allow/deny decision for mutating tools. For Phase 32, concrete denial logic is: mutating tools are denied when the subscription is expired and `plan !== 'free'`; read-only tools remain allowed. Mirror the server vocabulary from Plan 32-01 instead of inventing alternate field names.
  </action>
  <acceptance_criteria>
    - `packages/mcp/src/types.ts` contains the exact string `limit_reached`.
    - `packages/mcp/src/types.ts` contains the exact strings `upgradeHint`, `planLabel`, and `remaining`.
    - `packages/mcp/src/services/enforcement.service.ts` exists.
    - `packages/mcp/src/services/enforcement.service.ts` contains both `projectId` and `userId`.
    - `packages/mcp/src/services/index.ts` exports the new enforcement service.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/mcp/tsconfig.json</automated>
  </verify>
  <done>The MCP package has a first-class typed representation for tariff-limit denials and a dedicated service that can decide whether a tool call is allowed before mutation dispatch begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard mutating MCP tools and prove read tools still pass through</name>
  <read_first>
    - packages/mcp/src/index.ts
    - packages/mcp/src/index.test.ts
    - packages/mcp/src/public-tools.ts
    - packages/mcp/src/services/enforcement.service.ts
    - .planning/phases/32-backend-enforcement/32-01-PLAN.md
  </read_first>
  <files>packages/mcp/src/index.ts, packages/mcp/src/index.test.ts, packages/mcp/src/services/enforcement.service.ts</files>
  <behavior>
    - Test 1: `create_tasks`, `update_tasks`, `move_tasks`, `delete_tasks`, `link_tasks`, `unlink_tasks`, `shift_tasks`, and `recalculate_project` return a rejected normalized payload with `reason: 'limit_reached'` when enforcement denies.
    - Test 2: `ping`, `get_project_summary`, `get_task_context`, `get_schedule_slice`, `validate_schedule`, and `get_conversation_history` still bypass the mutation guard.
    - Test 3: Denied mutation tools return before `commitNormalizedCommand()` executes.
  </behavior>
  <action>
    Update `packages/mcp/src/index.ts` so `handleCallToolRequest()` classifies the exact public mutating tools `create_tasks`, `update_tasks`, `move_tasks`, `delete_tasks`, `link_tasks`, `unlink_tasks`, `shift_tasks`, and `recalculate_project`, and asks `enforcement.service.ts` for a decision before reaching any command commit or task-service mutation branch. When denied, return the normal JSON wrapper with `status: 'rejected'`, `reason: 'limit_reached'`, empty changed arrays, and the typed enforcement payload added in Task 1. Do not apply the guard to `ping`, `get_project_summary`, `get_task_context`, `get_schedule_slice`, `validate_schedule`, `get_conversation_history`, or `add_message` in this phase. Extend `packages/mcp/src/index.test.ts` with one denied mutation case asserting the exact structured fields and one allowed read-tool case asserting no enforcement blockage.
  </action>
  <acceptance_criteria>
    - `packages/mcp/src/index.ts` contains the exact strings `create_tasks`, `update_tasks`, `move_tasks`, `delete_tasks`, `link_tasks`, `unlink_tasks`, `shift_tasks`, and `recalculate_project` in the mutation-guard path.
    - `packages/mcp/src/index.ts` returns `reason: 'limit_reached'` from the guarded rejection path.
    - `packages/mcp/src/index.test.ts` contains at least one assertion for `reason: 'limit_reached'`.
    - `packages/mcp/src/index.test.ts` contains an assertion that a read tool such as `get_project_summary` still succeeds when the guard is configured.
    - `packages/mcp/src/index.ts` does not call `commitNormalizedCommand` when the enforcement decision is denied.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/mcp/tsconfig.json && node --test packages/mcp/src/index.test.ts packages/mcp/src/public-tools.test.ts</automated>
  </verify>
  <done>Mutating MCP tools can no longer bypass tariff enforcement, denied calls emit a structured limit payload, and the read-only tool surface remains usable for diagnostics and context loading.</done>
</task>

</tasks>

<verification>
Compile the MCP package and run the focused MCP tests. Confirm that denied mutation tools return `reason: 'limit_reached'` with structured limit metadata and that read-only tools still resolve normally.
</verification>

<success_criteria>
ENF-02 and the MCP half of ENF-03 are complete when mutating public MCP tools are guarded before execution, when rejected results carry structured limit metadata, and when read-only tools stay available for agent context gathering.
</success_criteria>

<output>
After completion, create `.planning/phases/32-backend-enforcement/32-02-SUMMARY.md`
</output>
