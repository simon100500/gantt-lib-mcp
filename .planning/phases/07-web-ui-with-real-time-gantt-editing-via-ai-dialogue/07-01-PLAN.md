---
phase: 07-web-ui-with-real-time-gantt-editing-via-ai-dialogue
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/mcp/package.json
  - packages/mcp/tsconfig.json
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/web/package.json
  - packages/web/tsconfig.json
  - packages/web/vite.config.ts
  - packages/web/index.html
  - tsconfig.json
  - .gitignore
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "npm install at root succeeds and resolves all workspace packages"
    - "packages/mcp, packages/server, packages/web directories exist with valid package.json files"
    - "npm run build:mcp compiles packages/mcp TypeScript without errors"
    - "npm run dev:web launches Vite dev server on port 5173"
    - "npm run dev:server starts the server package (even if stub) on port 3000"
  artifacts:
    - path: "package.json"
      provides: "Root workspace config with npm workspaces"
      contains: "workspaces"
    - path: "packages/mcp/package.json"
      provides: "MCP package definition"
      exports: ["name: @gantt/mcp"]
    - path: "packages/server/package.json"
      provides: "Server package definition with fastify, @libsql/client"
      exports: ["name: @gantt/server"]
    - path: "packages/web/package.json"
      provides: "Web package definition with React, Vite"
      exports: ["name: @gantt/web"]
    - path: "packages/web/vite.config.ts"
      provides: "Vite config with proxy /api and /ws to :3000"
  key_links:
    - from: "packages/web/vite.config.ts"
      to: "packages/server (port 3000)"
      via: "proxy config"
      pattern: "proxy.*3000"
    - from: "packages/mcp/package.json"
      to: "root node_modules/@modelcontextprotocol/sdk"
      via: "npm workspaces hoisting"
---

<objective>
Convert the existing single-package project into an npm workspaces monorepo with three packages: `@gantt/mcp`, `@gantt/server`, and `@gantt/web`.

Purpose: Establish the structural foundation for the web UI phase. All subsequent plans build on top of this scaffold.

Output:
- Root package.json with `"workspaces": ["packages/*"]`
- packages/mcp/ — TypeScript package containing the moved MCP server source
- packages/server/ — TypeScript package stub with Fastify + @libsql/client deps declared
- packages/web/ — React + Vite package stub with proxy config pointing to :3000
- All three packages compile/start without errors
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current project structure being migrated. -->
<!-- Existing files in src/ and agent/ will move into packages/mcp/ -->

Current root package.json:
```json
{
  "name": "gantt-lib-mcp-server",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@qwen-code/sdk": "^0.1.5",
    "dotenv": "^17.3.1"
  }
}
```

Current src/ files to move into packages/mcp/src/:
- src/index.ts     — MCP server entry point (stdio transport)
- src/store.ts     — In-memory TaskStore
- src/types.ts     — Task, TaskDependency, etc.
- src/scheduler.ts — Date recalculation engine
- src/config.ts    — Env/autosave config

agent/ files to move into packages/mcp/agent/:
- agent/agent.ts        — @qwen-code/sdk agent runner
- agent/prompts/system.md
- agent/tsconfig.json

Key constraint: packages/mcp MUST keep backward compatibility:
- `npm run build:mcp` must produce dist/index.js for MCP stdio use
- The MCP server is NOT replaced in this plan — only relocated
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root and move MCP sources</name>
  <files>
    package.json
    tsconfig.json
    packages/mcp/package.json
    packages/mcp/tsconfig.json
    packages/mcp/src/index.ts
    packages/mcp/src/store.ts
    packages/mcp/src/types.ts
    packages/mcp/src/scheduler.ts
    packages/mcp/src/config.ts
    packages/mcp/src/scheduler.test.ts
    packages/mcp/agent/agent.ts
    packages/mcp/agent/agent.test.js
    packages/mcp/agent/tsconfig.json
    packages/mcp/agent/prompts/system.md
  </files>
  <action>
    1. Update root package.json:
       - Keep name "gantt-lib-mcp-server" for backward compat
       - Add `"workspaces": ["packages/*"]`
       - Remove src-level dependencies (they move to packages)
       - Add root-level dev scripts:
         ```json
         "scripts": {
           "build": "npm run build --workspaces",
           "build:mcp": "npm run build -w packages/mcp",
           "build:server": "npm run build -w packages/server",
           "dev:server": "npm run dev -w packages/server",
           "dev:web": "npm run dev -w packages/web"
         }
         ```
       - Root devDependencies: typescript, @types/node only

    2. Create packages/mcp/ directory structure:
       - packages/mcp/package.json:
         ```json
         {
           "name": "@gantt/mcp",
           "version": "0.1.0",
           "type": "module",
           "main": "dist/index.js",
           "scripts": {
             "build": "tsc -p tsconfig.json",
             "start": "node dist/index.js",
             "build:agent": "tsc -p agent/tsconfig.json",
             "test": "node --test src/scheduler.test.ts"
           },
           "dependencies": {
             "@modelcontextprotocol/sdk": "^1.0.4",
             "@qwen-code/sdk": "^0.1.5",
             "dotenv": "^17.3.1"
           },
           "devDependencies": {
             "@types/node": "^22.10.2",
             "typescript": "^5.7.2"
           }
         }
         ```
       - packages/mcp/tsconfig.json: copy existing root tsconfig.json, adjust rootDir/outDir
         ```json
         {
           "compilerOptions": {
             "target": "ES2022",
             "module": "nodenext",
             "moduleResolution": "nodenext",
             "rootDir": "src",
             "outDir": "dist",
             "strict": true,
             "esModuleInterop": true
           },
           "include": ["src/**/*.ts"],
           "exclude": ["node_modules", "dist"]
         }
         ```

    3. Copy (do not delete yet) src/*.ts files to packages/mcp/src/:
       - Copy src/index.ts → packages/mcp/src/index.ts
       - Copy src/store.ts → packages/mcp/src/store.ts
       - Copy src/types.ts → packages/mcp/src/types.ts
       - Copy src/scheduler.ts → packages/mcp/src/scheduler.ts
       - Copy src/config.ts → packages/mcp/src/config.ts
       - Copy src/scheduler.test.ts → packages/mcp/src/scheduler.test.ts (if exists)

    4. Copy agent/ to packages/mcp/agent/:
       - Copy agent/agent.ts → packages/mcp/agent/agent.ts
       - Copy agent/agent.test.js → packages/mcp/agent/agent.test.js
       - Copy agent/prompts/system.md → packages/mcp/agent/prompts/system.md
       - Create packages/mcp/agent/tsconfig.json:
         ```json
         {
           "extends": "../tsconfig.json",
           "compilerOptions": {
             "rootDir": ".",
             "outDir": "../dist/agent"
           },
           "include": ["./**/*.ts"],
           "exclude": ["node_modules", "**/*.test.*"]
         }
         ```

    5. Update root tsconfig.json to be a base config (no rootDir/outDir — those are in each package):
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "nodenext",
           "moduleResolution": "nodenext",
           "strict": true,
           "esModuleInterop": true
         }
       }
       ```

    6. Update .gitignore to add: `packages/*/dist/` and `packages/*/node_modules/`

    NOTE: Keep the original src/ and agent/ directories in place for now — they are removed in a cleanup step after the MCP DB migration plan (07-02) verifies everything works.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm install && npm run build:mcp 2>&1 | tail -5</automated>
  </verify>
  <done>packages/mcp/dist/index.js exists after npm run build:mcp succeeds with zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold server and web packages with stubs</name>
  <files>
    packages/server/package.json
    packages/server/tsconfig.json
    packages/server/src/index.ts
    packages/web/package.json
    packages/web/tsconfig.json
    packages/web/vite.config.ts
    packages/web/index.html
    packages/web/src/main.tsx
    packages/web/src/App.tsx
  </files>
  <action>
    1. Create packages/server/package.json:
       ```json
       {
         "name": "@gantt/server",
         "version": "0.1.0",
         "type": "module",
         "main": "dist/index.js",
         "scripts": {
           "build": "tsc -p tsconfig.json",
           "dev": "node --watch dist/index.js",
           "start": "node dist/index.js"
         },
         "dependencies": {
           "@fastify/websocket": "^10.0.1",
           "@libsql/client": "^0.14.0",
           "@gantt/mcp": "*",
           "fastify": "^5.2.1"
         },
         "devDependencies": {
           "@types/node": "^22.10.2",
           "typescript": "^5.7.2"
         }
       }
       ```

    2. Create packages/server/tsconfig.json:
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "nodenext",
           "moduleResolution": "nodenext",
           "rootDir": "src",
           "outDir": "dist",
           "strict": true,
           "esModuleInterop": true
         },
         "include": ["src/**/*.ts"],
         "exclude": ["node_modules", "dist"]
       }
       ```

    3. Create packages/server/src/index.ts stub:
       ```typescript
       // packages/server/src/index.ts — stub, implemented in plan 07-03
       import Fastify from 'fastify';

       const server = Fastify({ logger: true });

       server.get('/health', async () => ({ status: 'ok' }));

       const PORT = Number(process.env.PORT ?? 3000);
       await server.listen({ port: PORT, host: '0.0.0.0' });
       console.log(`[server] Listening on :${PORT}`);
       ```

    4. Create packages/web/package.json:
       ```json
       {
         "name": "@gantt/web",
         "version": "0.1.0",
         "type": "module",
         "scripts": {
           "dev": "vite",
           "build": "tsc && vite build",
           "preview": "vite preview"
         },
         "dependencies": {
           "react": "^18.3.1",
           "react-dom": "^18.3.1"
         },
         "devDependencies": {
           "@types/react": "^18.3.12",
           "@types/react-dom": "^18.3.1",
           "@vitejs/plugin-react": "^4.3.4",
           "typescript": "^5.7.2",
           "vite": "^6.0.7"
         }
       }
       ```

    5. Create packages/web/tsconfig.json:
       ```json
       {
         "compilerOptions": {
           "target": "ES2020",
           "useDefineForClassFields": true,
           "lib": ["ES2020", "DOM", "DOM.Iterable"],
           "module": "ESNext",
           "skipLibCheck": true,
           "moduleResolution": "bundler",
           "allowImportingTsExtensions": true,
           "isolatedModules": true,
           "moduleDetection": "force",
           "noEmit": true,
           "jsx": "react-jsx",
           "strict": true
         },
         "include": ["src"]
       }
       ```

    6. Create packages/web/vite.config.ts with proxy:
       ```typescript
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';

       export default defineConfig({
         plugins: [react()],
         server: {
           port: 5173,
           proxy: {
             '/api': {
               target: 'http://localhost:3000',
               changeOrigin: true,
             },
             '/ws': {
               target: 'ws://localhost:3000',
               ws: true,
               changeOrigin: true,
             },
           },
         },
       });
       ```

    7. Create packages/web/index.html:
       ```html
       <!doctype html>
       <html lang="en">
         <head>
           <meta charset="UTF-8" />
           <meta name="viewport" content="width=device-width, initial-scale=1.0" />
           <title>Gantt Editor</title>
         </head>
         <body>
           <div id="root"></div>
           <script type="module" src="/src/main.tsx"></script>
         </body>
       </html>
       ```

    8. Create packages/web/src/main.tsx:
       ```tsx
       import { StrictMode } from 'react';
       import { createRoot } from 'react-dom/client';
       import App from './App.tsx';

       createRoot(document.getElementById('root')!).render(
         <StrictMode>
           <App />
         </StrictMode>
       );
       ```

    9. Create packages/web/src/App.tsx stub:
       ```tsx
       export default function App() {
         return <h1>Gantt Editor — coming soon</h1>;
       }
       ```

    10. Run npm install from root to link workspaces.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npm install 2>&1 | tail -3 && npm run build:server 2>&1 | tail -5</automated>
  </verify>
  <done>
    - packages/server/dist/index.js exists after build:server
    - packages/web/package.json has proxy config for /api and /ws pointing to :3000
    - npm run dev:web launches Vite on :5173 (verify manually or check process output)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm install` at root exits 0 with all three workspace packages linked
2. `npm run build:mcp` succeeds — packages/mcp/dist/index.js exists
3. `npm run build:server` succeeds — packages/server/dist/index.js exists
4. `node packages/server/dist/index.js` starts without crashing, GET /health returns `{"status":"ok"}`
5. packages/web/vite.config.ts contains proxy entries for /api and /ws targeting localhost:3000
</verification>

<success_criteria>
- npm workspaces monorepo structure is functional
- @gantt/mcp compiles successfully from its new location
- @gantt/server stub compiles with Fastify dependency declared
- @gantt/web Vite dev server can be launched on :5173 with :3000 proxy configured
- No files in original src/ or agent/ are deleted (preserved for safety until 07-02 validates the migration)
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-ui-with-real-time-gantt-editing-via-ai-dialogue/07-01-SUMMARY.md` using the standard summary template.
</output>
