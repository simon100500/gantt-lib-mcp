---
phase: 09-session-control
plan: 05
type: execute
wave: 4
depends_on:
  - 09-04
files_modified:
  - packages/web/package.json
  - packages/web/tailwind.config.js
  - packages/web/postcss.config.js
  - packages/web/components.json
  - packages/web/vite.config.ts
  - packages/web/tsconfig.json
  - packages/web/src/index.css
  - packages/web/src/main.tsx
  - packages/web/src/lib/utils.ts
  - packages/web/src/components/ui/ (generated by shadcn)
autonomous: true
requirements: []

must_haves:
  truths:
    - "Tailwind utility classes work in .tsx files (e.g. className='text-red-500' renders red)"
    - "shadcn components available in packages/web/src/components/ui/"
    - "@/ path alias resolves to packages/web/src/"
    - "TypeScript compiles with zero errors"
    - "Dev server starts and existing Gantt UI renders unchanged"
  artifacts:
    - path: "packages/web/tailwind.config.js"
      provides: "Tailwind config with shadcn theme tokens and tailwindcss-animate plugin"
    - path: "packages/web/components.json"
      provides: "shadcn component configuration"
    - path: "packages/web/src/lib/utils.ts"
      provides: "cn() utility function used by all shadcn components"
    - path: "packages/web/src/components/ui/button.tsx"
      provides: "shadcn Button component"
    - path: "packages/web/src/components/ui/card.tsx"
      provides: "shadcn Card component family"
    - path: "packages/web/src/components/ui/input.tsx"
      provides: "shadcn Input component"
    - path: "packages/web/src/components/ui/dropdown-menu.tsx"
      provides: "shadcn DropdownMenu component"
  key_links: []
---

<objective>
Install Tailwind CSS v3 + shadcn/ui in packages/web. Set up @/ path alias. Add Button, Input, Label, Card, DropdownMenu, and Separator components.

Zero-risk: existing inline-styled components (App.tsx, ChatSidebar.tsx) continue to work unchanged.
shadcn components are used exclusively by the new Auth UI in Plan 09-06.
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/web/package.json
@packages/web/vite.config.ts
@packages/web/tsconfig.json
@packages/web/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages + configure Tailwind CSS</name>
  <files>packages/web/package.json, packages/web/tailwind.config.js, packages/web/postcss.config.js, packages/web/src/index.css, packages/web/src/main.tsx</files>
  <action>
Run from project root (D:/Projects/gantt-lib-mcp):

**Step 1: Install devDependencies**
```bash
npm install -D tailwindcss@3 postcss autoprefixer tailwindcss-animate @types/node -w packages/web
```

**Step 2: Install runtime dependencies**
```bash
npm install class-variance-authority clsx tailwind-merge lucide-react -w packages/web
```

**Step 3: Write `packages/web/tailwind.config.js`**
```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: ["class"],
  content: [
    "./index.html",
    "./src/**/*.{ts,tsx,js,jsx}",
  ],
  theme: {
    extend: {
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      colors: {
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};
```

**Step 4: Write `packages/web/postcss.config.js`**
```js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

**Step 5: Create `packages/web/src/index.css`**

This file is new (doesn't exist). Create it with Tailwind directives + CSS variables.
The `--primary` value `221.2 83.2% 53.3%` = `#3b82f6`, matching the existing app's primary blue.

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.9%;
    --card: 0 0% 100%;
    --card-foreground: 0 0% 3.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 0 0% 3.9%;
    --primary: 221.2 83.2% 53.3%;
    --primary-foreground: 0 0% 98%;
    --secondary: 0 0% 96.1%;
    --secondary-foreground: 0 0% 9%;
    --muted: 0 0% 96.1%;
    --muted-foreground: 0 0% 45.1%;
    --accent: 0 0% 96.1%;
    --accent-foreground: 0 0% 9%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 89.8%;
    --input: 0 0% 89.8%;
    --ring: 221.2 83.2% 53.3%;
    --radius: 0.5rem;
  }

  * {
    @apply border-border;
  }

  body {
    @apply bg-background text-foreground;
  }
}
```

**Step 6: Import index.css in `packages/web/src/main.tsx`**

Add `import './index.css';` as the first import line (before the gantt-lib styles import).
  </action>
  <verify>
    <automated>node -e "require('./packages/web/tailwind.config.js')" && echo "tailwind config OK"</automated>
  </verify>
  <done>tailwind.config.js, postcss.config.js created. src/index.css created and imported in main.tsx. Packages installed.</done>
</task>

<task type="auto">
  <name>Task 2: Path alias + shadcn init + add components</name>
  <files>packages/web/vite.config.ts, packages/web/tsconfig.json, packages/web/components.json, packages/web/src/lib/utils.ts, packages/web/src/components/ui/</files>
  <action>
**Step 1: Update `packages/web/vite.config.ts`** — add path alias, preserve existing server/proxy config:

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
      '/ws': {
        target: 'ws://localhost:3000',
        ws: true,
        changeOrigin: true,
      },
    },
  },
});
```

**Step 2: Update `packages/web/tsconfig.json`** — add baseUrl + paths under compilerOptions:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
```

**Step 3: Create `packages/web/src/lib/utils.ts`**
```ts
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**Step 4: Write `packages/web/components.json`** (shadcn config — required for `npx shadcn add` to work non-interactively):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "src/index.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

**Step 5: Add shadcn components** (run from `packages/web` directory — components.json must be present):
```bash
cd packages/web && npx shadcn@latest add button input label card dropdown-menu separator --yes 2>&1
```

This generates in `packages/web/src/components/ui/`:
- `button.tsx` — Button with variants (default, outline, ghost, destructive, secondary)
- `input.tsx` — Input with focus ring
- `label.tsx` — Label with htmlFor
- `card.tsx` — Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
- `dropdown-menu.tsx` — full Radix DropdownMenu wrapper
- `separator.tsx` — horizontal/vertical divider

Also auto-installs: `@radix-ui/react-dropdown-menu`, `@radix-ui/react-label`, `@radix-ui/react-separator`, `@radix-ui/react-slot`
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsc --project packages/web/tsconfig.json --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
TypeScript compiles with zero errors.
packages/web/src/components/ui/ contains: button.tsx, input.tsx, label.tsx, card.tsx, dropdown-menu.tsx, separator.tsx.
packages/web/src/lib/utils.ts exists.
@/ alias resolves correctly (TS doesn't complain about @/components/ui imports).
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript clean
npx tsc --project packages/web/tsconfig.json --noEmit

# Components exist
ls packages/web/src/components/ui/

# utils.ts exists
cat packages/web/src/lib/utils.ts

# Dev server starts (existing UI unchanged)
npm run dev:web
```
</verification>

<success_criteria>
- Tailwind utility classes available in all .tsx files
- shadcn components importable as `import { Button } from '@/components/ui/button'`
- Existing App.tsx + ChatSidebar.tsx render unchanged (no regressions)
- Zero TypeScript errors
- gantt-lib CSS still loads (main.tsx import preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/09-session-control/09-05-SUMMARY.md`
</output>
