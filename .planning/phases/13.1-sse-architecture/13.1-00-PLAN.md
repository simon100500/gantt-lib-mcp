---
phase: 13.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/server/src/sse.test.ts
  - packages/web/src/hooks/useSSE.test.ts
  - vitest.config.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Vitest is configured for testing server and web packages"
    - "Test scaffold files exist for SSE server endpoints"
    - "Test scaffold files exist for SSE client hooks"
    - "Tests can be run with npm test -- --run"
  artifacts:
    - path: "packages/server/src/sse.test.ts"
      provides: "Test scaffold for SSE endpoints"
      min_lines: 50
    - path: "packages/web/src/hooks/useSSE.test.ts"
      provides: "Test scaffold for SSE client hooks"
      min_lines: 30
    - path: "vitest.config.ts"
      provides: "Vitest configuration for monorepo"
      contains: ["vitest", "workspace"]
  key_links: []

---

# Plan 13.1-00: Wave 0 Test Scaffold

**Objective:** Create test infrastructure and scaffold files for SSE architecture testing.

**Purpose:** Per Nyquist rule, every task with verification must have automated tests. This Wave 0 plan creates the test scaffolding before implementation.

**Output:** Vitest configuration, test scaffold files for SSE server and client.

## Context

@D:/Projects/gantt-lib-mcp/.planning/phases/13.1-sse-architecture/13.1-VALIDATION.md
@D:/Projects/gantt-lib-mcp/package.json

### Test Infrastructure

From VALIDATION.md:
- Framework: vitest
- Config file: vite.config.ts
- Quick run: `npm test -- --run`
- Estimated runtime: ~30 seconds

## Tasks

<task type="auto">
  <name>Task 1: Set up Vitest for monorepo testing</name>
  <files>vitest.config.ts, package.json</files>
  <action>
    1. Check if vitest.config.ts exists at root
    2. If not, create vitest.config.ts with:
       ```typescript
       import { defineConfig } from 'vitest/config'

       export default defineConfig({
         test: {
           workspace: [
             'packages/server',
             'packages/web',
           ],
         },
       })
       ```
    3. Add vitest as dev dependency in root package.json if not present:
       - `npm install --save-dev vitest @vitest/ui`
    4. Add test script to root package.json:
       - `"test": "vitest"`
       - `"test:run": "vitest --run"`
  </action>
  <verify>
    <automated>npx vitest --version</automated>
  </verify>
  <done>Vitest configured for workspace testing, test script added</done>
</task>

<task type="auto">
  <name>Task 2: Create SSE server test scaffold</name>
  <files>packages/server/src/sse.test.ts</files>
  <action>
    Create packages/server/src/sse.test.ts with test contracts:
    1. Import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
    2. Create test suite "SSE endpoints":
       - describe("GET /stream/tasks", () => {
           it("should require JWT auth", async () => { /* TODO: implement */ })
           it("should set correct SSE headers", async () => { /* TODO: implement */ })
           it("should add connection to registry", async () => { /* TODO: implement */ })
           it("should send connected message on accept", async () => { /* TODO: implement */ })
           it("should cleanup connection on close", async () => { /* TODO: implement */ })
         })
       - describe("broadcastToProject", () => {
           it("should send data to all project connections", async () => { /* TODO: implement */ })
           it("should handle dead connections gracefully", async () => { /* TODO: implement */ })
         })
       - describe("heartbeat", () => {
           it("should send heartbeat every 30s", async () => { /* TODO: implement */ })
         })
    3. Create test suite "AI streaming endpoint":
       - describe("GET /stream/ai", () => {
           it("should require JWT auth", async () => { /* TODO: implement */ })
           it("should stream tokens via SSE", async () => { /* TODO: implement */ })
         })
    4. All tests should have TODO placeholders with expected behavior comments
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/sse.test.ts</automated>
  </verify>
  <done>Test file created with all contract tests in TODO state</done>
</task>

<task type="auto">
  <name>Task 3: Create SSE client hooks test scaffold</name>
  <files>packages/web/src/hooks/useSSE.test.ts</files>
  <action>
    Create packages/web/src/hooks/useSSE.test.ts with test contracts:
    1. Import test utilities from vitest and @testing-library/react
    2. Create test suite "useTaskStream":
       - describe("useTaskStream", () => {
           it("should create EventSource on mount", () => { /* TODO */ })
           it("should include token in URL", () => { /* TODO */ })
           it("should call onMessage for SSE events", () => { /* TODO */ })
           it("should reconnect on error", () => { /* TODO */ })
           it("should cleanup on unmount", () => { /* TODO */ })
         })
    3. Create test suite "useAIStream":
       - describe("useAIStream", () => {
           it("should fetch with Authorization header", () => { /* TODO */ })
           it("should read stream response", () => { /* TODO */ })
           it("should parse SSE data format", () => { /* TODO */ })
           it("should call onMessage for tokens", () => { /* TODO */ })
           it("should abort on unmount", () => { /* TODO */ })
         })
    4. All tests should have TODO placeholders
  </action>
  <verify>
    <automated>cd packages/web && npx vitest run src/hooks/useSSE.test.ts</automated>
  </verify>
  <done>Test file created with all hook contract tests in TODO state</done>
</task>

<task type="auto">
  <name>Task 4: Verify Nyquist compliance</name>
  <files>.planning/phases/13.1-sse-architecture/13.1-VALIDATION.md</files>
  <action>
    1. Read 13.1-VALIDATION.md
    2. Update nyquist_compliant: true in frontmatter
    3. Update wave_0_complete: true in frontmatter
    4. Verify all Wave 0 requirements checked:
       - [x] packages/server/src/sse.test.ts — SSE endpoint tests
       - [x] packages/web/src/hooks/useSSE.test.ts — SSE client hooks tests
    5. Update validation sign-off section to mark Wave 0 complete
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/13.1-sse-architecture/13.1-VALIDATION.md</automated>
  </verify>
  <done>VALIDATION.md marked as Nyquist compliant, Wave 0 complete</done>
</task>

## Verification

- [ ] Vitest runs successfully on workspace
- [ ] Test scaffold files exist with TODO placeholders
- [ ] All test contracts from VALIDATION.md have corresponding test cases
- [ ] VALIDATION.md updated with nyquist_compliant: true

## Success Criteria

1. `npm test -- --run` executes without errors
2. All scaffold tests run (even if failing/todo)
3. Wave 0 requirements from VALIDATION.md satisfied

## Output

After completion, create `.planning/phases/13.1-sse-architecture/13.1-00-SUMMARY.md`
