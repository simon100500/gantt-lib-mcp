---
phase: 29-paywall-enhance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/billing.ts
  - packages/server/src/services/plan-config.ts
  - packages/web/src/components/PurchasePage.tsx
  - packages/web/src/components/AccountBillingPage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Годовые цены в billing.ts совпадают с v5: start=11900, team=47900, enterprise=129000"
    - "Серверные лимиты в plan-config.ts совпадают с v5 тарифной сеткой"
    - "PLAN_FEATURES полностью переписан: start (3 проекта, 25 AI/день, архив, ресурсы, PDF, ссылки), team (7 проектов, 50 AI/день, 5 участников, PDF+Excel), enterprise (безлимит, 100 AI/день, 20 участников, PDF+Excel+API)"
    - "FREE_FEATURES = ['1 проект', '20 AI-запросов (навсегда)', 'Гостевые ссылки']"
    - "На карточках тарифов при выборе 'Год' показывается экономия в рублях под ценой"
    - "Бейдж '-33%' убран с переключателя месяц/год"
    - "AccountBillingPage: лейбл 'AI-генерации' заменён на 'AI-запросы'"
    - "Кнопка 'Расширить' персонализирована по текущему тарифу"
    - "PurchasePage заголовок = 'Тарифы', кнопка free = 'Продолжить бесплатно'"
    - "Social proof blockquote добавлен между карточками и блоком Корпоративный"
    - "AccountBillingPage показывает alert при AI usage >= 80%"
    - "AccountBillingPage показывает состав free-плана для plan === 'free'"
  artifacts:
    - path: "packages/web/src/lib/billing.ts"
      provides: "PLAN_PRICES, PLAN_FEATURES, FREE_FEATURES constants"
      contains: "yearly: 11900"
    - path: "packages/server/src/services/plan-config.ts"
      provides: "Server-side plan limits matching v5"
      contains: "PLAN_FREE_PROJECTS', 1"
    - path: "packages/web/src/components/PurchasePage.tsx"
      provides: "Updated pricing page with social proof, savings, text fixes"
      contains: "Продолжить бесплатно"
    - path: "packages/web/src/components/AccountBillingPage.tsx"
      provides: "Billing page with upsell alerts, free plan details, personalized button"
      contains: "AI-запросы"
  key_links:
    - from: "packages/web/src/lib/billing.ts"
      to: "packages/web/src/components/PurchasePage.tsx"
      via: "PLAN_PRICES, PLAN_FEATURES, FREE_FEATURES imports"
      pattern: "PLAN_PRICES|PLAN_FEATURES|FREE_FEATURES"
    - from: "packages/web/src/lib/billing.ts"
      to: "packages/web/src/components/AccountBillingPage.tsx"
      via: "PLAN_LABELS, formatPrice imports"
      pattern: "PLAN_LABELS|formatPrice"
    - from: "packages/server/src/services/plan-config.ts"
      to: "packages/web/src/lib/billing.ts"
      via: "Prices and limits must match"
      pattern: "11900|47900|129000"
---

<objective>
Синхронизировать все цены, лимиты и фичи тарифов с тарифной сеткой v5. Применить CRO-улучшения на PurchasePage и AccountBillingPage: персонализация кнопок, social proof, экономия на год, upsell alerts, текстовые правки.

Purpose: Цены и фичи в коде устарели — не совпадают с актуальной v5 тарифной сеткой. CRO-улучшения повышают конверсию upgrade flow.
Output: Обновлённые billing.ts, plan-config.ts, PurchasePage.tsx, AccountBillingPage.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-paywall-enhance/29-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/web/src/lib/billing.ts:
```typescript
export type BillingPeriod = 'monthly' | 'yearly';
export type PlanId = 'free' | 'start' | 'team' | 'enterprise';
export type PaidPlanId = 'start' | 'team';

export const PLAN_LABELS: Record<PlanId, string> = {
  free: 'Бесплатный',
  start: 'Старт',
  team: 'Команда',
  enterprise: 'Корпоративный',
};

export const PLAN_PRICES: Record<'start' | 'team' | 'enterprise', Record<BillingPeriod, number>> = {
  start: { monthly: 1490, yearly: 12000 },
  team: { monthly: 4990, yearly: 59880 },
  enterprise: { monthly: 12900, yearly: 154800 },
};

export function formatPrice(price: number): string;
```

From packages/web/src/stores/useBillingStore.ts:
```typescript
export interface SubscriptionStatus {
  plan: string;
  periodEnd: string | null;
  aiUsed: number;
  aiLimit: number;
  isActive: boolean;
  limits: PlanLimits;
}
```

From packages/server/src/services/plan-config.ts:
```typescript
export type PlanKey = 'free' | 'start' | 'team' | 'enterprise';

export interface PlanLimits {
  projects: number;       // -1 = unlimited
  aiGenerations: number;  // -1 = unlimited
  aiRefinements: number;  // per project, -1 = unlimited
  resources: number;      // -1 = unlimited
  teamMembers: number;    // -1 = unlimited
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update billing constants, server config, and PurchasePage</name>
  <files>
    packages/web/src/lib/billing.ts
    packages/server/src/services/plan-config.ts
    packages/web/src/components/PurchasePage.tsx
  </files>
  <read_first>
    packages/web/src/lib/billing.ts
    packages/server/src/services/plan-config.ts
    packages/web/src/components/PurchasePage.tsx
  </read_first>
  <action>
Per A1 — Update PLAN_PRICES in billing.ts:
- start yearly: 12000 -> 11900
- team yearly: 59880 -> 47900
- enterprise yearly: 154800 -> 129000
- Monthly prices unchanged (1490, 4990, 12900)

Per A2 — Completely rewrite PLAN_FEATURES in billing.ts:
```
start: [
  '3 активных проекта',
  '25 AI-запросов в день',
  'Архив проектов',
  'Пул ресурсов',
  'Экспорт в PDF',
  'Гостевые ссылки',
],
team: [
  '7 активных проектов',
  '50 AI-запросов в день',
  'Архив проектов',
  'Пул ресурсов',
  '5 участников команды',
  'Экспорт PDF + Excel',
  'Гостевые ссылки',
],
enterprise: [
  'Безлимит проектов',
  '100 AI-запросов в день',
  '20 участников команды',
  'Экспорт PDF + Excel + API',
  'Приоритетная поддержка',
],
```

Per A3 — Replace FREE_FEATURES in PurchasePage.tsx:
```
const FREE_FEATURES = [
  '1 проект',
  '20 AI-запросов (навсегда)',
  'Гостевые ссылки',
];
```

Per server sync — Update plan-config.ts defaults:
- free: projects=1, aiGenerations=20, aiRefinements=-1, resources=-1, teamMembers=1
  (aiGenerations now means "AI requests total for free" — 20 навсегда)
  Wait: the server uses aiGenerations for daily counting on paid, but free has 20 total. The server envInt defaults must be:
  - free: PLAN_FREE_PROJECTS=1, PLAN_FREE_AI_GENERATIONS=20, PLAN_FREE_AI_REFINEMENTS=-1, PLAN_FREE_RESOURCES=-1, PLAN_FREE_TEAM_MEMBERS=1
  - start: PLAN_START_PROJECTS=3, PLAN_START_AI_GENERATIONS=25, PLAN_START_AI_REFINEMENTS=-1, PLAN_START_RESOURCES=-1, PLAN_START_TEAM_MEMBERS=1
    - start yearly: PLAN_START_PRICE_YEARLY=11900
  - team: PLAN_TEAM_PROJECTS=7, PLAN_TEAM_AI_GENERATIONS=50, PLAN_TEAM_AI_REFINEMENTS=-1, PLAN_TEAM_RESOURCES=-1, PLAN_TEAM_TEAM_MEMBERS=5
    - team yearly: PLAN_TEAM_PRICE_YEARLY=47900
  - enterprise: PLAN_ENTERPRISE_PROJECTS=-1, PLAN_ENTERPRISE_AI_GENERATIONS=100, PLAN_ENTERPRISE_AI_REFINEMENTS=-1, PLAN_ENTERPRISE_RESOURCES=-1, PLAN_ENTERPRISE_TEAM_MEMBERS=20
    - enterprise yearly: PLAN_ENTERPRISE_PRICE_YEARLY=129000

Per A4 — Remove the "-33%" badge from the year toggle button. The span `<span className="ml-1 text-xs text-emerald-400">-33%</span>` must be deleted entirely.

Per A4 + B5 — Add yearly savings display on each paid card. When `billingPeriod === 'yearly'`, show below the price:
```
Экономия {savings} ₽ в год
```
Where savings = monthly*12 - yearly:
- start: 1490*12 - 11900 = 5980
- team: 4990*12 - 47900 = 11980
- enterprise: 12900*12 - 129000 = 25800

Render as `<span className="mt-1 block text-sm text-emerald-600">Экономия {formatPrice(savings)} в год</span>` inside the price div, conditionally shown only when `billingPeriod === 'yearly'` and `plan !== 'free'`.

Per B4 — Add social proof blockquote between the 3-card grid and the enterprise block. Insert between the closing `</div>` of the grid and the enterprise `<div className="mt-8 ...">`:
```tsx
<div className="mt-8 rounded-3xl border border-slate-200 bg-slate-50 px-8 py-6 text-center">
  <blockquote className="text-lg font-medium text-slate-700">
    &laquo;Бомба, такого на рынке нет!&raquo;
  </blockquote>
  <p className="mt-2 text-sm text-slate-500">&mdash; прораб, ранний доступ</p>
</div>
```

Per B6 — Text changes in PurchasePage.tsx:
- Title `<h1>`: "Расширить тариф" -> "Тарифы"
- Free plan button text: "Остаться на free" -> "Продолжить бесплатно"

Also update the plan-config.ts comment block at top of file to reflect new v5 limits instead of the outdated ones.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && grep -c "yearly: 11900" packages/web/src/lib/billing.ts && grep -c "yearly: 47900" packages/web/src/lib/billing.ts && grep -c "yearly: 129000" packages/web/src/lib/billing.ts && grep -c "3 активных проекта" packages/web/src/lib/billing.ts && grep -c "7 активных проекта" packages/web/src/lib/billing.ts && grep -c "100 AI-запросов" packages/web/src/lib/billing.ts && grep -c "1 проект" packages/web/src/components/PurchasePage.tsx && grep -c "20 AI-запросов (навсегда)" packages/web/src/components/PurchasePage.tsx && grep -c "Продолжить бесплатно" packages/web/src/components/PurchasePage.tsx && grep -c "Тарифы" packages/web/src/components/PurchasePage.tsx && grep -c "Бомба, такого на рынке нет" packages/web/src/components/PurchasePage.tsx && grep -c "Экономия" packages/web/src/components/PurchasePage.tsx && ! grep -q "\-33%" packages/web/src/components/PurchasePage.tsx && grep -c "PLAN_FREE_PROJECTS.*1" packages/server/src/services/plan-config.ts && grep -c "PLAN_START_PRICE_YEARLY.*11900" packages/server/src/services/plan-config.ts && grep -c "PLAN_TEAM_PRICE_YEARLY.*47900" packages/server/src/services/plan-config.ts && grep -c "PLAN_ENTERPRISE_PRICE_YEARLY.*129000" packages/server/src/services/plan-config.ts && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - billing.ts contains `yearly: 11900` for start plan
    - billing.ts contains `yearly: 47900` for team plan
    - billing.ts contains `yearly: 129000` for enterprise plan
    - billing.ts PLAN_FEATURES start contains "3 активных проекта" and "25 AI-запросов в день"
    - billing.ts PLAN_FEATURES team contains "7 активных проекта" and "50 AI-запросов в день"
    - billing.ts PLAN_FEATURES enterprise contains "100 AI-запросов в день" and "20 участников команды"
    - PurchasePage.tsx FREE_FEATURES contains "1 проект" and "20 AI-запросов (навсегда)"
    - PurchasePage.tsx title says "Тарифы" (not "Расширить тариф")
    - PurchasePage.tsx free button says "Продолжить бесплатно" (not "Остаться на free")
    - PurchasePage.tsx has blockquote with "Бомба, такого на рынке нет"
    - PurchasePage.tsx has "Экономия" text for yearly savings
    - PurchasePage.tsx does NOT contain "-33%"
    - plan-config.ts free defaults: projects=1, aiGenerations=20
    - plan-config.ts start defaults: projects=3, aiGenerations=25, yearly=11900
    - plan-config.ts team defaults: projects=7, aiGenerations=50, teamMembers=5, yearly=47900
    - plan-config.ts enterprise defaults: aiGenerations=100, teamMembers=20, yearly=129000
  </acceptance_criteria>
  <done>
    Prices, features, and limits updated everywhere to v5. PurchasePage shows correct free features, yearly savings, social proof, and fixed text. Server config matches frontend constants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AccountBillingPage with label fix, upsell alerts, personalized button, and free plan details</name>
  <files>
    packages/web/src/components/AccountBillingPage.tsx
  </files>
  <read_first>
    packages/web/src/components/AccountBillingPage.tsx
    packages/web/src/lib/billing.ts
  </read_first>
  <action>
Per A5 — Change label on line 91 from `AI-генерации` to `AI-запросы`.

Per A6 — Progress bar stays on billing page (no changes needed for removal, it's already only on this page).

Per B2 — Add conditional alert block after the progress bar div (after the closing `</div>` of the progress bar section, still inside the subscription data `space-y-4` div):

When `aiUsagePercent >= 80 && aiUsagePercent < 100`:
```tsx
<div className="rounded-xl border border-amber-200 bg-amber-50 p-4">
  <p className="text-sm text-amber-800">Осталось мало запросов. Расширьте тариф, чтобы не останавливать работу.</p>
  <button
    type="button"
    onClick={() => { window.location.href = '/purchase'; }}
    className="mt-2 text-sm font-medium text-amber-700 underline hover:text-amber-900"
  >
    Расширить тариф
  </button>
</div>
```

When `aiUsagePercent >= 100`:
```tsx
<div className="rounded-xl border border-red-200 bg-red-50 p-4">
  <p className="text-sm text-red-800">AI-запросы закончились. Обновите тариф для продолжения работы.</p>
  <button
    type="button"
    onClick={() => { window.location.href = '/purchase'; }}
    className="mt-2 text-sm font-medium text-red-700 underline hover:text-red-900"
  >
    Обновить тариф
  </button>
</div>
```

Only show these when `!aiUnlimited` (paid plans with daily limits can hit limits too).

Per B3 — Personalize the "Расширить" button. Replace the static button text with conditional logic based on `subscription.plan`:

Import `PLAN_PRICES, formatPrice` from billing.ts (PLAN_PRICES is already imported indirectly, but verify imports include it).

Replace the button at the top of the subscription card:
```
free -> "Перейти на Старт — 1 490 ₽/мес"
start -> "Расширить до Команды"
team -> "Корпоративный — безлимит"
enterprise -> hide the button entirely (render null)
```

Implementation: Add a variable before the return, or inline conditional:
```tsx
const upgradeLabel = subscription?.plan === 'free'
  ? 'Перейти на Старт — 1 490 ₽/мес'
  : subscription?.plan === 'start'
    ? 'Расширить до Команды'
    : subscription?.plan === 'team'
      ? 'Корпоративный — безлимит'
      : null;

const showUpgradeButton = upgradeLabel !== null;
```

Then conditionally render the button with `{showUpgradeButton && (...)}` using `upgradeLabel` as text.

Per B7 — Add free plan details block for `plan === 'free'`. After the plan name/status section, before the AI usage progress bar, add:

```tsx
{subscription.plan === 'free' && (
  <div className="rounded-xl border border-slate-100 bg-slate-50 p-4">
    <ul className="space-y-1.5 text-sm text-slate-600">
      <li>1 проект</li>
      <li>20 AI-запросов (навсегда)</li>
      <li>Гостевые ссылки</li>
    </ul>
    <p className="mt-3 text-sm text-slate-500">
      Нужно больше? Тариф Старт — 3 проекта и 25 запросов в день.
    </p>
    <button
      type="button"
      onClick={() => { window.location.href = '/purchase'; }}
      className="mt-2 text-sm font-medium text-primary underline hover:text-primary/80"
    >
      Посмотреть тарифы
    </button>
  </div>
)}
```
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && grep -c "AI-запросы" packages/web/src/components/AccountBillingPage.tsx && grep -c "Осталось мало запросов" packages/web/src/components/AccountBillingPage.tsx && grep -c "AI-запросы закончились" packages/web/src/components/AccountBillingPage.tsx && grep -c "Перейти на Старт" packages/web/src/components/AccountBillingPage.tsx && grep -c "Расширить до Команды" packages/web/src/components/AccountBillingPage.tsx && grep -c "20 AI-запросов (навсегда)" packages/web/src/components/AccountBillingPage.tsx && grep -c "Нужно больше" packages/web/src/components/AccountBillingPage.tsx && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - AccountBillingPage.tsx contains "AI-запросы" label (not "AI-генерации")
    - AccountBillingPage.tsx contains amber alert with "Осталось мало запросов" for >=80% usage
    - AccountBillingPage.tsx contains red alert with "AI-запросы закончились" for 100% usage
    - AccountBillingPage.tsx button shows "Перейти на Старт" for free users
    - AccountBillingPage.tsx button shows "Расширить до Команды" for start users
    - AccountBillingPage.tsx button shows "Корпоративный — безлимит" for team users
    - AccountBillingPage.tsx hides upgrade button for enterprise users
    - AccountBillingPage.tsx shows free plan details block with "20 AI-запросов (навсегда)" when plan === 'free'
    - AccountBillingPage.tsx contains "Нужно больше? Тариф Старт" nudge for free users
  </acceptance_criteria>
  <done>
    AccountBillingPage updated with AI-запросы label, usage-based upsell alerts (80%/100%), personalized upgrade button per current plan, and free plan composition display.
  </done>
</task>

</tasks>

<verification>
1. `billing.ts` has correct v5 prices: start yearly=11900, team yearly=47900, enterprise yearly=129000
2. `plan-config.ts` server defaults match v5 limits and prices
3. `PurchasePage.tsx` shows updated features, social proof, yearly savings, correct text
4. `AccountBillingPage.tsx` shows AI-запросы label, upsell alerts, personalized button, free plan details
5. TypeScript compiles: `npx tsc --noEmit` passes in packages/web and packages/server
</verification>

<success_criteria>
- All v5 prices and limits are synchronized between billing.ts, plan-config.ts, and UI components
- PurchasePage shows yearly savings, social proof blockquote, correct title and free button text
- AccountBillingPage shows contextual upsell at 80%/100% usage, personalized button, free plan composition
- No "-33%" badge on year toggle
- All existing payment flow functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/29-paywall-enhance/29-01-SUMMARY.md`
</output>
