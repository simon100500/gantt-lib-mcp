---
phase: 41-initial-gen-refactor
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - packages/server/src/initial-generation/compiler.ts
  - packages/server/src/initial-generation/executor.ts
  - packages/server/src/initial-generation/compiler.test.ts
autonomous: true
requirements: [IGR-03]

must_haves:
  truths:
    - "An approved `ProjectPlan` can be compiled into one authoritative `create_tasks_batch` command and committed through `commandService`"
    - "Compilation respects working-day scheduling, RF production calendar defaults, phase rollups, and all four dependency types"
    - "Technical compile failures attempt maximal valid partial salvage before rejecting"
    - "Partial schedules are committed only when the locked salvage thresholds are met; otherwise the user gets a controlled error and no silent fallback to the mutation-agent flow"
  artifacts:
    - path: "packages/server/src/initial-generation/compiler.ts"
      provides: "Deterministic ProjectPlan-to-command compiler with nodeKey mapping and dependency/date resolution"
      exports: ["compileInitialProjectPlan"]
    - path: "packages/server/src/initial-generation/executor.ts"
      provides: "Compile, salvage, and authoritative batch-commit helper over CommandService"
      exports: ["executeInitialProjectPlan"]
    - path: "packages/server/src/initial-generation/compiler.test.ts"
      provides: "Compiler and salvage coverage for hierarchy, dependency types, and controlled failure"
      contains: "partial salvage"
  key_links:
    - from: "packages/server/src/initial-generation/executor.ts"
      to: "packages/mcp/src/services/command.service.ts"
      via: "Single authoritative batch commit using `create_tasks_batch`"
      pattern: "commitCommand"
    - from: "packages/server/src/initial-generation/compiler.ts"
      to: "packages/mcp/src/types.ts"
      via: "Batch task/dependency command shape"
      pattern: "create_tasks_batch"
---

<objective>
Implement the deterministic execution side of the two-stage pipeline.

Purpose: The planning stage is only valuable if the server can compile it into a real schedule without another LLM in the happy path. This plan owns the compiler, partial-salvage rules, and authoritative `commandService` commit path.
Output: Deterministic compiler, compile/commit executor, and focused tests for dependency handling, rollups, salvage, and controlled rejection.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-initial-gen-refactor/41-CONTEXT.md
@INITIAL-GENERATION-REFACTOR-PLAN.md
@packages/server/src/initial-generation/types.ts
@packages/mcp/src/services/command.service.ts
@packages/mcp/src/types.ts
@packages/server/src/routes/command-routes.ts
@.planning/reference/unified-scheduling-core-prd.md
@.planning/reference/scheduling-core-adoption-prd.md

<interfaces>
```typescript
export type DependencyType = 'FS' | 'SS' | 'FF' | 'SF';
```

```typescript
export type ProjectCommand =
  | { type: 'create_tasks_batch'; tasks: CreateTaskInput[]; }
  | { type: 'create_dependency'; taskId: string; dependency: TaskDependency; };
```

```typescript
async commitCommand(
  request: CommitProjectCommandRequest,
  actorType: ActorType,
  actorId?: string,
): Promise<CommitProjectCommandResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deterministic ProjectPlan compiler over the existing command contract</name>
  <files>packages/server/src/initial-generation/compiler.ts, packages/server/src/initial-generation/compiler.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/compiler.ts
    packages/server/src/initial-generation/compiler.test.ts
    packages/server/src/initial-generation/types.ts
    packages/mcp/src/types.ts
    packages/mcp/src/services/command.service.ts
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
    .planning/reference/unified-scheduling-core-prd.md
  </read_first>
  <behavior>
    - `compileInitialProjectPlan()` converts `ProjectPlan` nodes into one `create_tasks_batch` command with deterministic IDs mapped from `nodeKey`
    - Schedule start anchors to current server date, defaults to working-day mode, and assumes the existing RF production calendar semantics
    - Parent phases are emitted as containers whose date ranges come only from child rollup
    - Dependencies support `FS`, `SS`, `FF`, `SF`, and `lagDays`
  </behavior>
  <action>
    Implement the compiler per IGR-03:

    1. `compiler.ts`
       - export `compileInitialProjectPlan({ projectId, baseVersion, serverDate, plan })`
       - validate/normalize the incoming plan shape again at the compiler boundary
       - generate real task IDs for every retained `nodeKey`
       - calculate child task dates from `serverDate` using working-day semantics
       - emit phase containers plus child tasks in a single `create_tasks_batch` command payload
       - preserve dependency metadata for `FS/SS/FF/SF` with `lagDays`
       - return `CompiledInitialSchedule` containing `nodeKeyToTaskId`, `command`, `retainedNodeCount`, and compile diagnostics
    2. `compiler.test.ts`
       - cover mixed dependency types, lag handling, top-level phases with child tasks, and rollup-only parent ranges
       - assert `durationDays` is interpreted as working days
       - assert the compiler emits deterministic structures for the same `serverDate`

    Target the existing `create_tasks_batch`/`CommandService.commitCommand()` contract directly. Do not invent a new persistence path or new MCP tool surface.
  </action>
  <acceptance_criteria>
    - `rg -n "create_tasks_batch" packages/server/src/initial-generation/compiler.ts` finds the authoritative batch command target
    - `rg -n "FS|SS|FF|SF" packages/server/src/initial-generation/compiler.ts packages/server/src/initial-generation/compiler.test.ts` finds all dependency types
    - `rg -n "nodeKeyToTaskId|retainedNodeCount|serverDate" packages/server/src/initial-generation/compiler.ts` finds the deterministic compiler result fields
    - `rg -n "working day|rollup|dependency type" packages/server/src/initial-generation/compiler.test.ts` finds coverage for locked compile invariants
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/compiler.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    A validated `ProjectPlan` can now be deterministically lowered into the existing command-service contract with explicit node-key mapping, dependency support, and rollup-safe container semantics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add partial-salvage execution and controlled failure over CommandService</name>
  <files>packages/server/src/initial-generation/executor.ts, packages/server/src/initial-generation/compiler.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/executor.ts
    packages/server/src/initial-generation/compiler.test.ts
    packages/server/src/initial-generation/compiler.ts
    packages/server/src/initial-generation/types.ts
    packages/mcp/src/services/command.service.ts
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
    .planning/reference/scheduling-core-adoption-prd.md
  </read_first>
  <behavior>
    - `executeInitialProjectPlan()` tries a full compile first, then prunes invalid nodes/links for maximal salvage only on technical compile failure
    - Partial schedules are accepted only if at least 60% of nodes remain, at least 3 top-level phases remain, every retained phase has a child task, and no broken refs/empty containers remain
    - Successful compile or salvage commits exactly one authoritative batch through `commandService.commitCommand(..., 'agent', ...)`
    - If salvage is too weak, the function returns a controlled error and never falls back to normal mutation-agent execution
  </behavior>
  <action>
    Implement the compile/commit helper per IGR-03:

    1. `executor.ts`
       - export `executeInitialProjectPlan({ projectId, baseVersion, clientRequestId, actorId, plan, commandService, serverDate })`
       - run full compile
       - on technical compile failure, drop only nodes/links that cause invalid references, cycles, or empty-container violations
       - compute salvage thresholds exactly as locked in `41-CONTEXT.md`: `>= 60%` retained nodes, `>= 3` top-level phases, every phase keeps a child task, zero broken references after cleanup
       - if salvage passes, commit the salvaged `create_tasks_batch` command through `commandService.commitCommand`
       - if salvage fails, return a typed controlled error describing rejection without compiler-internal jargon
       - never call ordinary mutation-agent helpers from this file
    2. Extend `compiler.test.ts`
       - add a partial-salvage success case
       - add a too-weak salvage rejection case
       - add a test that verifies no fallback call-path symbol from `agent.ts` or mutation helpers is used

    Make partial success user-visible only as a partial-build outcome, not as "compiler salvage" wording.
  </action>
  <acceptance_criteria>
    - `rg -n "60%|0\\.6|3 top-level phases|every retained phase has a child task|broken references" packages/server/src/initial-generation/executor.ts` finds the locked salvage thresholds
    - `rg -n "commitCommand\\(" packages/server/src/initial-generation/executor.ts` finds the authoritative commit path
    - `rg -n "partial" packages/server/src/initial-generation/compiler.test.ts` finds both partial-success and controlled-failure coverage
    - `rg -n "runInitialGeneration|executeAgentAttempt|buildPrompt" packages/server/src/initial-generation/executor.ts` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/compiler.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The execution stage is deterministic and authoritative: it compiles and commits via `commandService`, salvages only when the locked quality floor holds, and otherwise returns a controlled rejection without hidden fallback behavior.
  </done>
</task>

</tasks>

<verification>
Confirm the compiler targets `create_tasks_batch` on the existing command-service boundary, supports the full dependency contract, and enforces the locked partial-salvage thresholds before any batch is committed.
</verification>

<success_criteria>
Phase 41 has a deterministic execution engine that can turn a `ProjectPlan` into a real schedule, commit it through the single authoritative command path, and reject weak salvage safely.
</success_criteria>

<output>
After completion, create `.planning/phases/41-initial-gen-refactor/41-03-SUMMARY.md`
</output>
