---
phase: 41-initial-gen-refactor
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - packages/server/src/initial-generation/brief.ts
  - packages/server/src/initial-generation/domain-reference.ts
  - packages/server/src/initial-generation/planner.ts
  - packages/server/src/initial-generation/quality-gate.ts
  - packages/server/src/initial-generation/planner.test.ts
autonomous: true
requirements: [IGR-02]

must_haves:
  truths:
    - "Initial-generation planning receives a short server-side brief plus domain reference instead of a hardcoded task template"
    - "Planning returns a machine-readable `ProjectPlan` with strict schema validation before compilation"
    - "Weak plans can trigger exactly one repair pass driven by explicit criticism, without evaluator-LLM fan-out or clarifying questions to the user"
    - "Unknown object types default to generic construction guidance interpreted as a private residential house baseline"
  artifacts:
    - path: "packages/server/src/initial-generation/brief.ts"
      provides: "GenerationBrief creation from the request and recognized object type"
      exports: ["buildGenerationBrief"]
    - path: "packages/server/src/initial-generation/domain-reference.ts"
      provides: "Domain-reference resolver over the construction map JSON with generic construction fallback"
      exports: ["resolveDomainReference"]
    - path: "packages/server/src/initial-generation/planner.ts"
      provides: "Planning prompt builder, ProjectPlan validation, and optional one-shot repair flow"
      exports: ["planInitialProject"]
    - path: "packages/server/src/initial-generation/quality-gate.ts"
      provides: "Rule-based plan verdicts for hierarchy, coverage, naming, and sequence realism"
      exports: ["evaluateProjectPlanQuality"]
  key_links:
    - from: "packages/server/src/initial-generation/planner.ts"
      to: "packages/server/src/initial-generation/brief.ts"
      via: "Planning prompt context assembly"
      pattern: "buildGenerationBrief"
    - from: "packages/server/src/initial-generation/planner.ts"
      to: "packages/server/src/initial-generation/quality-gate.ts"
      via: "One-shot accept-or-repair decision between planning and compilation"
      pattern: "evaluateProjectPlanQuality"
---

<objective>
Implement the planning side of the two-stage initial-generation pipeline.

Purpose: The refactor only works if initial generation produces a strong, validated `ProjectPlan` before any deterministic execution. This plan owns domain-context injection, planning contract enforcement, and the one-shot quality/repair loop.
Output: Brief/domain-reference helpers, planner module with schema validation and repair path, and focused tests for prompt assembly and quality-gate behavior.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-initial-gen-refactor/41-CONTEXT.md
@INITIAL-GENERATION-REFACTOR-PLAN.md
@.planning/reference/construction-work-intent-map-v3.json
@packages/server/src/initial-generation/types.ts
@packages/server/src/initial-generation/orchestrator.ts

<interfaces>
```typescript
export interface ProjectPlan {
  projectType: string;
  nodes: ProjectPlanNode[];
  assumptions: string[];
}
```

```typescript
export interface PlanQualityVerdict {
  accepted: boolean;
  reasons: RepairReason[];
  score: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the server-side generation brief and domain-reference resolver</name>
  <files>packages/server/src/initial-generation/brief.ts, packages/server/src/initial-generation/domain-reference.ts, packages/server/src/initial-generation/planner.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/brief.ts
    packages/server/src/initial-generation/domain-reference.ts
    packages/server/src/initial-generation/planner.test.ts
    packages/server/src/initial-generation/types.ts
    .planning/reference/construction-work-intent-map-v3.json
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
  </read_first>
  <behavior>
    - `buildGenerationBrief()` infers object type, scope/scale signals, and a requirement for a full starter schedule with non-filler naming
    - `resolveDomainReference()` reuses the construction reference JSON for recognized construction/repair cases
    - Unknown object types resolve to generic construction guidance with default interpretation `частный жилой дом`
    - No code path asks the user a clarifying question for broad empty-project generation
  </behavior>
  <action>
    Implement the brief/reference layer per IGR-02:

    1. `domain-reference.ts`
       - load or adapt `.planning/reference/construction-work-intent-map-v3.json`
       - export `resolveDomainReference({ userMessage, inferredObjectType })`
       - support recognized buckets for at least `private_house`, `kindergarten`, `office_renovation`, and generic `construction`
       - when the request is broad or unrecognized, return a compact generic-construction reference annotated with `defaultInterpretation: 'private_residential_house'`
    2. `brief.ts`
       - export `buildGenerationBrief({ userMessage, reference })`
       - include `objectType`, `scopeSignals`, `starterScheduleExpectation`, `namingBan`, and `domainContextSummary`
       - forbid filler titles such as `Этап 1`, `Задача 2`, `Stage 1`, `Task 3`
       - explicitly state that the server should infer a baseline rather than ask questions
    3. `planner.test.ts`
       - add tests proving recognized prompts inject the matched reference
       - add tests proving `Построй график` and similar broad prompts use the private-house generic fallback
       - add tests that brief text contains the no-filler and full-starter-schedule requirements
  </action>
  <acceptance_criteria>
    - `rg -n "defaultInterpretation: 'private_residential_house'|private_residential_house" packages/server/src/initial-generation/domain-reference.ts packages/server/src/initial-generation/planner.test.ts` finds the locked fallback
    - `rg -n "starterScheduleExpectation|namingBan|domainContextSummary" packages/server/src/initial-generation/brief.ts` finds the brief fields
    - `rg -n "clarifying question|уточня" packages/server/src/initial-generation/brief.ts` returns no matches
    - `rg -n "Построй график|детского садика|ремонта офиса" packages/server/src/initial-generation/planner.test.ts` finds the manual-scenario-derived tests
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/planner.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The planner input is now brief-first and reference-guided, with a locked generic-construction fallback that avoids clarifying questions and avoids task-template behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ProjectPlan validation, quality gate, and one-shot repair orchestration</name>
  <files>packages/server/src/initial-generation/planner.ts, packages/server/src/initial-generation/quality-gate.ts, packages/server/src/initial-generation/planner.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/planner.ts
    packages/server/src/initial-generation/quality-gate.ts
    packages/server/src/initial-generation/planner.test.ts
    packages/server/src/initial-generation/types.ts
    packages/server/src/initial-generation/brief.ts
    packages/server/src/initial-generation/domain-reference.ts
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
  </read_first>
  <behavior>
    - `planInitialProject()` performs exactly one planning call on the happy path and at most one repair call when the quality gate rejects the first result
    - ProjectPlan validation rejects duplicate `nodeKey`, top-level `task`, placeholder titles, missing durations, and broken dependency references
    - `assumptions` defaults to `[]`, dependency type defaults to `FS`, and `lagDays` defaults to `0`
    - If the repaired plan is still weak, the planner returns the best available repaired plan instead of looping indefinitely
  </behavior>
  <action>
    Implement the planner/quality modules per IGR-02:

    1. `quality-gate.ts`
       - export `evaluateProjectPlanQuality(plan, brief)`
       - score hierarchy depth, subject-matter specificity, coverage/scale, sequence realism, and placeholder naming
       - emit explicit `RepairReason` codes such as `missing_hierarchy`, `placeholder_titles`, `weak_coverage`, `weak_sequence`
       - keep thresholds internal except for the locked single repair behavior
    2. `planner.ts`
       - export `planInitialProject({ userMessage, brief, reference, modelDecision, sdkQuery })`
       - build a planning prompt that requests strict machine-readable `ProjectPlan` JSON only
       - validate parsed output against the locked schema rules
       - if the quality gate rejects the plan, send the full plan plus explicit criticism back once and only once
       - normalize defaults: `assumptions = []`, dependency type `FS`, `lagDays = 0`
       - return both the accepted plan and a `PlanQualityVerdict`
    3. `planner.test.ts`
       - add rejection tests for duplicate `nodeKey`, top-level `task`, placeholder names, and unknown dependency references
       - add accept/repair tests showing weak plans trigger one repair and persistent weakness stops after the repaired result

    Use a strict, typed validator in the server package; do not defer schema enforcement to compile time.
  </action>
  <acceptance_criteria>
    - `rg -n "missing_hierarchy|placeholder_titles|weak_coverage|weak_sequence" packages/server/src/initial-generation/quality-gate.ts` finds explicit repair reasons
    - `rg -n "assumptions.*\\[\\]|lagDays.*0|type.*'FS'" packages/server/src/initial-generation/planner.ts` finds schema-default normalization
    - `rg -n "duplicate.*nodeKey|top-level task|placeholder|dependency reference" packages/server/src/initial-generation/planner.test.ts` finds schema-rejection coverage
    - `rg -n "repairAttempt|maxRepairAttempts|1" packages/server/src/initial-generation/planner.ts` finds the single repair budget
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/planner.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The planning stage is strict, typed, and quality-gated: it produces a validated `ProjectPlan`, optionally repairs it once, and never asks the user questions or spins in a repair loop.
  </done>
</task>

</tasks>

<verification>
Confirm the planner uses brief/domain-reference injection instead of templates, validates the full `ProjectPlan` contract before compile, and implements the one-shot accept-or-repair quality gate.
</verification>

<success_criteria>
Phase 41 has a complete planning stage: domain-guided prompt assembly, strict `ProjectPlan` validation, explicit quality verdicts, and a capped repair loop that produces deterministic planner output for the compiler.
</success_criteria>

<output>
After completion, create `.planning/phases/41-initial-gen-refactor/41-02-SUMMARY.md`
</output>
