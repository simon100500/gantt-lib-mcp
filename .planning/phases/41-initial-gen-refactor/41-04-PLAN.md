---
phase: 41-initial-gen-refactor
plan: 04
type: execute
wave: 3
depends_on: ["41-02", "41-03"]
files_modified:
  - packages/server/src/agent.ts
  - packages/server/src/agent.test.ts
  - packages/server/src/debug-log.ts
  - packages/server/src/initial-generation/orchestrator.ts
  - packages/server/src/initial-generation/orchestrator.test.ts
  - .planning/phases/41-initial-gen-refactor/docs.md
autonomous: true
requirements: [IGR-04]

must_haves:
  truths:
    - "A single initial-generation run can be reconstructed end to end from route selection through final acceptance/rejection"
    - "The full pipeline runs as planning -> quality verdict -> optional repair -> deterministic compile/commit, without a hidden fallback into ordinary mutation flow"
    - "Automated tests cover routing, model selection, planning/quality outcomes, compile/salvage outcomes, and log payloads"
    - "Phase-local docs include manual verification scenarios for broad and specific construction prompts, including `Построй график`"
  artifacts:
    - path: "packages/server/src/initial-generation/orchestrator.ts"
      provides: "End-to-end orchestrator that sequences planner, quality gate, repair, compiler, and commit"
      exports: ["runInitialGeneration"]
    - path: "packages/server/src/initial-generation/orchestrator.test.ts"
      provides: "End-to-end orchestration and observability regression coverage"
      contains: "model_routing_decision"
    - path: "packages/server/src/agent.ts"
      provides: "Production server entrypoint wired to the completed initial-generation pipeline"
      contains: "initial_generation_result"
    - path: ".planning/phases/41-initial-gen-refactor/docs.md"
      provides: "Manual validation checklist and prompt matrix for the new initial-generation flow"
      contains: "Построй график"
  key_links:
    - from: "packages/server/src/initial-generation/orchestrator.ts"
      to: "packages/server/src/initial-generation/planner.ts"
      via: "Planning and one-shot repair stage"
      pattern: "planInitialProject"
    - from: "packages/server/src/initial-generation/orchestrator.ts"
      to: "packages/server/src/initial-generation/executor.ts"
      via: "Deterministic compile/commit stage"
      pattern: "executeInitialProjectPlan"
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/debug-log.ts"
      via: "Lifecycle event emission"
      pattern: "writeServerDebugLog"
---

<objective>
Wire the full initial-generation pipeline, observability, and verification layer.

Purpose: The phase is only shippable when the route shell, planner, and compiler run as one lifecycle with reconstructable logs and concrete regression/manual checks. This plan finishes the production wiring and the verification surface.
Output: Completed orchestrator wiring, expanded lifecycle logging, end-to-end tests, and a phase-local manual verification guide.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-initial-gen-refactor/41-CONTEXT.md
@INITIAL-GENERATION-REFACTOR-PLAN.md
@packages/server/src/agent.ts
@packages/server/src/debug-log.ts
@packages/server/src/initial-generation/orchestrator.ts
@packages/server/src/initial-generation/planner.ts
@packages/server/src/initial-generation/executor.ts

<interfaces>
```typescript
export async function writeServerDebugLog(
  event: string,
  payload: Record<string, unknown> = {},
): Promise<void>
```

```typescript
type ModelRoutingDecision = {
  route: 'initial_generation' | 'mutation';
  tier: 'strong' | 'cheap' | 'main_fallback';
  selectedModel: string;
  reason: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the completed planning-to-compile pipeline and lifecycle logs into the server agent</name>
  <files>packages/server/src/initial-generation/orchestrator.ts, packages/server/src/agent.ts, packages/server/src/debug-log.ts, packages/server/src/initial-generation/orchestrator.test.ts</files>
  <read_first>
    packages/server/src/initial-generation/orchestrator.ts
    packages/server/src/agent.ts
    packages/server/src/debug-log.ts
    packages/server/src/initial-generation/orchestrator.test.ts
    packages/server/src/initial-generation/planner.ts
    packages/server/src/initial-generation/executor.ts
    packages/server/src/initial-generation/types.ts
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
  </read_first>
  <behavior>
    - `runInitialGeneration()` performs planning, quality verdict, optional repair, deterministic execution, and final assistant response assembly
    - Logs include route selection, object-type inference, model tier, planning output, quality verdict, repair reasons, compile verdict, batch size/task count, dropped nodes/links, and final acceptance/rejection
    - `agent.ts` uses the completed orchestrator for the `initial_generation` branch and ordinary mutation flow remains intact for non-initial requests
    - Partial schedule success is communicated as a partial starter schedule without exposing compiler jargon
  </behavior>
  <action>
    Finish the production wiring per IGR-04:

    1. Implement `runInitialGeneration()` in `orchestrator.ts`
       - call `buildGenerationBrief` / `resolveDomainReference`
       - plan with the strong-model routing decision
       - evaluate the quality verdict and run at most one repair
       - compile and execute through `executeInitialProjectPlan`
       - add the assistant message to history and broadcast tasks on success
       - return a controlled assistant error when planning or compile/commit fails
    2. Update `agent.ts`
       - replace the placeholder initial-generation shell with the real orchestrator dependencies
       - keep fast-shift and ordinary mutation behavior unchanged
       - ensure mutation flow still uses cheap-model routing where available
    3. Extend logging
       - use `writeServerDebugLog()` events named `route_selection`, `object_type_inference`, `model_routing_decision`, `planning_output`, `plan_quality_verdict`, `plan_repair_requested`, `compile_verdict`, `initial_generation_result`
       - keep payloads typed and reconstructable
    4. `orchestrator.test.ts`
       - test the happy path, one-repair path, partial-success path, and controlled-rejection path
       - assert emitted log payloads include the event names above

    Do not add evaluator-LLM stages or a fallback into ordinary mutation-agent execution.
  </action>
  <acceptance_criteria>
    - `rg -n "route_selection|object_type_inference|model_routing_decision|planning_output|plan_quality_verdict|plan_repair_requested|compile_verdict|initial_generation_result" packages/server/src/initial-generation/orchestrator.ts packages/server/src/agent.ts` finds the required lifecycle events
    - `rg -n "executeInitialProjectPlan|planInitialProject" packages/server/src/initial-generation/orchestrator.ts` finds the two-stage orchestration calls
    - `rg -n "partial starter schedule|частично" packages/server/src/initial-generation/orchestrator.ts` finds partial-success user messaging
    - `rg -n "cheap model|main_fallback|mutation" packages/server/src/agent.ts` finds preserved ordinary-flow routing semantics
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/orchestrator.test.ts packages/server/src/agent.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The completed Phase 41 lifecycle is wired into production, fully logged, and covered by end-to-end orchestration tests without reopening the route or compiler contracts.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lock in the regression surface and manual validation checklist for Phase 41</name>
  <files>packages/server/src/agent.test.ts, packages/server/src/initial-generation/orchestrator.test.ts, .planning/phases/41-initial-gen-refactor/docs.md</files>
  <read_first>
    packages/server/src/agent.test.ts
    packages/server/src/initial-generation/orchestrator.test.ts
    .planning/phases/41-initial-gen-refactor/docs.md
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
  </read_first>
  <behavior>
    - Automated tests cover route selection, model routing, prompt/reference injection, schema rejection, quality-gate accept/repair, compile/salvage, and log payloads
    - Phase-local docs provide exact manual scenarios for broad and specific construction prompts
    - The docs explicitly include the vague prompt `Построй график` and expected behavior without clarifying questions
  </behavior>
  <action>
    Finalize the verification layer per IGR-04:

    1. Expand `agent.test.ts` / `orchestrator.test.ts` so the combined suite covers:
       - empty broad request -> `initial_generation`
       - ordinary edit -> mutation flow
       - strong-model vs cheap-model routing
       - recognized and fallback domain-reference injection
       - ProjectPlan schema rejection
       - quality gate accept vs one-shot repair
       - deterministic compile full success vs partial salvage vs controlled failure
       - required lifecycle log payloads
    2. Create `.planning/phases/41-initial-gen-refactor/docs.md` with a manual verification matrix for:
       - `Построй типичный график строительства`
       - `Построй график строительства детского садика`
       - `Построй график ремонта офиса 300 м2`
       - `Построй график строительства частного дома из газобетона`
       - `Построй график`
       - expected checks: subject specificity, hierarchy depth, realistic sequence, no clarifying question, partial-success wording if salvage occurs
    3. Include a short note in docs that this phase intentionally does not add new MCP tools and does not fall back to normal mutation-agent execution for failed initial generation.
  </action>
  <acceptance_criteria>
    - `rg -n "initial_generation|main_fallback|schema rejection|repair|partial salvage|compile verdict" packages/server/src/agent.test.ts packages/server/src/initial-generation/orchestrator.test.ts` finds the regression matrix
    - `rg -n "Построй типичный график строительства|Построй график строительства детского садика|Построй график ремонта офиса 300 м2|Построй график строительства частного дома из газобетона|Построй график" .planning/phases/41-initial-gen-refactor/docs.md` finds all manual scenarios
    - `rg -n "no new MCP tools|does not fall back|не задает уточня" .planning/phases/41-initial-gen-refactor/docs.md` finds the locked scope guardrails
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/initial-generation/orchestrator.test.ts packages/server/src/agent.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Phase 41 has a durable verification surface: the critical lifecycle has regression tests, and the phase directory includes a manual checklist that exercises both broad and specific construction prompts without ambiguity.
  </done>
</task>

</tasks>

<verification>
Confirm the completed orchestrator emits reconstructable logs, the route/model/planner/compiler regressions are covered in automated tests, and the phase directory includes the exact manual prompts required by the PRD-derived context.
</verification>

<success_criteria>
Phase 41 is executable end to end: the server can route an empty broad request through the new AI-first initial-generation lifecycle, operators can reconstruct a run from logs, and both automated and manual verification surfaces are present.
</success_criteria>

<output>
After completion, create `.planning/phases/41-initial-gen-refactor/41-04-SUMMARY.md`
</output>
