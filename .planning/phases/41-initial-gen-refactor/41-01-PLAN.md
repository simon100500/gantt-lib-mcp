---
phase: 41-initial-gen-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/agent.ts
  - packages/server/src/agent.test.ts
  - packages/server/src/initial-generation/types.ts
  - packages/server/src/initial-generation/route-selection.ts
  - packages/server/src/initial-generation/model-routing.ts
  - packages/server/src/initial-generation/orchestrator.ts
autonomous: true
requirements: [IGR-01]

must_haves:
  truths:
    - "Empty-project broad generation requests no longer use `initial_schedule_template` or any equivalent deterministic content shortcut"
    - "The server has a first-class internal route named `initial_generation` that is selected from request class plus empty-project state"
    - "Ordinary follow-up edits stay on the existing mutation flow and can use the cheap model when configured"
    - "Model choice is decided before each SDK run and recorded as a typed routing decision"
  artifacts:
    - path: "packages/server/src/initial-generation/types.ts"
      provides: "Typed contracts for initial-generation routing and orchestration"
      exports: ["GenerationBrief", "ProjectPlan", "PlanQualityVerdict", "RepairReason", "ModelRoutingDecision", "CompiledInitialSchedule"]
    - path: "packages/server/src/initial-generation/route-selection.ts"
      provides: "Initial-generation route classifier detached from template fast-path logic"
      exports: ["selectAgentRoute"]
    - path: "packages/server/src/initial-generation/model-routing.ts"
      provides: "Strong-vs-cheap model selection helper with typed decision payload"
      exports: ["resolveModelRoutingDecision"]
    - path: "packages/server/src/agent.ts"
      provides: "Main server agent entrypoint that delegates empty broad bootstrap requests to `initial_generation`"
      contains: "initial_generation"
  key_links:
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/initial-generation/route-selection.ts"
      via: "Route decision before any fast path or SDK run"
      pattern: "selectAgentRoute"
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/initial-generation/model-routing.ts"
      via: "Pre-run model selection for initial vs mutation flows"
      pattern: "resolveModelRoutingDecision"
---

<objective>
Create the interface-first routing shell for Phase 41.

Purpose: Phase 41 cannot be parallelized safely until routing, typed contracts, and model-selection boundaries are explicit. This plan removes the locked-out template shortcut and establishes the `initial_generation` entrypoint that later plans will fill in.
Output: Initial-generation type contracts, route-selection/model-routing helpers, orchestrator skeleton, and `agent.ts` routing/tests updated to use them.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-initial-gen-refactor/41-CONTEXT.md
@INITIAL-GENERATION-REFACTOR-PLAN.md
@packages/server/src/agent.ts
@packages/server/src/agent.test.ts
@packages/mcp/agent/agent.ts

<interfaces>
```typescript
function resolveEnv(): {
  OPENAI_API_KEY: string;
  OPENAI_BASE_URL: string;
  OPENAI_MODEL: string;
}
```

```typescript
export type CommitProjectCommandRequest = {
  projectId: string;
  clientRequestId: string;
  baseVersion: number;
  command: ProjectCommand;
};
```

```typescript
type InitialScheduleTemplateIntent = {
  domain: 'construction';
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define initial-generation routing and orchestration contracts</name>
  <files>packages/server/src/initial-generation/types.ts, packages/server/src/initial-generation/route-selection.ts, packages/server/src/initial-generation/model-routing.ts, packages/server/src/initial-generation/orchestrator.ts</files>
  <read_first>
    packages/server/src/initial-generation/types.ts
    packages/server/src/initial-generation/route-selection.ts
    packages/server/src/initial-generation/model-routing.ts
    packages/server/src/initial-generation/orchestrator.ts
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
    packages/server/src/agent.ts
    packages/mcp/agent/agent.ts
  </read_first>
  <behavior>
    - `selectAgentRoute()` returns a typed route result that distinguishes `initial_generation` from ordinary mutation flow using empty-project state plus request class
    - `resolveModelRoutingDecision()` returns `strong` for initial-generation planning and `cheap` or `main_fallback` for ordinary mutation flow
    - `types.ts` exports `GenerationBrief`, `ProjectPlan`, `PlanQualityVerdict`, `RepairReason`, `ModelRoutingDecision`, and `CompiledInitialSchedule`
    - `orchestrator.ts` exports a typed `runInitialGeneration(...)` entrypoint that later plans can implement without changing callers
  </behavior>
  <action>
    Create `packages/server/src/initial-generation/` and add four interface-first modules per IGR-01:

    1. `types.ts`
       - define `GenerationMode = 'initial_generation' | 'mutation'`
       - define `GenerationBrief`, `ProjectPlan`, `ProjectPlanNode`, `ProjectPlanDependency`, `PlanQualityVerdict`, `RepairReason`, `ModelRoutingDecision`, `CompiledInitialSchedule`
       - include locked enum/string literals: `phase`, `task`, `FS`, `SS`, `FF`, `SF`
    2. `route-selection.ts`
       - export `selectAgentRoute(input)` where input includes `userMessage`, `taskCount`, `hasHierarchy`, and a broad-request classification result
       - hardcode the route literal `initial_generation`
       - do not expose any `initial_schedule_template` naming
    3. `model-routing.ts`
       - export `resolveModelRoutingDecision({ route, env })`
       - read `OPENAI_MODEL` / `model` as strong model
       - read `OPENAI_CHEAP_MODEL` / `cheap_model` as optional cheap model
       - emit explicit decision reasons such as `initial_generation_requires_strong_model` and `cheap_model_missing_fallback_to_main`
    4. `orchestrator.ts`
       - export a typed `runInitialGeneration(...)` function stub returning `Promise<never>` or a typed placeholder rejection
       - keep the signature stable for later plans by including `projectId`, `sessionId`, `runId`, `userMessage`, `tasksBefore`, and service/logger dependencies

    Add unit coverage for route/model helpers in the plan-owned test surface or in `agent.test.ts`, but keep compiler/planner behavior out of this plan.
  </action>
  <acceptance_criteria>
    - `rg -n "export type GenerationMode = 'initial_generation' \\| 'mutation'" packages/server/src/initial-generation/types.ts` finds the route-mode contract
    - `rg -n "export function selectAgentRoute" packages/server/src/initial-generation/route-selection.ts` finds the route selector
    - `rg -n "OPENAI_CHEAP_MODEL|cheap_model" packages/server/src/initial-generation/model-routing.ts` finds cheap-model fallback handling
    - `rg -n "export async function runInitialGeneration|export function runInitialGeneration" packages/server/src/initial-generation/orchestrator.ts` finds the orchestrator entrypoint
    - `rg -n "initial_schedule_template" packages/server/src/initial-generation` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Contract files exist, export the locked Phase 41 types, and provide stable interfaces that downstream plans can implement without changing the main server agent call site.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the template fast path and route empty broad requests into the new initial-generation shell</name>
  <files>packages/server/src/agent.ts, packages/server/src/agent.test.ts</files>
  <read_first>
    packages/server/src/agent.ts
    packages/server/src/agent.test.ts
    packages/server/src/initial-generation/types.ts
    packages/server/src/initial-generation/route-selection.ts
    packages/server/src/initial-generation/model-routing.ts
    packages/server/src/initial-generation/orchestrator.ts
    .planning/phases/41-initial-gen-refactor/41-CONTEXT.md
    INITIAL-GENERATION-REFACTOR-PLAN.md
  </read_first>
  <behavior>
    - `agent.ts` no longer exposes or calls `parseInitialScheduleTemplateIntent()` / `tryInitialScheduleTemplateFastPath()` for broad initial generation
    - Empty-project broad generation requests call the `initial_generation` orchestrator shell instead of the ordinary mutation pipeline
    - Normal follow-up edits keep the existing mutation flow and can route to the cheap model when configured
    - Route selection and model selection produce typed debug-log payloads before the SDK run starts
  </behavior>
  <action>
    Refactor `packages/server/src/agent.ts` per IGR-01:

    1. Delete the `InitialScheduleTemplateIntent` type, `parseInitialScheduleTemplateIntent()`, `buildTypicalConstructionTemplate()`, `buildInitialConstructionTemplateMessage()`, and `tryInitialScheduleTemplateFastPath()` branch.
    2. Import `selectAgentRoute`, `resolveModelRoutingDecision`, and `runInitialGeneration`.
    3. At the top of `runAgentWithHistory()`:
       - compute empty-project state from `tasksBefore.length === 0`
       - call `selectAgentRoute({ userMessage, taskCount: tasksBefore.length, hasHierarchy: tasksBefore.some(...) })`
       - log `route_selection` with the chosen route and the classifier signals
    4. If the route is `initial_generation`, call `runInitialGeneration(...)` and return immediately; do not fall through to the template or ordinary mutation path.
    5. For ordinary mutation flow:
       - call `resolveModelRoutingDecision({ route: 'mutation', env })`
       - pass the chosen model into the SDK query options instead of always using `env.OPENAI_MODEL`
       - log a `model_routing_decision` event payload before starting the run
    6. Update `agent.test.ts` so route-selection tests now assert:
       - broad empty-project prompts go to `initial_generation`
       - vague prompt `Построй график` is treated as generation, not a clarifying-question trigger
       - ordinary edit prompts stay on mutation flow
       - cheap-model fallback behavior is deterministic when `OPENAI_CHEAP_MODEL` is missing

    Keep fast-shift behavior intact. Do not add any new MCP tool surface.
  </action>
  <acceptance_criteria>
    - `rg -n "tryInitialScheduleTemplateFastPath|parseInitialScheduleTemplateIntent|buildTypicalConstructionTemplate" packages/server/src/agent.ts` returns no matches
    - `rg -n "route_selection|model_routing_decision" packages/server/src/agent.ts` finds both debug events
    - `rg -n "runInitialGeneration" packages/server/src/agent.ts` finds the initial-generation delegation
    - `rg -n "OPENAI_CHEAP_MODEL|cheap_model" packages/server/src/agent.ts` finds cheap-model routing usage in the ordinary mutation path
    - `rg -n "Построй график|initial_generation|cheap_model_missing_fallback_to_main" packages/server/src/agent.test.ts` finds the new regression coverage
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The deterministic template shortcut is gone, routing decisions are explicit and typed, and the server has a stable `initial_generation` branch that later plans can implement without reopening route semantics.
  </done>
</task>

</tasks>

<verification>
Confirm the template fast path is fully removed, the route/model helpers exist as concrete modules, and the server still builds after `agent.ts` switches to the new interfaces.
</verification>

<success_criteria>
Phase 41 has an interface-first shell: `initial_generation` exists as a first-class route, cheap-vs-strong model routing is explicit, and there is no remaining broad-request content template shortcut in the main server agent.
</success_criteria>

<output>
After completion, create `.planning/phases/41-initial-gen-refactor/41-01-SUMMARY.md`
</output>
