---
phase: 24-astro-site-foundation
plan: 03
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - packages/site/src/components/Hero.astro
  - packages/site/src/components/RotatingWords.tsx
  - packages/site/src/pages/index.astro
  - packages/site/src/components/InputDemo.tsx
  - packages/site/src/components/GanttPreview.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
must_haves:
  truths:
    - "User can see hero section with tag ('Новый способ планировать проекты')"
    - "User can see rotating value props ('Из [текста/сметы/брифа/ТЗ/письма/таблицы] → Гантт за 30 секунд')"
    - "User can watch rotating words animation (slide up/down with fade, 2500ms interval)"
    - "User can see hero subheading ('Вставьте любой документ...')"
    - "User can click 'Попробовать бесплатно' CTA → redirects to ai.getgantt.ru"
    - "User can click 'Смотреть демо' CTA → scrolls to demo section"
    - "User can see input demo card with typewriter animation"
    - "User can see gantt preview card with grow-in animation"
    - "All animations respect prefers-reduced-motion"
    - "Hero section is responsive (mobile: 48px padding, tablet: 72px, desktop: 96px)"
  artifacts:
    - path: "packages/site/src/components/Hero.astro"
      provides: "Hero section with tag, h1 with rotating words, subheading, CTAs"
      contains: "hero-title with rotating value props"
    - path: "packages/site/src/components/RotatingWords.tsx"
      provides: "React component for rotating word animation (slide up/down)"
      exports: ["RotatingWords"]
    - path: "packages/site/src/components/InputDemo.tsx"
      provides: "React island for typewriter demo animation"
      exports: ["InputDemo"]
    - path: "packages/site/src/components/GanttPreview.tsx"
      provides: "React island for gantt preview with grow-in bars"
      exports: ["GanttPreview"]
    - path: "packages/site/src/pages/index.astro"
      provides: "Homepage with Hero section and demo cards"
      contains: "Hero.astro import and slot placement"
  key_links:
    - from: "packages/site/src/components/Hero.astro"
      to: "packages/site/src/components/RotatingWords.tsx"
      via: "client:load directive for React island"
      pattern: "RotatingWords.*client:load"
    - from: "packages/site/src/components/InputDemo.tsx"
      to: "packages/site/src/components/GanttPreview.tsx"
      via: "Typing animation triggers gantt preview grow-in"
      pattern: "animation-delay.*stagger"
    - from: "packages/site/src/pages/index.astro"
      to: "packages/site/src/components/Hero.astro"
      via: "Component import and placement in main"
      pattern: "import.*Hero.*from"
---

<objective>
Create Hero section with rotating value props animation, typewriter input demo, and gantt preview card with grow-in animation.

Purpose: Implement the core marketing hero that converts visitors. This is Wave 3 because it depends on Plan 02 (Layout with Header/Footer) and provides the main landing page experience.

Output: Working hero section with all animations (rotating words, typewriter, grow-in), responsive layout, CTAs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-astro-site-foundation/24-CONTEXT.md
@.planning/phases/24-astro-site-foundation/24-UI-SPEC.md
@.planning/reference/gantt-hero.html

@packages/site/src/layouts/Layout.astro (from 24-02)
@packages/site/src/components/Header.astro (from 24-02)
@packages/site/src/components/Footer.astro (from 24-02)
@packages/site/tailwind.config.js (from 24-01)
</context>

# Context Summary

**Locked decisions from 24-CONTEXT.md:**
- D-01: Stacked hero layout (title top, AI demo bottom)
- D-02: 3 rotating value props ("Из текста/сметы/ТЗ → Гантт за 30 секунд")
- D-03: Typewriter → fade-in animation pattern
- D-04: Social proof moved to Phase 26 (NOT in hero)
- D-05: Primary CTA "Попробовать бесплатно" (indigo → ai.getgantt.ru), Secondary "Смотреть демо" (outline → scroll to demo)

**UI-SPEC requirements:**
- Tag: pill with green dot + "Новый способ планировать проекты"
- h1: clamp(48px, 7vw, 84px), letter-spacing -3px, weight 800
- Rotating words: текста, сметы, брифа, ТЗ, письма, таблицы (6 variants)
- Animation: slide up/down with fade, 420ms transition, 2500ms interval
- Subheading: 17px, line-height 1.65
- CTAs: primary (blue) + secondary (white outline)
- Input demo: typewriter animation (28ms ± 22ms random), 3 texts rotate
- Gantt preview: 5 bars with grow-in (600ms, staggered delays 0.5s-1.1s)
- Hero padding: 96px top, 72px bottom (desktop), 72px/48px (tablet/mobile)
- Max-width: 900px (hero), 700px (demo cards)

**Reference:** gantt-hero.html lines 82-456 (hero section), 520-633 (JavaScript animations)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RotatingWords React component</name>
  <files>packages/site/src/components/RotatingWords.tsx</files>
  <read_first>
    - D:/Projects/gantt-lib-mcp/.planning/reference/gantt-hero.html (lines 108-180, 520-581 for rotation logic)
    - D:/Projects/gantt-lib-mcp/.planning/phases/24-astro-site-foundation/24-UI-SPEC.md (Rotating Value Props animation spec)
  </read_first>
  <action>
Create React component for rotating word animation:

1. Create packages/site/src/components/RotatingWords.tsx:
```tsx
import { useEffect, useRef, useState } from 'react';

interface RotatingWordsProps {
  words: string[];
  interval?: number; // ms between rotations
  className?: string;
}

export default function RotatingWords({
  words,
  interval = 2500,
  className = '',
}: RotatingWordsProps) {
  const [currentIndex, setCurrentIndex] = useState(0);
  const [nextIndex, setNextIndex] = useState(1);
  const [width, setWidth] = useState(0);
  const slotRef = useRef<HTMLSpanElement>(null);
  const measurerRef = useRef<HTMLSpanElement>(null);

  // Measure word widths
  const getWordWidth = (word: string) => {
    if (!measurerRef.current) return 0;
    measurerRef.current.textContent = word;
    return measurerRef.current.offsetWidth;
  };

  useEffect(() => {
    // Set initial width
    if (slotRef.current && measurerRef.current) {
      const initialWidth = getWordWidth(words[0]);
      setWidth(initialWidth);
      slotRef.current.style.width = `${initialWidth}px`;
    }
  }, [words]);

  useEffect(() => {
    const timer = setInterval(() => {
      setCurrentIndex((prev) => (prev + 1) % words.length);
      setNextIndex((prev) => (prev + 1) % words.length);
    }, interval);

    return () => clearInterval(timer);
  }, [words.length, interval]);

  // Animate width when word changes
  useEffect(() => {
    if (slotRef.current) {
      const newWidth = getWordWidth(words[currentIndex]);
      setWidth(newWidth);
      slotRef.current.style.width = `${newWidth}px`;
    }
  }, [currentIndex, words]);

  return (
    <>
      {/* Hidden measurer for width calculation */}
      <span
        ref={measurerRef}
        className="absolute -left-[9999px] top-0 font-extrabold"
        style={{
          fontFamily: 'Noto Sans, sans-serif',
          fontSize: 'clamp(48px, 7vw, 84px)',
          fontStyle: 'normal',
          fontWeight: 800,
        }}
      >
        {words[currentIndex]}
      </span>

      {/* Word slot with animated width */}
      <span
        ref={slotRef}
        className={`relative inline-block overflow-hidden align-baseline transition-all duration-[350ms] ease-out ${className}`}
        style={{ width: `${width}px` }}
      >
        {/* Current word (visible) */}
        <span
          key={currentIndex}
          className="block animate-word-in text-primary"
          style={{
            fontFamily: 'Noto Sans, sans-serif',
            fontSize: 'clamp(48px, 7vw, 84px)',
            fontStyle: 'normal',
            fontWeight: 800,
          }}
        >
          {words[currentIndex]}
        </span>

        {/* Previous word (exiting) */}
        {currentIndex > 0 && (
          <span
            key={currentIndex - 1}
            className="absolute left-0 top-0 block animate-word-out text-primary"
            style={{
              fontFamily: 'Noto Sans, sans-serif',
              fontSize: 'clamp(48px, 7vw, 84px)',
              fontStyle: 'normal',
              fontWeight: 800,
            }}
          >
            {words[currentIndex - 1]}
          </span>
        )}
      </span>
    </>
  );
}
```

2. Add animations to global.css (packages/site/src/styles/global.css):
```css
@layer utilities {
  /* Word rotate in (slide up from bottom) */
  @keyframes word-in {
    from {
      transform: translateY(110%);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  .animate-word-in {
    animation: word-in 0.42s cubic-bezier(0.4, 0, 0.2, 1) both;
  }

  /* Word rotate out (slide up to top) */
  @keyframes word-out {
    from {
      transform: translateY(0);
      opacity: 1;
    }
    to {
      transform: translateY(-110%);
      opacity: 0;
    }
  }

  .animate-word-out {
    animation: word-out 0.42s cubic-bezier(0.4, 0, 0.2, 1) both;
  }

  /* Respect reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .animate-word-in,
    .animate-word-out {
      animation: none;
    }
  }
}
```
  </action>
  <verify>
    <automated>cd packages/site && grep -q "animate-word-in" src/styles/global.css && grep -q "RotatingWords" src/components/RotatingWords.tsx</automated>
  </verify>
  <done>
    - RotatingWords.tsx component with words prop and interval
    - Word width measurement using hidden measurer span
    - Slide up/down animation (translateY ±110%)
    - Width animation (350ms cubic-bezier)
    - Reduced motion support
    - Animations added to global.css
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Hero section component</name>
  <files>packages/site/src/components/Hero.astro</files>
  <read_first>
    - D:/Projects/gantt-lib-mcp/.planning/reference/gantt-hero.html (lines 82-240, 407-456 for hero HTML/CSS)
    - D:/Projects/gantt-lib-mcp/.planning/phases/24-astro-site-foundation/24-UI-SPEC.md (Hero Section spec)
    - D:/Projects/gantt-lib-mcp/.planning/phases/24-astro-site-foundation/24-CONTEXT.md (D-01 through D-05)
  </read_first>
  <action>
Create Hero section with tag, rotating words, subheading, CTAs:

1. Create packages/site/src/components/Hero.astro:
```astro
---
// Hero.astro — Main hero section with rotating value props and CTAs
import RotatingWords from './RotatingWords';
---

<section class="relative mx-auto flex max-w-[900px] flex-col items-center px-4 py-12 text-center md:py-16 lg:py-24">
  {/* Tag */}
  <div class="mb-9 inline-flex items-center gap-[6px] rounded-full border border-border bg-card px-3 py-1 text-[12px] font-semibold text-secondary-foreground animate-fade-up">
    <div class="h-1.5 w-1.5 rounded-full bg-green-500 shadow-[0_0_0_2px_rgba(220,252,231,1)]"></div>
    Новый способ планировать проекты
  </div>

  {/* h1 with rotating words */}
  <h1 class="mb-7 flex w-full flex-col items-center gap-0 font-extrabold leading-none text-foreground animate-fade-up" style="animation-delay: 70ms; font-family: 'Noto Sans', sans-serif; font-size: clamp(48px, 7vw, 84px); letter-spacing: -3px;">
    {/* First line: Из [word] — в Гантт */}
    <div class="flex flex-wrap items-center justify-center gap-[18px]">
      <span class="whitespace-nowrap">Из</span>

      <RotatingWords
        words={['текста', 'сметы', 'брифа', 'ТЗ', 'письма', 'таблицы']}
        client:load
      />

      <span class="whitespace-nowrap">— в Гантт</span>
    </div>

    {/* Second line */}
    <span class="mt-1 block">за 30 секунд</span>
  </h1>

  {/* Subheading */}
  <p class="mb-7 max-w-[440px] text-[17px] leading-[1.65] text-secondary-foreground animate-fade-up" style="animation-delay: 140px;">
    Вставьте любой документ или напишите своими словами.<br />
    Получите <strong class="font-bold text-foreground">готовый график</strong> с задачами, сроками и командой.
  </p>

  {/* CTAs */}
  <div class="mb-8 flex flex-wrap justify-center gap-2.5 animate-fade-up" style="animation-delay: 210ms;">
    <a
      href="https://ai.getgantt.ru"
      class="inline-flex items-center justify-center rounded-[10px] bg-primary px-7 py-3.5 text-[15px] font-bold text-primary-foreground transition-all hover:opacity-90 hover:shadow-[0_10px_28px_rgba(29,78,216,0.28)] hover:-translate-y-0.5"
    >
      Попробовать бесплатно →
    </a>
    <button
      id="watch-demo-btn"
      class="inline-flex items-center justify-center rounded-[10px] border border-border bg-card px-7 py-3.5 text-[15px] font-bold text-foreground transition-all hover:bg-border"
    >
      Смотреть демо
    </button>
  </div>
</section>

<script>
  // "Смотреть демо" button scrolls to demo section
  const demoBtn = document.getElementById('watch-demo-btn');
  demoBtn?.addEventListener('click', () => {
    const demoSection = document.getElementById('demo-section');
    demoSection?.scrollIntoView({ behavior: 'smooth' });
  });
</script>
```

2. Note: Stagger delays use CSS animation-delay (70ms, 140ms, 210ms)
  </action>
  <verify>
    <automated>cd packages/site && grep -q "Новый способ планировать проекты" src/components/Hero.astro && grep -q "Попробовать бесплатно" src/components/Hero.astro</automated>
  </verify>
  <done>
    - Hero.astro has tag with green dot
    - h1 with "Из [rotating words] — в Гантт" + "за 30 секунд"
    - Subheading with "Вставьте любой документ..."
    - Two CTAs: "Попробовать бесплатно" (primary → ai.getgantt.ru), "Смотреть демо" (outline → scroll)
    - Responsive padding: 48px (mobile), 64px (tablet), 96px (desktop)
    - Max-width 900px centered
    - Fade-up animations with stagger delays
  </done>
</task>

<task type="auto">
  <name>Task 3: Create InputDemo React island (typewriter animation)</name>
  <files>packages/site/src/components/InputDemo.tsx</files>
  <read_first>
    - D:/Projects/gantt-lib-mcp/.planning/reference/gantt-hero.html (lines 242-298, 458-472, 583-621 for typewriter logic)
    - D:/Projects/gantt-lib-mcp/.planning/phases/24-astro-site-foundation/24-UI-SPEC.md (Input Demo Card spec)
  </read_first>
  <action>
Create React island for typewriter demo animation:

1. Create packages/site/src/components/InputDemo.tsx:
```tsx
import { useEffect, useRef, useState } from 'react';

const TEXTS = [
  'Разработка мобильного приложения для доставки еды. Старт 1 марта, дедлайн 30 мая. Команда: 2 разработчика, дизайнер, QA.',
  'Смета на ремонт офиса: демонтаж — 2 нед, отделка — 3 нед, мебель — 1 нед. Бюджет 2.4 млн.',
  'ТЗ на редизайн сайта. Этапы: аудит, прототип, дизайн, вёрстка, тестирование, запуск. 8 недель.',
];

const TYPING_SPEED = 28; // ms per character (base)
const TYPING_VARIANCE = 22; // ms random variance
const PAUSE_AT_END = 2200; // ms before erasing
const ERASE_SPEED = 12; // ms per character when erasing
const PAUSE_BETWEEN = 400; // ms between texts

export default function InputDemo() {
  const [text, setText] = useState('');
  const [isTyping, setIsTyping] = useState(true);
  const textIndex = useRef(0);
  const charIndex = useRef(0);
  const timeoutRef = useRef<NodeJS.Timeout>();

  useEffect(() => {
    const currentText = TEXTS[textIndex.current];

    function typeStep() {
      if (isTyping) {
        // Typing
        charIndex.current++;
        setText(currentText.slice(0, charIndex.current));

        if (charIndex.current >= currentText.length) {
          // Finished typing, pause
          setIsTyping(false);
          timeoutRef.current = setTimeout(typeStep, PAUSE_AT_END);
          return;
        }

        // Continue typing with random variance
        const delay = TYPING_SPEED + Math.random() * TYPING_VARIANCE;
        timeoutRef.current = setTimeout(typeStep, delay);
      } else {
        // Erasing
        charIndex.current--;
        setText(currentText.slice(0, charIndex.current));

        if (charIndex.current <= 0) {
          // Finished erasing, move to next text
          setIsTyping(true);
          textIndex.current = (textIndex.current + 1) % TEXTS.length;
          timeoutRef.current = setTimeout(typeStep, PAUSE_BETWEEN);
          return;
        }

        // Continue erasing
        timeoutRef.current = setTimeout(typeStep, ERASE_SPEED);
      }
    }

    // Start typing after initial delay
    timeoutRef.current = setTimeout(typeStep, 800);

    return () => {
      if (timeoutRef.current) clearTimeout(timeoutRef.current);
    };
  }, [isTyping]);

  return (
    <div className="relative mx-auto mt-12 max-w-[700px] animate-fade-up px-4 md:px-8" style={{ animationDelay: '350ms' }}>
      <div className="overflow-hidden rounded-[14px] border border-border bg-card shadow-[0_4px_24px_rgba(0,0,0,0.06),0_1px_3px_rgba(0,0,0,0.04)]">
        {/* Top: avatar + typing text */}
        <div className="flex items-start gap-2.5 border-b border-border bg-background p-3.5 px-4">
          <div className="flex shrink-0 items-center justify-center rounded-lg bg-primary text-[13px] font-extrabold text-primary-foreground" style={{ width: 32, height: 32 }}>
            G
          </div>
          <div className="pt-1 text-[14px] leading-[1.5] text-secondary-foreground" style={{ fontFamily: 'Noto Sans, sans-serif' }}>
            {text}
            <span className="ml-0.5 inline-block h-3.5 w-0.5 bg-primary animate-blink"></span>
          </div>
        </div>

        {/* Bottom: hint + button */}
        <div className="flex items-center justify-between bg-muted px-4 py-2.5">
          <span className="text-[12px] text-muted-foreground">
            Поддерживает PDF, DOCX, Excel, текст
          </span>
          <button className="inline-flex items-center gap-1.5 rounded-[7px] bg-primary px-4 py-1.5 text-[13px] font-extrabold text-primary-foreground transition-all hover:opacity-90 hover:-translate-y-0.5">
            Создать Гантт →
          </button>
        </div>
      </div>
    </div>
  );
}
```

2. Add blink animation to global.css:
```css
@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0; }
}

.animate-blink {
  animation: blink 1s step-end infinite;
}

@media (prefers-reduced-motion: reduce) {
  .animate-blink {
    animation: none;
  }
}
```
  </action>
  <verify>
    <automated>cd packages/site && grep -q "TYPING_SPEED.*28" src/components/InputDemo.tsx && grep -q "Создать Гантт" src/components/InputDemo.tsx</automated>
  </verify>
  <done>
    - InputDemo.tsx with 3 rotating texts
    - Typewriter animation (28ms ± 22ms random variance)
    - Pause at end (2200ms), erase speed (12ms)
    - Cursor blink animation (1s step-end)
    - Card layout: avatar + text top, hint + button bottom
    - Reduced motion support
  </done>
</task>

<task type="auto">
  <name>Task 4: Create GanttPreview React island (grow-in animation)</name>
  <files>packages/site/src/components/GanttPreview.tsx</files>
  <read_first>
    - D:/Projects/gantt-lib-mcp/.planning/reference/gantt-hero.html (lines 307-376, 474-518 for gantt preview HTML/CSS)
    - D:/Projects/gantt-lib-mcp/.planning/phases/24-astro-site-foundation/24-UI-SPEC.md (Gantt Preview Card spec)
  </read_first>
  <action>
Create React island for gantt preview with grow-in bars:

1. Create packages/site/src/components/GanttPreview.tsx:
```tsx
import { useEffect, useState } from 'react';

const BARS = [
  { label: 'Исследование', text: 'Анализ', left: 0, width: 22, color: '#1d4ed8', delay: 0.5 },
  { label: 'Дизайн', text: 'UI/UX', left: 20, width: 28, color: '#7c3aed', delay: 0.65 },
  { label: 'Разработка', text: 'Frontend + API', left: 36, width: 38, color: '#0891b2', delay: 0.8 },
  { label: 'Тестирование', text: 'QA', left: 68, width: 18, color: '#ea580c', delay: 0.95 },
  { label: 'Релиз', text: 'Deploy', left: 84, width: 12, color: '#16a34a', delay: 1.1 },
];

export default function GanttPreview() {
  const [timer, setTimer] = useState(0);

  useEffect(() => {
    const interval = setInterval(() => {
      setTimer((prev) => {
        const next = prev + 0.1;
        if (next >= 1.4) {
          clearInterval(interval);
          return 1.4;
        }
        return next;
      });
    }, 100);

    return () => clearInterval(interval);
  }, []);

  return (
    <div className="relative mx-auto mb-20 max-w-[700px] animate-fade-up px-4 md:px-8" style={{ animationDelay: '500ms' }}>
      <div className="overflow-hidden rounded-[14px] border border-border bg-card shadow-[0_4px_24px_rgba(0,0,0,0.06)]">
        {/* Header */}
        <div className="flex items-center gap-2 border-b border-border bg-muted px-4 py-3 text-[12px] font-bold text-secondary-foreground">
          <div className="h-2 w-2 rounded-full bg-green-500"></div>
          <span className="flex-1">График сгенерирован</span>
          <span className="font-medium text-muted-foreground tabular-nums">{timer.toFixed(1)} с</span>
        </div>

        {/* Rows */}
        <div className="py-2">
          {BARS.map((bar, index) => (
            <div key={index} className="grid grid-cols-[140px_1fr] items-center gap-3 px-4" style={{ height: 32 }}>
              <span className="whitespace-nowrap text-[11px] font-semibold text-secondary-foreground">
                {bar.label}
              </span>
              <div className="relative h-3.5">
                <div
                  className="absolute left-0 top-0 flex h-3.5 items-center overflow-hidden rounded-[4px] px-1.5 text-[9px] font-extrabold text-white/90 whitespace-nowrap animate-grow-in"
                  style={{
                    left: `${bar.left}%`,
                    width: `${bar.width}%`,
                    backgroundColor: bar.color,
                    animationDelay: `${bar.delay}s`,
                  }}
                >
                  {bar.text}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Footer badge */}
        <div className="flex items-center gap-1.5 border-t border-border bg-accent-bg px-4 py-2 text-[11px] font-semibold text-primary">
          <span>✦</span>
          Сгенерировано автоматически · 5 этапов · 12 задач · 3 исполнителя
        </div>
      </div>
    </div>
  );
}
```

2. Add grow-in animation to global.css:
```css
@keyframes grow-in {
  from {
    transform: scaleX(0);
    transform-origin: left;
    opacity: 0;
  }
  to {
    transform: scaleX(1);
    transform-origin: left;
    opacity: 1;
  }
}

.animate-grow-in {
  animation: grow-in 0.6s ease-out both;
}

@media (prefers-reduced-motion: reduce) {
  .animate-grow-in {
    animation: none;
  }
}
```
  </action>
  <verify>
    <automated>cd packages/site && grep -q "animate-grow-in" src/styles/global.css && grep -q "Сгенерировано автоматически" src/components/GanttPreview.tsx</automated>
  </verify>
  <done>
    - GanttPreview.tsx with 5 gantt bars
    - Grow-in animation (scaleX from left, 600ms)
    - Staggered delays (0.5s, 0.65s, 0.8s, 0.95s, 1.1s)
    - Timer animation (0.0s → 1.4s, 100ms interval)
    - Card layout: header (dot + label + timer), rows, footer badge
    - Reduced motion support
  </done>
</task>

<task type="auto">
  <name>Task 5: Update homepage with Hero section and demo cards</name>
  <files>packages/site/src/pages/index.astro</files>
  <read_first>
    - D:/Projects/gantt-lib-mcp/.planning/reference/gantt-hero.html (full HTML structure)
    - D:/Projects/gantt-lib-mcp/packages/site/src/layouts/Layout.astro (existing layout)
  </read_first>
  <action>
Update homepage to include Hero section and demo cards:

1. Replace packages/site/src/pages/index.astro:
```astro
---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import InputDemo from '../components/InputDemo';
import GanttPreview from '../components/GanttPreview';
---

<Layout title="GetGantt — Из текста в Гантт за 30 секунд">
  <main>
    {/* Hero Section */}
    <Hero />

    {/* Divider */}
    <hr class="mx-auto max-w-[900px] border-border" />

    {/* Demo Section */}
    <div id="demo-section" class="bg-background py-12">
      {/* Input Demo Card */}
      <InputDemo client:load />

      {/* Arrow Down */}
      <div class="my-6 flex justify-center text-muted-foreground animate-bob">
        <svg viewBox="0 0 24 24" fill="none" class="h-5 w-5">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
        </svg>
      </div>

      {/* Gantt Preview Card */}
      <GanttPreview client:load />
    </div>
  </main>
</Layout>

<style>
  /* Bob animation for arrow */
  @keyframes bob {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(5px);
    }
  }

  .animate-bob {
    animation: bob 2s ease-in-out infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-bob {
      animation: none;
    }
  }
</style>
```

2. Note: client:load directive loads React islands immediately on page load
  </action>
  <verify>
    <automated>cd packages/site && grep -q "Hero.*client:" src/pages/index.astro && grep -q "demo-section" src/pages/index.astro</automated>
  </verify>
  <done>
    - index.astro imports Hero, InputDemo, GanttPreview components
    - Hero section renders at top
    - Divider line separates hero from demo
    - InputDemo card with typewriter animation
    - Arrow down with bob animation
    - GanttPreview card with grow-in bars
    - All React islands use client:load directive
    - Smooth scroll to demo section on "Смотреть демо" click
  </done>
</task>

</tasks>

<verification>
1. Run `npm run dev -w packages/site` — dev server starts
2. Visit http://localhost:4321 — see full hero section
3. Verify rotating words: "текста" → "сметы" → "брифа" → "ТЗ" → "письма" → "таблицы" (cycles every 2500ms)
4. Verify typewriter animation: text types character by character, pauses, erases, next text
5. Verify gantt preview: bars grow from left with stagger (0.5s, 0.65s, 0.8s, 0.95s, 1.1s)
6. Verify timer: counts up 0.0s → 1.4s
7. Click "Попробовать бесплатно" → navigates to https://ai.getgantt.ru
8. Click "Смотреть демо" → smooth scrolls to demo section
9. Resize browser to mobile (< 768px) — verify responsive layout
10. Check browser console — no errors, all animations working
11. Test with prefers-reduced-motion — animations simplify or disable
</verification>

<success_criteria>
- Hero section with tag, rotating words, subheading, CTAs
- Rotating words animation (6 words, 2500ms interval, slide up/down)
- Typewriter demo animation (3 texts, 28ms ± 22ms variance)
- Gantt preview grow-in animation (5 bars, staggered delays)
- Timer counts up to 1.4s
- "Попробовать бесплатно" CTA links to ai.getgantt.ru
- "Смотреть демо" CTA smooth scrolls to demo section
- Responsive layout (mobile, tablet, desktop breakpoints)
- All animations respect prefers-reduced-motion
- No console errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-astro-site-foundation/24-03-SUMMARY.md`
</output>
