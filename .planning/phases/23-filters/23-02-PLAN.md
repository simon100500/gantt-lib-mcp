---
phase: 23
slug: filters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/layout/Toolbar.tsx
  - packages/web/src/components/FilterPopup.tsx
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Кнопка фильтров видна в Toolbar справа от viewMode переключателя"
    - "Кнопка подсвечивается (variant='secondary') когда есть активные фильтры"
    - "При клике открывается попап с контролами фильтрации"
    - "Попап содержит: чекбокс 'Без зависимостей', чекбокс 'Просроченные', текстовый инпут 'Поиск', инпуты даты 'От' и 'До', кнопку 'Сбросить все'"
    - "При изменении любого контроля GanttChart обновляется в real-time"
    - "Кнопка 'Сбросить все' отключена если нет активных фильтров"
  artifacts:
    - path: "packages/web/src/components/layout/Toolbar.tsx"
      provides: "Filter button integration"
      contains: "Filter button with Funnel icon, filter state reading, FilterPopup component"
    - path: "packages/web/src/components/FilterPopup.tsx"
      provides: "Filter popup UI component"
      min_lines: 80
      exports: ["FilterPopup"]
  key_links:
    - from: "Toolbar.tsx"
      to: "FilterPopup.tsx"
      via: "import statement and DropdownMenuTrigger"
      pattern: "import.*FilterPopup.*from.*FilterPopup"
    - from: "FilterPopup.tsx"
      to: "useUIStore"
      via: "filter state getters and setters"
      pattern: "useUIStore\\(\\(state\\) => state\\.filter"
    - from: "FilterPopup.tsx"
      to: "gantt-lib/filters"
      via: "not used directly (useTaskFilter hook handles this)"
      pattern: ""
    - from: "Toolbar filter button"
      to: "GanttChart"
      via: "computed taskFilter from useTaskFilter hook"
      pattern: "taskFilter=\\{taskFilter\\}"
---

<objective>
Создать UI для фильтрации задач: кнопку фильтров в Toolbar и выпадающий попап с контролами фильтрации (чекбоксы, текстовый поиск, диапазон дат, кнопка сброса).

Purpose: Предоставить пользователю интерфейс для управления фильтрами задач с визуальной индикацией активных фильтров
Output: Toolbar с кнопкой фильтров + FilterPopup компонент с контролами фильтрации
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-filters/23-CONTEXT.md
@.planning/phases/23-filters/23-UI-SPEC.md
@packages/web/src/components/layout/Toolbar.tsx (existing UI patterns)
@packages/web/src/components/ui/button.tsx (Button variants)
@packages/web/src/components/ui/dropdown-menu.tsx (DropdownMenu components)
@packages/web/src/components/ui/input.tsx (Input component)
@packages/web/src/components/ui/label.tsx (Label component)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterPopup component with all filter controls</name>
  <files>packages/web/src/components/FilterPopup.tsx</files>
  <read_first>
    1. packages/web/src/components/layout/Toolbar.tsx — existing DropdownMenu pattern (lines 167-209)
    2. packages/web/src/components/ui/dropdown-menu.tsx — DropdownMenu components API
    3. packages/web/src/components/ui/input.tsx — Input component API
    4. packages/web/src/components/ui/label.tsx — Label component API
    5. .planning/phases/23-filters/23-UI-SPEC.md — copywriting and visual specs
  </read_first>
  <action>
    Create new file `packages/web/src/components/FilterPopup.tsx` with the following content:

    ```typescript
    import { Input } from './ui/input';
    import { Label } from './ui/label';
    import {
      DropdownMenu,
      DropdownMenuContent,
      DropdownMenuItem,
      DropdownMenuSeparator,
      DropdownMenuTrigger,
    } from './ui/dropdown-menu';
    import { useUIStore } from '../stores/useUIStore';
    import { Button } from './ui/button';

    interface FilterPopupProps {
      children: React.ReactNode;
    }

    export function FilterPopup({ children }: FilterPopupProps) {
      const filterWithoutDeps = useUIStore((state) => state.filterWithoutDeps);
      const filterExpired = useUIStore((state) => state.filterExpired);
      const filterSearchText = useUIStore((state) => state.filterSearchText);
      const filterDateFrom = useUIStore((state) => state.filterDateFrom);
      const filterDateTo = useUIStore((state) => state.filterDateTo);

      const setFilterWithoutDeps = useUIStore((state) => state.setFilterWithoutDeps);
      const setFilterExpired = useUIStore((state) => state.setFilterExpired);
      const setFilterSearchText = useUIStore((state) => state.setFilterSearchText);
      const setFilterDateFrom = useUIStore((state) => state.setFilterDateFrom);
      const setFilterDateTo = useUIStore((state) => state.setFilterDateTo);
      const resetFilters = useUIStore((state) => state.resetFilters);

      const hasActiveFilters =
        filterWithoutDeps ||
        filterExpired ||
        filterSearchText.trim().length > 0 ||
        (filterDateFrom && filterDateTo);

      const handleReset = () => {
        resetFilters();
      };

      return (
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            {children}
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end" className="w-56">
            {/* Without deps checkbox */}
            <DropdownMenuItem
              onSelect={(event) => {
                event.preventDefault();
                setFilterWithoutDeps(!filterWithoutDeps);
              }}
              className="flex cursor-pointer items-center gap-2"
            >
              <input
                type="checkbox"
                checked={filterWithoutDeps}
                readOnly
                className="h-4 w-4 shrink-0 rounded border-slate-300 accent-primary pointer-events-none"
              />
              <span className="text-sm">Без зависимостей</span>
            </DropdownMenuItem>

            {/* Expired checkbox */}
            <DropdownMenuItem
              onSelect={(event) => {
                event.preventDefault();
                setFilterExpired(!filterExpired);
              }}
              className="flex cursor-pointer items-center gap-2"
            >
              <input
                type="checkbox"
                checked={filterExpired}
                readOnly
                className="h-4 w-4 shrink-0 rounded border-slate-300 accent-primary pointer-events-none"
              />
              <span className="text-sm">Просроченные</span>
            </DropdownMenuItem>

            <DropdownMenuSeparator />

            {/* Search input */}
            <div className="px-2 py-1.5">
              <Label htmlFor="filter-search" className="text-xs font-medium">
                Поиск
              </Label>
              <Input
                id="filter-search"
                type="text"
                placeholder="Название задачи"
                value={filterSearchText}
                onChange={(e) => setFilterSearchText(e.target.value)}
                className="h-8 text-sm"
              />
            </div>

            <DropdownMenuSeparator />

            {/* Date range inputs */}
            <div className="px-2 py-1.5 space-y-2">
              <div className="space-y-1">
                <Label htmlFor="filter-date-from" className="text-xs font-medium">
                  От
                </Label>
                <Input
                  id="filter-date-from"
                  type="date"
                  value={filterDateFrom}
                  onChange={(e) => setFilterDateFrom(e.target.value)}
                  className="h-8 text-sm"
                />
              </div>
              <div className="space-y-1">
                <Label htmlFor="filter-date-to" className="text-xs font-medium">
                  До
                </Label>
                <Input
                  id="filter-date-to"
                  type="date"
                  value={filterDateTo}
                  onChange={(e) => setFilterDateTo(e.target.value)}
                  className="h-8 text-sm"
                />
              </div>
            </div>

            <DropdownMenuSeparator />

            {/* Reset button */}
            <div className="px-2 py-1.5">
              <Button
                variant="destructive"
                size="sm"
                onClick={handleReset}
                disabled={!hasActiveFilters}
                className="w-full h-8 text-xs"
              >
                Сбросить все
              </Button>
            </div>
          </DropdownMenuContent>
        </DropdownMenu>
      );
    }
    ```

    **IMPORTANT implementation details:**
    - Follow existing Toolbar DropdownMenu pattern (lines 167-209 in Toolbar.tsx)
    - Use DropdownMenuItem with inline checkbox pattern (not DropdownMenuCheckboxItem)
    - Checkbox inputs have `readOnly` and `pointer-events-none` to prevent default browser behavior
    - Use `onSelect` with `event.preventDefault()` to handle clicks properly
    - Search input uses placeholder text "Название задачи" per UI-SPEC
    - Date inputs use `type="date"` (native browser date picker)
    - Date inputs use h-8 height (smaller than default h-10)
    - Reset button uses `variant="destructive"` and is disabled when no active filters
    - All inputs have `text-sm` className for consistent sizing
    - FilterPopup wraps children in DropdownMenuTrigger
    - Export FilterPopup component as default export
  </action>
  <verify>
    <automated>grep -n "export.*FilterPopup\|Без зависимостей\|Просроченные\|Сбросить все" packages/web/src/components/FilterPopup.tsx</automated>
  </verify>
  <done>
    - FilterPopup.tsx file exists
    - Component exports FilterPopup as named export
    - Component accepts children prop and wraps in DropdownMenuTrigger
    - DropdownMenuContent has align="end" and className="w-56"
    - Checkbox items use "Без зависимостей" and "Просроченные" labels
    - Search input has placeholder "Название задачи"
    - Date inputs have labels "От" and "До"
    - Reset button has text "Сбросить все" and variant="destructive"
    - Reset button disabled when hasActiveFilters is false
    - All inputs read from and write to useUIStore filter state
  </done>
</task>

<task type="auto">
  <name>Task 2: Add filter button to Toolbar and wire up useFilterPersistence hook</name>
  <files>packages/web/src/components/layout/Toolbar.tsx, packages/web/src/components/ProjectWorkspace.tsx</files>
  <read_first>
    1. packages/web/src/components/layout/Toolbar.tsx — current structure, viewMode switcher placement
    2. packages/web/src/components/FilterPopup.tsx — FilterPopup component API
    3. packages/web/src/hooks/useFilterPersistence.ts — persistence hook API
    4. packages/web/src/hooks/useTaskFilter.ts — computed filter hook API
    5. .planning/phases/23-filters/23-UI-SPEC.md — filter button placement specs
  </read_first>
  <action>
    **Step 1: Add imports to Toolbar.tsx**

    Add after the lucide-react imports (after line 11):

    ```typescript
    import { Funnel } from 'lucide-react';
    ```

    Add after the DropdownMenu imports (after line 19):

    ```typescript
    import { FilterPopup } from '../FilterPopup';
    import { useTaskFilter } from '../hooks/useTaskFilter';
    ```

    **Step 2: Add filter button to Toolbar**

    In the component return statement, add the filter button between the viewMode switcher (after line 165) and the Ellipsis menu (before line 167):

    ```typescript
    <FilterPopup>
      <Button
        size="sm"
        variant={hasActiveFilters ? 'secondary' : 'ghost'}
        className="h-7 gap-1.5 text-slate-600 hover:text-slate-900"
        title="Показать фильтры задач"
      >
        <Funnel className="h-3.5 w-3.5" />
        <span className="hidden md:inline text-xs">Фильтры</span>
      </Button>
    </FilterPopup>
    ```

    **Step 3: Add filter state reading and taskFilter computation**

    After the existing useUIStore calls (after line 56), add:

    ```typescript
    // Filter state
    const filterWithoutDeps = useUIStore((state) => state.filterWithoutDeps);
    const filterExpired = useUIStore((state) => state.filterExpired);
    const filterSearchText = useUIStore((state) => state.filterSearchText);
    const filterDateFrom = useUIStore((state) => state.filterDateFrom);
    const filterDateTo = useUIStore((state) => state.filterDateTo);
    const hasActiveFilters =
      filterWithoutDeps ||
      filterExpired ||
      filterSearchText.trim().length > 0 ||
      (filterDateFrom && filterDateTo);
    ```

    **Step 4: Find and update ProjectWorkspace to use persistence and filter hooks**

    First, find the ProjectWorkspace component:

    ```bash
    find packages/web/src/components -name "*Workspace.tsx" -type f
    ```

    Then read the file and add the hooks in the component body (after other hooks, before return):

    ```typescript
    import { useFilterPersistence } from '../hooks/useFilterPersistence';
    import { useTaskFilter } from '../hooks/useTaskFilter';

    // Inside component:
    useFilterPersistence();
    const taskFilter = useTaskFilter();
    ```

    **Step 5: Pass taskFilter to GanttChart**

    In the same workspace component, find the GanttChart component and add the taskFilter prop:

    ```typescript
    <GanttChart
      tasks={tasks}
      taskFilter={taskFilter}
      {/* existing props */}
    />
    ```

    **IMPORTANT:**
    - Filter button placement: between viewMode switcher and Ellipsis menu (line 166)
    - Button uses Funnel icon from lucide-react
    - Button variant: 'secondary' when hasActiveFilters, 'ghost' otherwise
    - Button text: "Фильтры" (hidden on mobile, visible on md+)
    - useFilterPersistence hook must be called in workspace component (not Toolbar)
    - useTaskFilter hook returns TaskPredicate | undefined
    - taskFilter prop must be passed to GanttChart component
    - hasActiveFilters computation: same logic as in FilterPopup
  </action>
  <verify>
    <automated>grep -n "FilterPopup\|Funnel\|Фильтры" packages/web/src/components/layout/Toolbar.tsx</automated>
    <automated>grep -n "useFilterPersistence\|useTaskFilter\|taskFilter" packages/web/src/components/*Workspace.tsx</automated>
  </verify>
  <done>
    - Toolbar.tsx imports Funnel icon from lucide-react
    - Toolbar.tsx imports FilterPopup component
    - Toolbar.tsx has filter button between viewMode and Ellipsis menu
    - Filter button uses Funnel icon (h-3.5 w-3.5)
    - Filter button shows "Фильтры" text on md+ breakpoints
    - Filter button variant changes based on hasActiveFilters
    - hasActiveFilters computed from all 5 filter state fields
    - ProjectWorkspace component calls useFilterPersistence hook
    - ProjectWorkspace component calls useTaskFilter hook
    - GanttChart component receives taskFilter prop
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Полная реализация UI фильтров задач: кнопка в Toolbar, выпадающий попап с контролями, persistence в localStorage, интеграция с GanttChart через taskFilter prop</what-built>
  <how-to-verify>
    1. Откройте приложение в браузере (http://localhost:5173 или dev server)
    2. Убедитесь что кнопка "Фильтры" видна в Toolbar справа от переключателя режима просмотра
    3. Кликните на кнопку "Фильтры" — должен открыться попап
    4. Проверьте каждый контроль:
       - Чекбокс "Без зависимостей" — при клике должен подсвечиваться
       - Чекбокс "Просроченные" — при клике должен подсвечиваться
       - Текстовый инпут "Поиск" — введите текст, задачи должны фильтроваться в real-time
       - Инпуты даты "От" и "До" — выберите диапазон, задачи должны фильтроваться
    5. Проверьте визуальную индикацию:
       - Когда есть активные фильтры, кнопка должна быть variant="secondary" (серый фон)
       - Когда нет активных фильтров, кнопка должна быть variant="ghost"
    6. Проверьте кнопку "Сбросить все":
       - Когда есть активные фильтры, кнопка активна
       - При клике все фильтры сбрасываются, кнопка становится неактивной
    7. Проверьте persistence:
       - Установите фильтры, обновите страницу (F5)
       - Фильтры должны восстановиться из localStorage
    8. Проверьте фильтрацию на GanttChart:
       - Задачи должны скрываться/показываться в соответствии с фильтрами
       - Фильтры комбинируются через AND (все условия должны выполняться)
  </how-to-verify>
  <resume-signal>Напишите "approved" если всё работает корректно, или опишите найденные проблемы</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npm run build --workspace=packages/web`
2. Filter button visible in Toolbar between viewMode switcher and Ellipsis menu
3. Filter button variant changes based on hasActiveFilters
4. FilterPopup opens on click, shows all controls
5. All filter controls update useUIStore state
6. useFilterPersistence hook saves/loads from localStorage
7. useTaskFilter hook returns computed TaskPredicate
8. GanttChart receives taskFilter prop and filters tasks
9. Reset button clears all filters and disables itself
</verification>

<success_criteria>
1. UI: Filter button in correct position with proper styling
2. Popup: All controls present and functional
3. State: Filter state stored in useUIStore
4. Persistence: Filters survive page refresh
5. Integration: GanttChart filters tasks based on active filters
6. UX: Real-time filter updates, visual feedback for active filters
</success_criteria>

<output>
After completion, create `.planning/phases/23-filters/23-02-SUMMARY.md`
</output>
