---
phase: 23
slug: filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/stores/useUIStore.ts
  - packages/web/src/components/GanttChart.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Пользователь может видеть состояние фильтров в useUIStore"
    - "GanttChart компонент принимает taskFilter prop"
    - "Фильтры сохраняются в localStorage и загружаются при старте"
    - "Вычисляемый taskFilter комбинирует все активные фильтры через AND"
  artifacts:
    - path: "packages/web/src/stores/useUIStore.ts"
      provides: "Filter state management in Zustand store"
      contains: "filterWithoutDeps, filterExpired, filterSearchText, filterDateFrom, filterDateTo, setFilterWithoutDeps, setFilterExpired, setFilterSearchText, setFilterDateFrom, setFilterDateTo, resetFilters"
    - path: "packages/web/src/components/GanttChart.tsx"
      provides: "taskFilter prop interface"
      exports: ["taskFilter?: TaskPredicate"]
    - path: "packages/web/src/hooks/useFilterPersistence.ts"
      provides: "localStorage persistence for filter state"
      min_lines: 30
  key_links:
    - from: "packages/web/src/stores/useUIStore.ts"
      to: "localStorage"
      via: "FILTER_STORAGE_KEY constant"
      pattern: "localStorage\\.(get|set)Item\\(['\"]gantt-filters['\"]"
    - from: "packages/web/src/components/GanttChart.tsx"
      to: "gantt-lib/filters"
      via: "import statement"
      pattern: "import.*from ['\"]gantt-lib/filters['\"]"
    - from: "useUIStore filter state"
      to: "GanttChart taskFilter prop"
      via: "computed filter predicate using useMemo"
      pattern: "useMemo\\(\\(\\) => \\{.*and\\(.*predicates"
---

<objective>
Добавить состояние фильтров в useUIStore и интегрировать taskFilter prop в GanttChart компонент для поддержки фильтрации задач.

Purpose: Создать фундамент для UI фильтров — state management + persistence + integration с gantt-lib filtering API
Output: Расширенный useUIStore с полями фильтров, persistence hook, GanttChart с поддержкой taskFilter
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-filters/23-CONTEXT.md
@.planning/phases/23-filters/23-UI-SPEC.md
@d:/Projects/gantt-lib/docs/REFERENCE.md (§7.3 Task Filtering API)
@packages/web/src/stores/useUIStore.ts
@packages/web/src/components/GanttChart.tsx
@packages/web/src/stores/useTaskStore.ts (localStorage persistence pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useUIStore with filter state and create persistence hook</name>
  <files>packages/web/src/stores/useUIStore.ts, packages/web/src/hooks/useFilterPersistence.ts</files>
  <read_first>
    1. packages/web/src/stores/useUIStore.ts — current store structure
    2. packages/web/src/stores/useTaskStore.ts — localStorage persistence pattern (lines 68-77 for load, 285 for save)
  </read_first>
  <action>
    **Step 1: Extend UIState interface in useUIStore.ts**

    Add to the `UIState` interface (after line 29, before `setWorkspace`):

    ```typescript
    // Filter state
    filterWithoutDeps: boolean;
    filterExpired: boolean;
    filterSearchText: string;
    filterDateFrom: string;
    filterDateTo: string;

    // Filter actions
    setFilterWithoutDeps: (value: boolean) => void;
    setFilterExpired: (value: boolean) => void;
    setFilterSearchText: (value: string) => void;
    setFilterDateFrom: (value: string) => void;
    setFilterDateTo: (value: string) => void;
    resetFilters: () => void;
    ```

    **Step 2: Add initial values to store**

    In the initial store object (after line 58, before `setWorkspace`), add:

    ```typescript
    filterWithoutDeps: false,
    filterExpired: false,
    filterSearchText: '',
    filterDateFrom: '',
    filterDateTo: '',
    ```

    **Step 3: Add action implementations**

    In the actions object (after `setSavingState`, before the closing `}));`), add:

    ```typescript
    setFilterWithoutDeps: (filterWithoutDeps) => set({ filterWithoutDeps }),
    setFilterExpired: (filterExpired) => set({ filterExpired }),
    setFilterSearchText: (filterSearchText) => set({ filterSearchText }),
    setFilterDateFrom: (filterDateFrom) => set({ filterDateFrom }),
    setFilterDateTo: (filterDateTo) => set({ filterDateTo }),
    resetFilters: () => set({
      filterWithoutDeps: false,
      filterExpired: false,
      filterSearchText: '',
      filterDateFrom: '',
      filterDateTo: '',
    }),
    ```

    **Step 4: Create useFilterPersistence.ts hook**

    Create new file `packages/web/src/hooks/useFilterPersistence.ts` with the following content:

    ```typescript
    import { useEffect } from 'react';
    import { useUIStore } from '../stores/useUIStore';

    const FILTER_STORAGE_KEY = 'gantt-filters';

    export function useFilterPersistence() {
      const filterWithoutDeps = useUIStore((state) => state.filterWithoutDeps);
      const filterExpired = useUIStore((state) => state.filterExpired);
      const filterSearchText = useUIStore((state) => state.filterSearchText);
      const filterDateFrom = useUIStore((state) => state.filterDateFrom);
      const filterDateTo = useUIStore((state) => state.filterDateTo);

      const setFilterWithoutDeps = useUIStore((state) => state.setFilterWithoutDeps);
      const setFilterExpired = useUIStore((state) => state.setFilterExpired);
      const setFilterSearchText = useUIStore((state) => state.setFilterSearchText);
      const setFilterDateFrom = useUIStore((state) => state.setFilterDateFrom);
      const setFilterDateTo = useUIStore((state) => state.setFilterDateTo);

      // Load filters from localStorage on mount
      useEffect(() => {
        const stored = localStorage.getItem(FILTER_STORAGE_KEY);
        if (stored) {
          try {
            const saved = JSON.parse(stored);
            if (typeof saved.filterWithoutDeps === 'boolean') setFilterWithoutDeps(saved.filterWithoutDeps);
            if (typeof saved.filterExpired === 'boolean') setFilterExpired(saved.filterExpired);
            if (typeof saved.filterSearchText === 'string') setFilterSearchText(saved.filterSearchText);
            if (typeof saved.filterDateFrom === 'string') setFilterDateFrom(saved.filterDateFrom);
            if (typeof saved.filterDateTo === 'string') setFilterDateTo(saved.filterDateTo);
          } catch (error) {
            console.error('[useFilterPersistence] Failed to load filters:', error);
          }
        }
      }, []); // Run only on mount

      // Save filters to localStorage on any change
      useEffect(() => {
        const filters = {
          filterWithoutDeps,
          filterExpired,
          filterSearchText,
          filterDateFrom,
          filterDateTo,
        };
        localStorage.setItem(FILTER_STORAGE_KEY, JSON.stringify(filters));
      }, [filterWithoutDeps, filterExpired, filterSearchText, filterDateFrom, filterDateTo]);
    }
    ```

    **IMPORTANT:**
    - Follow the exact pattern from useTaskStore.ts for localStorage persistence
    - Use type checking (`typeof x === 'expected type'`) when loading to avoid invalid data
    - Wrap localStorage.getItem in try-catch to handle corrupted JSON
    - Load effect has empty dependency array (runs once on mount)
    - Save effect depends on all filter state fields
  </action>
  <verify>
    <automated>grep -n "filterWithoutDeps\|filterExpired\|filterSearchText\|filterDateFrom\|filterDateTo" packages/web/src/stores/useUIStore.ts | head -20</automated>
  </verify>
  <done>
    - UIState interface has 5 filter state fields and 6 filter actions
    - Store initial values set all filter fields to default (false/empty strings)
    - Store implements all 6 filter actions
    - useFilterPersistence.ts hook exists with load/save logic
    - Hook uses FILTER_STORAGE_KEY = 'gantt-filters'
    - Load effect runs once on mount with empty dependency array
    - Save effect depends on all 5 filter state fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Add taskFilter prop to GanttChart and create computed filter hook</name>
  <files>packages/web/src/components/GanttChart.tsx, packages/web/src/hooks/useTaskFilter.ts</files>
  <read_first>
    1. packages/web/src/components/GanttChart.tsx — current props interface
    2. d:/Projects/gantt-lib/docs/REFERENCE.md §7.3 — TaskPredicate type and filter functions
  </read_first>
  <action>
    **Step 1: Add taskFilter prop to GanttChartProps interface**

    In `packages/web/src/components/GanttChart.tsx`, add to the `GanttChartProps` interface (after line 31, before the closing `}`):

    ```typescript
    taskFilter?: import('gantt-lib').TaskPredicate;
    ```

    **Step 2: Pass taskFilter to GanttLibChart**

    In the component return statement (after line 109, before the closing `/>`), add the prop:

    ```typescript
    taskFilter={taskFilter}
    ```

    **Step 3: Create useTaskFilter hook**

    Create new file `packages/web/src/hooks/useTaskFilter.ts` with the following content:

    ```typescript
    import { useMemo } from 'react';
    import { and, withoutDeps, expired, nameContains, inDateRange } from 'gantt-lib/filters';
    import type { TaskPredicate } from 'gantt-lib';
    import { useUIStore } from '../stores/useUIStore';

    export function useTaskFilter(): TaskPredicate | undefined {
      const filterWithoutDeps = useUIStore((state) => state.filterWithoutDeps);
      const filterExpired = useUIStore((state) => state.filterExpired);
      const filterSearchText = useUIStore((state) => state.filterSearchText);
      const filterDateFrom = useUIStore((state) => state.filterDateFrom);
      const filterDateTo = useUIStore((state) => state.filterDateTo);

      return useMemo(() => {
        const predicates: Array<TaskPredicate> = [];

        if (filterWithoutDeps) {
          predicates.push(withoutDeps());
        }

        if (filterExpired) {
          predicates.push(expired());
        }

        if (filterSearchText.trim()) {
          predicates.push(nameContains(filterSearchText.trim()));
        }

        if (filterDateFrom && filterDateTo) {
          predicates.push(inDateRange(new Date(filterDateFrom), new Date(filterDateTo)));
        }

        // No active filters
        if (predicates.length === 0) {
          return undefined;
        }

        // Combine all predicates with AND logic
        return and(...predicates);
      }, [filterWithoutDeps, filterExpired, filterSearchText, filterDateFrom, filterDateTo]);
    }
    ```

    **IMPORTANT:**
    - Import TaskPredicate from 'gantt-lib' (not 'gantt-lib/filters')
    - Import filter functions from 'gantt-lib/filters'
    - Trim search text before passing to nameContains (avoid empty string filters)
    - Only add inDateRange filter if BOTH dateFrom and dateTo are set
    - Return undefined when no predicates (shows all tasks)
    - Use useMemo to avoid recreating filter on every render
  </action>
  <verify>
    <automated>grep -n "taskFilter" packages/web/src/components/GanttChart.tsx</automated>
    <automated>grep -n "useTaskFilter\|from 'gantt-lib/filters'" packages/web/src/hooks/useTaskFilter.ts</automated>
  </verify>
  <done>
    - GanttChartProps interface has `taskFilter?: import('gantt-lib').TaskPredicate`
    - GanttChart component passes taskFilter prop to GanttLibChart
    - useTaskFilter.ts hook exists
    - Hook imports: and, withoutDeps, expired, nameContains, inDateRange from 'gantt-lib/filters'
    - Hook imports TaskPredicate from 'gantt-lib'
    - Hook reads all 5 filter fields from useUIStore
    - Hook uses useMemo with dependency array containing all 5 filter fields
    - Hook returns undefined when no predicates, returns and(...predicates) otherwise
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npm run build --workspace=packages/web`
2. useUIStore exports include new filter fields and actions
3. GanttChart accepts taskFilter prop
4. useFilterPersistence hook loads/saves to localStorage key 'gantt-filters'
5. useTaskFilter hook returns TaskPredicate | undefined
</verification>

<success_criteria>
1. State management: useUIStore has all filter state fields and actions
2. Persistence: Filters load from localStorage on mount, save on change
3. Integration: GanttChart accepts taskFilter prop via import('gantt-lib').TaskPredicate
4. Computed filter: useTaskFilter combines active filters with AND logic
5. Type safety: All TypeScript types match gantt-lib API
</success_criteria>

<output>
After completion, create `.planning/phases/23-filters/23-01-SUMMARY.md`
</output>
