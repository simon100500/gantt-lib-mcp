---
phase: 42-mcp-mutation-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/agent.ts
  - packages/server/src/agent.test.ts
  - packages/server/src/mutation/types.ts
  - packages/server/src/mutation/intent-classifier.ts
  - packages/server/src/mutation/execution-routing.ts
  - packages/server/src/mutation/orchestrator.ts
autonomous: true
requirements:
  - MMR-01

must_haves:
  truths:
    - "Ordinary conversational edits enter a first-class staged mutation shell before the legacy freeform mutation attempt"
    - "The server classifies short edits into explicit mutation families instead of one generic mutation bucket"
    - "Execution mode is selected explicitly as `deterministic`, `hybrid`, or `full_agent` before any mutation attempt runs"
  artifacts:
    - path: "packages/server/src/mutation/types.ts"
      provides: "Typed staged-mutation contracts"
      exports: ["MutationIntent", "ResolvedMutationContext", "MutationPlan", "MutationExecutionResult"]
    - path: "packages/server/src/mutation/intent-classifier.ts"
      provides: "Intent-family classification for ordinary mutation prompts"
      exports: ["classifyMutationIntent"]
    - path: "packages/server/src/mutation/execution-routing.ts"
      provides: "Execution-mode selection for staged mutation flow"
      exports: ["selectMutationExecutionMode"]
    - path: "packages/server/src/mutation/orchestrator.ts"
      provides: "Top-level staged mutation shell with legacy fallback boundary"
      exports: ["runStagedMutation"]
  key_links:
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/mutation/orchestrator.ts"
      via: "ordinary-edit handoff before legacy mutation attempt"
      pattern: "runStagedMutation"
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/intent-classifier.ts"
      via: "stage 1 classification"
      pattern: "classifyMutationIntent"
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/execution-routing.ts"
      via: "execution mode selection"
      pattern: "selectMutationExecutionMode"
---

<objective>
Establish the staged mutation lifecycle shell for ordinary conversational edits.

Purpose: MMR-01 requires the architecture change before resolver or deterministic execution work can land safely. This plan introduces explicit mutation contracts, family classification, and execution-mode routing while preserving current behavior through a controlled legacy fallback.
Output: A new `packages/server/src/mutation/` subsystem, explicit intent and mode selection, and `agent.ts` wired to the staged shell.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
@.planning/reference/mcp-mutation-refactor-prd.md
@.planning/phases/41-initial-gen-refactor/41-01-PLAN.md
@packages/server/src/agent.ts
@packages/server/src/agent.test.ts
@packages/server/src/initial-generation/orchestrator.ts
@packages/server/src/initial-generation/route-selection.ts
@packages/server/src/initial-generation/types.ts

<interfaces>
From packages/server/src/initial-generation/types.ts:
```typescript
export type GenerationMode = 'initial_generation' | 'mutation';
export type ModelRoutingDecision = {
  route: GenerationMode;
  tier: 'strong' | 'cheap' | 'main_fallback';
  selectedModel: string;
  reason: 'initial_generation_requires_strong_model' | 'mutation_prefers_cheap_model' | 'cheap_model_missing_fallback_to_main';
};
```

From packages/server/src/agent.ts:
```typescript
export async function runAgentWithHistory(
  userMessage: string,
  projectId: string,
  sessionId: string
): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define staged mutation contracts, intent taxonomy, and mode routing</name>
  <files>packages/server/src/mutation/types.ts, packages/server/src/mutation/intent-classifier.ts, packages/server/src/mutation/execution-routing.ts, packages/server/src/mutation/orchestrator.ts</files>
  <read_first>
    packages/server/src/mutation/types.ts
    packages/server/src/mutation/intent-classifier.ts
    packages/server/src/mutation/execution-routing.ts
    packages/server/src/mutation/orchestrator.ts
    packages/server/src/agent.ts
    packages/server/src/initial-generation/types.ts
    packages/server/src/initial-generation/orchestrator.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export type MutationIntentType = 'add_single_task' \\| 'add_repeated_fragment' \\| 'shift_relative' \\| 'move_to_date' \\| 'move_in_hierarchy' \\| 'link_tasks' \\| 'unlink_tasks' \\| 'delete_task' \\| 'rename_task' \\| 'update_metadata' \\| 'expand_wbs' \\| 'restructure_branch' \\| 'validate_only' \\| 'unsupported_or_ambiguous'" packages/server/src/mutation/types.ts` finds the locked family list
    - `rg -n "export type MutationExecutionMode = 'deterministic' \\| 'hybrid' \\| 'full_agent'" packages/server/src/mutation/types.ts` finds the execution-mode contract
    - `rg -n "anchor_not_found|container_not_resolved|placement_not_resolved|verification_failed" packages/server/src/mutation/types.ts` finds the typed failure-reason contract
    - `rg -n "export function classifyMutationIntent" packages/server/src/mutation/intent-classifier.ts` finds the classifier entrypoint
    - `rg -n "export function selectMutationExecutionMode" packages/server/src/mutation/execution-routing.ts` finds the mode selector
    - `rg -n "legacyFallbackAllowed|deferred_to_legacy" packages/server/src/mutation/orchestrator.ts` finds the shell fallback boundary
  </acceptance_criteria>
  <action>
    Create a new `packages/server/src/mutation/` subsystem that mirrors the explicit staged boundaries from Phase 41, per MMR-01.

    In `types.ts`, export these exact contracts:
    - `MutationIntentType` with the locked literals `add_single_task`, `add_repeated_fragment`, `shift_relative`, `move_to_date`, `move_in_hierarchy`, `link_tasks`, `unlink_tasks`, `delete_task`, `rename_task`, `update_metadata`, `expand_wbs`, `restructure_branch`, `validate_only`, `unsupported_or_ambiguous`
    - `MutationExecutionMode = 'deterministic' | 'hybrid' | 'full_agent'`
    - `MutationFailureReason` with `anchor_not_found`, `multiple_low_confidence_targets`, `container_not_resolved`, `placement_not_resolved`, `unsupported_mutation_shape`, `deterministic_execution_failed`, `verification_failed`, `group_scope_not_resolved`, `expansion_anchor_not_resolved`
    - `MutationIntent`, `ResolvedMutationContext`, `MutationPlan`, `MutationExecutionResult`, and `MutationOrchestrationResult`
    - include the locked fields `rawRequest`, `normalizedRequest`, `entitiesMentioned`, `requiresResolution`, `requiresSchedulingPlacement`, `executionMode`, `projectId`, `projectVersion`, `resolutionQuery`, `selectedContainerId`, `selectedPredecessorTaskId`, `selectedSuccessorTaskId`, `placementPolicy`, `planType`, `operations`, `why`, `expectedChangedTaskIds`, `canExecuteDeterministically`, and `needsAgentExecution`

    In `intent-classifier.ts`:
    - export `classifyMutationIntent(userMessage: string): MutationIntent`
    - classify the PRD prompt set exactly: `добавь сдачу технадзору` -> `add_single_task`, `сдвинь штукатурку на 2 дня` -> `shift_relative`, `перенеси фундамент на 2026-05-10` -> `move_to_date`, `добавь покраску обоев на каждый этаж` -> `add_repeated_fragment`, `сделай эту задачу красной` -> `update_metadata`, `распиши подробнее пункт "Инженерные системы"` -> `expand_wbs`
    - populate `requiresResolution` and `requiresSchedulingPlacement` with explicit booleans instead of computing them later in `agent.ts`

    In `execution-routing.ts`:
    - export `selectMutationExecutionMode(intent: MutationIntent): MutationExecutionMode`
    - route `add_single_task`, `shift_relative`, `move_to_date`, `move_in_hierarchy`, `link_tasks`, `unlink_tasks`, `delete_task`, `rename_task`, and `update_metadata` to `deterministic`
    - route `add_repeated_fragment` and `expand_wbs` to `hybrid`
    - route `restructure_branch` and `unsupported_or_ambiguous` to `full_agent`

    In `orchestrator.ts`:
    - export `runStagedMutation(...)`
    - make it run Stage 1 classification and execution-mode selection immediately
    - return a typed placeholder result with `legacyFallbackAllowed: true` and `status: 'deferred_to_legacy'` for any path not implemented yet
    - do not call MCP tools or `commandService` in this plan
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/mutation/intent-classifier.test.ts packages/server/src/mutation/orchestrator.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The repo contains a first-class staged-mutation subsystem with locked intent families, typed execution modes, typed failure reasons, and a stable orchestrator entrypoint for later resolver and execution plans.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand ordinary edits from `agent.ts` into the staged mutation shell</name>
  <files>packages/server/src/agent.ts, packages/server/src/agent.test.ts, packages/server/src/mutation/orchestrator.ts</files>
  <read_first>
    packages/server/src/agent.ts
    packages/server/src/agent.test.ts
    packages/server/src/mutation/orchestrator.ts
    packages/server/src/mutation/types.ts
    packages/server/src/mutation/intent-classifier.ts
    packages/server/src/mutation/execution-routing.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "runStagedMutation" packages/server/src/agent.ts` finds the staged-mutation handoff
    - `rg -n "mutation_lifecycle_started|intent_classified|execution_mode_selected" packages/server/src/agent.ts` finds the new lifecycle log events
    - `rg -n "legacyFallbackAllowed|deferred_to_legacy" packages/server/src/agent.ts` finds the controlled fallback handling
    - `rg -n "добавь сдачу технадзору|перенеси фундамент на 2026-05-10|добавь покраску обоев на каждый этаж" packages/server/src/agent.test.ts` finds classifier and routing regressions
    - `rg -n "no valid mutation tool call" packages/server/src/agent.test.ts` does not add any new staged-flow assertions that depend on the old generic outcome
  </acceptance_criteria>
  <action>
    Refactor `packages/server/src/agent.ts` so ordinary mutation requests enter the staged shell before the existing freeform mutation attempt, per MMR-01.

    Make these exact changes:
    - import `runStagedMutation` from `./mutation/orchestrator.js`
    - after the `initial_generation` branch and after `tryDirectShiftFastPath(...)`, call `runStagedMutation(...)` for `likelyMutationRequest === true`
    - pass `userMessage`, `projectId`, `sessionId`, `runId`, `tasksBefore`, `env`, `messageService`, `taskService`, `commandService`, `broadcastToSession`, and a `logger.debug(...)` adapter
    - write the lifecycle events `mutation_lifecycle_started`, `intent_classified`, and `execution_mode_selected` before any legacy SDK run begins
    - if `runStagedMutation(...)` returns `status: 'deferred_to_legacy'`, continue into the existing `executeAgentAttempt(...)` branch unchanged
    - if it returns `handled: true`, stop before the legacy prompt path and use its returned assistant response and task snapshot

    Update `packages/server/src/agent.test.ts` to cover:
    - the exact classifier outputs for the locked Russian prompt set
    - that an ordinary edit now calls the staged shell before the legacy mutation path
    - that unresolved later stages can still return `deferred_to_legacy` without breaking the current behavior

    Keep `initial_generation` and the direct-shift fast path intact. Do not redesign the prompt or retry loop in this plan.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/mutation/intent-classifier.test.ts packages/server/src/mutation/orchestrator.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    `agent.ts` has an explicit staged-mutation entrypoint for ordinary edits, lifecycle logging starts before any legacy freeform mutation run, and the repo still has a safe fallback boundary until later plans replace more of the old path.
  </done>
</task>

</tasks>

<verification>
Confirm that `agent.ts` delegates mutation requests into the new subsystem, the new contracts exist under `packages/server/src/mutation/`, and the old SDK mutation path only runs through a typed fallback result.
</verification>

<success_criteria>
Phase 42 now has a first-class staged mutation shell: ordinary edits are classified into explicit families, execution mode is selected explicitly, and `agent.ts` no longer jumps straight from user text into one opaque freeform mutation attempt.
</success_criteria>

<output>
After completion, create `.planning/phases/42-mcp-mutation-refactor/42-01-SUMMARY.md`
</output>
