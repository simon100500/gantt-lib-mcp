---
phase: 42-mcp-mutation-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "42-01"
files_modified:
  - packages/mcp/src/services/task.service.ts
  - packages/server/src/agent.test.ts
  - packages/server/src/mutation/types.ts
  - packages/server/src/mutation/resolver.ts
  - packages/server/src/mutation/resolver.test.ts
  - packages/server/src/mutation/orchestrator.ts
autonomous: true
requirements:
  - MMR-02
  - MMR-04

must_haves:
  truths:
    - "The server can resolve task, container, predecessor, successor, and group-scope anchors for short natural-language edits without requiring the model to invent IDs"
    - "Low-confidence or missing anchors stop the staged flow with typed controlled reasons instead of falling through as a blind mutation attempt"
  artifacts:
    - path: "packages/server/src/mutation/resolver.ts"
      provides: "Server-side resolution stage for task/container/group anchors"
      exports: ["resolveMutationContext"]
    - path: "packages/mcp/src/services/task.service.ts"
      provides: "Read-only search helpers for task and branch resolution"
      exports: ["TaskService"]
    - path: "packages/server/src/mutation/resolver.test.ts"
      provides: "Regression coverage for resolution confidence and failure reasons"
      contains: "group_scope_not_resolved"
  key_links:
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/resolver.ts"
      via: "stage 2 resolution"
      pattern: "resolveMutationContext"
    - from: "packages/server/src/mutation/resolver.ts"
      to: "packages/mcp/src/services/task.service.ts"
      via: "read-only search helpers"
      pattern: "findTasksByName|findContainerCandidates|findGroupScopes"
---

<objective>
Build the server-side resolution layer for short natural-language edits.

Purpose: MMR-02 is the main missing product contract. Before deterministic execution can be reliable, the server must resolve anchors, containers, and group scopes with typed evidence and controlled failures.
Output: Read-only search helpers, a typed resolver, and orchestrator wiring that stops unresolved simple intents with typed reasons.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
@.planning/reference/mcp-mutation-refactor-prd.md
@packages/mcp/src/services/task.service.ts
@packages/server/src/mutation/types.ts
@packages/server/src/mutation/orchestrator.ts
@packages/server/src/agent.ts
@packages/server/src/agent.test.ts

<interfaces>
From packages/server/src/mutation/types.ts:
```typescript
export type ResolvedMutationContext = {
  projectId: string;
  projectVersion: number;
  resolutionQuery: string;
  containers: unknown[];
  tasks: unknown[];
  predecessors: unknown[];
  successors: unknown[];
  selectedContainerId?: string;
  selectedPredecessorTaskId?: string;
  selectedSuccessorTaskId?: string;
  placementPolicy?: string;
  confidence: number;
};
```

From packages/mcp/src/services/task.service.ts:
```typescript
async list(projectId?: string, parentId?: string | null, limit?: number, offset?: number): Promise<{ tasks: Task[]; hasMore: boolean; total: number }>;
async get(id: string, includeChildren?: boolean | 'shallow' | 'deep'): Promise<Task | undefined>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add read-only search helpers for task, container, branch, and group resolution</name>
  <files>packages/mcp/src/services/task.service.ts, packages/server/src/mutation/resolver.ts, packages/server/src/mutation/resolver.test.ts</files>
  <read_first>
    packages/mcp/src/services/task.service.ts
    packages/server/src/mutation/resolver.ts
    packages/server/src/mutation/types.ts
    packages/server/src/agent.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "async findTasksByName\\(" packages/mcp/src/services/task.service.ts` finds the task-name search helper
    - `rg -n "async findContainerCandidates\\(" packages/mcp/src/services/task.service.ts` finds the container search helper
    - `rg -n "async listBranchTasks\\(" packages/mcp/src/services/task.service.ts` finds the branch-summary helper
    - `rg -n "async findGroupScopes\\(" packages/mcp/src/services/task.service.ts` finds the group-scope helper
    - `rg -n "export async function resolveMutationContext" packages/server/src/mutation/resolver.ts` finds the resolution entrypoint
    - `rg -n "selectedContainerId|selectedPredecessorTaskId|selectedSuccessorTaskId|placementPolicy" packages/server/src/mutation/resolver.ts` finds the locked resolution outputs
  </acceptance_criteria>
  <action>
    Extend the read path and create a dedicated server-side resolver per MMR-02.

    In `packages/mcp/src/services/task.service.ts`, add read-only helpers with these exact names and outputs:
    - `findTasksByName(projectId: string, query: string, limit = 8)` returning compact matches with `taskId`, `name`, `parentId`, `path`, `startDate`, `endDate`, `matchType`, and `score`
    - `findContainerCandidates(projectId: string, query: string, limit = 8)` returning only container-like tasks, defined as tasks that currently have children or tasks whose names match phase/container words such as `этап`, `phase`, `раздел`, `секция`, `этаж`, `корпус`
    - `listBranchTasks(projectId: string, rootTaskId: string)` returning the root plus descendants for compact branch summaries
    - `findGroupScopes(projectId: string, hint: string)` returning repeated sibling/container groups for `этаж`, `секция`, `корпус`, `блок`, `floor`, `section`, `building`, and `zone`

    In `packages/server/src/mutation/resolver.ts`, implement `resolveMutationContext(...)` that:
    - accepts `projectId`, `projectVersion`, `intent`, `userMessage`, and `taskService`
    - builds `resolutionQuery` from `intent.normalizedRequest`
    - resolves exact task matches before fuzzy matches
    - resolves closeout-style add intents like `сдача` and `технадзор` toward container candidates whose names contain `сдача`, `closeout`, `handover`, `исполнительная документация`, or `приемка`
    - resolves group fan-out phrases like `каждый этаж` and `по всем секциям` through `findGroupScopes`
    - sets `placementPolicy` to one of `after_predecessor`, `before_successor`, `tail_of_container`, or `group_tail`

    Keep this module read-only. Do not commit any commands here.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/mutation/resolver.test.ts && npm run build -w packages/mcp && npm run build -w packages/server</automated>
  </verify>
  <done>
    The repo has reusable read-only helpers for name, container, branch, and group resolution, and the server can construct a typed `ResolvedMutationContext` for narrow edit intents.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the resolver into the staged orchestrator and return typed controlled failures on unresolved anchors</name>
  <files>packages/server/src/mutation/orchestrator.ts, packages/server/src/mutation/types.ts, packages/server/src/agent.test.ts, packages/server/src/mutation/resolver.test.ts</files>
  <read_first>
    packages/server/src/mutation/orchestrator.ts
    packages/server/src/mutation/types.ts
    packages/server/src/mutation/resolver.ts
    packages/server/src/agent.test.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "resolution_started|resolution_result" packages/server/src/mutation/orchestrator.ts` finds the resolution-stage log events
    - `rg -n "anchor_not_found|multiple_low_confidence_targets|container_not_resolved|group_scope_not_resolved|expansion_anchor_not_resolved" packages/server/src/mutation/orchestrator.ts` finds typed unresolved-anchor handling
    - `rg -n "confidence >= 0\\.7|confidence < 0\\.7" packages/server/src/mutation/orchestrator.ts` finds the explicit confidence gate
    - `rg -n "group_scope_not_resolved|container_not_resolved|anchor_not_found" packages/server/src/mutation/resolver.test.ts` finds failure-mode regression tests
    - `rg -n "добавь сдачу технадзору|добавь покраску обоев на каждый этаж|распиши подробнее пункт" packages/server/src/agent.test.ts` finds resolver-backed staging tests
  </acceptance_criteria>
  <action>
    Update the staged orchestrator so Stage 2 is real and unresolved simple intents stop with typed reasons instead of jumping into a blind freeform mutation attempt.

    In `packages/server/src/mutation/orchestrator.ts`:
    - call `resolveMutationContext(...)` immediately after classification for intents where `requiresResolution === true`
    - write `resolution_started` before calling the resolver and `resolution_result` after it returns
    - treat `confidence >= 0.7` as resolved enough for the common path
    - return typed failures with these exact mappings:
      - no target task for `shift_relative`, `move_to_date`, `rename_task`, `update_metadata`, `delete_task`, `link_tasks`, `unlink_tasks` -> `anchor_not_found`
      - multiple same-score task matches -> `multiple_low_confidence_targets`
      - add intent with no container candidate -> `container_not_resolved`
      - add intent with container but no predecessor/successor/tail placement -> `placement_not_resolved`
      - `add_repeated_fragment` with no repeated groups -> `group_scope_not_resolved`
      - `expand_wbs` with no matching anchor branch -> `expansion_anchor_not_resolved`

    Update `MutationOrchestrationResult` in `types.ts` so controlled failures already carry:
    - `handled: true`
    - `status: 'failed'`
    - `failureReason`
    - `resolutionContext`
    - `legacyFallbackAllowed: false`

    Add regression tests for the locked Russian prompts and the unresolved-anchor cases above. Keep the legacy fallback only for `full_agent` and `unsupported_or_ambiguous` at this stage.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/mutation/resolver.test.ts packages/server/src/mutation/orchestrator.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The staged flow now has a real resolution stage, unresolved anchors produce typed failure reasons, and simple ordinary edits no longer skip directly from classification to a blind mutation attempt.
  </done>
</task>

</tasks>

<verification>
Confirm that resolution is implemented as a first-class stage, the server can resolve containers and group scopes without MCP prompt gymnastics, and unresolved simple intents return typed failure reasons before any freeform mutation run.
</verification>

<success_criteria>
Phase 42 can now resolve task, container, predecessor, successor, and group anchors server-side. Ordinary edits that cannot be resolved stop with typed controlled reasons instead of generic mutation failure behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/42-mcp-mutation-refactor/42-02-SUMMARY.md`
</output>
