---
phase: 42-mcp-mutation-refactor
plan: 04
type: execute
wave: 4
depends_on:
  - "42-01"
  - "42-02"
  - "42-03"
files_modified:
  - packages/server/src/debug-log.ts
  - packages/server/src/agent.ts
  - packages/server/src/agent.test.ts
  - packages/server/src/mutation/messages.ts
  - packages/server/src/mutation/orchestrator.ts
  - packages/server/src/mutation/orchestrator.test.ts
  - packages/mcp/agent/prompts/system.md
autonomous: true
requirements:
  - MMR-04
  - MMR-05

must_haves:
  truths:
    - "Simple ordinary mutation failures return typed controlled reasons with user-facing messages instead of the generic no-valid-tool-call outcome"
    - "One debug-log trace reconstructs intent classification, resolution evidence, plan choice, execution mode, authoritative verification, and final outcome"
    - "The regression suite locks Russian ordinary-edit prompts across success and failure paths"
  artifacts:
    - path: "packages/server/src/mutation/messages.ts"
      provides: "Typed user-facing failure and success message mapping"
      exports: ["buildMutationFailureMessage", "buildMutationSuccessMessage"]
    - path: "packages/server/src/mutation/orchestrator.test.ts"
      provides: "End-to-end staged mutation regression suite"
      contains: "verification_failed"
    - path: "packages/mcp/agent/prompts/system.md"
      provides: "Full-agent prompt instructions that consume structured resolution context instead of inventing IDs or dates from scratch"
      contains: "ResolvedMutationContext"
  key_links:
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/messages.ts"
      via: "typed controlled user-facing outcomes"
      pattern: "buildMutationFailureMessage"
    - from: "packages/server/src/agent.ts"
      to: "packages/server/src/debug-log.ts"
      via: "full mutation lifecycle events"
      pattern: "final_outcome|verification_result"
---

<objective>
Finish the staged mutation refactor with controlled failures, observability, and regression coverage.

Purpose: MMR-04 and MMR-05 are the product-facing finish line. The system must surface typed failure reasons, log the full lifecycle, and lock the Russian prompt set so the common failure class does not regress.
Output: User-facing failure messaging, complete lifecycle telemetry, prompt alignment for `full_agent`, and an end-to-end regression suite.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
@.planning/reference/mcp-mutation-refactor-prd.md
@packages/server/src/debug-log.ts
@packages/server/src/agent.ts
@packages/server/src/agent.test.ts
@packages/server/src/mutation/orchestrator.ts
@packages/mcp/agent/prompts/system.md

<interfaces>
From packages/server/src/debug-log.ts:
```typescript
export async function writeServerDebugLog(event: string, payload: Record<string, unknown> = {}): Promise<void>;
```

From packages/server/src/mutation/types.ts:
```typescript
export type MutationFailureReason =
  | 'anchor_not_found'
  | 'multiple_low_confidence_targets'
  | 'container_not_resolved'
  | 'placement_not_resolved'
  | 'unsupported_mutation_shape'
  | 'deterministic_execution_failed'
  | 'verification_failed'
  | 'group_scope_not_resolved'
  | 'expansion_anchor_not_resolved';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace generic simple-edit failures with typed controlled user-facing outcomes and lifecycle telemetry</name>
  <files>packages/server/src/debug-log.ts, packages/server/src/agent.ts, packages/server/src/mutation/messages.ts, packages/server/src/mutation/orchestrator.ts</files>
  <read_first>
    packages/server/src/debug-log.ts
    packages/server/src/agent.ts
    packages/server/src/mutation/orchestrator.ts
    packages/server/src/mutation/types.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export function buildMutationFailureMessage|export function buildMutationSuccessMessage" packages/server/src/mutation/messages.ts` finds the message-mapping module
    - `rg -n "intent_classified|resolution_started|resolution_result|mutation_plan_built|execution_mode_selected|deterministic_execution_started|execution_committed|verification_result|final_outcome" packages/server/src/mutation/orchestrator.ts packages/server/src/agent.ts` finds all locked lifecycle events
    - `rg -n "anchor_not_found|container_not_resolved|group_scope_not_resolved|verification_failed" packages/server/src/mutation/messages.ts` finds user-facing typed failure coverage
    - `rg -n "model did not perform a valid mutation tool call|не выполнила ни одного валидного mutation tool call" packages/server/src/agent.ts` finds the generic message only inside the legacy full-agent fallback branch, not the staged simple-path branch
  </acceptance_criteria>
  <action>
    Finish the product-facing failure contract and telemetry per MMR-04 and MMR-05.

    In `packages/server/src/mutation/messages.ts`, add exact functions:
    - `buildMutationFailureMessage(reason, context)` returning Russian user-facing text for `anchor_not_found`, `multiple_low_confidence_targets`, `container_not_resolved`, `placement_not_resolved`, `group_scope_not_resolved`, `expansion_anchor_not_resolved`, `unsupported_mutation_shape`, `deterministic_execution_failed`, and `verification_failed`
    - `buildMutationSuccessMessage(result)` returning short factual Russian confirmations based on authoritative changed tasks

    Update `packages/server/src/mutation/orchestrator.ts` and `packages/server/src/agent.ts` so:
    - staged failures always use `buildMutationFailureMessage(...)`
    - staged successes use `buildMutationSuccessMessage(...)`
    - the generic legacy text `модель не выполнила ни одного валидного mutation tool call` remains only for legacy `full_agent` fallback failures
    - every run logs these exact events in order when reached: `intent_classified`, `resolution_started`, `resolution_result`, `mutation_plan_built`, `execution_mode_selected`, `deterministic_execution_started`, `execution_committed`, `verification_result`, `final_outcome`
    - `final_outcome` includes `status`, `executionMode`, `failureReason`, `changedTaskIds`, and `verificationVerdict`

    Keep the existing `writeServerDebugLog(...)` infrastructure. Do not invent a second logging channel.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/mutation/orchestrator.test.ts packages/server/src/agent.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Simple ordinary edits now produce typed user-facing failures and complete lifecycle logs, and the generic no-valid-tool-call message is no longer the normal outcome for staged mutation requests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the Russian regression suite and align the `full_agent` prompt to structured resolution context</name>
  <files>packages/server/src/agent.test.ts, packages/server/src/mutation/orchestrator.test.ts, packages/mcp/agent/prompts/system.md</files>
  <read_first>
    packages/server/src/agent.test.ts
    packages/server/src/mutation/orchestrator.test.ts
    packages/mcp/agent/prompts/system.md
    packages/server/src/mutation/types.ts
    packages/server/src/mutation/orchestrator.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "добавь сдачу технадзору|добавь техприсоединение|сдвинь .* на 2 дня|перенеси .* на 2026-05-10|добавь покраску обоев на каждый этаж|свяжи|переименуй|сделай .* красн|удали|распиши подробнее пункт" packages/server/src/agent.test.ts packages/server/src/mutation/orchestrator.test.ts` finds the locked Russian regression prompt set
    - `rg -n "container_not_resolved|group_scope_not_resolved|expansion_anchor_not_resolved|verification_failed" packages/server/src/agent.test.ts packages/server/src/mutation/orchestrator.test.ts` finds failure-regression coverage
    - `rg -n "ResolvedMutationContext|MutationPlan|Do not invent task IDs|Do not invent dates" packages/mcp/agent/prompts/system.md` finds prompt alignment for structured `full_agent` fallback
    - `rg -n "full_agent" packages/server/src/mutation/orchestrator.test.ts` finds explicit coverage that only complex or unsupported shapes escalate to the agentic path
  </acceptance_criteria>
  <action>
    Add the end-to-end regression suite and align the remaining agent prompt with the staged contracts.

    In `packages/server/src/agent.test.ts` and `packages/server/src/mutation/orchestrator.test.ts`, add coverage for the exact PRD prompt set:
    - success or typed controlled failure for `добавь сдачу технадзору`
    - success or typed controlled failure for `добавь техприсоединение`
    - deterministic execution for `сдвинь штукатурку на 2 дня`
    - deterministic execution for `перенеси фундамент на 2026-05-10`
    - group fan-out success or `group_scope_not_resolved` for `добавь покраску обоев на каждый этаж`
    - deterministic link/unlink, rename, metadata, and delete cases
    - structured fragment path for `распиши подробнее пункт "Инженерные системы"`
    - explicit failure regression that unresolved simple intents do not end with the generic no-valid-tool-call outcome

    In `packages/mcp/agent/prompts/system.md`, add a short staged-mutation section for the remaining `full_agent` path:
    - the prompt receives `ResolvedMutationContext` and, when applicable, an existing `MutationPlan`
    - the agent must consume those structures instead of inventing task IDs, `parentId`, or dates from scratch
    - the agent is only for `full_agent` and constrained hybrid fragment help, not for deterministic simple edits

    Do not expand the public MCP tool list in this plan. Keep prompt changes limited to the staged-contract alignment.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/mutation/orchestrator.test.ts && npm run build -w packages/server && npm run build -w packages/mcp</automated>
  </verify>
  <done>
    The phase is locked by regression coverage for the Russian prompt set, full lifecycle observability is visible in logs, and the remaining `full_agent` path is constrained by structured server-provided context.
  </done>
</task>

</tasks>

<verification>
Confirm that typed failure messages replace the generic staged-flow outcome, lifecycle logs reconstruct one run end to end, and the Russian prompt regression suite covers both successful and controlled-failure paths.
</verification>

<success_criteria>
Phase 42 is complete when one log trace reconstructs the whole staged lifecycle, common ordinary edits return confirmed changes or typed controlled failures, and the Russian prompt suite locks the current failure class out of the product path.
</success_criteria>

<output>
After completion, create `.planning/phases/42-mcp-mutation-refactor/42-04-SUMMARY.md`
</output>
