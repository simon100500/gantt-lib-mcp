---
phase: 42-mcp-mutation-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - "42-01"
  - "42-02"
files_modified:
  - packages/server/src/agent.test.ts
  - packages/server/src/mutation/types.ts
  - packages/server/src/mutation/domain-defaults.ts
  - packages/server/src/mutation/fragment-planner.ts
  - packages/server/src/mutation/plan-builder.ts
  - packages/server/src/mutation/execution.ts
  - packages/server/src/mutation/orchestrator.ts
autonomous: true
requirements:
  - MMR-03

must_haves:
  truths:
    - "Common ordinary mutation families execute through deterministic or tightly constrained server-side paths with authoritative changed-set verification"
    - "The model remains the semantic interpretation layer for fragment expansion or ranked ambiguity, not the final payload synthesizer for narrow edits"
  artifacts:
    - path: "packages/server/src/mutation/plan-builder.ts"
      provides: "Typed `MutationPlan` formation from intent and resolved context"
      exports: ["buildMutationPlan"]
    - path: "packages/server/src/mutation/execution.ts"
      provides: "Deterministic command compilation and authoritative execution"
      exports: ["executeMutationPlan"]
    - path: "packages/server/src/mutation/fragment-planner.ts"
      provides: "Structured fragment planning for hybrid fan-out and WBS expansion"
      exports: ["planStructuredFragment"]
  key_links:
    - from: "packages/server/src/mutation/plan-builder.ts"
      to: "packages/server/src/mutation/execution.ts"
      via: "typed MutationPlan handoff"
      pattern: "executeMutationPlan"
    - from: "packages/server/src/mutation/execution.ts"
      to: "packages/mcp/src/services/command.service.ts"
      via: "authoritative commit boundary"
      pattern: "commitCommand"
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/plan-builder.ts"
      via: "stage 3 mutation-plan formation"
      pattern: "buildMutationPlan"
---

<objective>
Implement typed mutation-plan formation and deterministic execution for common ordinary edits.

Purpose: MMR-03 requires the happy path to stop depending on freeform normalized-tool payload synthesis. This plan turns resolved intents into typed mutation plans and commits them through `commandService`.
Output: A plan builder, deterministic executor, hybrid structured-fragment support, and orchestrator wiring for common mutation families.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
@.planning/reference/mcp-mutation-refactor-prd.md
@packages/server/src/mutation/types.ts
@packages/server/src/mutation/orchestrator.ts
@packages/mcp/src/services/command.service.ts
@packages/mcp/src/types.ts
@packages/server/src/agent.ts

<interfaces>
From packages/mcp/src/types.ts:
```typescript
export type ProjectCommand =
  | { type: 'move_task'; taskId: string; startDate: string; }
  | { type: 'update_task_fields'; taskId: string; fields: { name?: string; color?: string | null; progress?: number; parentId?: string | null; dependencies?: TaskDependency[]; }; }
  | { type: 'create_task'; task: CreateTaskInput; }
  | { type: 'create_tasks_batch'; tasks: CreateTaskInput[]; }
  | { type: 'delete_task'; taskId: string; }
  | { type: 'create_dependency'; taskId: string; dependency: TaskDependency; }
  | { type: 'remove_dependency'; taskId: string; depTaskId: string; }
  | { type: 'reparent_task'; taskId: string; newParentId: string | null; };
```

From packages/mcp/src/services/command.service.ts:
```typescript
async commitCommand(
  request: CommitProjectCommandRequest,
  actorType: ActorType,
  actorId?: string,
): Promise<CommitProjectCommandResponse>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build typed `MutationPlan` formation for deterministic and hybrid families</name>
  <files>packages/server/src/mutation/types.ts, packages/server/src/mutation/domain-defaults.ts, packages/server/src/mutation/fragment-planner.ts, packages/server/src/mutation/plan-builder.ts</files>
  <read_first>
    packages/server/src/mutation/types.ts
    packages/server/src/mutation/resolver.ts
    packages/server/src/mutation/orchestrator.ts
    packages/mcp/src/types.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "append_task_after|append_task_before|append_task_to_container|shift_task_by_delta|move_task_to_date|update_task_metadata|fanout_fragment_to_groups|expand_branch_from_plan" packages/server/src/mutation/plan-builder.ts` finds the semantic operation set
    - `rg -n "DEFAULT_MUTATION_DURATIONS" packages/server/src/mutation/domain-defaults.ts` finds the server-side duration defaults map
    - `rg -n "сдача|технадзор|исполнительная документация|приемка|пусконаладка|покраска обоев|чистовая отделка" packages/server/src/mutation/domain-defaults.ts` finds the locked hint keys
    - `rg -n "export async function planStructuredFragment|export function planStructuredFragment" packages/server/src/mutation/fragment-planner.ts` finds the hybrid fragment planner
    - `rg -n "canExecuteDeterministically|needsAgentExecution|expectedChangedTaskIds" packages/server/src/mutation/plan-builder.ts` finds the required `MutationPlan` fields
  </acceptance_criteria>
  <action>
    Implement Stage 3 plan formation with explicit semantic operations and server defaults.

    In `packages/server/src/mutation/domain-defaults.ts`, export `DEFAULT_MUTATION_DURATIONS` with these exact fallback durations in working days:
    - `сдача`: `1`
    - `технадзор`: `1`
    - `исполнительная документация`: `3`
    - `приемка`: `1`
    - `пусконаладка`: `3`
    - `покраска обоев`: `2`
    - `чистовая отделка`: `5`

    In `packages/server/src/mutation/plan-builder.ts`, export `buildMutationPlan(...)` that maps resolved intents to semantic operations:
    - `add_single_task` -> `append_task_after`, `append_task_before`, or `append_task_to_container`
    - `shift_relative` -> `shift_task_by_delta`
    - `move_to_date` -> `move_task_to_date`
    - `move_in_hierarchy` -> `move_task_in_hierarchy`
    - `link_tasks` -> `link_tasks`
    - `unlink_tasks` -> `unlink_tasks`
    - `delete_task` -> `delete_task`
    - `rename_task` -> `rename_task`
    - `update_metadata` -> `update_task_metadata`
    - `add_repeated_fragment` -> `fanout_fragment_to_groups`
    - `expand_wbs` -> `expand_branch_from_plan`

    For `add_repeated_fragment` and `expand_wbs`, create `packages/server/src/mutation/fragment-planner.ts` with a structured fragment contract:
    - `FragmentNode = { nodeKey: string; title: string; durationDays: number; dependsOnNodeKeys: string[] }`
    - `StructuredFragmentPlan = { title: string; nodes: FragmentNode[]; why: string }`
    - use the model only to fill this structure, not to emit final task IDs, `parentId`, or dates

    Mark only `restructure_branch` and `unsupported_or_ambiguous` as `needsAgentExecution: true`. Keep all common families on deterministic or hybrid plans.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/mutation/plan-builder.test.ts packages/server/src/mutation/fragment-planner.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    The staged flow can now turn a resolved intent into a typed `MutationPlan`, with server-side duration defaults and a constrained fragment contract for fan-out and WBS expansion.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute typed mutation plans through `commandService` and authoritative verification</name>
  <files>packages/server/src/mutation/execution.ts, packages/server/src/mutation/orchestrator.ts, packages/server/src/agent.test.ts</files>
  <read_first>
    packages/server/src/mutation/execution.ts
    packages/server/src/mutation/plan-builder.ts
    packages/server/src/mutation/orchestrator.ts
    packages/mcp/src/services/command.service.ts
    packages/mcp/src/types.ts
    packages/server/src/agent.ts
    .planning/phases/42-mcp-mutation-refactor/42-CONTEXT.md
    .planning/reference/mcp-mutation-refactor-prd.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "export async function executeMutationPlan" packages/server/src/mutation/execution.ts` finds the execution entrypoint
    - `rg -n "create_task|create_tasks_batch|move_task|update_task_fields|create_dependency|remove_dependency|reparent_task|delete_task" packages/server/src/mutation/execution.ts` finds the compiled authoritative command types
    - `rg -n "verificationVerdict|changedTaskIds|commandService\\.commitCommand" packages/server/src/mutation/execution.ts` finds changed-set verification against the authoritative response
    - `rg -n "mutation_plan_built|deterministic_execution_started|execution_committed|verification_result" packages/server/src/mutation/orchestrator.ts` finds Stage 3-5 lifecycle events
    - `rg -n "добавь сдачу технадзору|сдвинь штукатурку на 2 дня|перенеси фундамент на 2026-05-10|свяжи|переименуй|сделай .* красн" packages/server/src/agent.test.ts` finds deterministic-path regressions
  </acceptance_criteria>
  <action>
    Implement Stage 4 deterministic execution and wire it into the orchestrator.

    In `packages/server/src/mutation/execution.ts`, export `executeMutationPlan(...)` that:
    - compiles `append_task_after`, `append_task_before`, and `append_task_to_container` into `create_task` plus `create_dependency` commands as needed
    - compiles `shift_task_by_delta` and `move_task_to_date` into `move_task`
    - compiles `move_task_in_hierarchy` into `reparent_task`
    - compiles `rename_task` and `update_task_metadata` into `update_task_fields`
    - compiles `link_tasks` into `create_dependency`
    - compiles `unlink_tasks` into `remove_dependency`
    - compiles `delete_task` into `delete_task`
    - compiles `fanout_fragment_to_groups` and `expand_branch_from_plan` into deterministic `create_task` / `create_tasks_batch` plus `create_dependency` sequences
    - calls `commandService.commitCommand(...)` as the only commit boundary
    - compares the authoritative `changedTaskIds` returned by `commandService` against `plan.expectedChangedTaskIds` and sets `verificationVerdict` to `accepted` or `failed`

    Update `packages/server/src/mutation/orchestrator.ts` so:
    - after successful resolution it builds a `MutationPlan`
    - `deterministic` and `hybrid` plans execute through `executeMutationPlan(...)`
    - `full_agent` is reserved for `restructure_branch` and `unsupported_or_ambiguous`
    - the legacy freeform mutation path is no longer used for the deterministic families listed in MMR-03

    Add tests that lock the deterministic path for:
    - `добавь сдачу технадзору`
    - `сдвинь штукатурку на 2 дня`
    - `перенеси фундамент на 2026-05-10`
    - `переименуй клининг`
    - `сделай эту задачу красной`
    - `свяжи исполнительную документацию и акт приемки`

    Keep authoritative success defined by the changed set returned from `commandService`, not by text output or planned command count.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsx --test packages/server/src/agent.test.ts packages/server/src/mutation/plan-builder.test.ts packages/server/src/mutation/execution.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>
    Common mutation families now commit through typed server-side plans and `commandService`, and success is grounded in authoritative changed-set verification instead of freeform normalized-tool payload synthesis.
  </done>
</task>

</tasks>

<verification>
Confirm that the orchestrator now forms typed mutation plans, deterministic families no longer depend on the legacy freeform mutation run, and every accepted result is backed by authoritative changed-set verification from `commandService`.
</verification>

<success_criteria>
Phase 42 can now deterministically or tightly constrain the common mutation families: add, shift, move-to-date, move-in-hierarchy, link, unlink, delete, rename, metadata update, group fan-out, and WBS expansion.
</success_criteria>

<output>
After completion, create `.planning/phases/42-mcp-mutation-refactor/42-03-SUMMARY.md`
</output>
