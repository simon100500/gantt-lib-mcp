---
phase: 38-paywall-trial-transition
plan: 05
type: execute
wave: 3
depends_on: ["38-02", "38-03"]
files_modified:
  - packages/web/src/stores/useBillingStore.ts
  - packages/web/src/lib/constraintUi.ts
  - packages/web/src/components/LimitReachedModal.tsx
  - packages/web/src/components/TrialOfferModal.tsx
  - packages/web/src/components/TrialExpiryScreen.tsx
  - packages/web/src/components/TrialReminderBanner.tsx
autonomous: false
requirements: [FR-4, FR-7]

must_haves:
  truths:
    - "Frontend billing store exposes billingState and trial metadata"
    - "Trial offer modal shows after value trigger events with Russian copy"
    - "Trial reminder banner shows at 7, 3, 1 days before expiry"
    - "Trial expiry screen shows after trial ends with data-safe messaging"
    - "LimitReachedModal shows post-trial copy referencing experienced value"
  artifacts:
    - path: "packages/web/src/components/TrialOfferModal.tsx"
      provides: "Trial invitation modal component"
      contains: "Включить 14 дней бесплатно"
    - path: "packages/web/src/components/TrialExpiryScreen.tsx"
      provides: "Trial expiry screen component"
      contains: "Пробный доступ закончился"
    - path: "packages/web/src/components/TrialReminderBanner.tsx"
      provides: "Trial countdown reminder banner"
      contains: "осталось"
    - path: "packages/web/src/stores/useBillingStore.ts"
      provides: "Trial state in billing store"
      contains: "billingState"
  key_links:
    - from: "packages/web/src/stores/useBillingStore.ts"
      to: "/api/billing/subscription"
      via: "fetchSubscription returns billingState + trial metadata"
      pattern: "billingState"
---

<objective>
Add frontend trial UX components and wire billing store with trial state.

Purpose: End users need to see trial offers, reminders, and expiry messaging. The billing store needs to surface billingState so the UI can adapt.
Output: Trial offer modal, reminder banner, expiry screen, post-trial feature gate copy, billing store trial integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-paywall-trial-transition/38-CONTEXT.md
@.planning/phases/38-paywall-trial-transition/38-02-SUMMARY.md

<interfaces>
<!-- BillingSubscriptionStatus (enhanced in 38-02) -->
```typescript
interface BillingSubscriptionStatus {
  plan: PlanId;
  periodEnd: string | null;
  billingState: 'free' | 'trial_active' | 'trial_expired' | 'paid_active' | 'paid_expired';
  trialStartedAt: string | null;
  trialEndsAt: string | null;
  trialSource: string | null;
  aiUsed: number;
  aiLimit: number;
  isActive: boolean;
  planMeta: { id; label; pricing };
  limits: Record<LimitKey, unknown>;
  usage: Record<LimitKey, ConstraintUsageSnapshot>;
  remaining: Record<LimitKey, ConstraintRemainingSnapshot>;
}
```

<!-- Current useBillingStore.SubscriptionStatus (source) -->
```typescript
export interface SubscriptionStatus {
  plan: string;
  periodEnd: string | null;
  aiUsed: number;
  aiLimit: number;
  isActive: boolean;
  planMeta: { id; label; pricing };
  limits: Record<string, unknown>;
  usage: Record<string, UsageEntry>;
  remaining: Record<string, RemainingEntry>;
}
```

<!-- constraintUi buildConstraintModalContent -->
```typescript
export function buildConstraintModalContent(
  denial: ConstraintDenialPayload,
  usage?: BillingUsageSource,
): ConstraintModalContent
```

<!-- Trial copy from PRD/CONTEXT.md -->
Offer: "Попробуйте 14 дней тарифа Старт" / "Включить 14 дней бесплатно" / secondary: "Пока не нужно"
Expiry: "Пробный доступ закончился" / "Ваши графики сохранены" / "Один проект остаётся доступен на бесплатном тарифе"
Reminders: show at 7, 3, 1 days before expiry
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add billingState and trial fields to frontend billing store + create TrialOfferModal, TrialReminderBanner, TrialExpiryScreen</name>
  <files>
    packages/web/src/stores/useBillingStore.ts,
    packages/web/src/components/TrialOfferModal.tsx,
    packages/web/src/components/TrialReminderBanner.tsx,
    packages/web/src/components/TrialExpiryScreen.tsx,
    packages/web/src/lib/constraintUi.ts
  </files>
  <read_first>
    packages/web/src/stores/useBillingStore.ts
    packages/web/src/lib/constraintUi.ts
    packages/web/src/components/LimitReachedModal.tsx
    packages/web/src/lib/billing.ts
  </read_first>
  <behavior>
    STORE:
    - SubscriptionStatus includes billingState, trialStartedAt, trialEndsAt, trialSource fields
    - Helper: isTrialActive(status) returns true when billingState=trial_active
    - Helper: getTrialDaysRemaining(status) returns number of days until trial ends
    - Helper: isTrialExpired(status) returns true when billingState=trial_expired

    TRIAL OFFER MODAL:
    - Props: open, onAccept, onDecline, triggerFeature?: string
    - Renders: title="Попробуйте 14 дней тарифа Старт", subtitle about features unlocked, primary CTA="Включить 14 дней бесплатно", secondary="Пока не нужно"
    - On accept: calls onAccept callback (parent component handles trial start API call)
    - On decline: calls onDecline callback (dismisses, records decline event)

    TRIAL REMINDER BANNER:
    - Props: daysRemaining: number
    - Shows only at 7, 3, 1 days remaining
    - Content: "До конца пробного периода {N} дн." with dismiss button
    - Links to billing/upgrade page

    TRIAL EXPIRY SCREEN:
    - Props: open, onUpgrade, onClose
    - Renders: "Пробный доступ закончился", "Ваши графики сохранены", "Один проект остаётся доступен на бесплатном тарифе", CTA="Перейти на Старт"

    CONSTRAINT UI:
    - buildConstraintModalContent detects billingState=trial_expired and shows post-trial copy
    - Post-trial copy references experienced features: "Вы использовали архив, экспорт и несколько проектов"
  </behavior>
  <action>
    1. Update `packages/web/src/stores/useBillingStore.ts`:
    Add to SubscriptionStatus interface:
    ```typescript
    export interface SubscriptionStatus {
      // ... existing fields
      billingState: string;
      trialStartedAt: string | null;
      trialEndsAt: string | null;
      trialSource: string | null;
    }
    ```

    Add helper functions after the existing selectors:
    ```typescript
    export function isTrialActive(status: SubscriptionStatus | null): boolean {
      return status?.billingState === 'trial_active';
    }

    export function isTrialExpired(status: SubscriptionStatus | null): boolean {
      return status?.billingState === 'trial_expired';
    }

    export function getTrialDaysRemaining(status: SubscriptionStatus | null): number {
      if (!status?.trialEndsAt) return 0;
      return Math.max(0, Math.ceil((new Date(status.trialEndsAt).getTime() - Date.now()) / (24 * 60 * 60 * 1000)));
    }
    ```

    2. Create `packages/web/src/components/TrialOfferModal.tsx`:
    ```tsx
    interface TrialOfferModalProps {
      open: boolean;
      onAccept: () => void;
      onDecline: () => void;
      triggerFeature?: string;
    }

    export function TrialOfferModal({ open, onAccept, onDecline, triggerFeature }: TrialOfferModalProps) {
      if (!open) return null;

      return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40">
          <div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-xl">
            <h2 className="text-xl font-semibold text-slate-900">Попробуйте 14 дней тарифа Старт</h2>
            <p className="mt-3 text-sm text-slate-600">
              Сделайте ещё объекты, экспортируйте график и работайте без ручных пересчётов
            </p>
            {triggerFeature && (
              <p className="mt-2 text-xs text-slate-500">
                Вы пытаетесь использовать: {triggerFeature}
              </p>
            )}
            <div className="mt-6 flex gap-3">
              <button
                type="button"
                onClick={onAccept}
                className="flex-1 rounded-xl bg-primary px-4 py-3 text-sm font-medium text-white hover:bg-primary/90"
              >
                Включить 14 дней бесплатно
              </button>
              <button
                type="button"
                onClick={onDecline}
                className="rounded-xl border border-slate-200 px-4 py-3 text-sm text-slate-700 hover:bg-slate-50"
              >
                Пока не нужно
              </button>
            </div>
          </div>
        </div>
      );
    }
    ```

    3. Create `packages/web/src/components/TrialReminderBanner.tsx`:
    ```tsx
    interface TrialReminderBannerProps {
      daysRemaining: number;
      onDismiss: () => void;
    }

    const REMINDER_DAYS = new Set([7, 3, 1]);

    export function TrialReminderBanner({ daysRemaining, onDismiss }: TrialReminderBannerProps) {
      if (!REMINDER_DAYS.has(daysRemaining)) return null;

      return (
        <div className="flex items-center justify-between gap-4 rounded-xl border border-blue-200 bg-blue-50 px-4 py-3">
          <div className="text-sm text-blue-800">
            До конца пробного периода <strong>{daysRemaining} дн.</strong> После этого один проект останется доступен на бесплатном тарифе.
          </div>
          <button
            type="button"
            onClick={onDismiss}
            className="shrink-0 text-xs text-blue-600 hover:text-blue-800"
          >
            Закрыть
          </button>
        </div>
      );
    }
    ```

    4. Create `packages/web/src/components/TrialExpiryScreen.tsx`:
    ```tsx
    interface TrialExpiryScreenProps {
      open: boolean;
      onUpgrade: () => void;
      onClose: () => void;
    }

    export function TrialExpiryScreen({ open, onUpgrade, onClose }: TrialExpiryScreenProps) {
      if (!open) return null;

      return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40">
          <div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-xl">
            <h2 className="text-xl font-semibold text-slate-900">Пробный доступ закончился</h2>
            <div className="mt-4 space-y-2 text-sm text-slate-600">
              <p>Ваши графики сохранены.</p>
              <p>Один проект остаётся доступен на бесплатном тарифе.</p>
              <p>Чтобы продолжить работу со всеми объектами и экспортом, перейдите на Старт.</p>
            </div>
            <div className="mt-6 flex gap-3">
              <button
                type="button"
                onClick={onUpgrade}
                className="flex-1 rounded-xl bg-primary px-4 py-3 text-sm font-medium text-white hover:bg-primary/90"
              >
                Перейти на Старт
              </button>
              <button
                type="button"
                onClick={onClose}
                className="rounded-xl border border-slate-200 px-4 py-3 text-sm text-slate-700 hover:bg-slate-50"
              >
                Понятно
              </button>
            </div>
          </div>
        </div>
      );
    }
    ```

    5. Update `packages/web/src/lib/constraintUi.ts`:
    Add post-trial copy detection in buildConstraintModalContent. When the billing state context indicates trial_expired, add specific post-trial description:

    After the existing `if (denial.code === 'SUBSCRIPTION_EXPIRED')` block, add:
    ```typescript
    // Check for post-trial context - when denial comes from a trial_expired user
    // the description references experienced value
    const isPostTrial = denial.reasonCode === 'post_trial_feature_gate'
      || denial.upgradeHint?.includes('trial');

    if (isPostTrial) {
      title = `${limitLabel} — пробный период закончился`;
      description = 'Вы использовали расширенные возможности тарифа Старт. Перейдите на платный тариф, чтобы сохранить доступ.';
    }
    ```

    This allows the backend to send `reasonCode: 'post_trial_feature_gate'` in trial_expired denials.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsc -p packages/web/tsconfig.json --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - useBillingStore SubscriptionStatus has billingState, trialStartedAt, trialEndsAt, trialSource
    - isTrialActive, isTrialExpired, getTrialDaysRemaining helper functions exist
    - TrialOfferModal component renders Russian copy with "Включить 14 дней бесплатно" CTA
    - TrialReminderBanner renders only at 7, 3, 1 days remaining
    - TrialExpiryScreen renders expiry copy with "Перейти на Старт" CTA
    - constraintUi handles post-trial feature gate copy
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Trial UX components: offer modal, reminder banner, expiry screen, and billing store trial integration</what-built>
  <how-to-verify>
    1. Verify TrialOfferModal renders correctly:
       - Check that "Попробуйте 14 дней тарифа Старт" title appears
       - Check that "Включить 14 дней бесплатно" CTA button exists
       - Check that "Пока не нужно" secondary button exists
    2. Verify TrialReminderBanner:
       - Renders at daysRemaining=7 with "До конца пробного периода 7 дн."
       - Does NOT render at daysRemaining=5
    3. Verify TrialExpiryScreen:
       - Shows "Пробный доступ закончился"
       - Shows "Ваши графики сохранены"
       - Shows "Перейти на Старт" CTA
    4. Verify billing store types compile with new fields
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compilation passes for web package
2. TrialOfferModal.tsx exists with correct Russian copy
3. TrialExpiryScreen.tsx exists with correct Russian copy
4. TrialReminderBanner.tsx exists and filters 7/3/1 days
5. useBillingStore has billingState field and trial helpers
</verification>

<success_criteria>
- Frontend billing store exposes trial state
- Trial offer modal shows with PRD-specified copy
- Reminder banner shows at 7, 3, 1 days
- Expiry screen shows data-safe messaging
- Post-trial feature gate copy references experienced value
</success_criteria>

<output>
After completion, create `.planning/phases/38-paywall-trial-transition/38-05-SUMMARY.md`
</output>
