---
phase: 33-frontend-constraints-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/LimitReachedModal.tsx
  - packages/web/src/lib/billing.ts
  - packages/web/src/lib/constraintUi.ts
  - packages/web/src/stores/useBillingStore.ts
autonomous: true
requirements:
  - FUX-02
must_haves:
  truths:
    - "Frontend can map a structured denial payload into one contextual limit modal instead of hard-coded free/paid scenarios."
    - "The limit modal can state which limit was hit, which plan is active, and what the upgrade path costs."
    - "Projects and AI chat surfaces can consume one shared frontend constraint contract before proactive guards are wired."
  artifacts:
    - path: "packages/web/src/lib/constraintUi.ts"
      provides: "Frontend normalization helpers for usage snapshots and denial payloads."
    - path: "packages/web/src/components/LimitReachedModal.tsx"
      provides: "Contextual limit modal that can render structured limit and upgrade details."
    - path: "packages/web/src/stores/useBillingStore.ts"
      provides: "Typed access to normalized usage data that downstream UI guards can read."
  key_links:
    - from: "packages/web/src/components/LimitReachedModal.tsx"
      to: "packages/web/src/lib/constraintUi.ts"
      via: "modal props and copy derive from shared limit-ui helpers"
      pattern: "constraintUi|limitKey|upgradeHint|planLabel"
    - from: "packages/web/src/lib/constraintUi.ts"
      to: "packages/web/src/lib/billing.ts"
      via: "upgrade CTA pricing and plan labels come from frontend billing constants"
      pattern: "PLAN_LABELS|PLAN_PRICES|formatPrice"
    - from: "packages/web/src/lib/constraintUi.ts"
      to: "packages/web/src/stores/useBillingStore.ts"
      via: "usage helpers accept the normalized /api/usage store shape"
      pattern: "usage|remaining|ai_queries|projects"
---

<objective>
Create the shared frontend constraint UX contract that Phase 33 needs for usage indicators, contextual modal improvements, and proactive guards.

Purpose: Replace the Phase 29 scenario-only modal flow with a structured frontend model based on Phase 31 normalized usage data and Phase 32 denial payloads, so the next plan can wire indicators and disabled states without re-parsing backend contracts in multiple places.
Output: Shared limit-UI helpers, an upgraded `LimitReachedModal`, and typed usage-store access patterns that expose plan, limit, remaining, and upgrade-price context.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-paywall-enhance/29-02-SUMMARY.md
@.planning/phases/31-usage-tracking/31-02-PLAN.md
@.planning/phases/32-backend-enforcement/32-01-SUMMARY.md
@packages/web/src/components/LimitReachedModal.tsx
@packages/web/src/stores/useBillingStore.ts
@packages/web/src/lib/billing.ts
@packages/server/src/middleware/constraint-middleware.ts

<interfaces>
From packages/server/src/middleware/constraint-middleware.ts:
```ts
type DenialPayload = {
  code: string;
  limitKey: LimitKey | null;
  reasonCode: ConstraintCheckResult['reasonCode'] | 'subscription_expired';
  remaining: number | 'unlimited' | null;
  plan: PlanId;
  planLabel: string;
  upgradeHint: string;
  used?: number;
  limit?: number | 'unlimited';
};
```

From packages/web/src/stores/useBillingStore.ts:
```ts
export interface UsageStatus {
  plan: string;
  planMeta: { id: string; label: string; pricing: { monthly: number; yearly: number } };
  limits: Record<string, unknown>;
  usage: Record<string, UsageEntry>;
  remaining: Record<string, RemainingEntry>;
}
```

From packages/web/src/lib/billing.ts:
```ts
export const PLAN_LABELS: Record<PlanId, string>;
export const PLAN_PRICES: Record<'start' | 'team' | 'enterprise', Record<BillingPeriod, number>>;
export function formatPrice(price: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a reusable frontend constraint UX contract</name>
  <read_first>
    - packages/web/src/stores/useBillingStore.ts
    - packages/web/src/lib/billing.ts
    - packages/server/src/middleware/constraint-middleware.ts
    - .planning/phases/31-usage-tracking/31-02-PLAN.md
    - .planning/phases/32-backend-enforcement/32-01-SUMMARY.md
  </read_first>
  <files>packages/web/src/lib/constraintUi.ts, packages/web/src/stores/useBillingStore.ts, packages/web/src/lib/billing.ts</files>
  <action>
    Create a dedicated frontend helper module that normalizes Phase 31 usage snapshots plus Phase 32 denial payloads into one UI-friendly contract for Phase 33. The helper must explicitly handle `projects`, `ai_queries`, and `SUBSCRIPTION_EXPIRED`/`*_LIMIT_REACHED` cases, expose the active plan label, compute the next paid upgrade tier, and format the upgrade price using existing billing constants instead of duplicating tariff numbers. Keep `useBillingStore` as the source of truth for `usage`, but add typed selectors/helpers or exported types that make `projects` and `ai_queries` consumption grep-obvious for downstream components.
  </action>
  <acceptance_criteria>
    - `packages/web/src/lib/constraintUi.ts` exists and contains the exact strings `projects`, `ai_queries`, `limitKey`, and `upgradeHint`.
    - `packages/web/src/lib/constraintUi.ts` contains a frontend type or interface for the denial payload with `plan`, `planLabel`, and `remaining`.
    - `packages/web/src/lib/constraintUi.ts` references `PLAN_LABELS` or `PLAN_PRICES` from `packages/web/src/lib/billing.ts`.
    - `packages/web/src/stores/useBillingStore.ts` still contains the exact string `/api/usage` and exposes typed `usage` + `remaining` access for downstream UI consumers.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json</automated>
  </verify>
  <done>A shared frontend constraint contract exists, is typed against the normalized usage payload, and can produce consistent limit context without ad-hoc parsing inside components.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade LimitReachedModal to render structured limit context</name>
  <read_first>
    - packages/web/src/components/LimitReachedModal.tsx
    - packages/web/src/lib/constraintUi.ts
    - packages/web/src/lib/billing.ts
    - .planning/phases/29-paywall-enhance/29-02-SUMMARY.md
    - packages/server/src/middleware/constraint-middleware.ts
  </read_first>
  <files>packages/web/src/components/LimitReachedModal.tsx, packages/web/src/lib/constraintUi.ts</files>
  <action>
    Replace the scenario-only copy matrix with a structured modal API that can render: reached limit name, current plan label, remaining/used context when available, server-provided `upgradeHint`, and a concrete upgrade offer such as `Старт — 1 490 ₽/мес` or `Команда — 4 990 ₽/мес`. Preserve the existing `/purchase` CTA flow, but make the component accept real denial metadata and normalized usage context so the next plan can pass project-limit and AI-limit payloads directly. Backward compatibility is acceptable only if it keeps the new structured props primary and leaves no new hard-coded `free-ai` vs `paid-ai` branching in downstream consumers.
  </action>
  <acceptance_criteria>
    - `packages/web/src/components/LimitReachedModal.tsx` contains the exact strings `limitKey`, `planLabel`, and `upgradeHint`.
    - `packages/web/src/components/LimitReachedModal.tsx` contains the exact string `/purchase`.
    - `packages/web/src/components/LimitReachedModal.tsx` contains a visible price rendering path that uses `formatPrice(` or `PLAN_PRICES`.
    - `packages/web/src/components/LimitReachedModal.tsx` no longer relies exclusively on the exact type union `'free-ai' | 'paid-ai' | 'project-limit'` for its main rendering branch.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json</automated>
  </verify>
  <done>The modal can explain which limit blocked the user, what plan they are on, and what the next upgrade costs, using structured payload fields rather than brittle scenario copy.</done>
</task>

</tasks>

<verification>
Compile the web package and grep the new helper/component contracts to confirm the modal is driven by `limitKey`, `planLabel`, `upgradeHint`, and upgrade pricing rather than Phase 29-only scenario strings.
</verification>

<success_criteria>
FUX-02 is satisfied when the frontend has one reusable limit-UX contract and `LimitReachedModal` can render structured project/AI denial details, active tariff identity, and upgrade pricing without backend changes.
</success_criteria>

<output>
After completion, create `.planning/phases/33-frontend-constraints-ux/33-01-SUMMARY.md`
</output>
