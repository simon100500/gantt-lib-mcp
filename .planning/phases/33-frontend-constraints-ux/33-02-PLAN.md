---
phase: 33-frontend-constraints-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/components/ChatSidebar.tsx
  - packages/web/src/components/ProjectSwitcher.tsx
  - packages/web/src/components/layout/ProjectMenu.tsx
  - packages/web/src/components/workspace/ProjectWorkspace.tsx
  - packages/web/src/stores/useAuthStore.ts
autonomous: true
requirements:
  - FUX-01
  - FUX-03
must_haves:
  truths:
    - "The projects surface shows how many active projects are used or remaining near the create-project action."
    - "The AI chat surface shows AI usage context and stops submission with an explanatory disabled state when the limit is exhausted."
    - "When project creation or AI chat is denied, the modal shows the same structured context as the proactive guards."
  artifacts:
    - path: "packages/web/src/components/layout/ProjectMenu.tsx"
      provides: "Project usage indicator plus create-project guard state in the shell header/sidebar."
    - path: "packages/web/src/components/ProjectSwitcher.tsx"
      provides: "Disabled new-project action with tooltip/title explaining the projects limit."
    - path: "packages/web/src/components/ChatSidebar.tsx"
      provides: "AI usage indicator and disabled submit/input affordance with reason copy."
    - path: "packages/web/src/App.tsx"
      provides: "Unified usage fetch, denial interception, and structured modal wiring for project and AI actions."
  key_links:
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/components/LimitReachedModal.tsx"
      via: "structured denial payload and usage context flow into modal state"
      pattern: "AI_LIMIT_REACHED|PROJECT_LIMIT_REACHED|SUBSCRIPTION_EXPIRED|setLimitModal"
    - from: "packages/web/src/components/layout/ProjectMenu.tsx"
      to: "packages/web/src/stores/useBillingStore.ts"
      via: "projects usage and remaining data drive indicators and disabled create affordances"
      pattern: "usage\\.projects|remaining\\.projects|fetchUsage"
    - from: "packages/web/src/components/ChatSidebar.tsx"
      to: "packages/web/src/stores/useBillingStore.ts"
      via: "ai usage and remaining data drive the chat footer/header indicator and disabled state"
      pattern: "usage\\.ai_queries|remaining\\.ai_queries"
---

<objective>
Wire the Phase 33 frontend UX so usage indicators, contextual modal improvements, and proactive guards appear where users create projects and send AI chat requests.

Purpose: Use the shared frontend constraint contract from Plan 33-01 to put normalized usage and denial context into the actual project menu and AI chat surfaces, instead of only reacting after a failed request.
Output: Project-limit and AI-limit indicators, disabled create/send affordances with explanatory tooltip text, and modal wiring that consumes structured denial payloads from existing backend routes.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-paywall-enhance/29-02-SUMMARY.md
@.planning/phases/32-backend-enforcement/32-01-SUMMARY.md
@.planning/phases/33-frontend-constraints-ux/33-01-PLAN.md
@packages/web/src/App.tsx
@packages/web/src/components/ChatSidebar.tsx
@packages/web/src/components/ProjectSwitcher.tsx
@packages/web/src/components/layout/ProjectMenu.tsx
@packages/web/src/components/workspace/ProjectWorkspace.tsx
@packages/web/src/stores/useAuthStore.ts
@packages/web/src/stores/useBillingStore.ts

<interfaces>
From packages/web/src/App.tsx:
```ts
const [limitModalScenario, setLimitModalScenario] =
  useState<'free-ai' | 'paid-ai' | 'project-limit' | null>(null);
const projectLimitReached = useAuthStore((s) => s.projectLimitReached);
```

From packages/web/src/components/ChatSidebar.tsx:
```ts
interface ChatSidebarProps {
  onSend: (text: string) => void;
  disabled: boolean;
  connected: boolean;
}
```

From packages/web/src/components/ProjectSwitcher.tsx:
```ts
interface ProjectSwitcherProps {
  onCreateNew: () => void;
  footer?: ReactNode;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ad-hoc limit interception with structured modal and usage state wiring</name>
  <read_first>
    - packages/web/src/App.tsx
    - packages/web/src/stores/useAuthStore.ts
    - packages/web/src/stores/useBillingStore.ts
    - packages/web/src/lib/constraintUi.ts
    - packages/web/src/components/LimitReachedModal.tsx
    - .planning/phases/29-paywall-enhance/29-02-SUMMARY.md
    - .planning/phases/32-backend-enforcement/32-01-SUMMARY.md
  </read_first>
  <files>packages/web/src/App.tsx, packages/web/src/stores/useAuthStore.ts, packages/web/src/stores/useBillingStore.ts</files>
  <action>
    Replace the current `limitModalScenario` flow in `App.tsx` with a structured modal state that stores the denial payload plus any normalized usage snapshot needed by `LimitReachedModal`. Fetch normalized usage on authenticated shell entry and after create-project/chat denial paths so the UI stays current. Update `useAuthStore.createProject()` to preserve the real 403 payload instead of reducing it to a boolean flag, then route project create and AI chat denials through the shared helper from Plan 33-01. Keep the existing backend contracts unchanged: the UI must consume `PROJECT_LIMIT_REACHED`, `AI_LIMIT_REACHED`, and `SUBSCRIPTION_EXPIRED` as-is.
  </action>
  <acceptance_criteria>
    - `packages/web/src/App.tsx` contains the exact strings `PROJECT_LIMIT_REACHED`, `AI_LIMIT_REACHED`, and `SUBSCRIPTION_EXPIRED`.
    - `packages/web/src/App.tsx` contains the exact string `fetchUsage(` or `getState().fetchUsage(`.
    - `packages/web/src/stores/useAuthStore.ts` no longer stores project-limit state only as the exact boolean field `projectLimitReached: boolean` without accompanying denial metadata.
    - `packages/web/src/App.tsx` renders `LimitReachedModal` with structured props derived from the denial payload instead of only `scenario={...}`.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json</automated>
  </verify>
  <done>Frontend shell state carries structured limit denials and up-to-date usage snapshots, so both project creation and AI chat can show the upgraded modal without duplicating parsing logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add visible usage indicators and proactive disabled guards to project and chat actions</name>
  <read_first>
    - packages/web/src/components/layout/ProjectMenu.tsx
    - packages/web/src/components/ProjectSwitcher.tsx
    - packages/web/src/components/workspace/ProjectWorkspace.tsx
    - packages/web/src/components/ChatSidebar.tsx
    - packages/web/src/stores/useBillingStore.ts
    - packages/web/src/lib/constraintUi.ts
  </read_first>
  <files>packages/web/src/components/layout/ProjectMenu.tsx, packages/web/src/components/ProjectSwitcher.tsx, packages/web/src/components/workspace/ProjectWorkspace.tsx, packages/web/src/components/ChatSidebar.tsx</files>
  <action>
    Surface project and AI usage next to the actual actions users take. In `ProjectMenu` and `ProjectSwitcher`, show a compact projects usage label (`used/limit` or `remaining`), disable the create-project control when `projects` remaining is `0`, and add a tooltip/title string that explains the exact reason in Russian. In the chat surface (`ProjectWorkspace` plus `ChatSidebar`), add an AI usage indicator near the assistant header or input, pass a dedicated disabled-reason prop instead of reusing `aiThinking`, and block submission when `ai_queries` remaining is `0` or the subscription is expired. Keep the tooltip copy and disabled behavior aligned with the same shared limit context used by the modal so FUX-01 and FUX-03 read as one coherent UX.
  </action>
  <acceptance_criteria>
    - `packages/web/src/components/layout/ProjectMenu.tsx` contains one of the exact strings `remaining.projects` or `usage.projects`.
    - `packages/web/src/components/ProjectSwitcher.tsx` contains the exact string `disabled={` and a title or tooltip string explaining the project limit in Russian.
    - `packages/web/src/components/ChatSidebar.tsx` contains one of the exact strings `remaining.ai_queries` or `usage.ai_queries` via props or rendered copy.
    - `packages/web/src/components/ChatSidebar.tsx` contains a disabled-reason placeholder or title distinct from the existing `Assistant думает…` state.
    - `packages/web/src/components/workspace/ProjectWorkspace.tsx` passes usage/guard props into `ChatSidebar` instead of relying only on `disabled={aiThinking}`.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c npx tsc -p packages/web/tsconfig.json</automated>
  </verify>
  <done>Users can see remaining project and AI capacity near the relevant actions, and both create/send controls proactively stop with an explanatory tooltip before the backend rejects the request.</done>
</task>

</tasks>

<verification>
Compile the web package, then grep `App.tsx`, `ProjectMenu.tsx`, `ProjectSwitcher.tsx`, `ProjectWorkspace.tsx`, and `ChatSidebar.tsx` for structured denial codes plus `usage.projects` / `usage.ai_queries` references to confirm the UX is proactive and contract-driven.
</verification>

<success_criteria>
FUX-01 and FUX-03 are satisfied when the project shell and AI chat both display normalized usage context, disable exhausted actions with explanatory tooltip/title copy, and open the structured limit modal with the same backend denial metadata when a blocked action is attempted.
</success_criteria>

<output>
After completion, create `.planning/phases/33-frontend-constraints-ux/33-02-SUMMARY.md`
</output>
