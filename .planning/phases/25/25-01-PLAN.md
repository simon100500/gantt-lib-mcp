---
phase: 25-interactive-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/site/package.json
  - packages/site/src/components/GanttPreview.tsx
autonomous: true
requirements:
  - INTER-01
  - INTER-05
user_setup: []

must_haves:
  truths:
    - Developer can install gantt-lib package in site package
    - GanttPreview component imports and renders GanttChart from gantt-lib
    - Chart renders with basic sample data on homepage
    - gantt-lib CSS styles are loaded and chart is visible
    - Component uses Astro React island pattern (client:load directive)
  artifacts:
    - path: packages/site/package.json
      provides: "Project dependencies"
      contains: "gantt-lib@^0.28.0"
    - path: packages/site/src/components/GanttPreview.tsx
      provides: "Interactive gantt chart component"
      imports: ["GanttChart", "Task", "gantt-lib/styles.css"]
      exports: ["GanttPreview"]
    - path: packages/site/src/pages/index.astro
      provides: "Homepage with interactive component"
      contains: "client:load"
  key_links:
    - from: "packages/site/src/components/GanttPreview.tsx"
      to: "gantt-lib"
      via: "npm package import"
      pattern: "import.*GanttChart.*from.*gantt-lib"
    - from: "packages/site/src/pages/index.astro"
      to: "GanttPreview.tsx"
      via: "Astro component import with client:load"
      pattern: "GanttPreview.*client:load"
---

<objective>
Install gantt-lib dependency and create basic interactive GanttPreview component that renders a working gantt chart on the homepage.

Purpose: Replace the animated placeholder with a real interactive gantt chart that users can drag and edit. This plan establishes the foundation by installing the package and creating a minimal working component.

Output: Working gantt chart on homepage with basic drag-to-edit functionality using gantt-lib.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25/25-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/web/src/components/GanttChart.tsx
@packages/site/src/components/GanttPreview.tsx
@packages/site/astro.config.js

<interfaces>
<!-- Key types from gantt-lib that the executor needs -->

From gantt-lib package:
```typescript
interface Task {
  id: string;
  name: string;
  startDate: string | Date;
  endDate: string | Date;
  color?: string;
  progress?: number;         // 0-100
  parentId?: string;         // For hierarchy
  accepted?: boolean;        // If progress === 100, true = green
}

interface GanttChartProps {
  tasks: Task[];
  month: Date;
  dayWidth?: number;
  rowHeight?: number;
  onChange?: (tasks: Task[]) => void;
  collapsedParentIds?: Set<string>;
  onToggleCollapse?: (parentId: string) => void;
  // ... other props (see research doc for full list)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install gantt-lib dependency</name>
  <files>packages/site/package.json</files>
  <action>
    Install gantt-lib@^0.28.0 as a dependency in packages/site:

    ```bash
    cd packages/site
    npm install gantt-lib@^0.28.0
    ```

    Verify installation: package.json should contain "gantt-lib": "^0.28.0" in dependencies.
  </action>
  <verify>grep "gantt-lib" packages/site/package.json | grep "0.28.0"</verify>
  <done>gantt-lib@^0.28.0 is listed in packages/site/package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create basic interactive GanttPreview component</name>
  <files>packages/site/src/components/GanttPreview.tsx</files>
  <action>
    Replace the entire contents of GanttPreview.tsx with a functional gantt-lib integration:

    1. Import GanttChart, Task types, and CSS from gantt-lib
    2. Create basic sample data (4-6 tasks with ISO dates in March 2026)
    3. Add useState for tasks state
    4. Render GanttChart with proper props:
       - tasks state
       - month: new Date('2026-03-01')
       - dayWidth: 40
       - rowHeight: 40
       - onChange handler for state updates
    5. Keep the outer container styling (max-w-[900px], mb-20, etc.)

    Reference implementation pattern from packages/web/src/components/GanttChart.tsx.

    **CRITICAL:**
    - Import CSS: `import 'gantt-lib/styles.css';` at the top
    - Use ISO date strings: '2026-03-24' not new Date()
    - State updates must use functional form: `setTasks(prev => updatedTasks)`
    - Do NOT include all the advanced props from web app (keep it simple)

    Sample tasks structure:
    - Task 1: "Анализ требований" (2026-03-24 to 2026-03-28, blue, progress 100)
    - Task 2: "Прототипирование" (2026-03-29 to 2026-04-05, purple, progress 40)
    - Task 3: "Разработка Frontend" (2026-04-06 to 2026-04-15, cyan, progress 0)
    - Task 4: "Тестирование" (2026-04-16 to 2026-04-20, orange, progress 0)
  </action>
  <verify>
    <automated>grep -q "import.*GanttChart.*from.*gantt-lib" packages/site/src/components/GanttPreview.tsx && grep -q "gantt-lib/styles.css" packages/site/src/components/GanttPreview.tsx</automated>
  </verify>
  <done>GanttPreview.tsx imports and renders GanttChart from gantt-lib with sample tasks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive gantt chart on homepage with drag-to-edit functionality</what-built>
  <how-to-verify>
    1. Start dev server: cd packages/site && npm run dev
    2. Open http://localhost:4321
    3. Scroll to demo section (below hero)
    4. Verify chart is visible with 4 task bars
    5. Try dragging a task bar horizontally:
       - Click and hold on a task bar
       - Drag left/right to reschedule
       - Release to confirm move
    6. Try resizing a task:
       - Hover over right edge of task bar
       - Drag edge to change duration
    7. Check browser console for errors (should be none)

    Expected: Task bars move smoothly, dates update, no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" if chart renders and drag works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Overall phase checks:
- gantt-lib package installed in packages/site
- GanttPreview.tsx imports from gantt-lib (not placeholder animation)
- Chart renders on homepage with visible task bars
- Drag-to-edit works (tasks move when dragged)
- Resize handles work (task duration changes)
- No console errors in browser
- Chart uses site theme colors (not default gantt-lib colors)
</verification>

<success_criteria>
Measurable completion:
- npm run dev starts without errors
- Homepage loads at http://localhost:4321
- Gantt chart is visible in demo section
- 4 sample tasks render with correct dates and colors
- Dragging a task reschedules it (date changes visible)
- Resizing a task changes its duration
- gantt-lib CSS is loaded (chart has proper styling)
</success_criteria>

<output>
After completion, create `.planning/phases/25/25-01-SUMMARY.md`
</output>
