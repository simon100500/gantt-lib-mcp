---
phase: 17-integration-cleanup
plan: 04
type: execute
wave: 4
depends_on: [17-01, 17-02, 17-03]
files_modified:
  - packages/server/src/ws.ts
  - packages/server/src/index.ts
autonomous: false
requirements:
  - INT-04
  - CLN-03
user_setup: []

must_haves:
  truths:
    - "WebSocket broadcasts work with new Prisma services"
    - "Agent responses stream via WebSocket correctly"
    - "Task updates from AI broadcast to all clients"
    - "No raw SQL queries in WebSocket code"
  artifacts:
    - path: "packages/server/src/ws.ts"
      provides: "WebSocket message broadcasting"
      contains: "broadcast"
      excludes: ["execute(", "sql:", "getDb()"]
  key_links:
    - from: "packages/server/src/ws.ts"
      to: "taskService"
      via: "import statement"
      pattern: "from '@gantt/mcp/services'"
    - from: "packages/server/src/ws.ts"
      to: "WebSocket clients"
      via: "broadcast function"
      pattern: "broadcast\("
---

<objective>
Verify and test WebSocket integration with Prisma services, ensuring real-time updates work correctly.

Purpose: Complete end-to-end verification that WebSocket broadcasting works with Prisma-backed services for real-time collaboration.

Output: WebSocket broadcasts verified working, all agent streaming functional, no raw SQL queries
</objective>

<execution_context>
@C:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-integration-cleanup/17-01-SUMMARY.md
@.planning/phases/17-integration-cleanup/17-02-SUMMARY.md
@.planning/phases/17-integration-cleanup/17-03-SUMMARY.md

<interfaces>
<!-- WebSocket broadcast functions (packages/server/src/ws.ts) -->
```typescript
export function broadcast(message: unknown): void
export function broadcastToSession(sessionId: string, message: unknown): void
export function onChatMessage(callback: (msg: string, userId: string, projectId: string, sessionId: string) => void): void
```

<!-- Agent with history (packages/server/src/agent.ts) -->
```typescript
export function runAgentWithHistory(
  message: string,
  projectId: string,
  sessionId: string
): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no raw SQL in WebSocket code</name>
  <files>packages/server/src/ws.ts</files>
  <action>
Verify that packages/server/src/ws.ts contains no raw SQL queries:

1. Check for common SQL patterns:
   ```bash
   grep -n "execute(" packages/server/src/ws.ts
   grep -n "sql:" packages/server/src/ws.ts
   grep -n "getDb()" packages/server/src/ws.ts
   grep -n "SELECT\|INSERT\|UPDATE\|DELETE" packages/server/src/ws.ts
   ```

2. Verify imports:
   - Should NOT import from @gantt/mcp/db
   - Should NOT import from @gantt/mcp/store
   - Should NOT import @libsql/client
   - Should only import services if needed

3. If any SQL found, replace with service calls (unlikely - ws.ts is just WebSocket plumbing)

Expected: ws.ts contains no database operations (only broadcasts message objects)
  </action>
  <verify>
    <automated>grep -E "(execute\(|sql:|getDb|SELECT|INSERT|UPDATE|DELETE)" packages/server/src/ws.ts || echo "No SQL found in ws.ts"</automated>
  </verify>
  <done>
    - No raw SQL queries in ws.ts
    - No database client imports in ws.ts
    - WebSocket code is pure messaging (no DB operations)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify agent uses taskService for broadcasts</name>
  <files>packages/server/src/agent.ts</files>
  <action>
Verify that packages/server/src/agent.ts uses taskService and broadcasts correctly:

1. Check imports:
   ```bash
   grep "from '@gantt/mcp" packages/server/src/agent.ts
   ```
   Should show: `import { taskService } from '@gantt/mcp/services';`
   Should NOT show: store, auth-store, db

2. Check WebSocket broadcast calls:
   ```bash
   grep -n "broadcast" packages/server/src/agent.ts
   ```
   Should show broadcasts after task operations

3. Verify task operation flow:
   - Agent calls taskService.create/update/delete
   - Agent receives updated task
   - Agent calls broadcast() or broadcastToSession() with updated task
   - WebSocket clients receive update

4. Check for proper error handling:
   - Try/catch around taskService calls
   - Error broadcasts on failure
   - No silent failures

Expected: Agent already migrated in plan 17-02, this is verification
  </action>
  <verify>
    <automated>grep "taskService" packages/server/src/agent.ts | head -1</automated>
  </verify>
  <done>
    - agent.ts imports taskService from @gantt/mcp/services
    - agent.ts has no taskStore or authStore references
    - WebSocket broadcasts called after task operations
    - Error handling in place for service failures
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify server index.ts WebSocket integration</name>
  <files>packages/server/src/index.ts</files>
  <action>
Verify that packages/server/src/index.ts properly integrates WebSocket and services:

1. Check imports:
   - Should import: `import { taskService, messageService } from '@gantt/mcp/services';`
   - Should import WebSocket routes: `import { registerWsRoutes, broadcast, broadcastToSession, onChatMessage } from './ws.js';`
   - Should NOT import store, auth-store, db

2. Verify chat message handler:
   ```bash
   grep -A5 "onChatMessage" packages/server/src/index.ts
   ```
   Should show runAgentWithHistory called with WebSocket callback

3. Verify WebSocket route registration:
   ```bash
   grep "registerWsRoutes" packages/server/src/index.ts
   ```
   Should show routes registered before server.listen()

4. Check API routes use taskService/messageService (already done in plan 17-02)

Expected: All integration complete from previous plans
  </action>
  <verify>
    <automated>grep -E "(taskService|messageService)" packages/server/src/index.ts | head -2</automated>
  </verify>
  <done>
    - index.ts imports services from @gantt/mcp/services
    - WebSocket routes registered correctly
    - Chat message handler integrates with agent
    - No legacy imports (store, auth-store, db)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete WebSocket integration with Prisma services, verified no raw SQL in WebSocket code</what-built>
  <how-to-verify>
1. Start server: `cd packages/server && npm run dev`
2. Start web app: `cd packages/web && npm run dev`
3. Open browser to http://localhost:5173
4. Login with OTP
5. Open Browser Console (F12) → Network tab → WS tab
6. Test AI agent:
   - Type message in chat: "Create a task called 'Test Task' starting tomorrow"
   - Verify WebSocket connection is open (WS tab shows connected state)
   - Verify agent response streams via WebSocket (should see tokens arriving)
   - Verify task created in Gantt chart
7. Test real-time updates:
   - Open second browser window to http://localhost:5173
   - Login with same account (or use share link)
   - In first window, drag a task to move it
   - Verify second window sees the change immediately
8. Check for errors:
   - Console should show no errors
   - WebSocket should stay connected
   - Task changes should persist
9. Verify broadcasts:
   - When agent creates task, should see WebSocket message with type: 'tasks'
   - Message should contain updated task list
   - UI should update automatically
  </how-to-verify>
  <resume-signal>Type "approved" if WebSocket streaming and real-time updates work correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. packages/server/src/ws.ts contains no raw SQL queries
2. packages/server/src/agent.ts uses taskService for all operations
3. packages/server/src/index.ts properly integrates WebSocket and services
4. Agent responses stream via WebSocket to clients
5. Task updates broadcast to all connected clients
6. Real-time collaboration works (multiple clients see changes)
7. No database errors in server logs
8. WebSocket connections stay stable
</verification>

<success_criteria>
1. No raw SQL queries in WebSocket code
2. WebSocket broadcasts work with Prisma services
3. Agent streaming works via WebSocket
4. Real-time updates functional across multiple clients
5. No console errors related to database or WebSocket
6. Task changes persist after page refresh
</success_criteria>

<output>
After completion, create `.planning/phases/17-integration-cleanup/17-04-SUMMARY.md`
</output>
