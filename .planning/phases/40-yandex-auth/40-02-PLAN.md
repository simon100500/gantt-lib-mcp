---
phase: 40-yandex-auth
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/components/OtpModal.tsx
  - packages/web/src/components/YandexAuthButton.tsx
  - packages/web/src/components/YandexCallbackPage.tsx
  - packages/web/src/lib/apiTypes.ts
autonomous: true
requirements: [YA-01, YA-03, YA-04]

must_haves:
  truths:
    - "Unauthenticated users see Yandex login as the default primary action"
    - "OTP is still available as a reserve fallback without breaking the existing OTP verification flow"
    - "The callback route `/auth/yandex/callback` works inside the SPA/web bundle and relays the token back to the opener flow"
    - "Successful Yandex login reuses the same post-login success path already used by OTP"
  artifacts:
    - path: "packages/web/src/components/OtpModal.tsx"
      provides: "Yandex-first auth UI with OTP fallback preserved"
      contains: "Войти по почте"
    - path: "packages/web/src/components/YandexAuthButton.tsx"
      provides: "SDK loading and widget bootstrap"
      exports: ["YandexAuthButton"]
    - path: "packages/web/src/components/YandexCallbackPage.tsx"
      provides: "Callback handler for /auth/yandex/callback"
      exports: ["YandexCallbackPage"]
    - path: "packages/web/src/App.tsx"
      provides: "Route handling for auth=yandex/auth=otp and callback screen"
      contains: "auth/yandex/callback"
  key_links:
    - from: "packages/web/src/components/YandexAuthButton.tsx"
      to: "packages/server/src/routes/auth-routes.ts"
      via: "POST /api/auth/yandex"
      pattern: "/api/auth/yandex"
---

<objective>
Implement the Yandex-first login UX in the web app while preserving OTP as a fallback.

Purpose: Phase 40 only delivers user value if the widget becomes the visible default login path and the callback flow can return the token into the existing app. This plan owns the browser-side integration end to end.
Output: Yandex-first auth modal, callback route handling, and shared success/failure wiring into existing auth state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-yandex-auth/40-CONTEXT.md
@.planning/phases/40-yandex-auth/40-01-PLAN.md

<interfaces>
<!-- Existing modal open behavior -->
```typescript
useEffect(() => {
  if (auth.isAuthenticated) return;
  const params = new URLSearchParams(route.search);
  if (params.get('auth') !== 'otp') return;
  setShowOtpModal(true);
}, [auth.isAuthenticated, route.search, setShowOtpModal]);
```

<!-- Existing success path -->
```typescript
const handleOtpSuccess = useCallback(async (result: AuthSuccessResponse) => {
  auth.login(result, result.user, result.project);
  setShowOtpModal(false);
  // imports local tasks + migrates local project name
}, [auth, localTasks, setShowOtpModal]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Yandex widget bootstrap component and callback screen</name>
  <files>packages/web/src/components/YandexAuthButton.tsx, packages/web/src/components/YandexCallbackPage.tsx</files>
  <read_first>
    .planning/phases/40-yandex-auth/docs.md
    packages/web/src/App.tsx
    packages/web/package.json
  </read_first>
  <behavior>
    - Widget bootstrap component loads the Yandex SDK only on demand/client side
    - Component initializes the widget/suggest flow with `client_id`, `response_type: 'token'`, `redirect_uri`, and `tokenPageOrigin` exactly per Yandex suggest docs
    - On success it receives the widget token payload and calls `POST /api/auth/yandex`
    - Callback screen exists at `/auth/yandex/callback` and executes the Yandex token-helper handoff flow
  </behavior>
  <action>
    1. Create `YandexAuthButton.tsx` to:
       - lazy-load the Yandex suggest SDK script
       - read `VITE_YANDEX_CLIENT_ID` from frontend env
       - configure `client_id`, `response_type: 'token'`, `redirect_uri: 'https://ai.getgantt.ru/auth/yandex/callback'`, and `tokenPageOrigin`
       - start the widget/suggest handler
       - POST the resulting token to `/api/auth/yandex`
       - surface loading/error states suitable for the modal UI
    2. Create `YandexCallbackPage.tsx` to:
       - load the Yandex token helper script
       - call the helper with the web-app origin `https://ai.getgantt.ru`
       - render a minimal "Возвращаем вас в приложение" fallback message
    3. Add small local typings/helpers for the Yandex globals if needed.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npm run build</automated>
  </verify>
  <done>
    - Browser bundle includes a dedicated Yandex login component
    - Callback route exists and can complete the postMessage token handoff
    - TypeScript build passes for new Yandex globals/components
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert the current OTP-first modal into a Yandex-first auth modal with OTP fallback</name>
  <files>packages/web/src/components/OtpModal.tsx</files>
  <read_first>
    packages/web/src/components/OtpModal.tsx
    packages/web/src/components/ui/button.tsx
  </read_first>
  <behavior>
    - Modal first screen shows Yandex as the primary login action
    - OTP remains reachable via a clear fallback action such as "Войти по почте"
    - Existing OTP email/code UX still works with minimal internal changes
    - Yandex errors do not block OTP fallback
  </behavior>
  <action>
    Update `OtpModal.tsx` so it becomes functionally an auth modal:

    1. Keep the existing OTP steps (`email`, `otp`) as the fallback branch
    2. Add a default branch/screen that shows:
       - title/description for quick login
       - `YandexAuthButton`
       - fallback trigger to enter the existing email OTP flow
    3. Preserve the close button, error rendering, and current OTP verification logic
    4. Ensure any Yandex success path calls the same `onSuccess(AuthSuccessResponse)` callback already used by OTP
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npm run build</automated>
  </verify>
  <done>
    - Primary auth UI is Yandex-first
    - OTP still works as backup inside the same modal
    - Existing OTP verification flow remains intact
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Route auth entry and callback handling through App.tsx without breaking existing login success behavior</name>
  <files>packages/web/src/App.tsx, packages/web/src/lib/apiTypes.ts</files>
  <read_first>
    packages/web/src/App.tsx
    packages/web/src/lib/apiTypes.ts
    packages/web/src/components/YandexCallbackPage.tsx
  </read_first>
  <behavior>
    - App recognizes `/auth/yandex/callback` and renders the callback screen instead of the workspace
    - Query-param auth routing supports both Yandex-first and OTP fallback (`auth=yandex` / `auth=otp`)
    - Existing post-login side effects are shared by OTP and Yandex success
  </behavior>
  <action>
    1. Update route parsing in `App.tsx`:
       - render `YandexCallbackPage` when `pathname === '/auth/yandex/callback'`
       - open the auth modal for `auth=yandex`
       - preserve explicit `auth=otp`
       - optionally default to Yandex-first when the modal is opened from existing login buttons
    2. Rename the local success handler if helpful (`handleAuthSuccess`) and ensure both OTP and Yandex paths use it
    3. Keep `AuthSuccessResponse` unchanged unless frontend typing needs minor extension.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npm run build</automated>
  </verify>
  <done>
    - Callback route is handled without backend SSR/static-route assumptions
    - Both auth methods share one success path
    - App still builds and route logic remains understandable
  </done>
</task>

</tasks>
