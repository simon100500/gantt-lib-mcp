---
phase: 40-yandex-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/routes/auth-routes.ts
  - packages/mcp/src/services/auth.service.ts
  - packages/server/src/services/yandex-auth-service.ts
  - packages/server/src/services/yandex-auth-service.test.ts
autonomous: true
requirements: [YA-01, YA-02, YA-03]

must_haves:
  truths:
    - "Backend exposes a Yandex login endpoint that accepts a widget-issued Yandex OAuth access token and returns the same local auth payload shape as OTP login"
    - "Yandex token validation and profile lookup are isolated in a dedicated service/helper instead of being inlined in the route"
    - "OTP and Yandex login share one session issuance path so project bootstrap and JWT semantics stay consistent"
    - "Login fails cleanly when Yandex identity cannot be resolved to a usable email"
    - "Any Yandex Client Secret handling remains backend-only and is never referenced from the web package"
  artifacts:
    - path: "packages/server/src/routes/auth-routes.ts"
      provides: "POST /api/auth/yandex route wired into existing auth router"
      contains: "/api/auth/yandex"
    - path: "packages/server/src/services/yandex-auth-service.ts"
      provides: "Yandex token validation and profile normalization"
      exports: ["YandexAuthService"]
    - path: "packages/mcp/src/services/auth.service.ts"
      provides: "Shared login/session issuance helper reusable by OTP and Yandex flows"
      contains: "findOrCreateUser"
  key_links:
    - from: "packages/server/src/routes/auth-routes.ts"
      to: "packages/mcp/src/services/auth.service.ts"
      via: "Shared local account bootstrap and session issuance"
      pattern: "createSession|updateSessionTokens|findOrCreateUser"
---

<objective>
Implement the backend half of Yandex quick login without disturbing the current OTP fallback.

Purpose: The widget can only become the primary auth path if the server can validate a Yandex token, map it onto the existing local user/project/session model, and return the same response contract already consumed by the web app.
Output: Yandex auth service, `/api/auth/yandex` route, and shared session issuance path used by both OTP and Yandex login.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-yandex-auth/40-CONTEXT.md

<interfaces>
<!-- Existing OTP success response contract -->
```typescript
interface AuthSuccessResponse {
  accessToken: string;
  refreshToken: string;
  user: { id: string; email: string };
  project: {
    id: string;
    name: string;
    status: 'active' | 'archived';
    ganttDayMode: 'business' | 'calendar';
    calendarId?: string | null;
    calendarDays?: Array<{ date: string; kind: 'working' | 'non_working' | 'shortened' }>;
    archivedAt?: string | null;
    deletedAt?: string | null;
  };
}
```

<!-- Current OTP route shape -->
```typescript
fastify.post('/api/auth/verify-otp', async (req, reply) => {
  // consumes OTP
  // findOrCreateUser(email)
  // ensure default project
  // create session
  // sign access+refresh token
  // return AuthSuccessResponse
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Yandex token verification service and normalized profile contract</name>
  <files>packages/server/src/services/yandex-auth-service.ts, packages/server/src/services/yandex-auth-service.test.ts</files>
  <read_first>
    .planning/phases/40-yandex-auth/docs.md
    packages/server/package.json
    packages/server/src/routes/auth-routes.ts
  </read_first>
  <behavior>
    - Service accepts a Yandex OAuth access token issued by the widget/suggest flow
    - Service requests Yandex user info from the official identity endpoint and normalizes `{ id, defaultEmail, emails, displayName }`
    - Service chooses a canonical email for local login and rejects when no usable email is present
    - Error surfaces are classified into invalid token, upstream failure, and profile-without-email cases
    - If backend credentials are needed for the chosen verification/exchange variant, they are read from server env only
  </behavior>
  <action>
    Create `packages/server/src/services/yandex-auth-service.ts` with:

    1. `YandexAuthService` class exposing `getProfile(accessToken: string): Promise<NormalizedYandexProfile>`
    2. Internal fetch to Yandex identity endpoint using the widget-issued token
    3. Small normalization helper that prefers the default/primary email, falls back to first non-empty email, and trims/lowercases it
    4. Explicit error messages for:
       - missing token
       - Yandex returned unauthorized/invalid token
       - Yandex profile has no email
       - upstream request failed
    5. If the implementation path needs server credentials, read them from backend env such as `YANDEX_CLIENT_SECRET`; do not add any secret handling to `packages/web`

    Add focused tests that mock `fetch` and cover the success path plus the three failure classes above.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsc -p packages/server/tsconfig.json --noEmit</automated>
  </verify>
  <done>
    - Yandex token verification is isolated in its own service
    - Service returns normalized profile data with a canonical email
    - Test coverage exists for success and expected failures
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor auth bootstrap so OTP and Yandex share one local session issuance path</name>
  <files>packages/mcp/src/services/auth.service.ts, packages/server/src/routes/auth-routes.ts</files>
  <read_first>
    packages/mcp/src/services/auth.service.ts
    packages/server/src/routes/auth-routes.ts
    packages/server/src/auth.ts
  </read_first>
  <behavior>
    - Route-level code no longer duplicates "find/create user -> ensure default project -> create session -> sign tokens -> return payload"
    - Shared helper accepts an email and returns the existing `AuthSuccessResponse`-shaped payload
    - Existing OTP flow keeps the same observable response contract
  </behavior>
  <action>
    1. Extract the current local login bootstrap into a shared helper, either:
       - a new method on `AuthService`, or
       - a small auth-session helper in `auth-routes.ts`
    2. Ensure the helper:
       - finds or creates the local user by email
       - creates a default project when none exists
       - creates a session row first
       - signs local access/refresh tokens with the actual session id
       - updates stored session tokens
       - returns the same JSON shape currently used by OTP login
    3. Update the OTP route to call the shared helper and keep its behavior unchanged.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsc -p packages/server/tsconfig.json --noEmit</automated>
  </verify>
  <done>
    - OTP login still returns the same payload shape
    - Shared bootstrap helper is reusable by the upcoming Yandex route
    - Token/session semantics stay unchanged
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose POST /api/auth/yandex using the shared session bootstrap</name>
  <files>packages/server/src/routes/auth-routes.ts</files>
  <read_first>
    packages/server/src/routes/auth-routes.ts
    packages/server/src/services/yandex-auth-service.ts
    packages/mcp/src/services/auth.service.ts
  </read_first>
  <behavior>
    - `POST /api/auth/yandex` accepts `{ accessToken: string }`
    - Route validates the request body, resolves Yandex profile, and logs the user in via the shared bootstrap helper
    - On success, response matches OTP `AuthSuccessResponse`
    - On failure, route returns a stable user-facing error for invalid token / missing email / upstream failure
  </behavior>
  <action>
    Add a new route to `packages/server/src/routes/auth-routes.ts`:

    1. Parse and validate `accessToken`
    2. Call `YandexAuthService.getProfile(accessToken)`
    3. Pass the normalized email into the shared local login bootstrap helper
    4. Return the bootstrap result as JSON
    5. Map expected failures to 400/401/502-level responses with concise messages the frontend can render
    6. Keep OTP routes untouched beyond the shared-helper refactor
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp && npx tsc -p packages/server/tsconfig.json --noEmit</automated>
  </verify>
  <done>
    - `/api/auth/yandex` exists and is wired into the main auth router
    - Backend can transform a Yandex access token into the app's local auth session
    - Failure cases are explicit and frontend-safe
  </done>
</task>

</tasks>
