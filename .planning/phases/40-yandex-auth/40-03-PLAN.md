---
phase: 40-yandex-auth
plan: 03
type: execute
wave: 3
depends_on: ["40-01", "40-02"]
files_modified:
  - packages/web/.env
  - .planning/phases/40-yandex-auth/docs.md
autonomous: true
requirements: [YA-04, YA-05]

must_haves:
  truths:
    - "Phase 40 does not modify `packages/site` or ask Astro pages to control login"
    - "Environment/config requirements for the Yandex widget are documented where the phase team can find them"
    - "Manual verification explicitly covers both the primary Yandex path and the OTP reserve path"
    - "Client ID is documented as frontend env and Client Secret as backend-only env"
  artifacts:
    - path: "packages/web/.env"
      provides: "Frontend widget configuration placeholder"
      contains: "VITE_YANDEX_CLIENT_ID"
    - path: ".planning/phases/40-yandex-auth/docs.md"
      provides: "Phase-local implementation notes, env checklist, and verification checklist"
      contains: "VITE_YANDEX_CLIENT_ID"
---

<objective>
Finish the config and verification layer for Yandex-first login so implementation can ship safely.

Purpose: This phase should not spill into Astro or marketing-site routing. The remaining work is to document the exact env contract, keep secrets in the correct place, and lock in a verification checklist that proves OTP still survives as the reserve path.
Output: Env/docs updates and a concise manual verification matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-yandex-auth/40-CONTEXT.md
@.planning/phases/40-yandex-auth/40-01-PLAN.md
@.planning/phases/40-yandex-auth/40-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document frontend and backend env split for Yandex credentials</name>
  <files>.planning/phases/40-yandex-auth/docs.md, packages/web/.env</files>
  <read_first>
    .planning/phases/40-yandex-auth/docs.md
    packages/web/.env
  </read_first>
  <behavior>
    - Docs state that `VITE_YANDEX_CLIENT_ID` is the only Yandex credential allowed in frontend env
    - Docs state that `YANDEX_CLIENT_SECRET` is backend-only and must not appear in `packages/web`
    - `.env` examples or local env placeholders reflect this split clearly for the frontend, while backend-only secret storage is documented in phase docs
  </behavior>
  <action>
    1. Extend `.planning/phases/40-yandex-auth/docs.md` with:
       - exact suggest/widget flow parameters
       - `redirect_uri = https://ai.getgantt.ru/auth/yandex/callback`
       - `tokenPageOrigin = https://ai.getgantt.ru/auth/yandex/callback`
       - `YaSendSuggestToken('https://ai.getgantt.ru', extraData)` on the callback page
       - frontend/backend env split
    2. Update `packages/web/.env` to include a placeholder for `VITE_YANDEX_CLIENT_ID`
    3. Document `YANDEX_CLIENT_SECRET` only as backend env in phase docs; do not create or require a new server env file as part of this phase plan
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npm run build</automated>
  </verify>
  <done>
    - Frontend and backend credential boundaries are explicit
    - No secret is planned for storage in the web package
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lock in a manual verification checklist for web-only auth flow</name>
  <files>.planning/phases/40-yandex-auth/docs.md</files>
  <read_first>
    .planning/phases/40-yandex-auth/docs.md
  </read_first>
  <behavior>
    - Local phase docs include the exact callback URL and a short verification checklist for both Yandex and OTP fallback
    - Checklist is scoped only to `packages/web` + backend
    - Docs explicitly say `packages/site` is out of scope for this phase
  </behavior>
  <action>
    Add a concise verification checklist covering:
    - Yandex widget starts from the web auth modal
    - callback page returns token to the opener
    - backend login succeeds and returns existing app auth payload
    - existing post-login import flow still works
    - OTP fallback still works end to end
    - no Astro/site changes are involved
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npm run build</automated>
  </verify>
  <done>
    - Config requirements are written down next to the phase
    - Developers can validate both login paths without reverse-engineering the flow
    - The phase boundary is clearly web-only
  </done>
</task>

</tasks>
