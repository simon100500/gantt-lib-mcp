---
phase: 40-yandex-auth
plan: 03
type: execute
wave: 3
depends_on: ["40-01", "40-02"]
files_modified:
  - packages/site/src/components/Header.astro
  - packages/site/src/components/Footer.astro
  - packages/site/src/pages/features.astro
  - packages/site/src/pages/faq.astro
  - packages/site/src/pages/pricing.astro
  - packages/web/.env
  - .planning/phases/40-yandex-auth/docs.md
autonomous: true
requirements: [YA-04, YA-05]

must_haves:
  truths:
    - "Public product entry points stop hardcoding OTP-first deep-links and send users into the Yandex-first flow"
    - "Environment/config requirements for the Yandex widget are documented where the phase team can find them"
    - "Manual verification explicitly covers both the primary Yandex path and the OTP reserve path"
  artifacts:
    - path: "packages/site/src/components/Header.astro"
      provides: "Yandex-first auth CTA"
      contains: "auth=yandex"
    - path: "packages/site/src/pages/pricing.astro"
      provides: "Pricing CTA aligned with Yandex-first login"
      contains: "auth=yandex"
    - path: ".planning/phases/40-yandex-auth/docs.md"
      provides: "Phase-local implementation notes, env checklist, and verification checklist"
      contains: "VITE_YANDEX_CLIENT_ID"
  key_links:
    - from: "packages/site/src/components/Header.astro"
      to: "packages/web/src/App.tsx"
      via: "Deep-link query parameter to open Yandex-first auth modal"
      pattern: "auth=yandex"
---

<objective>
Finish the rollout for Yandex-first login so the feature is discoverable and operational after implementation.

Purpose: A working widget hidden behind OTP-first deep-links would miss the product goal. This plan updates entry points, records config requirements, and locks in a verification checklist that proves OTP still survives as the reserve path.
Output: Yandex-first CTA rollout, env/docs updates, and a concise manual verification matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-yandex-auth/40-CONTEXT.md
@.planning/phases/40-yandex-auth/40-01-PLAN.md
@.planning/phases/40-yandex-auth/40-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Switch marketing and product deep-links from OTP-first to Yandex-first</name>
  <files>packages/site/src/components/Header.astro, packages/site/src/components/Footer.astro, packages/site/src/pages/features.astro, packages/site/src/pages/faq.astro, packages/site/src/pages/pricing.astro</files>
  <read_first>
    packages/site/src/components/Header.astro
    packages/site/src/components/Footer.astro
    packages/site/src/pages/features.astro
    packages/site/src/pages/faq.astro
    packages/site/src/pages/pricing.astro
  </read_first>
  <behavior>
    - Entry points that currently use `?auth=otp` move to `?auth=yandex` or the new default auth entry
    - No user-facing CTA loses the ability to log in; only the primary method changes
    - Reserve OTP access remains available inside the app modal, so public pages do not need dedicated OTP-first links by default
  </behavior>
  <action>
    Replace OTP-first deep-links in the site package with the Yandex-first auth entry. Keep changes narrow to auth/login CTA targets and avoid unrelated copy/layout edits.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/site && npm run build</automated>
  </verify>
  <done>
    - Public CTAs no longer force OTP as the first auth step
    - Yandex-first path is discoverable from marketing surfaces
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document widget configuration and fallback verification</name>
  <files>.planning/phases/40-yandex-auth/docs.md, packages/web/.env</files>
  <read_first>
    .planning/phases/40-yandex-auth/docs.md
    packages/web/.env
  </read_first>
  <behavior>
    - Local phase docs state the required frontend config values for the Yandex widget flow
    - Docs include the exact callback URL and a short verification checklist for both Yandex and OTP fallback
    - `.env` example or local env file clearly reflects the new client-side variable name(s)
  </behavior>
  <action>
    1. Extend `.planning/phases/40-yandex-auth/docs.md` with:
       - required env vars (for example `VITE_YANDEX_CLIENT_ID`)
       - callback URL
       - manual verification checklist
    2. Update `packages/web/.env` to include a development placeholder for the Yandex client id if that file is already used for local frontend runtime config.
  </action>
  <verify>
    <automated>cd D:/Projects/gantt-lib-mcp/packages/web && npm run build</automated>
  </verify>
  <done>
    - Config requirements are written down next to the phase
    - Developers can validate both login paths without reverse-engineering the flow
  </done>
</task>

</tasks>
