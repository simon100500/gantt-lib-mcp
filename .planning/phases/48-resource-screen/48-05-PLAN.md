---
phase: 48-resource-screen
plan: 05
title: Hardening tests and ResourceTimelineGrid deprecation handling
type: execute
wave: 5
depends_on:
  - 48-04
files_modified:
  - packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
  - packages/web/src/components/workspace/ResourceTimelineGrid.tsx
  - packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx
  - packages/web/src/components/workspace/__tests__/ResourceTimelineGrid.test.tsx
  - packages/web/src/components/workspace/__tests__/M004.resource-planning-flow.test.tsx
autonomous: true
requirements_addressed:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-TESTS
  - PRD-RESOURCE-A11Y
  - PRD-RESOURCE-MOVE
requirements:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-TESTS
  - PRD-RESOURCE-A11Y
  - PRD-RESOURCE-MOVE
must_haves:
  truths:
    - "Phase 48 regression tests cover mapper, metadata helper, filters, renderItem states, conflict action, move flows, readonly/locked blocking, empty rows, and error rollback."
    - "ResourceTimelineGrid is explicitly deprecated/fallback-only and not imported by ResourcePlannerWorkspace."
    - "Unauthenticated users see readonly explanatory copy and no mutation callbacks are emitted."
  artifacts:
    - path: "packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx"
      provides: "End-to-end component regression coverage for the resource screen"
    - path: "packages/web/src/components/workspace/ResourceTimelineGrid.tsx"
      provides: "Deprecated fallback/test fixture marker only"
  key_links:
    - from: "test suite"
      to: "locked PRD decisions"
      via: "coverage assertions for renderer, filters, moves, readonly, empty rows, and rollback"
---

<objective>
Harden the implementation with complete targeted coverage and make the old local grid clearly fallback-only.

Purpose: close the PRD acceptance criteria and prevent regression back to local calendar geometry.
Output: regression tests, legacy grid deprecation marker/test update, build verification.
</objective>

<context>
@.planning/phases/48-resource-screen/48-CONTEXT.md
@.planning/phases/48-resource-screen/48-UI-SPEC.md
@RESOURCE-MANAGEMENT-SCREEN-PRD.md
@RESOURCE-PLANNER-MODE-PRD.md
@packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
@packages/web/src/components/workspace/ResourceTimelineGrid.tsx
@packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx
@packages/web/src/components/workspace/__tests__/ResourceTimelineGrid.test.tsx
@packages/web/src/components/workspace/__tests__/M004.resource-planning-flow.test.tsx

Constraints:
- Do not delete ResourceTimelineGrid unless all existing tests and imports are updated safely; it may remain as deprecated fallback/test fixture.
- Add no new product scope beyond PRDs.
- Keep Russian UI copy exactly where specified.
- Build must pass after all prior plans.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete resource screen regression coverage</name>
  <files>packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx, packages/web/src/components/workspace/__tests__/M004.resource-planning-flow.test.tsx</files>
  <behavior>
    - Tests cover adapter and metadata helper indirectly through rendered resources and directly via plan 48-01 tests.
    - Tests cover filters, renderItem visual states, conflict action, date move flow, reassignment flow, readonly/locked blocking, empty rows, and error rollback.
    - Tests verify unauthenticated readonly copy: `Войдите, чтобы изменять ресурсы. Сейчас календарь открыт только для просмотра.`
    - Tests verify UI-SPEC copy for save error, version conflict, and partial combined failure.
  </behavior>
  <action>Audit the accumulated tests from plans 48-01 through 48-04 against the PRD acceptance list. Add missing cases to ResourcePlannerWorkspace and M004 flow tests using existing jsdom/mocked fetch patterns. Prefer assertions on props/callbacks, fetch calls, visible copy, and store state over brittle visual snapshots.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx packages/web/src/components/workspace/__tests__/M004.resource-planning-flow.test.tsx</automated>
  </verify>
  <acceptance_criteria>All required test areas from 48-CONTEXT Testing section are represented by named tests and pass.</acceptance_criteria>
  <done>Phase 48 regression tests cover all required mapper, filter, render, move, readonly, empty row, and rollback behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Mark ResourceTimelineGrid fallback-only and verify no primary import remains</name>
  <files>packages/web/src/components/workspace/ResourceTimelineGrid.tsx, packages/web/src/components/workspace/__tests__/ResourceTimelineGrid.test.tsx, packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx</files>
  <action>Add a concise `@deprecated` JSDoc above `ResourceTimelineGrid` export stating it is a fallback/test fixture and not the resource screen primary renderer. Update legacy tests only if naming/copy changed during prior plans. Verify `ResourcePlannerWorkspace.tsx` does not import or render `ResourceTimelineGrid`. Do not remove the file if any legacy tests still exercise it as a fixture.</action>
  <verify>
    <automated>rg -n "ResourceTimelineGrid" packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourceTimelineGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>Old grid is documented as deprecated fallback-only, its fixture tests still pass, and ResourcePlannerWorkspace has no ResourceTimelineGrid import/reference.</acceptance_criteria>
  <done>ResourceTimelineGrid is fallback-only and ResourcePlannerWorkspace has no reference to it.</done>
</task>

<task type="auto">
  <name>Task 3: Run final web build and focused test pack</name>
  <files>packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx, packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx</files>
  <action>Run the full focused verification pack for Phase 48 and fix any type/test regressions caused by integration. Keep fixes scoped to Phase 48 files. Do not refactor unrelated workspace behavior.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerAdapter.test.ts packages/web/src/components/workspace/__tests__/resourcePlannerFilters.test.ts packages/web/src/components/workspace/__tests__/resourcePlannerMoves.test.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx packages/web/src/components/workspace/__tests__/M004.resource-planning-flow.test.tsx</automated>
    <automated>npm run build -w packages/web</automated>
  </verify>
  <acceptance_criteria>Focused Phase 48 tests and web build pass.</acceptance_criteria>
  <done>Focused Phase 48 test pack and `npm run build -w packages/web` pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tests -> mutation mocks | Regression tests simulate authenticated mutation APIs. |
| Deprecated fallback -> future imports | Legacy grid could be accidentally reused as primary renderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-48-05-01 | Tampering | mocked mutation tests | mitigate | Assert request method/body/headers for command and assignment endpoints, not only UI copy. |
| T-48-05-02 | Elevation of Privilege | readonly/locked regressions | mitigate | Add negative tests that no mutation callback/network call occurs without auth or when item locked. |
| T-48-05-03 | Denial of Service | large resource sets | accept | No new performance harness; build and memoized filters are verified, large-set manual perf remains residual risk. |
</threat_model>

<verification>
Run:
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerAdapter.test.ts packages/web/src/components/workspace/__tests__/resourcePlannerFilters.test.ts packages/web/src/components/workspace/__tests__/resourcePlannerMoves.test.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx packages/web/src/components/workspace/__tests__/M004.resource-planning-flow.test.tsx`
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourceTimelineGrid.test.tsx`
- `npm run build -w packages/web`
</verification>

<success_criteria>
- Required Phase 48 coverage is present and passing.
- Old local grid cannot silently remain the primary renderer.
- Readonly, locked, empty-row, conflict, move, and rollback behavior are regression-tested.
- Web package builds successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/48-resource-screen/48-05-SUMMARY.md`.
</output>
