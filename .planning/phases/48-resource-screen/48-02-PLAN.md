---
phase: 48-resource-screen
plan: 02
title: Filters, selection, details, and accessible fallback actions
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
  - packages/web/src/components/workspace/resourcePlannerFilters.ts
  - packages/web/src/components/workspace/ResourceAssignmentDetailsPanel.tsx
  - packages/web/src/components/workspace/__tests__/resourcePlannerFilters.test.ts
  - packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx
autonomous: true
requirements_addressed:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-FILTERS
  - PRD-RESOURCE-A11Y
requirements:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-FILTERS
  - PRD-RESOURCE-A11Y
must_haves:
  truths:
    - "Users can filter resources client-side by query, type, conflict-only, and inactive visibility."
    - "Scope switch remains the only filter action that reloads `/api/resources/planner?scope=...`."
    - "Clicking or keyboard-opening a bar shows assignment details and fallback forms/actions."
  artifacts:
    - path: "packages/web/src/components/workspace/resourcePlannerFilters.ts"
      provides: "Pure client-side filtering helpers"
    - path: "packages/web/src/components/workspace/ResourceAssignmentDetailsPanel.tsx"
      provides: "Details drawer/panel for selected assignment"
  key_links:
    - from: "ResourcePlannerWorkspace.tsx"
      to: "resourcePlannerFilters.ts"
      via: "filtered timeline resources memo"
    - from: "GanttChart renderItem"
      to: "ResourceAssignmentDetailsPanel.tsx"
      via: "selected item metadata"
---

<objective>
Add the operational controls around the resource planner: client-side filters, selection, assignment details, and accessible non-drag actions.

Purpose: make the gantt-lib timeline usable as a full resource management screen, not just a rendered chart.
Output: filter helper, details panel, UI wiring, and targeted tests.
</objective>

<context>
@.planning/phases/48-resource-screen/48-CONTEXT.md
@.planning/phases/48-resource-screen/48-UI-SPEC.md
@packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
@packages/web/src/components/workspace/resourcePlannerAdapter.ts
@packages/web/src/components/workspace/resourceAssignmentUtils.ts
@packages/web/src/stores/useProjectStore.ts
@packages/web/src/stores/useUIStore.ts

Constraints:
- Implement PRD filters exactly: scope, text search over resource/task/project, resource type, conflict-only, include inactive.
- Scope switch reloads backend; all other filters are client-side.
- Preserve empty resource rows where they match the resource filters and when needed for selected/pending context.
- Details copy from UI-SPEC remains Russian: `Детали назначения`, `Открыть задачу`, `Исправить конфликт`, `Сменить ресурс`, `Убрать ресурс с задачи`.
- Full keyboard drag/drop is not required; details panel must expose fallback forms for changing dates and resource.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement pure resource planner filters</name>
  <files>packages/web/src/components/workspace/resourcePlannerFilters.ts, packages/web/src/components/workspace/__tests__/resourcePlannerFilters.test.ts</files>
  <behavior>
    - Query matches resource name, item title/task name, and project subtitle/name case-insensitively.
    - Resource type filter uses ProjectResource catalog data by resource id.
    - Conflict-only keeps resources/items with conflicts and hides non-matching non-empty rows.
    - Inactive resources are hidden unless includeInactive is true.
    - Empty resource rows are preserved when the resource itself matches active/type/query filters.
  </behavior>
  <action>Create typed filter state and `filterResourceTimelineResources(resources, catalogResources, filters, options?)`. Keep this pure and independent from React. Do not fetch in filter helpers. Use metadata helper from plan 48-01 for item-level project/conflict data.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests cover query, type, conflict-only, inactive visibility, and empty row preservation.</acceptance_criteria>
  <done>Pure filter helper implements all PRD filters without backend calls and passes focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire filter controls and selected assignment details</name>
  <files>packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx, packages/web/src/components/workspace/ResourceAssignmentDetailsPanel.tsx, packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx</files>
  <behavior>
    - Screen title is `Ресурсы`; subtitle is `Текущий проект` or `Все проекты workspace`.
    - Header actions include `Создать ресурс`, `Обновить`, and `Вернуться в проект`.
    - All filter controls have visible labels and focusable form controls.
    - Bar click and Enter/Space on a focused bar select the assignment and open details.
    - Esc closes details and does not mutate planner state.
    - Details action `Исправить конфликт` preserves `onCorrectConflict` flow.
  </behavior>
  <action>Restructure ResourcePlannerWorkspace around the UI-SPEC shell: header, status strip, summary, filters, center timeline, and details panel. Add selected item/resource state. Update `renderItem` content to include a focusable control or keyboard handler that opens details with Enter/Space. Details panel should render task, project, resource, dates, assignment id, conflict ids, disabled-state explanations, and explicit callback props for fallback date/resource forms; plan 48-04 will implement those callbacks using the same persistence path as drag.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx</automated>
  </verify>
  <acceptance_criteria>Tests prove filters do not refetch except scope switch, details open/close behavior works, Russian copy matches UI-SPEC, and conflict action still emits PlannerCorrectionTarget.</acceptance_criteria>
  <done>Resource screen exposes filters, selected assignment details, and accessible open/close behavior with required Russian copy.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User filter input -> renderer | User-controlled text filters local data. |
| Timeline item selection -> details actions | User selects assignment ids that later plans will use for mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-48-02-01 | Denial of Service | client-side filters | mitigate | Use memoized pure filtering and avoid regex construction from user text. |
| T-48-02-02 | Spoofing | details selected item | mitigate | Details must read ids through typed metadata helper, not DOM text. |
| T-48-02-03 | Information Disclosure | all-projects details | mitigate | Show only backend-returned planner fields and disabled explanations; no extra cross-project fetch in this plan. |
</threat_model>

<verification>
Run:
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerFilters.test.ts`
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx`
- `npm run build -w packages/web`
</verification>

<success_criteria>
- Filters are client-side except scope switch.
- Details panel/drawer exposes assignment facts and accessible fallback actions.
- Timeline remains keyboard-accessible for opening selected assignment details.
- UI copy specified in the UI-SPEC remains Russian.
</success_criteria>

<output>
After completion, create `.planning/phases/48-resource-screen/48-02-SUMMARY.md`.
</output>
