---
phase: 48-resource-screen
plan: 04
title: Controlled move persistence for dates and resources
type: execute
wave: 4
depends_on:
  - 48-03
files_modified:
  - packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
  - packages/web/src/components/workspace/resourcePlannerMoves.ts
  - packages/web/src/components/workspace/ResourceAssignmentDetailsPanel.tsx
  - packages/web/src/components/workspace/__tests__/resourcePlannerMoves.test.ts
  - packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx
autonomous: true
requirements_addressed:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-MOVE
  - PRD-RESOURCE-A11Y
requirements:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-MOVE
  - PRD-RESOURCE-A11Y
must_haves:
  truths:
    - "Date-only drops persist through the existing project command commit flow."
    - "Resource reassignment drops replace only the moved resource while preserving the task's other resource assignments."
    - "Combined date/resource drops apply date first, reassignment second, and report partial success if the second step fails."
    - "Successful mutations reload `/api/resources/planner?scope=...`; failures clear pending state and preserve last successful data."
  artifacts:
    - path: "packages/web/src/components/workspace/resourcePlannerMoves.ts"
      provides: "Pure move classification and assignment replacement helpers"
    - path: "packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx"
      provides: "onResourceItemMove controlled persistence flow"
  key_links:
    - from: "GanttChart onResourceItemMove"
      to: "useCommandCommit / /api/commands/commit"
      via: "move_task or resize_task command"
    - from: "resource reassignment"
      to: "/api/tasks/:taskId/assignments"
      via: "full resourceIds replacement"
    - from: "successful mutation"
      to: "/api/resources/planner?scope=..."
      via: "authoritative reload"
---

<objective>
Wire `onResourceItemMove` as controlled persistence for date moves, resource reassignment, combined moves, pending/error states, and details fallback forms.

Purpose: make drag/drop and accessible fallback actions save real backend state without false optimistic conflict state.
Output: move helper module, persistence handlers, pending UI, rollback/error tests.
</objective>

<context>
@.planning/phases/48-resource-screen/48-CONTEXT.md
@.planning/phases/48-resource-screen/48-UI-SPEC.md
@packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
@packages/web/src/components/workspace/ResourceAssignmentDetailsPanel.tsx
@packages/web/src/components/workspace/resourcePlannerAdapter.ts
@packages/web/src/components/workspace/resourceAssignmentUtils.ts
@packages/web/src/hooks/useCommandCommit.ts
@packages/web/src/hooks/useProjectCommands.ts
@packages/web/src/stores/useProjectStore.ts
@packages/server/src/routes/resource-routes.ts
@packages/server/src/routes/command-routes.ts
@D:/Projects/gantt-lib/docs/reference/10-drag-interactions.md
@D:/Projects/gantt-lib/docs/reference/12-validation.md

Constraints:
- `onResourceItemMove` fires once on mouseup; do not persist during mousemove.
- Same-resource date move with preserved duration uses `move_task` with new `startDate`.
- Edge/duration changes use `resize_task` or a coordinated command sequence if both edges changed.
- Reassignment uses `POST /api/tasks/:taskId/assignments` with full `resourceIds[]`.
- Combined moves apply date first and assignment second; if assignment fails, reload authoritative state and show `Даты назначения сохранены, но ресурс не изменён. Календарь обновлён по данным сервера.`
- No `accessToken`, readonly, locked, and forbidden cross-project states must not emit mutation callbacks.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create pure move classification and assignment replacement helpers</name>
  <files>packages/web/src/components/workspace/resourcePlannerMoves.ts, packages/web/src/components/workspace/__tests__/resourcePlannerMoves.test.ts</files>
  <behavior>
    - Classifies date-only, resource-only, combined, and no-op moves.
    - Converts Date payloads to `YYYY-MM-DD` UTC strings.
    - Builds command sequence: preserved-duration date move -> `move_task`; start edge -> `resize_task start`; end edge -> `resize_task end`; both edge/duration change -> two `resize_task` commands in safe order.
    - Builds replacement `resourceIds[]` by replacing only `fromResourceId` with `toResourceId` and deduping while preserving other assigned resources.
    - Rejects missing task id/metadata and locked moves with typed reasons usable by UI.
  </behavior>
  <action>Implement helpers independent of React/fetch. Reuse `FrontendProjectCommand` types from web types and metadata helper from adapter. Cover edge cases in tests before wiring UI.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerMoves.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests cover date move, reassignment, combined move, no-op, locked rejection, missing task id, resource replacement, and ISO date normalization.</acceptance_criteria>
  <done>Move helper classifies and builds commands/resource replacements for all PRD move cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist date/resource moves and fallback detail forms</name>
  <files>packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx, packages/web/src/components/workspace/ResourceAssignmentDetailsPanel.tsx, packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx</files>
  <behavior>
    - `onResourceItemMove` sets pending item id, disables further mutation for that item, persists once, and reloads planner after success.
    - Date commands use `useCommandCommit` with history title `Перенос назначения` and update project store from returned snapshot.
    - Reassignment POST sends full `resourceIds` replacement and updates store assignments from response when safe.
    - If assignment data for a cross-project item is unavailable, reload `/api/project`; if still insufficient, show controlled error and reload planner.
    - Save failures show `Не удалось сохранить изменение. Данные возвращены к последнему состоянию сервера.`, clear pending state, and preserve last successful planner data.
    - Details fallback forms call the same persistence path as drag.
  </behavior>
  <action>Use `useCommandCommit(accessToken)` directly or existing project command helper if cleaner. Add `pendingMoveIds: Set<string>` and save error state. Pass `readonly` and `disableResourceReassignment` to gantt-lib according to accessToken/readOnly/scope permissions. Ensure readonly/locked branches omit mutation callbacks or return before network calls. After any accepted mutation, call `loadPlanner(plannerScope, { keepData: true })`.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx</automated>
  </verify>
  <acceptance_criteria>Tests cover command commit for date move, reassignment POST body, combined partial failure message, pending clearing, planner reload after success/failure, readonly/locked blocking, and fallback form persistence.</acceptance_criteria>
  <done>Drag and details fallback forms persist date/resource changes through existing backend flows with pending/error/reload handling.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `onResourceItemMove` -> command commit | User-controlled drag becomes authenticated project schedule mutation. |
| `onResourceItemMove` -> assignment endpoint | User-controlled resource target becomes authenticated assignment replacement. |
| Backend response -> project store | Authoritative snapshots/assignments update local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-48-04-01 | Tampering | move payload | mitigate | Derive task/resource ids from typed metadata and current store; validate locked/readonly before any request. |
| T-48-04-02 | Elevation of Privilege | cross-project reassignment | mitigate | Do not bypass backend auth; if current store cannot prove assignment list, reload authoritative project state or fail controlled. |
| T-48-04-03 | Repudiation | command commit | transfer | Use existing `/api/commands/commit` history context with authenticated actor. |
| T-48-04-04 | Information Disclosure | error handling | mitigate | Show user-safe messages and avoid dumping backend stack/details into UI. |
</threat_model>

<verification>
Run:
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerMoves.test.ts`
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx`
- `npm run build -w packages/web`
</verification>

<success_criteria>
- Date moves, resource moves, and combined moves persist through existing backend APIs.
- Every successful mutation reloads planner state.
- Failed saves clear pending state and do not leave false optimistic state.
- Readonly/locked states do not emit mutation callbacks or network calls.
</success_criteria>

<output>
After completion, create `.planning/phases/48-resource-screen/48-04-SUMMARY.md`.
</output>
