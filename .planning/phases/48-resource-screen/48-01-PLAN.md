---
phase: 48-resource-screen
plan: 01
title: Adapter and gantt-lib renderer swap
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
  - packages/web/src/components/workspace/resourcePlannerAdapter.ts
  - packages/web/src/components/workspace/__tests__/resourcePlannerAdapter.test.ts
  - packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx
autonomous: true
requirements_addressed:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-MAPPER
  - PRD-RESOURCE-A11Y
requirements:
  - PRD-RESOURCE-SCREEN
  - PRD-RESOURCE-MAPPER
  - PRD-RESOURCE-A11Y
must_haves:
  truths:
    - "Resource screen primary timeline is gantt-lib resource planner mode, not local ResourceTimelineGrid."
    - "Empty resource rows remain visible as resource planner rows."
    - "Conflict correction target still flows through onCorrectConflict with PlannerCorrectionTarget fields."
  artifacts:
    - path: "packages/web/src/components/workspace/resourcePlannerAdapter.ts"
      provides: "Typed ResourcePlannerResult to ResourceTimelineResource adapter and metadata helper"
    - path: "packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx"
      provides: "GanttChart mode=resource-planner integration"
  key_links:
    - from: "ResourcePlannerWorkspace.tsx"
      to: "gantt-lib"
      via: "GanttChart mode=\"resource-planner\" with resources from adapter"
    - from: "renderItem conflict action"
      to: "onCorrectConflict"
      via: "PlannerCorrectionTarget built from metadata helper"
---

<objective>
Replace the local resource grid as the primary renderer with `gantt-lib` resource planner mode and introduce the typed adapter that all later plans build on.

Purpose: deliver the library-backed timeline foundation required by the PRDs and locked renderer decisions.
Output: adapter module, metadata helper, GanttChart resource planner rendering, and focused tests.
</objective>

<context>
@.planning/phases/48-resource-screen/48-CONTEXT.md
@.planning/phases/48-resource-screen/48-UI-SPEC.md
@RESOURCE-MANAGEMENT-SCREEN-PRD.md
@RESOURCE-PLANNER-MODE-PRD.md
@packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx
@packages/web/src/components/workspace/ResourceTimelineGrid.tsx
@packages/web/src/components/workspace/resourceAssignmentUtils.ts
@packages/web/src/stores/useUIStore.ts
@packages/runtime-core/src/types.ts
@D:/Projects/gantt-lib/docs/reference/15-resource-planner.md
@D:/Projects/gantt-lib/docs/reference/04-props.md

<interfaces>
Use `GanttChart` from `gantt-lib` with `mode="resource-planner"`, `resources`, `dayWidth={36}`, `laneHeight={40}`, `rowHeaderWidth={220}`, `headerHeight={40}`, `readonly`, `disableResourceReassignment`, `renderItem`, `getItemClassName`, and `onResourceItemMove`.

`ResourcePlannerInterval` includes `assignmentId`, `resourceId`, `resourceName`, `projectId`, `projectName`, `taskId`, `taskName`, `startDate`, `endDate`, `assignmentCreatedAt`, `hasConflict`, `conflictCount`, and `conflictAssignmentIds`.

`PlannerCorrectionTarget` is `{ projectId: string; taskId: string; assignmentId: string; resourceId: string }`.
</interfaces>

Constraints:
- Implement locked decisions from 48-CONTEXT: use `GanttChart mode="resource-planner"` or direct `ResourceTimelineChart`; preferred is `GanttChart`.
- Do not duplicate calendar geometry or preserve `ResourceTimelineGrid` as primary renderer.
- Ensure `gantt-lib/styles.css` is already imported globally; if absent, import it through the existing global app entry rather than local CSS geometry.
- Preserve existing Russian copy where specified in 48-UI-SPEC.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create planner adapter and metadata helper</name>
  <files>packages/web/src/components/workspace/resourcePlannerAdapter.ts, packages/web/src/components/workspace/__tests__/resourcePlannerAdapter.test.ts</files>
  <behavior>
    - Maps every ResourcePlannerResource to a ResourceTimelineResource with id/name/items.
    - Maps every ResourcePlannerInterval to a ResourceTimelineItem with id/resourceId/taskId/title/subtitle/startDate/endDate.
    - Carries typed metadata: projectId, projectName, taskId, assignmentId, resourceId, resourceName, hasConflict, conflictCount, conflictAssignmentIds, assignmentCreatedAt, source.
    - `getPlannerItemMetadata` returns metadata only when source is `resource-planner-result`; otherwise null.
    - Empty resources remain present with `items: []`.
  </behavior>
  <action>Create exported `ResourcePlannerItemMetadata`, `ResourcePlannerTimelineItem`, `mapResourcePlannerResultToTimelineResources`, and `getPlannerItemMetadata`. Use `import type` from `gantt-lib` for `ResourceTimelineResource` / `ResourceTimelineItem` and from `../../lib/apiTypes.ts` for planner payloads. Do not use `any`; metadata access must be narrowed through helper guards.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerAdapter.test.ts</automated>
  </verify>
  <acceptance_criteria>Adapter tests prove full field mapping, empty rows, conflict metadata, and helper rejection of unknown metadata.</acceptance_criteria>
  <done>Adapter exports typed mapper/helper, tests pass, and empty resource rows plus metadata are preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap ResourcePlannerWorkspace to gantt-lib resource planner</name>
  <files>packages/web/src/components/workspace/ResourcePlannerWorkspace.tsx, packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx</files>
  <behavior>
    - ResourcePlannerWorkspace renders GanttChart resource planner when planner data exists.
    - The old ResourceTimelineGrid import is removed from the primary render path.
    - Empty resources display as timeline rows through the gantt-lib resources prop.
    - Conflict bars render a compact Russian badge and an `Исправить конфликт` action.
    - Clicking the conflict action calls `onCorrectConflict` with PlannerCorrectionTarget from typed metadata.
  </behavior>
  <action>Import `GanttChart` from `gantt-lib` and adapter helpers. Replace `<ResourceTimelineGrid>` usage with an accessible timeline section named `Ресурсный календарь` that renders `<GanttChart mode="resource-planner" resources={timelineResources} dayWidth={36} laneHeight={40} rowHeaderWidth={220} headerHeight={40} ... />`. Implement compact `renderItem` and `getItemClassName` for normal/conflict states using metadata helper. Keep existing summary cards, scope switch reload behavior, planner load normalization, and `onCorrectConflict` integration.</action>
  <verify>
    <automated>npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx</automated>
  </verify>
  <acceptance_criteria>Tests no longer assert local grid as the main renderer; they assert resource planner props, resource rows including empty rows, Russian conflict action, and correction target wiring.</acceptance_criteria>
  <done>ResourcePlannerWorkspace uses gantt-lib resource planner as primary renderer and preserves conflict correction behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `/api/resources/planner` -> web renderer | Auth-protected backend payload becomes UI model and metadata. |
| User click -> conflict correction flow | Selected assignment identifiers cross into workspace navigation/state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-48-01 | Tampering | `resourcePlannerAdapter.ts` metadata | mitigate | Validate metadata shape through helper before callbacks; never parse unknown metadata ad hoc. |
| T-48-02 | Information Disclosure | `renderItem` | accept | Shows data already returned by authenticated planner API; do not add hidden debug payload dumps. |
| T-48-03 | Elevation of Privilege | conflict correction callback | mitigate | Callback only passes existing `PlannerCorrectionTarget`; no mutation is performed in this plan. |
</threat_model>

<verification>
Run:
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/resourcePlannerAdapter.test.ts`
- `npx vitest run --config vitest.config.ts packages/web/src/components/workspace/__tests__/ResourcePlannerWorkspace.test.tsx`
- `npm run build -w packages/web`
</verification>

<success_criteria>
- Primary renderer is `GanttChart mode="resource-planner"` or a direct `ResourceTimelineChart`.
- `ResourceTimelineGrid` is not used as the main renderer.
- Adapter preserves all PRD metadata and empty resource rows.
- Timeline has accessible name `Ресурсный календарь`.
- Conflict correction action still calls `onCorrectConflict` with the existing target shape.
</success_criteria>

<output>
After completion, create `.planning/phases/48-resource-screen/48-01-SUMMARY.md`.
</output>
