---
phase: 15-prisma-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/package.json
  - packages/mcp/prisma/schema.prisma
  - packages/mcp/prisma/migrations/20260313_init/migration.sql
  - packages/mcp/src/prisma.ts
  - packages/mcp/tsconfig.json
  - .env.example
  - .gitignore
autonomous: false
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-05
  - POOL-01
  - POOL-02
  - POOL-03

must_haves:
  truths:
    - "Prisma schema defines all 10 tables with correct types and relationships"
    - "Prisma client generates successfully without errors"
    - "DATABASE_URL connects to PostgreSQL with connection pooling"
    - "First migration creates all tables in PostgreSQL database"
    - "Prisma client is exportable from packages/mcp for packages/server to use"
    - "Connection pool settings are configured (limit=10, timeouts)"
    - "Graceful shutdown disconnects Prisma connections"
  artifacts:
    - path: "packages/mcp/prisma/schema.prisma"
      provides: "Prisma schema with all 10 models"
      contains: "model User, model Project, model Session, model OtpCode, model Task, model Dependency, model Message, model ShareLink, model TaskRevision, model TaskMutation"
      min_lines: 50
    - path: "packages/mcp/src/prisma.ts"
      provides: "Singleton Prisma Client with connection pooling"
      exports: ["getPrisma"]
      min_lines: 30
    - path: "packages/mcp/prisma/migrations"
      provides: "Prisma migration history"
    - path: ".env.example"
      provides: "DATABASE_URL documentation"
      contains: "DATABASE_URL=postgresql://"
  key_links:
    - from: "packages/mcp/src/prisma.ts"
      to: "packages/mcp/prisma/schema.prisma"
      via: "Prisma Client generation"
      pattern: "@prisma/client"
    - from: "packages/mcp/package.json"
      to: "packages/mcp/src/prisma.ts"
      via: "Exports field in package.json"
      pattern: '"./prisma"'
    - from: "DATABASE_URL env var"
      to: "PostgreSQL database"
      via: "Prisma connection string"
      pattern: "postgresql://.*connection_limit"

user_setup:
  - service: postgresql
    why: "Prisma requires PostgreSQL database for migration"
    env_vars:
      - name: DATABASE_URL
        source: "Existing PostgreSQL instance or local development database"
        format: "postgresql://user:password@host:5432/database?connection_limit=10&pool_timeout=20&connect_timeout=10"

---

<objective>
PostgreSQL database with Prisma ORM ready for development

Purpose: Replace @libsql/client (SQLite) with Prisma ORM + PostgreSQL for production scalability with concurrent users. Prisma provides type-safe database access, migration management, and connection pooling.

Output: Prisma schema defined, migrations created, Prisma client singleton available to both packages/mcp and packages/server
</objective>

<execution_context>
@D:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@D:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-prisma-setup/15-CONTEXT.md

# Existing SQLite schema to migrate
@packages/mcp/src/db.ts

# Current package exports pattern
@packages/mcp/package.json
@packages/server/package.json
</context>

<interfaces>
<!-- From packages/mcp/src/db.ts: Current SQLite schema -->
Current tables with columns:
- users: id (TEXT PK), email (TEXT UNIQUE), created_at (TEXT)
- projects: id (TEXT PK), user_id (TEXT FK users), name (TEXT), created_at (TEXT)
- sessions: id (TEXT PK), user_id (TEXT FK users), project_id (TEXT FK projects), access_token (TEXT UNIQUE), refresh_token (TEXT UNIQUE), expires_at (TEXT), created_at (TEXT)
- otp_codes: id (TEXT PK), email (TEXT), code (TEXT), expires_at (TEXT), used (INTEGER DEFAULT 0)
- tasks: id (TEXT PK), project_id (TEXT FK projects), name (TEXT), start_date (TEXT), end_date (TEXT), color (TEXT), progress (REAL DEFAULT 0), parent_id (TEXT FK tasks), sort_order (INTEGER DEFAULT 0)
- dependencies: id (TEXT PK), task_id (TEXT FK tasks), dep_task_id (TEXT), type (TEXT CHECK FS/SS/FF/SF), lag (REAL DEFAULT 0)
- messages: id (TEXT PK), project_id (TEXT FK projects), role (TEXT CHECK user/assistant), content (TEXT), created_at (TEXT)
- share_links: id (TEXT PK), project_id (TEXT FK projects), created_at (TEXT)
- task_revisions: project_id (TEXT PK), revision (INTEGER DEFAULT 0), updated_at (TEXT)
- task_mutations: id (TEXT PK), project_id (TEXT), run_id (TEXT), session_id (TEXT), source (TEXT CHECK agent/manual-save/api/system), mutation_type (TEXT CHECK create/update/delete/delete_all/import), task_id (TEXT), created_at (TEXT)

Type mappings (SQLite -> Prisma/PostgreSQL):
- TEXT -> String
- INTEGER -> Int
- REAL -> Float
- CHECK constraints -> enum or @check
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Prisma and configure schema.prisma</name>
  <files>packages/mcp/package.json, packages/mcp/prisma/schema.prisma</files>
  <action>
    1. Add dependencies to packages/mcp/package.json:
       - "prisma": "^5.22.0" (devDependency)
       - "@prisma/client": "^5.22.0" (dependency)

    2. Create packages/mcp/prisma/schema.prisma with:
       - datasource db { provider = "postgresql", url = env("DATABASE_URL") }
       - generator client { provider = "prisma-client-js", output = "../dist/prisma-client" }
       - All 10 models (User, Project, Session, OtpCode, Task, Dependency, Message, ShareLink, TaskRevision, TaskMutation)
       - Exact field types mapping from SQLite schema
       - All foreign key relationships with onDelete: Cascade (matching SQLite ON DELETE CASCADE)
       - Task parent_id self-reference with onDelete: SetNull (matching SQLite ON DELETE SET NULL)
       - Enum for DependencyType (FS, SS, FF, SF)
       - Enum for MessageRole (user, assistant)
       - Enum for MutationSource (agent, manual-save, api, system)
       - Enum for MutationType (create, update, delete, delete_all, import)

    Schema location per user decision: packages/mcp/prisma/schema.prisma
    Client output location per user decision: packages/mcp/dist/prisma-client

    Use defaultId for TEXT primary keys (cuid() or uuid() - user discretion, suggest uuid())
    Use DateTime for timestamp fields (created_at, updated_at, expires_at)
  </action>
  <verify>
    <automated>cd packages/mcp && npx prisma validate</automated>
  </verify>
  <done>schema.prisma exists, validates successfully, all 10 models defined with correct relationships</done>
</task>

<task type="auto">
  <name>Task 2: Create Prisma Client singleton with connection pooling</name>
  <files>packages/mcp/src/prisma.ts, packages/mcp/package.json, packages/mcp/tsconfig.json</files>
  <action>
    1. Create packages/mcp/src/prisma.ts with singleton Prisma Client:
       - Import { PrismaClient } from '@prisma/client'
       - Declare global var for hot reload safety
       - Export function getPrisma() returning singleton instance
       - Configure connection pool in PrismaClient constructor:
         * datasources: { db: { url: process.env.DATABASE_URL } }
       - Add process.on('SIGTERM') handler for graceful shutdown (prisma.$disconnect())
       - Add process.on('beforeExit') handler for cleanup

    2. Update packages/mcp/package.json exports:
       - Add "./prisma": "./dist/prisma-client.js" to exports field
       - This allows packages/server to import Prisma types

    3. Update packages/mcp/tsconfig.json if needed:
       - Ensure "moduleResolution": "bundler" or "nodenext" for proper ESM imports
       - Verify "esModuleInterop": true if using CommonJS interop

    Connection pool settings per user decision:
    - connection_limit = 10 (via DATABASE_URL query param)
    - pool_timeout = 20 seconds (via DATABASE_URL query param)
    - connect_timeout = 10 seconds (via DATABASE_URL query param)
  </action>
  <verify>
    <automated>cd packages/mcp && npm run build && node -e "import('./dist/prisma.js').then(m => console.log('Prisma singleton imported:', typeof m.getPrisma))"</automated>
  </verify>
  <done>prisma.ts compiled, exports getPrisma function, graceful shutdown handlers registered</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Prisma schema (10 models) and Prisma Client singleton with connection pooling configuration</what-built>
  <how-to-verify>
    1. Set DATABASE_URL in .env or environment (e.g., export DATABASE_URL="postgresql://user:pass@localhost:5432/gantt?connection_limit=10&pool_timeout=20&connect_timeout=10")
    2. Ensure PostgreSQL database is running and accessible
    3. Run: cd packages/mcp && npx prisma migrate dev --name init
    4. Verify migration creates all tables: check packages/mcp/prisma/migrations/ for new migration.sql
    5. Run: npx prisma generate (should complete without errors)
    6. Run: npx prisma studio (optional, opens web UI to verify schema)
  </how-to-verify>
  <resume-signal>Type "migrated" when Prisma migration succeeds and all tables are created in PostgreSQL</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update .env.example and .gitignore for Prisma</name>
  <files>.env.example, .gitignore</files>
  <action>
    1. Update .env.example:
       - Add DATABASE_URL documentation: "postgresql://user:password@host:5432/database?connection_limit=10&pool_timeout=20&connect_timeout=10"
       - Add comment explaining connection pooling parameters
       - Keep DB_PATH for reference (will be removed in Phase 17)

    2. Update .gitignore:
       - Add ".prisma/" (local Prisma cache)
       - Add "*.db-journal" (SQLite journal files)
       - DO NOT add "prisma/migrations/" (migrations must be committed)

    3. Create packages/mcp/prisma/.gitkeep (empty file) to ensure migrations directory is tracked

    Per user decision: Add .prisma/ to .gitignore, but keep migrations/ in version control
  </action>
  <verify>
    <automated>grep -q ".prisma" .gitignore && grep -q "DATABASE_URL" .env.example</automated>
  </verify>
  <done>.env.example documents DATABASE_URL, .gitignore excludes .prisma cache</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **Schema validation**: `cd packages/mcp && npx prisma validate` passes
2. **Client generation**: `cd packages/mcp && npx prisma generate` completes without errors
3. **Migration success**: `cd packages/mcp && npx prisma migrate dev --name init` creates migration.sql
4. **Type exports**: `packages/mcp/dist/prisma-client.js` exists and exports Prisma Client
5. **Singleton test**: Import getPrisma from packages/mcp/dist/prisma.js returns singleton instance
6. **Connection pooling**: DATABASE_URL query params include connection_limit, pool_timeout, connect_timeout
7. **Graceful shutdown**: SIGTERM handler calls prisma.$disconnect()

## Database Verification

Connect to PostgreSQL and verify tables:
```sql
SELECT tablename FROM pg_tables WHERE schemaname = 'public';
-- Should show: users, projects, sessions, otp_codes, tasks, dependencies, messages, share_links, task_revisions, task_mutations
```

## Foreign Key Verification

```sql
SELECT
    tc.table_name,
    kcu.column_name,
    ccu.table_name AS foreign_table_name,
    ccu.column_name AS foreign_column_name,
    rc.delete_rule
FROM
    information_schema.table_constraints AS tc
    JOIN information_schema.key_column_usage AS kcu ON tc.constraint_name = kcu.constraint_name
    JOIN information_schema.constraint_column_usage AS ccu ON ccu.constraint_name = tc.constraint_name
    JOIN information_schema.referential_constraints AS rc ON tc.constraint_name = rc.constraint_name
WHERE
    tc.constraint_type = 'FOREIGN KEY';
```
</verification>

<success_criteria>
## Measurable Completion

1. **Prisma schema exists** at packages/mcp/prisma/schema.prisma with 10 models
2. **Prisma Client generates** at packages/mcp/dist/prisma-client/ without errors
3. **Migration created** at packages/mcp/prisma/migrations/ with timestamp_init folder
4. **PostgreSQL tables created** matching SQLite schema (verified via SQL query)
5. **Foreign keys cascade** correctly matching SQLite ON DELETE behavior
6. **Connection pool configured** via DATABASE_URL query params
7. **Graceful shutdown** implemented in prisma.ts
8. **.env.example updated** with DATABASE_URL documentation
9. **.gitignore configured** to exclude .prisma/ cache

## Ready for Phase 16

- Prisma Client accessible from packages/mcp via "./prisma" export
- packages/server can import Prisma types from @gantt/mcp/prisma
- Database schema stable and version-controlled via migrations
- No blocking issues preventing services layer development
</success_criteria>

<output>
After completion, create `.planning/phases/15-prisma-setup/15-01-SUMMARY.md`
</output>
