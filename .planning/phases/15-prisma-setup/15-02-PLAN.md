---
phase: 15-prisma-setup
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - packages/mcp/prisma/migrations
  - packages/mcp/dist/prisma-client
autonomous: false
requirements:
  - DB-04
  - POOL-03

must_haves:
  truths:
    - "Prisma migration runs successfully on PostgreSQL database"
    - "All 10 tables created in PostgreSQL with correct schema"
    - "Foreign key constraints match SQLite CASCADE/SET_NULL behavior"
    - "Prisma Client generated and importable from packages/mcp"
    - "Database connection pool established successfully"
    - "Graceful shutdown disconnects connections cleanly"
  artifacts:
    - path: "packages/mcp/prisma/migrations"
      provides: "Migration history for schema version control"
      must_contain: "migration_lock.toml, 20260313_init/migration.sql"
    - path: "packages/mcp/dist/prisma-client"
      provides: "Generated Prisma Client with TypeScript types"
      exports: ["PrismaClient", "* as models"]
  key_links:
    - from: "Prisma Migrate CLI"
      to: "PostgreSQL database"
      via: "DATABASE_URL environment variable"
      pattern: "npx prisma migrate dev"
    - from: "packages/mcp/dist/prisma-client"
      to: "packages/mcp/src/prisma.ts"
      via: "ES module import"
      pattern: "from '@prisma/client'"

user_setup:
  - service: postgresql
    why: "Target database for initial migration"
    env_vars:
      - name: DATABASE_URL
        source: "User must provide working PostgreSQL connection string"
        required_for: "Migration execution"
        instructions: |
          1. Ensure PostgreSQL is running (local or remote)
          2. Create database: createdb gantt (or use existing)
          3. Set DATABASE_URL with connection pooling params:
             export DATABASE_URL="postgresql://user:password@localhost:5432/gantt?connection_limit=10&pool_timeout=20&connect_timeout=10"
    database_config:
      - task: "Create empty database"
        command: "createdb gantt || psql -c 'CREATE DATABASE gantt;'"
        optional: true
      - task: "Verify database connectivity"
        command: "psql $DATABASE_URL -c 'SELECT 1;'"

---

<objective>
Initial Prisma migration creates PostgreSQL database schema

Purpose: Execute first Prisma migration to create all tables in PostgreSQL. This establishes the production database schema and verifies that the Prisma configuration connects correctly.

Output: PostgreSQL database with 10 tables, Prisma migration history, generated Prisma Client
</objective>

<execution_context>
@D:/Users/Volobuev/.claude/get-shit-done/workflows/execute-plan.md
@D:/Users/Volobuev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-prisma-setup/15-CONTEXT.md
@.planning/phases/15-prisma-setup/15-01-PLAN.md

# Prisma schema from previous plan
@packages/mcp/prisma/schema.prisma

# Prisma singleton from previous plan
@packages/mcp/src/prisma.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Prisma schema and singleton from Plan 15-01 must exist</what-built>
  <how-to-verify>
    Verify prerequisites:
    1. packages/mcp/prisma/schema.prisma exists (10 models defined)
    2. packages/mcp/src/prisma.ts exists (getPrisma function)
    3. packages/mcp/package.json has prisma and @prisma/client dependencies
    4. DATABASE_URL environment variable is set (check: echo $DATABASE_URL)
    5. PostgreSQL database is running and accessible (check: psql $DATABASE_URL -c 'SELECT 1;')
  </how-to-verify>
  <resume-signal>Type "ready" when all prerequisites verified and DATABASE_URL is configured</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generate Prisma Client before migration</name>
  <files>packages/mcp/dist/prisma-client</files>
  <action>
    Run: cd packages/mcp && npx prisma generate

    This generates the Prisma Client to packages/mcp/dist/prisma-client/ based on schema.prisma.
    Required before running migrations because Prisma Migrate uses the generated client.

    Prisma will create:
    - packages/mcp/dist/prisma-client/index.js (ESM entry point)
    - packages/mcp/dist/prisma-client/index.d.ts (TypeScript types)
    - Runtime library files for querying PostgreSQL
  </action>
  <verify>
    <automated>ls -la packages/mcp/dist/prisma-client/index.js && ls -la packages/mcp/dist/prisma-client/index.d.ts</automated>
  </verify>
  <done>Prisma Client generated successfully, index.js and index.d.ts exist</done>
</task>

<task type="auto">
  <name>Task 3: Execute initial Prisma migration</name>
  <files>packages/mcp/prisma/migrations/20260313_init/migration.sql</files>
  <action>
    Run: cd packages/mcp && npx prisma migrate dev --name init

    This will:
    1. Create packages/mcp/prisma/migrations/20260313_init/ folder with timestamp
    2. Generate migration.sql with CREATE TABLE statements for all 10 models
    3. Execute migration.sql against DATABASE_URL (PostgreSQL)
    4. Create _prisma_migrations table to track migration history
    5. Run npx prisma generate automatically

    Expected tables created:
    - users
    - projects
    - sessions
    - otp_codes
    - tasks
    - dependencies
    - messages
    - share_links
    - task_revisions
    - task_mutations

    Per user decision: Use Prisma Migrate (not db push) for migration history.
  </action>
  <verify>
    <automated>cd packages/mcp && npx prisma migrate status</automated>
  </verify>
  <done>Migration status shows "No pending migrations", all tables created in PostgreSQL</done>
</task>

<task type="auto">
  <name>Task 4: Verify database schema and foreign keys</name>
  <files>No files created (verification only)</files>
  <action>
    Run verification queries to ensure schema matches SQLite:

    1. List all tables:
       psql $DATABASE_URL -c "SELECT tablename FROM pg_tables WHERE schemaname = 'public' ORDER BY tablename;"

    2. Verify foreign key cascades:
       psql $DATABASE_URL -c "
       SELECT
           tc.table_name,
           kcu.column_name,
           ccu.table_name AS foreign_table_name,
           rc.delete_rule
       FROM
           information_schema.table_constraints AS tc
           JOIN information_schema.key_column_usage AS kcu ON tc.constraint_name = kcu.constraint_name
           JOIN information_schema.constraint_column_usage AS ccu ON ccu.constraint_name = tc.constraint_name
           JOIN information_schema.referential_constraints AS rc ON tc.constraint_name = rc.constraint_name
       WHERE
           tc.constraint_type = 'FOREIGN KEY'
       ORDER BY tc.table_name;
       "

    Expected CASCADE deletions (matching SQLite ON DELETE CASCADE):
    - projects.user_id -> users.id (CASCADE)
    - sessions.user_id -> users.id (CASCADE)
    - sessions.project_id -> projects.id (CASCADE)
    - tasks.project_id -> projects.id (CASCADE)
    - dependencies.task_id -> tasks.id (CASCADE)
    - messages.project_id -> projects.id (CASCADE)
    - share_links.project_id -> projects.id (CASCADE)

    Expected SET NULL deletion (matching SQLite ON DELETE SET NULL):
    - tasks.parent_id -> tasks.id (SET NULL)
  </action>
  <verify>
    <automated>psql $DATABASE_URL -c "SELECT COUNT(*) as table_count FROM pg_tables WHERE schemaname = 'public';" | grep -q "10"</automated>
  </verify>
  <done>10 tables exist, foreign key constraints match SQLite CASCADE/SET_NULL behavior</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PostgreSQL database with complete schema matching SQLite structure</what-built>
  <how-to-verify>
    Manual verification steps:

    1. Open Prisma Studio to visualize data:
       cd packages/mcp && npx prisma studio
       (Opens web UI at http://localhost:5555)

    2. Verify all 10 models appear in sidebar:
       - User, Project, Session, OtpCode, Task, Dependency, Message, ShareLink, TaskRevision, TaskMutation

    3. Click each model and verify columns match SQLite schema:
       - User: id, email, created_at
       - Project: id, user_id, name, created_at
       - Task: id, project_id, name, start_date, end_date, color, progress, parent_id, sort_order
       - etc.

    4. Test inserting a record via Prisma Studio (create dummy User)

    5. Check migration SQL file:
       cat packages/mcp/prisma/migrations/20260313_init/migration.sql
       Verify CREATE TABLE statements look correct
  </how-to-verify>
  <resume-signal>Type "verified" when database schema confirmed correct in Prisma Studio</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Commit migration files and generated Prisma Client</name>
  <files>packages/mcp/prisma/migrations, packages/mcp/dist/prisma-client</files>
  <action>
    Commit the following to git:
    1. packages/mcp/prisma/schema.prisma (source of truth)
    2. packages/mcp/prisma/migrations/ (migration history)
    3. packages/mcp/dist/prisma-client/ (generated client, commit for stable builds)
    4. packages/mcp/src/prisma.ts (singleton)

    Do NOT commit:
    - .env (contains secrets)
    - .prisma/ (local cache, in .gitignore)
    - Any local database files (*.db, *.db-journal)

    Commit message format:
    "feat(15-02): initialize Prisma schema and PostgreSQL migration"
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "feat(15-02)"</automated>
  </verify>
  <done>Migration committed to git, Prisma Client in repository for stable builds</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **Migration status**: `cd packages/mcp && npx prisma migrate status` shows "No pending migrations"
2. **Table count**: PostgreSQL has exactly 10 tables (verified via SQL query)
3. **Foreign key rules**: CASCADE and SET NULL match SQLite ON DELETE behavior
4. **Prisma Client importable**: `import { PrismaClient } from '@prisma/client'` works from packages/mcp
5. **Migration history**: packages/mcp/prisma/migrations/ folder contains timestamped migration
6. **Prisma Studio**: Opens successfully and displays all models
7. **Git commit**: Migration files and generated client committed

## Database Schema Verification

```bash
# Verify tables
psql $DATABASE_URL -c "SELECT tablename FROM pg_tables WHERE schemaname = 'public';"

# Verify foreign keys
psql $DATABASE_URL -c "
SELECT
    tc.table_name,
    kcu.column_name,
    ccu.table_name AS foreign_table_name,
    rc.delete_rule
FROM information_schema.table_constraints AS tc
JOIN information_schema.key_column_usage AS kcu ON tc.constraint_name = kcu.constraint_name
JOIN information_schema.constraint_column_usage AS ccu ON ccu.constraint_name = tc.constraint_name
JOIN information_schema.referential_constraints AS rc ON tc.constraint_name = rc.constraint_name
WHERE tc.constraint_type = 'FOREIGN KEY'
ORDER BY tc.table_name;
"
```

## Graceful Shutdown Test

```bash
# Test SIGTERM handling
node -e "
import('./packages/mcp/dist/prisma.js').then(({ getPrisma }) => {
  const prisma = getPrisma();
  console.log('Prisma Client connected');
  process.kill(process.pid, 'SIGTERM');
});
"
# Should log "Prisma Client connected" then disconnect cleanly
```
</verification>

<success_criteria>
## Measurable Completion

1. **Migration executed**: `20260313_init` migration exists in packages/mcp/prisma/migrations/
2. **Tables created**: All 10 tables exist in PostgreSQL database
3. **Foreign keys correct**: CASCADE/SET NULL rules match SQLite schema
4. **Prisma Client generated**: packages/mcp/dist/prisma-client/ exists with index.js and index.d.ts
5. **Prisma Studio works**: Web UI opens at http://localhost:5555 showing all models
6. **Migration tracked**: _prisma_migrations table records the init migration
7. **Graceful shutdown**: SIGTERM handler disconnects Prisma connections
8. **Committed to git**: Migration files and Prisma Client in repository

## Ready for Phase 16

- Database schema stable and version-controlled
- Prisma Client accessible to both packages/mcp and packages/server
- No blocking issues preventing services layer development
- Connection pooling tested and working
</success_criteria>

<output>
After completion, create `.planning/phases/15-prisma-setup/15-02-SUMMARY.md`
</output>
