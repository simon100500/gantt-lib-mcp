---
phase: 47-agent-routing-fast-path
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - packages/server/src/mutation/types.ts
  - packages/server/src/mutation/resolver.ts
  - packages/server/src/mutation/orchestrator.ts
  - packages/server/src/split-task.ts
  - packages/server/src/mutation/orchestrator.test.ts
  - packages/server/src/mutation/execution.test.ts
  - packages/server/src/split-task.test.ts
autonomous: true
requirements:
  - ARFP-03
  - ARFP-04
must_haves:
  truths:
    - "Confident decomposition requests hand off from chat into the isolated split-task executor."
    - "Specialized executor routing never turns decomposition into a new low-level mutation command."
    - "Agent escalation is reserved for unresolved, low-confidence, or truly broad work rather than being the default for structural phrasing."
  artifacts:
    - path: "packages/server/src/split-task.ts"
      provides: "Isolated decomposition executor retained as the execution seam"
      contains: "export async function runDirectSplitTask"
    - path: "packages/server/src/mutation/orchestrator.ts"
      provides: "Specialized fast-path handoff to split-task"
      contains: "runDirectSplitTask"
    - path: "packages/server/src/split-task.test.ts"
      provides: "Regression coverage for specialized handoff without generic mutation-loop fallback"
      contains: "specialized_fast_path"
  key_links:
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/split-task.ts"
      via: "specialized executor handoff"
      pattern: "runDirectSplitTask"
    - from: "packages/server/src/split-task.ts"
      to: "packages/server/src/mutation/execution.ts"
      via: "authoritative typed command commit boundary"
      pattern: "executeMutationPlan"
    - from: "packages/server/src/mutation/types.ts"
      to: "packages/server/src/mutation/execution.ts"
      via: "routing primitive stays out of low-level command union"
      pattern: "kind: 'decompose_task'"
---

<objective>
Wire the first specialized fast executor route so decomposition requests leave the ordinary mutation loop and execute through the existing isolated `split-task` path.

Purpose: Implement D-specialized-01 and D-agent-01 from `47-CONTEXT.md` while preserving the non-negotiable split-task isolation boundary.
Output: A route-level `decompose_task` handoff, explicit specialized executor resolution, and regression coverage proving no silent fallback to the generic mutation loop.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
@.planning/agent-routing-fast-path-prd.md
@.planning/phases/42-mcp-mutation-refactor/42-02-SUMMARY.md
@packages/server/src/mutation/orchestrator.ts
@packages/server/src/mutation/resolver.ts
@packages/server/src/mutation/types.ts
@packages/server/src/split-task.ts
@packages/server/src/mutation/execution.ts

<interfaces>
From `packages/server/src/split-task.ts` today:

```ts
export type RunDirectSplitTaskInput = {
  projectId: string;
  taskId: string;
  details?: string;
  env: SplitTaskEnv;
  services: SplitTaskServices;
  sessionId: string;
  runId: string;
  broadcastToSession: (sessionId: string, payload: ServerMessage) => void;
};

export async function runDirectSplitTask(input: RunDirectSplitTaskInput): Promise<void>;
```

From `packages/server/src/mutation/execution.ts` today:

```ts
export async function executeMutationPlan(input: {
  projectId: string;
  projectVersion: number;
  tasksBefore: MutationTaskSnapshot[];
  plan: MutationPlan;
  commandService: { commitCommand: (...) => Promise<...> };
}): Promise<MutationExecutionResult>;
```

The specialized route must call the existing isolated executor and keep `executeMutationPlan()` as the authoritative write boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit specialized executor resolution for decompose_task</name>
  <files>packages/server/src/mutation/types.ts, packages/server/src/mutation/resolver.ts, packages/server/src/mutation/orchestrator.test.ts</files>
  <read_first>
    - packages/server/src/mutation/types.ts
    - packages/server/src/mutation/resolver.ts
    - packages/server/src/mutation/orchestrator.test.ts
    - packages/server/src/split-task.ts
    - .planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: decomposition resolution returns explicit executor metadata: `executor: 'split_task'`, `targetTaskId`, `targetTaskName`, `mode`, optional range fields, and confidence.
    - Test 2: low-confidence decomposition resolution fails fast into `clarify` or `agent_path`, never generic legacy fallback.
    - Test 3: `decompose_task` remains a routing primitive, not a new `MutationPlanOperation`.
  </behavior>
  <action>Extend the Phase 47 mutation contracts with a dedicated specialized executor handoff object, for example `SpecializedExecutorResolution`, that carries exact fields `executor`, `targetTaskId`, `targetTaskName`, `mode`, `rangeFrom`, `rangeTo`, and `confidence`. Update `resolveMutationContext()` so `decompose_task` resolution populates this object only on high-confidence matches per D-resolution-01 and D-specialized-01. Support the motivating example `Разбей Бетонирование перекрытий 12-17 этажей поэтажно` by preserving optional floor/range parameters in the resolved handoff metadata, but do not introduce a low-level `decompose_task` command into `MutationPlanOperation`.</action>
  <acceptance_criteria>
    - `packages/server/src/mutation/types.ts` contains `executor: 'split_task'`
    - `packages/server/src/mutation/types.ts` contains `targetTaskId` and `targetTaskName`
    - `packages/server/src/mutation/resolver.ts` contains `decompose_task`
    - `packages/server/src/mutation/resolver.ts` contains `split_task`
    - `packages/server/src/mutation/orchestrator.test.ts` contains `targetTaskId`
    - `packages/server/src/mutation/types.ts` does not contain `kind: 'decompose_task'`
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test packages/server/src/mutation/orchestrator.test.ts</automated>
  </verify>
  <done>The resolver produces an explicit specialized handoff contract for decomposition, and the contracts make it impossible to mistake `decompose_task` for a low-level typed command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand decompose_task into the isolated split-task executor</name>
  <files>packages/server/src/mutation/orchestrator.ts, packages/server/src/split-task.ts, packages/server/src/split-task.test.ts, packages/server/src/mutation/orchestrator.test.ts, packages/server/src/mutation/execution.test.ts</files>
  <read_first>
    - packages/server/src/mutation/orchestrator.ts
    - packages/server/src/split-task.ts
    - packages/server/src/mutation/execution.test.ts
    - packages/server/src/mutation/orchestrator.test.ts
    - packages/server/src/mutation/types.ts
    - .planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `runStagedMutation()` calls `runDirectSplitTask()` when the route is `specialized_fast_path` and the intent is `decompose_task`.
    - Test 2: successful decomposition returns a route-aware operational response and a handled result, not `deferred_to_legacy`.
    - Test 3: the isolated split executor still compiles through `buildMutationPlan()` and `executeMutationPlan()` rather than bypassing authoritative commands.
  </behavior>
  <action>Implement the specialized branch in `runStagedMutation()` so a high-confidence `decompose_task` request calls `runDirectSplitTask()` directly per D-specialized-01. Keep the split executor isolated: do not move its prompt, planning, or command-commit logic into the generic mutation orchestrator. Update `split-task.ts` only as needed to accept the new route/resolution metadata and to return operational details usable by chat, while preserving its bounded flow `load -> narrow planning call -> buildMutationPlan -> executeMutationPlan`. Add focused tests that prove decomposition requests now take the specialized fast path, low-confidence decomposition requests do not silently fall through to legacy mutation, and authoritative command execution remains the only write boundary.</action>
  <acceptance_criteria>
    - `packages/server/src/mutation/orchestrator.ts` contains `runDirectSplitTask(`
    - `packages/server/src/mutation/orchestrator.ts` contains `specialized_fast_path`
    - `packages/server/src/mutation/orchestrator.ts` does not contain a `deferred_to_legacy` branch specific to `decompose_task`
    - `packages/server/src/split-task.ts` contains `buildMutationPlan(`
    - `packages/server/src/split-task.ts` contains `executeMutationPlan(`
    - `packages/server/src/split-task.test.ts` contains `runDirectSplitTask`
    - `packages/server/src/mutation/execution.test.ts` contains a guard or assertion that `decompose_task` is not compiled as a low-level command
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test packages/server/src/mutation/orchestrator.test.ts packages/server/src/split-task.test.ts packages/server/src/mutation/execution.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>Decomposition requests route into the isolated split executor, the ordinary generic mutation loop is no longer the fallback for this class, and tests prove the authoritative typed-command boundary remains intact.</done>
</task>

</tasks>

<verification>
Check that decomposition is now a first-class route and not a silent fallback case:
- specialized fast-path requests call the isolated executor
- low-confidence decomposition requests clarify or escalate instead of falling through
- no low-level `decompose_task` command appears in the authoritative mutation plan union
</verification>

<success_criteria>
Chat can hand a bounded decomposition request into `split-task.ts` through a strict specialized route, and the codebase still enforces split-task isolation plus authoritative typed command commits.
</success_criteria>

<output>
After completion, create `.planning/phases/47-agent-routing-fast-path/47-agent-routing-fast-path-02-SUMMARY.md`
</output>
