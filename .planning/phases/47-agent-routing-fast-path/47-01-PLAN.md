---
phase: 47-agent-routing-fast-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/mutation/types.ts
  - packages/server/src/mutation/intent-classifier.ts
  - packages/server/src/mutation/execution-routing.ts
  - packages/server/src/mutation/resolver.ts
  - packages/server/src/mutation/orchestrator.ts
  - packages/server/src/mutation/intent-classifier.test.ts
  - packages/server/src/mutation/orchestrator.test.ts
autonomous: true
requirements:
  - ARFP-01
  - ARFP-02
must_haves:
  truths:
    - "Conversational mutation requests are classified into a strict route envelope before execution begins."
    - "Resolver confidence and risk band are explicit enough to block unsafe structural requests before any legacy fallback."
    - "The staged orchestrator exposes route selection and pre-execution gating as typed, testable behavior."
  artifacts:
    - path: "packages/server/src/mutation/types.ts"
      provides: "Strict route envelope, risk bands, and compatibility contracts for Phase 47"
      contains: "export type MutationRoute = 'fast_path' | 'specialized_fast_path' | 'agent_path' | 'clarify';"
    - path: "packages/server/src/mutation/intent-classifier.ts"
      provides: "Cheap structured router output with decompose_task and ambiguity extraction"
      contains: "intentType: 'decompose_task'"
    - path: "packages/server/src/mutation/orchestrator.ts"
      provides: "Pre-execution route selection, clarify gating, and no-silent-fallthrough behavior"
      contains: "route_selected"
  key_links:
    - from: "packages/server/src/mutation/intent-classifier.ts"
      to: "packages/server/src/mutation/types.ts"
      via: "MutationRouteEnvelope parser output"
      pattern: "routeEnvelope"
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/execution-routing.ts"
      via: "route compatibility and agent escalation mapping"
      pattern: "selectMutationExecutionMode|route_selected"
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/resolver.ts"
      via: "resolver confidence gate before execution"
      pattern: "resolution_result"
---

<objective>
Introduce the Phase 47 routing contract ahead of execution so the server decides between fast path, specialized fast path, agent path, and clarification before any mutation work starts.

Purpose: Implement D-routing-01, D-risk-01, and D-resolution-01 from `47-CONTEXT.md` as code-level contracts instead of freeform branching.
Output: Strict route envelope types, cheap router parsing, route-compatible orchestration, and pre-execution regression coverage.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
@.planning/agent-routing-fast-path-prd.md
@packages/server/src/mutation/types.ts
@packages/server/src/mutation/intent-classifier.ts
@packages/server/src/mutation/execution-routing.ts
@packages/server/src/mutation/resolver.ts
@packages/server/src/mutation/orchestrator.ts

<interfaces>
From `packages/server/src/mutation/types.ts` today:

```ts
export type MutationExecutionMode = 'deterministic' | 'hybrid' | 'full_agent';
export type MutationIntent = {
  intentType: MutationIntentType;
  confidence: number;
  executionMode: MutationExecutionMode;
  requiresResolution: boolean;
  requiresSchedulingPlacement: boolean;
};
export type ResolvedMutationContext = {
  resolutionQuery: string;
  confidence: number;
  tasks: MutationResolutionEntity[];
  containers: MutationResolutionEntity[];
};
```

Phase 47 must preserve compatibility for downstream planner/executor code, but add a stricter route-first contract above it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the strict route envelope and cheap router output</name>
  <files>packages/server/src/mutation/types.ts, packages/server/src/mutation/intent-classifier.ts, packages/server/src/mutation/intent-classifier.test.ts</files>
  <read_first>
    - packages/server/src/mutation/types.ts
    - packages/server/src/mutation/intent-classifier.ts
    - packages/server/src/mutation/intent-classifier.test.ts
    - .planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
    - .planning/agent-routing-fast-path-prd.md
  </read_first>
  <behavior>
    - Test 1: `classifyMutationIntent()` returns a strict route envelope with `route`, `intentFamily`, `intentType`, `confidence`, `riskLevel`, `params`, and `ambiguities`.
    - Test 2: `Разбей ... поэтажно` maps to `intentType: 'decompose_task'`, `route: 'specialized_fast_path'`, `riskLevel: 'S2'`, and `params.executor: 'split_task'`.
    - Test 3: ambiguous structural prompts return `route: 'clarify'` or `route: 'agent_path'`, never a silent deterministic default.
  </behavior>
  <action>Extend `MutationIntentType` with the route-level intent `decompose_task` per D-routing-01. Add `MutationRoute = 'fast_path' | 'specialized_fast_path' | 'agent_path' | 'clarify'`, `MutationRiskLevel = 'S0' | 'S1' | 'S2' | 'S3'`, and a strict `MutationRouteEnvelope` type with exact fields `route`, `intentFamily`, `intentType`, `confidence`, `riskLevel`, `params`, and `ambiguities`. Update `MutationIntent` so the current compatibility fields (`executionMode`, `requiresResolution`, `requiresSchedulingPlacement`) are derived from the route envelope instead of being trusted as primary model output. Evolve `classifyMutationIntent()` into the cheap structured router described in D-routing-01 and D-risk-01: the prompt must request schema-constrained routing JSON, must emit `decompose_task` for decomposition requests, and must never invent task IDs or DB payloads. Add focused test coverage for deterministic fast-path requests, specialized decomposition routing, and clarify/agent escalation behavior.</action>
  <acceptance_criteria>
    - `packages/server/src/mutation/types.ts` contains `export type MutationRoute = 'fast_path' | 'specialized_fast_path' | 'agent_path' | 'clarify';`
    - `packages/server/src/mutation/types.ts` contains `export type MutationRiskLevel = 'S0' | 'S1' | 'S2' | 'S3';`
    - `packages/server/src/mutation/types.ts` contains `intentType: 'decompose_task'` or a union member `'decompose_task'`
    - `packages/server/src/mutation/types.ts` contains `routeEnvelope: MutationRouteEnvelope`
    - `packages/server/src/mutation/intent-classifier.ts` contains the literal strings `specialized_fast_path`, `agent_path`, and `clarify`
    - `packages/server/src/mutation/intent-classifier.test.ts` contains `decompose_task`
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test packages/server/src/mutation/intent-classifier.test.ts</automated>
  </verify>
  <done>The router contract is encoded in shared types, the classifier emits the new envelope for routing decisions, and tests prove decomposition and ambiguity are classified without DB payload synthesis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume the route envelope in orchestration and resolver gating</name>
  <files>packages/server/src/mutation/execution-routing.ts, packages/server/src/mutation/resolver.ts, packages/server/src/mutation/orchestrator.ts, packages/server/src/mutation/orchestrator.test.ts</files>
  <read_first>
    - packages/server/src/mutation/execution-routing.ts
    - packages/server/src/mutation/resolver.ts
    - packages/server/src/mutation/orchestrator.ts
    - packages/server/src/mutation/orchestrator.test.ts
    - packages/server/src/mutation/types.ts
    - .planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: the orchestrator logs a structured `route_selected` event before any resolver or executor branch runs.
    - Test 2: low-confidence structural routes stop at `clarify` or `agent_path` per D-resolution-01 and D-agent-01, not `deferred_to_legacy`.
    - Test 3: compatibility mapping still preserves `deterministic`, `hybrid`, and `full_agent` for downstream code, but those are derived from the new route contract.
  </behavior>
  <action>Refactor `selectMutationExecutionMode()` so it becomes a compatibility-layer projection from the new route envelope rather than the primary router. Update resolver-facing types to preserve explicit confidence, ambiguity, and specialized-executor prerequisites per D-resolution-01. In `runStagedMutation()`, log `route_selected` before resolution, branch on `fast_path`, `specialized_fast_path`, `agent_path`, and `clarify`, and add controlled failures for low-confidence structural routing so `decompose_task` and other `specialized_fast_path` intents do not silently drift into the generic ordinary mutation loop. Plan 01 must stop at the new orchestration seam: set up the specialized branch and typed gating, but leave the actual isolated `split-task` handoff for Plan 02.</action>
  <acceptance_criteria>
    - `packages/server/src/mutation/orchestrator.ts` contains `route_selected`
    - `packages/server/src/mutation/orchestrator.ts` contains checks for `specialized_fast_path`, `agent_path`, and `clarify`
    - `packages/server/src/mutation/execution-routing.ts` contains a compatibility mapping from route classes to `deterministic`, `hybrid`, or `full_agent`
    - `packages/server/src/mutation/orchestrator.test.ts` contains assertions for `route_selected`
    - `packages/server/src/mutation/orchestrator.test.ts` contains a case where `decompose_task` does not return `deferred_to_legacy`
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test packages/server/src/mutation/orchestrator.test.ts packages/server/src/mutation/intent-classifier.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>The staged orchestrator is route-first, logs route choice before execution, and blocks unsafe structural fallthrough while still preserving downstream execution-mode compatibility.</done>
</task>

</tasks>

<verification>
Confirm the Phase 47 route contract exists before specialized execution work starts:
- route envelope types compile in the shared mutation contracts
- route selection is logged before resolution and execution
- structural ambiguity no longer defaults into generic legacy fallback
</verification>

<success_criteria>
The server has a route-first conversational mutation shell with strict route/risk typing, decompose-task classification, and testable clarify/agent gating before any specialized executor is invoked.
</success_criteria>

<output>
After completion, create `.planning/phases/47-agent-routing-fast-path/47-agent-routing-fast-path-01-SUMMARY.md`
</output>
