---
phase: 47-agent-routing-fast-path
plan: 03
type: execute
wave: 3
depends_on:
  - 47-01
  - 47-02
files_modified:
  - packages/server/src/mutation/messages.ts
  - packages/server/src/mutation/orchestrator.ts
  - packages/server/src/mutation/intent-classifier.test.ts
  - packages/server/src/mutation/orchestrator.test.ts
  - packages/server/src/mutation/execution.test.ts
autonomous: true
requirements:
  - ARFP-05
  - ARFP-06
must_haves:
  truths:
    - "Successful fast and specialized routes respond with operational summaries instead of generic acknowledgements."
    - "Failed routing or resolution reports the actual failed route or step instead of the legacy no-tool-call message."
    - "Telemetry and tests make route decisions, escalation boundaries, and isolation rules inspectable."
  artifacts:
    - path: "packages/server/src/mutation/messages.ts"
      provides: "Route-aware operational success and failure messaging"
      contains: "specialized_fast_path"
    - path: "packages/server/src/mutation/orchestrator.ts"
      provides: "Structured telemetry for route choice, specialized execution, and escalation"
      contains: "specialized_executor_started"
    - path: "packages/server/src/mutation/orchestrator.test.ts"
      provides: "Regression coverage for route-aware success/failure and agent escalation boundaries"
      contains: "route_selected"
  key_links:
    - from: "packages/server/src/mutation/orchestrator.ts"
      to: "packages/server/src/mutation/messages.ts"
      via: "route-aware response rendering"
      pattern: "buildMutationFailureMessage|buildMutationSuccessMessage"
    - from: "packages/server/src/mutation/orchestrator.test.ts"
      to: "packages/server/src/mutation/orchestrator.ts"
      via: "telemetry and escalation assertions"
      pattern: "specialized_executor_started|agent_escalation_selected"
    - from: "packages/server/src/mutation/execution.test.ts"
      to: "packages/server/src/mutation/types.ts"
      via: "source guard that decomposition stays out of low-level command contracts"
      pattern: "decompose_task"
---

<objective>
Lock Phase 47 behavior with route-aware telemetry, operational messaging, and regression guards so the new boundary cannot silently collapse back into generic mutation-loop failures.

Purpose: Implement D-response-01 and the Phase 4 observability/testing scope from `47-CONTEXT.md`.
Output: Route-aware user-facing messages, structured route telemetry, and regression/source guards for specialized isolation and agent escalation rules.
</objective>

<execution_context>
@C:/Users/Volobuev/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Volobuev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
@.planning/phases/42-mcp-mutation-refactor/42-04-SUMMARY.md
@.planning/phases/43-initial-gen-no-regexp/43-03-SUMMARY.md
@packages/server/src/mutation/messages.ts
@packages/server/src/mutation/orchestrator.ts
@packages/server/src/mutation/orchestrator.test.ts
@packages/server/src/mutation/execution.test.ts

<interfaces>
From `packages/server/src/mutation/messages.ts` and `orchestrator.ts` today:

```ts
buildMutationFailureMessage(reason, { resolutionContext })
buildMutationSuccessMessage(intent, execution)
logger.debug('final_outcome', { executionMode, failureReason, changedTaskIds })
```

Phase 47 should extend this pattern so route decisions and specialized-path outcomes are visible before and after execution, and failures name the failed route/step instead of falling back to the generic no-tool-call wording.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make mutation messages and telemetry route-aware</name>
  <files>packages/server/src/mutation/messages.ts, packages/server/src/mutation/orchestrator.ts, packages/server/src/mutation/orchestrator.test.ts</files>
  <read_first>
    - packages/server/src/mutation/messages.ts
    - packages/server/src/mutation/orchestrator.ts
    - packages/server/src/mutation/orchestrator.test.ts
    - packages/server/src/mutation/types.ts
    - .planning/phases/47-agent-routing-fast-path/47-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: successful deterministic and specialized routes mention what was recognized, what changed, how many tasks changed, and whether warnings exist.
    - Test 2: route failures mention the failed route or failed resolution step instead of the generic no-tool-call message.
    - Test 3: telemetry emits `route_selected`, `specialized_executor_started`, `specialized_executor_completed`, and `agent_escalation_selected` with route/risk metadata.
  </behavior>
  <action>Extend the existing message-builder pattern from Phase 42 so success and failure text is route-aware per D-response-01. For specialized decomposition success, require operational text that names the target task and child-count/result. For failed routing or resolution, name the failed route or step and avoid the generic `модель не выполнила ни одного валидного mutation tool call` wording when the real problem was routing, resolution, or escalation. Add structured debug events `route_selected`, `specialized_executor_started`, `specialized_executor_completed`, and `agent_escalation_selected`, each carrying at least `route`, `intentType`, `riskLevel`, and confidence-related context.</action>
  <acceptance_criteria>
    - `packages/server/src/mutation/orchestrator.ts` contains `specialized_executor_started`
    - `packages/server/src/mutation/orchestrator.ts` contains `specialized_executor_completed`
    - `packages/server/src/mutation/orchestrator.ts` contains `agent_escalation_selected`
    - `packages/server/src/mutation/messages.ts` contains `specialized_fast_path` or `decompose_task`
    - `packages/server/src/mutation/orchestrator.test.ts` contains `specialized_executor_started`
    - `packages/server/src/mutation/orchestrator.test.ts` does not rely on the generic `no valid mutation tool call` wording for decomposition-route failures
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test packages/server/src/mutation/orchestrator.test.ts</automated>
  </verify>
  <done>Route-aware success/failure text and telemetry are emitted consistently, and route or resolution failures are no longer masked by the legacy generic mutation-loop message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the specialized isolation and escalation boundary with regression guards</name>
  <files>packages/server/src/mutation/intent-classifier.test.ts, packages/server/src/mutation/orchestrator.test.ts, packages/server/src/mutation/execution.test.ts</files>
  <read_first>
    - packages/server/src/mutation/intent-classifier.test.ts
    - packages/server/src/mutation/orchestrator.test.ts
    - packages/server/src/mutation/execution.test.ts
    - packages/server/src/mutation/types.ts
    - packages/server/src/split-task.ts
    - .planning/agent-routing-fast-path-prd.md
  </read_first>
  <behavior>
    - Test 1: decomposition prompts are routed to `specialized_fast_path` and never to default legacy fallback.
    - Test 2: S3/optimization prompts escalate to `agent_path` explicitly.
    - Test 3: source guards fail if `decompose_task` becomes a low-level mutation command or if the split executor stops using authoritative typed command helpers.
  </behavior>
  <action>Add final regression coverage across router, orchestrator, and execution seams for the rules in D-specialized-01, D-agent-01, and D-non-negotiable-01. Include at least one explicit S3/optimization prompt that must route to `agent_path`, one low-confidence structural prompt that must end in `clarify` or typed failure, and one decomposition prompt that must stay on `specialized_fast_path`. Add a source-level guard in the execution test seam that rejects reintroducing `decompose_task` as a `MutationPlanOperation` or executor-compiled command, and another guard that keeps `split-task.ts` wired through `buildMutationPlan()` plus `executeMutationPlan()`.</action>
  <acceptance_criteria>
    - `packages/server/src/mutation/intent-classifier.test.ts` contains `specialized_fast_path`
    - `packages/server/src/mutation/intent-classifier.test.ts` contains `agent_path`
    - `packages/server/src/mutation/orchestrator.test.ts` contains `clarify`
    - `packages/server/src/mutation/execution.test.ts` contains `decompose_task`
    - `packages/server/src/mutation/execution.test.ts` contains `buildMutationPlan` or reads `packages/server/src/split-task.ts`
    - `packages/server/src/mutation/execution.test.ts` contains `executeMutationPlan`
  </acceptance_criteria>
  <verify>
    <automated>npx tsx --test packages/server/src/mutation/intent-classifier.test.ts packages/server/src/mutation/orchestrator.test.ts packages/server/src/mutation/execution.test.ts && npm run build -w packages/server</automated>
  </verify>
  <done>The routing boundary is locked by automated coverage: decomposition stays specialized, S3 work escalates to agent_path explicitly, and the isolated split executor cannot silently collapse into a generic mutation command path.</done>
</task>

</tasks>

<verification>
Validate the final Phase 47 contract end to end:
- route-aware telemetry is emitted before and after execution
- fast and specialized replies are operational, not generic
- regression guards prevent specialized decomposition from drifting back into generic loop behavior or low-level command unions
</verification>

<success_criteria>
Phase 47 ends with a visible, test-locked routing boundary: fast path for simple edits, isolated specialized execution for decomposition, and explicit agent escalation only for genuinely broad or high-risk work.
</success_criteria>

<output>
After completion, create `.planning/phases/47-agent-routing-fast-path/47-agent-routing-fast-path-03-SUMMARY.md`
</output>
