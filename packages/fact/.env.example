# Standalone env for the fact frontend itself.
# Deployment secrets stay in the repository root `.env`.
# This Vite value is baked into the static frontend at build time.

VITE_FACT_API_BASE_URL=https://ai.getgantt.ru
