export function buildWholeProjectStructureLines(): string[] {
  return [
    'Build one whole-project hierarchy in a single pass.',
    'Use full-project milestones and balanced cross-phase workstreams.',
  ];
}

export function buildWholeProjectSchedulingLines(): string[] {
  return ['Build a realistic non-trivial dependency graph for the whole project with no cycles.'];
}
