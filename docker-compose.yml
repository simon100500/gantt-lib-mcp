services:
  gantt:
    build:
      context: .
      args:
        VITE_LANDING_SITE_URL: ${VITE_LANDING_SITE_URL:-https://getgantt.ru/}
    ports:
      - "8080:80"
    volumes:
      - gantt-data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  gantt-data:
